
chemi_similar <- function(query, minSim, maxSim, simType, coerce = FALSE){

  if(missing(minSim) == TRUE){minSim <- 0.85}
  if(missing(maxSim) == TRUE){maxSim <- 1}
  if(missing(simType) == TRUE){simType <- 'tainimoto'}





  return(df)
}
