name: Release

on:
  workflow_dispatch:
    inputs:
      version_type:
        description: 'Version type to bump (patch, minor, major)'
        required: true
        default: 'patch'
        type: choice
        options:
          - none
          - patch
          - minor
          - major

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.RELEASE_PAT || secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: quarto-dev/quarto-actions/setup@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::desc, any::devtools, any::autonewsmd, any::here, any::rcmdcheck

      - name: Git identity
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"

      - name: Bump version and document
        if: github.event.inputs.version_type != 'none'
        run: |
          # Non-interactive bump (desc avoids usethis's interactive .9000 dev-version behavior)
          desc::desc_bump_version("${{ github.event.inputs.version_type }}")
          # Document (NEWS.md will be generated after tagging)
          devtools::document()
        shell: Rscript {0}

      - name: Resolve version and guard against existing tag
        run: |
          VERSION=$(grep -m 1 '^Version:' DESCRIPTION | cut -d ' ' -f 2)
          echo "Resolved version: $VERSION"
          if git ls-remote --exit-code --tags origin "refs/tags/v$VERSION" >/dev/null 2>&1; then
            echo "::error::Tag v$VERSION already exists on origin. Choose a higher bump type (patch/minor/major) or delete the stale tag."
            exit 1
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Commit version bump
        if: github.event.inputs.version_type != 'none'
        run: |
          git add DESCRIPTION man/ NAMESPACE
          git commit -m "chore: bump version to ${{ env.VERSION }}"

      - name: Check package
        run: rcmdcheck::rcmdcheck(args = "--no-manual", error_on = "error")
        shell: Rscript {0}

      - name: Build package
        run: R CMD build .

      - name: Push changes and create tag
        run: |
          git push origin main
          git tag v${{ env.VERSION }}
          git push origin --tags

      - name: Generate NEWS.md after tagging
        run: |
          # Now that the tag exists, regenerate NEWS.md so commits appear under the tag
          library(autonewsmd)
          an <- autonewsmd$new(repo_name = "ComptoxR", repo_path = getwd())
          an$generate()
          an$write(force = TRUE)
        shell: Rscript {0}

      - name: Commit and push NEWS.md
        run: |
          git add NEWS.md
          if git diff --cached --quiet; then
            echo "No NEWS.md changes to commit"
          else
            git commit -m "docs: update NEWS.md for v${{ env.VERSION }} [skip ci]"
            git push origin main
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.RELEASE_PAT || secrets.GITHUB_TOKEN }}
          tag_name: v${{ env.VERSION }}
          name: Release v${{ env.VERSION }}
          body: |
            ## Changes in v${{ env.VERSION }}
            See [NEWS.md](https://github.com/${{ github.repository }}/blob/main/NEWS.md) for details.
          files: ComptoxR_${{ env.VERSION }}.tar.gz
          draft: false
          prerelease: false
