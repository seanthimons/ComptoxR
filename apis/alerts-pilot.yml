id: alerts-pilot
schemas:
  files:
  - schema/chemi-alerts-prod.json
selection:
  include:
  - GET /api/alerts/groups/{id}
helper: generic_request
documentation: yes
operations:
  GET /api/alerts/groups/{id}:
    name: chemi_alerts_groups_by_id
    file: R/chemi_alerts_groups_by_id.R
    implementation: generated
    inputs:
      id:
        required: yes
        type: character
        description: 'Primary query parameter. Type: string'
    request:
      arguments:
        query:
          from:
          - params
          - id
        endpoint:
          value: alerts/groups/
        method:
          value: GET
        batch_limit:
          value: 1.0
        server:
          value: chemi_burl
        auth:
          value: no
        tidy:
          value: no
    docs:
      title: Alerts Groups
      lifecycle: experimental
      return: Returns a tibble with results
      tags:
        apiStage: public
      examples:
      - id: DTXSID7020182
contracts_file: tests/testthat/fixtures/specmill-pilot-contracts.rds
