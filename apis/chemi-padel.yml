id: chemi-padel
schemas:
  files:
  - schema/chemi-padel-prod.json
selection:
  methods:
  - GET
  - POST
  exclude:
  - render|replace|add|freeze|metadata|version|reports|webtest/report|download|export|protocols|preflight|universalpreflight|caspreflight|file|admin|auth|login|logout|register|index|errorreport|testpage|curators|reaction/editor|chemdelete|reactiondelete|visibility|mapfix|newchem|newreact|newlibrary|_DL|_dl|chemtemp|resolver/(?:casharvest|universalharvest(?:_cart)?)$
helper: generic_chemi_request
policy_version: comptox-maintenance-1
hook_config: inst/hook_config.yml
names:
  GET /api/padel: chemi_padel
  POST /api/padel: chemi_padel_bulk
operations:
  GET /api/padel:
    inputs:
      smiles:
        required: yes
      x2d:
        required: no
        default: yes
        type: logical
      x3d:
        required: no
        default: no
        type: logical
      fp:
        required: no
        default: no
        type: logical
      headers:
        required: no
        default: no
        type: logical
      timeout:
        required: no
        default: ~
        type: object
      output:
        required: no
        default:
        - wide
        - raw
        type: character
    file: R/chemi_padel.R
    helper: generic_request
    request:
      arguments:
        endpoint:
          from:
          - hook_state
          - request
          - endpoint
        method:
          from:
          - hook_state
          - request
          - method
        batch_limit:
          value: 0.0
        server:
          from:
          - hook_state
          - request
          - server
        auth:
          value: no
        tidy:
          value: no
        content_type:
          from:
          - hook_state
          - request
          - content_type
        options:
          from:
          - hook_state
          - request
          - options
    post_state: hook_state
    post_on_skip: yes
    docs:
      title: Padel
      return: Returns a tibble with results
      lifecycle: experimental
      parameters:
        smiles: One SMILES string or resolvable chemical identifier
        x2d: 'Calculate two-dimensional descriptors (default: TRUE)'
        x3d: 'Calculate three-dimensional descriptors (default: FALSE)'
        fp: 'Calculate fingerprints (default: FALSE)'
        headers: 'Request upstream descriptor headers (default: FALSE)'
        timeout: Optional upstream calculation timeout
        output: 'Output contract: validated wide tibble or raw payload'
      tags:
        apiStage: public
      examples:
      - smiles: DTXSID7020182
  POST /api/padel:
    inputs:
      query:
        required: yes
      x2d:
        required: no
        default: yes
        type: logical
      x3d:
        required: no
        default: no
        type: logical
      fp:
        required: no
        default: no
        type: logical
      headers:
        required: no
        default: no
        type: logical
      timeout:
        required: no
        default: ~
        type: object
      output:
        required: no
        default:
        - wide
        - raw
        type: character
    file: R/chemi_padel.R
    helper: generic_chemi_request
    request:
      arguments:
        endpoint:
          from:
          - hook_state
          - request
          - endpoint
        server:
          from:
          - hook_state
          - request
          - server
        auth:
          value: no
        tidy:
          value: no
        body:
          from:
          - hook_state
          - request
          - body
        content_type:
          from:
          - hook_state
          - request
          - content_type
    post_state: hook_state
    post_on_skip: yes
    docs:
      title: Padel
      return: Returns a tibble with results
      lifecycle: experimental
      parameters:
        query: Chemical structures or resolvable identifiers
        x2d: Calculate two-dimensional descriptors
        x3d: Calculate three-dimensional descriptors
        fp: Calculate fingerprints
        headers: Request descriptor headers
        timeout: Optional upstream calculation timeout
        output: 'Output contract: validated wide tibble or raw payload'
      tags:
        apiStage: public
      examples:
      - query: DTXSID1024122
documentation: yes
contracts_file: tests/testthat/fixtures/apipak/chemi-padel.rds
