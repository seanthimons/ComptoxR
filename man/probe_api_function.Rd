% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probe_api_function.R
\name{probe_api_function}
\alias{probe_api_function}
\title{Probe an API wrapper across configured servers}
\usage{
probe_api_function(function_name, ...)
}
\arguments{
\item{function_name}{A single exported \verb{chemi_*} or \verb{ct_*} function name.
Matching is case-insensitive.}

\item{...}{Arguments forwarded unchanged to the wrapper.}
}
\value{
A tibble with one row per server and columns \code{function},
\code{environment}, \code{server_id}, \code{configured_url}, \code{observed_urls},
\code{status_codes}, \code{result}, \code{valid}, and \code{error}. The observed values and
result are list-columns. A row is \code{valid} only when the call succeeds,
returns nonempty data, every observed response has status 200, and every
observed URL uses the configured server.
}
\description{
Runs one exported \verb{chemi_*} or \verb{ct_*} API wrapper against each server
recognized by \code{\link[=chemi_server]{chemi_server()}} or \code{\link[=ctx_server]{ctx_server()}}. HTTP responses are observed
internally without changing the wrapper's return value or public signature.
}
\keyword{internal}
