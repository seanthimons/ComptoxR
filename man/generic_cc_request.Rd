% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/z_generic_request.R
\name{generic_cc_request}
\alias{generic_cc_request}
\title{Generic CAS Common Chemistry API Request Function}
\usage{
generic_cc_request(
  endpoint,
  method = "GET",
  server = "cc_burl",
  auth = TRUE,
  tidy = TRUE,
  content_type = "application/json",
  paginate = FALSE,
  max_pages = 100,
  pagination_strategy = NULL,
  ...
)
}
\arguments{
\item{endpoint}{The API endpoint path (e.g., "detail", "search", "export").}

\item{method}{The HTTP method to use: "GET" (default) or "POST".}

\item{server}{Environment variable name for the base URL. Defaults to "cc_burl".}

\item{auth}{Boolean; whether to include the API key header. Defaults to TRUE.}

\item{tidy}{Boolean; whether to convert the result to a tidy tibble. Defaults to TRUE.
If FALSE, returns a cleaned list structure.}

\item{content_type}{Expected response content type. Defaults to "application/json".}

\item{paginate}{Boolean; whether to automatically fetch all pages. Defaults to FALSE.}

\item{max_pages}{Maximum pages to fetch when paginate=TRUE. Defaults to 100.}

\item{pagination_strategy}{Pagination strategy (usually "offset_limit" for CC). Defaults to NULL.}

\item{...}{Additional parameters passed as query parameters to the API.}
}
\value{
Depends on content_type and tidy parameter:
- JSON with tidy=TRUE: A tidy tibble.
- JSON with tidy=FALSE: A cleaned list structure.
- text/plain: A character string.
If no results are found, returns an empty tibble or list.
}
\description{
This is a specialized wrapper for CAS Common Chemistry API requests.
It handles the specific authentication (cc_api_key) and server configuration
for the Common Chemistry API endpoints.
}
