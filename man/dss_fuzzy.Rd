% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dss_functions.R
\name{dss_fuzzy}
\alias{dss_fuzzy}
\title{Fuzzy search DSSTox by string similarity}
\usage{
dss_fuzzy(
  query,
  method = c("jaro_winkler", "levenshtein", "damerau_levenshtein", "jaccard"),
  threshold = NULL,
  cols = "PREFERRED_NAME",
  limit = 20L,
  con = NULL
)
}
\arguments{
\item{query}{A single character string to fuzzy-match.}

\item{method}{Distance method. One of \code{"jaro_winkler"} (default),
\code{"levenshtein"}, \code{"damerau_levenshtein"}, or \code{"jaccard"}.}

\item{threshold}{Similarity/distance cutoff. For jaro_winkler and jaccard,
a minimum similarity (0–1, default 0.85). For levenshtein and
damerau_levenshtein, a maximum edit distance (default 3).}

\item{cols}{Which \code{parent_col} values to search. Defaults to
\code{"PREFERRED_NAME"}. Set to \code{NULL} to search all columns (slower).}

\item{limit}{Maximum results to return. Default 20.}

\item{con}{An optional \code{DBI::DBIConnection}.}
}
\value{
A tibble with DTXSID, parent_col, values, and a similarity or
distance score column, ordered by best match first.
}
\description{
Finds approximate matches for chemical names using DuckDB's native string
distance functions. Searches are length-blocked to keep performance
reasonable against the 15M-row table.
}
\examples{
\dontrun{
dss_fuzzy("Atrazin")
dss_fuzzy("Clorpyrifos", threshold = 0.75)
dss_fuzzy("CC(=O)O", method = "levenshtein", cols = "SMILES", threshold = 2)
}
}
\seealso{
Other dsstox: 
\code{\link{dss_cas}()},
\code{\link{dss_connect}()},
\code{\link{dss_disconnect}()},
\code{\link{dss_install}()},
\code{\link{dss_query}()},
\code{\link{dss_resolve}()},
\code{\link{dss_search}()},
\code{\link{dss_synonyms}()}
}
\concept{dsstox}
