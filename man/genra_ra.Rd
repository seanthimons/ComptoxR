% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genra.R
\name{genra_ra}
\alias{genra_ra}
\title{Searches for nearest neighbors and requests read across prediction by DTXSID}
\usage{
genra_ra(
  query,
  n = 12,
  s = 0.1,
  fp = c("chm_mrgn", "chm_httr", "chm_ct", "chm_aim", "bio_txct", "tox_txrf"),
  sel_by = c("tox_txrf", "bio_txct", "no_filter"),
  summ = c(NULL, "tox_txrf", "bio_txct", "tox_txrf_dosage")
)
}
\arguments{
\item{query}{A single DTXSID (in quotes).}

\item{n}{Number of nearest neighbors to search for. Defaults to 12.}

\item{s}{Minimum amount of Jaccard similarity to filter compounds by. Defaults to 0.1.}

\item{fp}{A fingerprint to search by. Defaults to Morgan fingerprints.}

\item{sel_by}{Select compounds by data sources. Defaults to \code{ToxRef}database.}

\item{summ}{Summarize results by data source. Defaults to \code{NULL} (no summarization).}
}
\value{
Returns a tibble with results.
}
\description{
Searches for nearest neighbors and requests read across prediction by DTXSID
}
