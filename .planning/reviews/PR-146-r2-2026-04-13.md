# PR #146 Review: feat: ToxValDB integration, PT curation script, and DB release workflows

**Branch:** feat/toxvaldb-integration -> integration
**Files changed:** 47 (14 new, 33 modified) | ~3,986 lines added
**Mergeability:** CLEAN
**Review round:** 2
**Date:** 2026-04-13

---

## Round 1 HIGH findings — verification

| # | Finding | Status |
|---|---------|--------|
| 1 | All ToxValDB tests call `tox_*` but functions exported as `toxval_*` | ✅ FIXED |
| 2 | Env var `"toxval_burl()"` read inconsistently without parens | ✅ FIXED |
| 3 | `human_eco` filter crashes — column absent from v9.7.0 schema | ✅ FIXED |
| 4 | GitHub Actions workflows missing `permissions: contents: write` | ✅ FIXED |
| 5 | `data-raw/pt.R` uses `polite` and `tidyverse` not in DESCRIPTION | ✅ FIXED |

**Notes on each:**
- **#1**: Test files now uniformly call `toxval_results()`, `toxval_path()`, `toxval_server()`, etc. Verified in both test files.
- **#2**: `zzz.R` and `tox_connection.R` now consistently use `"toxval_burl"` (no parens) in all `Sys.setenv()` / `Sys.getenv()` calls. The routing logic in `.tox_get_con()` and `.tox_route()` will now correctly match the set env var.
- **#3**: `human_eco` does not appear anywhere in `R/tox_functions.R`. The parameter, filter clause, and documentation were all removed.
- **#4**: All three `db-*.yml` workflows have a `permissions: contents: write` block at the job level. Verified in the diff.
- **#5**: `library(polite)` and `library(tidyverse)` are gone from `data-raw/pt.R`. The script now uses individual packages (`dplyr`, `tidyr`, `stringr`, `purrr`, `tibble`, `rvest`, `duckdb`, `ComptoxR`) — all of which are declared in DESCRIPTION Imports or Suggests.

---

## HIGH -- Must fix before merge

1. **Stale `tox_install()` references in 6 user-visible messages** (`R/tox_connection.R:53,54,107`, `R/tox_functions.R:26`, `R/zzz.R:513,923`) `[claude]`
   - The rename from `tox_install()` to `toxval_install()` (the round 1 HIGH #1 fix) was applied to test files and the function definition, but error messages and CLI alerts were not updated. Six locations still point users to `tox_install()` which does not exist:
     - `tox_connection.R:53-54`: "Run `tox_install()` to download from GitHub Releases. Or run `tox_install(source = ...)` to install from a local file." — shown when database is not found
     - `tox_connection.R:107`: "Use `tox_install(overwrite = TRUE)` to replace it." — shown when database exists and overwrite is FALSE
     - `tox_functions.R:26`: "Run `tox_install()` to set up the local database." — shown when DuckDB path doesn't exist
     - `zzz.R:513`: "Run `tox_install()` to set up." — shown during `toxval_server(1)` when DB missing
     - `zzz.R:923`: "Run `tox_install()` to enable local ToxValDB queries." — shown in startup header
   - Any first-time user who hits one of these errors (the database-missing path is hit on every first install attempt) will be directed to run a function that doesn't exist, getting `Error: could not find function "tox_install"`. This is a user-facing silent failure.
   - Fix: Replace all 6 occurrences of `tox_install` with `toxval_install` in the three files above. This is a simple search-and-replace.

---

## Verified correct

- **All 5 round 1 HIGH items resolved**: See table above.
- **No regressions in CI workflows**: The three `db-*.yml` workflows retain all functionality with the permissions block added correctly.
- **No regressions in test coverage**: Test files correctly reference `toxval_*` exported functions and internal `.tox_*` helpers. Env var name `"toxval_burl"` is used consistently in test fixtures (`withr::with_envvar`).
- **`data-raw/pt.R` API key guard added**: `if (!nzchar(Sys.getenv("ctx_api_key"))) stop(...)` is present at line 27 — this resolves the round 1 MEDIUM #11 finding as a bonus fix.
- **`toxval_install()` function definition is correctly named**: The function signature at `tox_connection.R:95` uses `toxval_install`. Only the error messages inside and elsewhere reference the old name.

---

**Recommendation:** Ship after fixing item 1. The single remaining HIGH is a trivial 6-line search-and-replace (`tox_install` → `toxval_install`) across three files. No architectural changes are needed. Once that fix lands, all round 1 blockers are resolved and no new functionality regressions exist.

---

**Convergence:** Round 2/3 | 1 open HIGH | 0 new issues filed | Verdict: Fix item 1, then ship
