---
phase: 14-integration-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-pipeline-integration.R
  - tests/testthat/fixtures/_vcr/integration-ctx-hazard.yml
  - tests/testthat/fixtures/_vcr/integration-chemi-safety.yml
autonomous: true

must_haves:
  truths:
    - "Integration tests parse real production schemas from schema/ directory"
    - "Generated stubs are syntactically valid R code (parse without error)"
    - "Generated functions execute and return data (with VCR-mocked API calls)"
    - "Both OpenAPI 3.0 (ctx) and Swagger 2.0 (chemi) schemas are tested"
  artifacts:
    - path: "tests/testthat/test-pipeline-integration.R"
      provides: "End-to-end integration tests for stub generation pipeline"
      min_lines: 80
      contains: "vcr::use_cassette"
    - path: "tests/testthat/fixtures/_vcr/integration-ctx-hazard.yml"
      provides: "VCR cassette for CompTox Dashboard API responses"
    - path: "tests/testthat/fixtures/_vcr/integration-chemi-safety.yml"
      provides: "VCR cassette for Cheminformatics API responses"
  key_links:
    - from: "tests/testthat/test-pipeline-integration.R"
      to: "schema/ctx-hazard-prod.json"
      via: "jsonlite::fromJSON()"
      pattern: "ctx-hazard-prod"
    - from: "tests/testthat/test-pipeline-integration.R"
      to: "schema/chemi-safety-prod.json"
      via: "jsonlite::fromJSON()"
      pattern: "chemi-safety-prod"
    - from: "tests/testthat/test-pipeline-integration.R"
      to: "helper-pipeline.R::source_pipeline_files()"
      via: "function call"
      pattern: "source_pipeline_files\\(\\)"
    - from: "tests/testthat/test-pipeline-integration.R"
      to: "generated function invocation"
      via: "eval(parse(text = stub)) then call with test DTXSID"
      pattern: "DTXSID\\d+"

epi_suite_exclusion: |
  EPI Suite schemas excluded from integration tests.
  Reason: No epi-*-prod.json schemas exist in schema/ directory.
  The schema/ directory only contains ctx-* and chemi-* schemas.
  CONTEXT.md decision to test all 3 microservices was made before
  schema inventory was verified. EPI Suite integration can be added
  when/if EPI Suite schemas become available.
---

<objective>
Create end-to-end integration tests that verify the complete stub generation pipeline works with real production schemas.

Purpose: Validate that the pipeline can parse real OpenAPI/Swagger schemas, generate valid R function stubs, and that those stubs can actually execute (with VCR-mocked API calls).

Output: test-pipeline-integration.R with VCR cassettes for ctx and chemi microservices

**Note on EPI Suite:** CONTEXT.md specified testing 3 microservices (CompTox, Cheminformatics, EPI Suite), but no EPI Suite schemas exist in schema/ directory. Integration tests cover the 2 microservices with available schemas. EPI Suite can be added when schemas become available.
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-integration-ci/14-CONTEXT.md
@.planning/phases/14-integration-ci/14-RESEARCH.md

# Existing test infrastructure
@tests/testthat/helper-pipeline.R
@tests/testthat/helper-vcr.R

# Production schemas to test (representative samples)
@schema/ctx-hazard-prod.json
@schema/chemi-safety-prod.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test file with E2E tests</name>
  <files>tests/testthat/test-pipeline-integration.R</files>
  <action>
Create tests/testthat/test-pipeline-integration.R with end-to-end integration tests.

**Structure:**
```r
# Pipeline integration tests - End-to-end verification
# Tests complete pipeline: schema -> stub -> execution

describe("E2E: CompTox Dashboard Pipeline", {
  test_that("generates valid stubs from ctx-hazard-prod schema", {
    skip_on_cran()  # Integration tests only in CI/local dev

    # Skip if no cassette AND no API key (first-run scenario)
    cassette_path <- here::here("tests/testthat/fixtures/_vcr/integration-ctx-hazard.yml")
    has_cassette <- file.exists(cassette_path)
    has_api_key <- nzchar(Sys.getenv("ctx_api_key"))
    skip_if(!has_cassette && !has_api_key,
            message = "First run requires ctx_api_key to record cassette")

    vcr::use_cassette("integration-ctx-hazard", {
      # 1. Load real production schema
      schema_path <- here::here("schema/ctx-hazard-prod.json")
      schema <- jsonlite::fromJSON(schema_path, simplifyVector = FALSE)

      # 2. Source pipeline
      source_pipeline_files()

      # 3. Parse schema to spec
      spec <- openapi_to_spec(schema)
      expect_s3_class(spec, "data.frame")
      expect_true(nrow(spec) > 0)

      # 4. Generate stub for first endpoint
      first_endpoint <- spec[1, ]
      stub <- build_function_stub(
        fn = paste0("ct_", sanitize_name(first_endpoint$endpoint)),
        endpoint = first_endpoint$endpoint,
        # ... extract parameters from spec row
      )

      # 5. Verify stub is valid R code
      expect_type(stub, "character")
      parsed <- tryCatch(parse(text = stub), error = function(e) NULL)
      expect_false(is.null(parsed), info = "Generated stub must be valid R syntax")

      # 6. Execute stub to define the function
      eval(parse(text = stub))

      # 7. Call generated function with test DTXSID and verify result
      test_dtxsid <- "DTXSID7020182"  # Aspirin - known chemical
      fn_name <- paste0("ct_", sanitize_name(first_endpoint$endpoint))
      generated_fn <- get(fn_name)
      result <- generated_fn(test_dtxsid)

      # Verify result is valid (tibble or list with data)
      expect_true(
        is.data.frame(result) || is.list(result),
        info = "Generated function must return data frame or list"
      )
      if (is.data.frame(result)) {
        expect_true(nrow(result) >= 0, info = "Result should be a tibble")
      }
    })
  })
})

describe("E2E: Cheminformatics Pipeline", {
  # Similar pattern for Swagger 2.0 schema (chemi-safety-prod.json)
  # Include skip_if() for cassette/API key scenario
  # Include step 7 to call generated function with test DTXSID
})

# Note: EPI Suite excluded - no epi-*-prod.json schemas in schema/ directory
```

**Key patterns:**
- Use describe() blocks to group by microservice
- Use vcr::use_cassette() to record/replay API calls
- Use skip_on_cran() since integration tests require VCR cassettes
- **Add skip_if() for first-run scenario: no cassette AND no API key**
- Parse schema with jsonlite::fromJSON(simplifyVector = FALSE)
- Call source_pipeline_files() to load all pipeline functions
- Verify stub syntax with parse(text = stub)
- **Step 7: Call generated function with test DTXSID and verify result type**
- Test at least one endpoint per microservice actually executes

**Coverage:**
- CompTox Dashboard (ctx-hazard-prod.json) - OpenAPI 3.0
- Cheminformatics (chemi-safety-prod.json) - Swagger 2.0
- **EPI Suite - EXCLUDED: no epi-* schemas exist in schema/ directory**

**Error handling:**
- If stub generation fails, capture the stub and error for debugging
- Use tryCatch to gracefully handle parsing failures
- Use skip_if() to gracefully handle missing cassette + no API key scenario
  </action>
  <verify>
Run: `Rscript -e "testthat::test_file('tests/testthat/test-pipeline-integration.R')"`
Expect: All tests pass (may need API key first run to record cassettes) OR skip gracefully if no cassette + no API key
  </verify>
  <done>
- test-pipeline-integration.R exists with describe() blocks for ctx and chemi
- Tests parse real production schemas
- Tests verify generated stubs are valid R syntax
- Tests use vcr::use_cassette() for API mocking
- Tests include skip_if() for missing cassette + no API key scenario
- Tests call generated function with test DTXSID and verify result type
- Comment documents EPI Suite exclusion (no schemas available)
  </done>
</task>

<task type="auto">
  <name>Task 2: Record VCR cassettes for integration tests</name>
  <files>
tests/testthat/fixtures/_vcr/integration-ctx-hazard.yml
tests/testthat/fixtures/_vcr/integration-chemi-safety.yml
  </files>
  <action>
Run the integration tests with a valid API key to record VCR cassettes.

**Process:**
1. Ensure ctx_api_key environment variable is set
2. Delete any existing cassettes: rm tests/testthat/fixtures/_vcr/integration-*.yml
3. Run tests to record: Rscript -e "devtools::test(filter = 'pipeline-integration')"
4. Verify cassettes created in tests/testthat/fixtures/_vcr/
5. Check cassettes for security: Run check_cassette_safety() from helper-vcr.R pattern
6. Ensure API keys show as <<<API_KEY>>> (not real keys)

**If no API key available:**
- Tests will skip gracefully due to skip_if() logic added in Task 1
- Document that first run requires API key to record cassettes
- Create a placeholder README or comment noting cassettes need recording
- Subsequent runs use recorded cassettes (no key needed)

**First-run detection logic (already in test file from Task 1):**
```r
cassette_path <- here::here("tests/testthat/fixtures/_vcr/integration-ctx-hazard.yml")
has_cassette <- file.exists(cassette_path)
has_api_key <- nzchar(Sys.getenv("ctx_api_key"))
skip_if(!has_cassette && !has_api_key,
        message = "First run requires ctx_api_key to record cassette")
```

**Cassette naming convention:**
- integration-{microservice}-{endpoint}.yml
- Example: integration-ctx-hazard.yml, integration-chemi-safety.yml

**Cassette location:**
- tests/testthat/fixtures/_vcr/ (matches vcr_dir in helper-vcr.R)
  </action>
  <verify>
Check cassettes exist: `ls tests/testthat/fixtures/_vcr/integration-*.yml`
Check security: `grep -r "api_key" tests/testthat/fixtures/_vcr/integration-*.yml` should show <<<API_KEY>>> not real keys
Run tests again without API key: Tests should pass using recorded cassettes OR skip if cassettes don't exist
  </verify>
  <done>
- VCR cassettes exist for ctx and chemi microservices (if API key was available)
- Cassettes have API keys sanitized (show <<<API_KEY>>>)
- Tests pass using recorded cassettes (no live API calls needed)
- Tests skip gracefully if no cassettes and no API key (first-run scenario)
  </done>
</task>

</tasks>

<verification>
1. `testthat::test_file("tests/testthat/test-pipeline-integration.R")` - all tests pass or skip gracefully
2. VCR cassettes exist in fixtures/_vcr/ (if API key was available for recording)
3. API keys sanitized in cassettes (no real keys exposed)
4. Tests work offline (using recorded cassettes)
5. Both OpenAPI 3.0 (ctx) and Swagger 2.0 (chemi) schemas tested
6. EPI Suite exclusion documented (no schemas available)
7. Generated functions called with test DTXSID and results verified
</verification>

<success_criteria>
- Integration tests parse real production schemas from schema/ directory
- Generated stubs are syntactically valid R code
- Generated functions can execute and return data (with VCR mocking)
- VCR cassettes recorded and sanitized (when API key available)
- Tests pass on subsequent runs without API key
- Tests skip gracefully on first run without API key (no cassettes yet)
- EPI Suite exclusion documented with clear reasoning
</success_criteria>

<output>
After completion, create `.planning/phases/14-integration-ci/14-01-SUMMARY.md`
</output>
