---
phase: 02-validate-and-regenerate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/ct_chemical_search_equal.R
autonomous: true

must_haves:
  truths:
    - "ct_chemical_search_equal_bulk() signature includes query parameter"
    - "devtools::document() completes without errors"
    - "Generated roxygen2 documentation is valid"
  artifacts:
    - path: "R/ct_chemical_search_equal.R"
      provides: "Regenerated ct_chemical_search_equal_bulk() with query parameter"
      contains: "ct_chemical_search_equal_bulk <- function(query"
    - path: "man/ct_chemical_search_equal_bulk.Rd"
      provides: "Generated documentation for bulk function"
  key_links:
    - from: "dev/endpoint_eval/07_stub_generation.R"
      to: "R/ct_chemical_search_equal.R"
      via: "scaffold_files() output"
      pattern: "function\\(query"
---

<objective>
Regenerate ct_chemical_search_equal_bulk() function stub and validate it has correct signature

Purpose: Verify that Phase 1 fixes (extract_body_properties + build_function_stub) produce correct function signatures for POST endpoints with simple body schemas
Output: Regenerated R/ct_chemical_search_equal.R with proper query parameter and passing devtools::document()
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-validate-and-regenerate/02-RESEARCH.md
@.planning/phases/01-fix-body-parameter-extraction/01-01-SUMMARY.md
@.planning/phases/01-fix-body-parameter-extraction/01-02-SUMMARY.md
@dev/ct_endpoint_eval.R
@R/ct_chemical_search_equal.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate ct_chemical_search_equal_bulk stub</name>
  <files>R/ct_chemical_search_equal.R</files>
  <action>
Delete the existing ct_chemical_search_equal_bulk() function from R/ct_chemical_search_equal.R (lines 1-28, keep the GET function ct_chemical_search_equal starting at line 31).

Then regenerate the POST function stub using the modified pipeline:

```r
# In R session:
source(here::here("dev", "endpoint_eval_utils.R"))
library(jsonlite)
library(tidyverse)

# Configuration
ct_config <- list(
  wrapper_function = "generic_request",
  param_strategy = "extra_params",
  example_query = "DTXSID7020182",
  lifecycle_badge = "experimental"
)

# Load chemical schema
openapi <- jsonlite::fromJSON(
  here::here("schema", "ctx_chemical_prod.json"),
  simplifyVector = FALSE
)

# Parse and filter to target endpoint
endpoints <- openapi_to_spec(openapi)
target <- endpoints %>%
  filter(grepl("chemical/search/equal", route), method == "POST")

# Apply same transformations as main script
target <- target %>%
  mutate(
    route = strip_curly_params(route, leading_slash = 'remove'),
    file = "ct_chemical_search_equal.R",
    fn = "ct_chemical_search_equal_bulk",
    batch_limit = NULL  # POST endpoint
  )

# Render stub
spec_with_text <- render_endpoint_stubs(target, config = ct_config)

# Inspect generated code
cat(spec_with_text$text)
```

The generated stub should have:
- `function(query)` signature (not empty)
- `paste(query, collapse = "\n")` for newline collapsing
- `Sys.getenv("batch_limit", "1000")` for batch configuration
- Proper roxygen2 documentation with @param query

Prepend the regenerated function to R/ct_chemical_search_equal.R before the existing GET function.
  </action>
  <verify>
```r
# Verify function signature
source("R/ct_chemical_search_equal.R")
fn_text <- readLines("R/ct_chemical_search_equal.R")
# Check for query parameter in bulk function signature
stopifnot(any(grepl("ct_chemical_search_equal_bulk <- function\\(query", fn_text)))
# Check for newline collapse
stopifnot(any(grepl('paste\\(query, collapse = "\\\\n"\\)', fn_text)))
# Check for Sys.getenv batch_limit
stopifnot(any(grepl('Sys\\.getenv\\("batch_limit"', fn_text)))
```
  </verify>
  <done>R/ct_chemical_search_equal.R contains ct_chemical_search_equal_bulk(query) with newline collapse and Sys.getenv batch_limit</done>
</task>

<task type="auto">
  <name>Task 2: Validate function signature programmatically</name>
  <files>R/ct_chemical_search_equal.R</files>
  <action>
After regeneration, verify the function signature meets VAL-01 requirement using R's formals() function:

```r
# Load package functions
devtools::load_all()

# Get formal parameters
fn <- ct_chemical_search_equal_bulk
params <- names(formals(fn))

# VAL-01: Check for query parameter
# Note: REQUIREMENTS.md says "words" but Phase 1 implementation uses "query"
# Using "query" for consistency with Phase 1 decisions
if (!"query" %in% params) {
  cli::cli_abort("VAL-01 FAILED: ct_chemical_search_equal_bulk missing 'query' parameter")
}

cli::cli_alert_success("VAL-01 PASSED: ct_chemical_search_equal_bulk has 'query' parameter")

# Verify no other unexpected parameters (should only have query)
expected_params <- c("query")
if (!setequal(params, expected_params)) {
  cli::cli_warn("Unexpected parameters: {setdiff(params, expected_params)}")
}
```
  </action>
  <verify>
```r
devtools::load_all()
params <- names(formals(ct_chemical_search_equal_bulk))
stopifnot("query" %in% params)
print(paste("Function parameters:", paste(params, collapse = ", ")))
```
  </verify>
  <done>ct_chemical_search_equal_bulk has query parameter in signature (VAL-01 satisfied)</done>
</task>

<task type="auto">
  <name>Task 3: Run devtools::document() and verify success</name>
  <files>man/ct_chemical_search_equal_bulk.Rd, NAMESPACE</files>
  <action>
Run devtools::document() to generate/update .Rd documentation files from roxygen2 comments:

```r
# Run documentation generation
devtools::document()

# VAL-02: If we get here without error, documentation generation succeeded
cli::cli_alert_success("VAL-02 PASSED: devtools::document() completed successfully")

# Verify .Rd file was created/updated
rd_file <- "man/ct_chemical_search_equal_bulk.Rd"
if (!file.exists(rd_file)) {
  cli::cli_abort("Documentation file not created: {rd_file}")
}

# Verify NAMESPACE has export
namespace_lines <- readLines("NAMESPACE")
if (!any(grepl("export\\(ct_chemical_search_equal_bulk\\)", namespace_lines))) {
  cli::cli_abort("Function not exported in NAMESPACE")
}

cli::cli_alert_success("Documentation and NAMESPACE verified")
```

If devtools::document() fails with roxygen2 errors:
1. Check for unescaped special characters in @param or @description
2. Verify all roxygen2 tags are closed properly
3. Check for invalid @return or @examples syntax
  </action>
  <verify>
```bash
# Run R CMD check to validate documentation
cd "C:\Users\sxthi\Documents\ComptoxR" && R CMD check --no-tests --no-examples --no-vignettes . 2>&1 | head -50
```

Or in R:
```r
file.exists("man/ct_chemical_search_equal_bulk.Rd")
any(grepl("export\\(ct_chemical_search_equal_bulk\\)", readLines("NAMESPACE")))
```
  </verify>
  <done>devtools::document() runs without errors, .Rd file exists, function is exported (VAL-02 satisfied)</done>
</task>

</tasks>

<verification>
All three validation checks must pass:

1. **Signature Check (VAL-01):**
```r
devtools::load_all()
"query" %in% names(formals(ct_chemical_search_equal_bulk))
```

2. **Documentation Check (VAL-02):**
```r
devtools::document()  # Must complete without error
file.exists("man/ct_chemical_search_equal_bulk.Rd")
```

3. **Code Generation Check:**
```r
fn_text <- readLines("R/ct_chemical_search_equal.R")
any(grepl('paste\\(query, collapse = "\\\\n"\\)', fn_text))
any(grepl('Sys\\.getenv\\("batch_limit"', fn_text))
```
</verification>

<success_criteria>
- ct_chemical_search_equal_bulk(query) exists with proper signature
- Function has newline collapse for string array input
- Function uses Sys.getenv("batch_limit") for runtime configuration
- devtools::document() completes without errors
- man/ct_chemical_search_equal_bulk.Rd exists
- Function is exported in NAMESPACE
</success_criteria>

<output>
After completion, create `.planning/phases/02-validate-and-regenerate/02-01-SUMMARY.md`
</output>
