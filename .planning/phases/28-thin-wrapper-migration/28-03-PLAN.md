---
phase: 28-thin-wrapper-migration
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - R/ct_hazard.R
  - R/ct_cancer.R
  - R/ct_env_fate.R
  - R/ct_demographic_exposure.R
  - R/ct_functional_use.R
  - R/ct_descriptors.R
  - R/ct_genotox.R
  - man/ct_hazard.Rd
  - man/ct_cancer.Rd
  - man/ct_env_fate.Rd
  - man/ct_demographic_exposure.Rd
  - man/ct_functional_use.Rd
  - man/ct_functional_use_probability.Rd
  - man/ct_descriptors.Rd
  - NAMESPACE
autonomous: true
requirements:
  - HOOK-28-05
  - HOOK-28-06

must_haves:
  truths:
    - "8 pure pass-through wrappers are deleted (ct_hazard, ct_cancer, ct_env_fate, ct_demographic_exposure, ct_general_exposure, ct_functional_use, ct_functional_use_probability, ct_genotox)"
    - "ct_descriptors (deprecated INDIGO endpoint) is deleted"
    - "Package builds cleanly after deletions (devtools::document() + devtools::check())"
    - "Generated stubs that the deleted wrappers delegated to still exist and work"
  artifacts:
    - path: "NAMESPACE"
      provides: "Updated exports without deleted wrapper functions"
  key_links:
    - from: "NAMESPACE"
      to: "R/ct_hazard_toxval_search.R"
      via: "ct_hazard_toxval_search_bulk still exported after ct_hazard deleted"
      pattern: "export.*ct_hazard_toxval_search_bulk"
---

<objective>
Delete all pure pass-through wrapper functions and the deprecated ct_descriptors function. These wrappers are 1-line delegations to generated stubs that already exist — the wrappers add no behavior worth preserving.

Purpose: Clean break per user decision. Users call generated stub names directly (e.g., ct_hazard_toxval_search_bulk instead of ct_hazard). Removes 10 maintenance files.
Output: Deleted R files, updated NAMESPACE and man/ pages
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-thin-wrapper-migration/28-CONTEXT.md

<interfaces>
<!-- Functions to DELETE and their delegation targets (all confirmed to exist as generated stubs) -->
ct_hazard(query) -> ct_hazard_toxval_search_bulk(query)    [R/ct_hazard_toxval_search.R]
ct_cancer(query) -> ct_hazard_cancer_search_bulk(query)     [R/ct_hazard_cancer_search.R]
ct_env_fate(query) -> ct_chemical_fate_search_bulk(query)   [R/ct_chemical_fate_search.R]
ct_demographic_exposure(query) -> ct_exposure_seem_demographic_search_bulk(query)
ct_general_exposure(query) -> ct_exposure_seem_general_search_bulk(query)
ct_functional_use(query) -> ct_exposure_functional_use_search_bulk(query)
ct_functional_use_probability(query) -> ct_exposure_functional_use_probability_search(dtxsid)
ct_genotox(query) -> ct_hazard_genetox_search_bulk(query)   [verify this target exists]
ct_descriptors(query, type) -> DELETE (deprecated INDIGO endpoint, no generated stub)

<!-- ct_synonym: Also delete if empty file -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify delegation targets exist, then delete wrappers</name>
  <files>R/ct_hazard.R, R/ct_cancer.R, R/ct_env_fate.R, R/ct_demographic_exposure.R, R/ct_functional_use.R, R/ct_descriptors.R, R/ct_genotox.R</files>
  <action>
BEFORE deleting anything, verify each delegation target function actually exists as a generated stub:

1. Confirm these files exist in R/:
   - ct_hazard_toxval_search.R (exports ct_hazard_toxval_search_bulk)
   - ct_hazard_cancer_search.R (exports ct_hazard_cancer_search_bulk)
   - ct_chemical_fate_search.R (exports ct_chemical_fate_search_bulk)
   - ct_exposure_seem_demographic_search.R (exports bulk variant)
   - ct_exposure_seem_general_search.R (exports bulk variant)
   - ct_exposure_functional_use_search.R (exports bulk variant)
   - ct_exposure_functional_use_probability_search.R (exports search variant)
   - ct_hazard_genetox_search.R OR ct_hazard_genetox_details_search.R (for ct_genotox target — READ ct_genotox.R first to confirm exact target)

2. If ANY target is missing, DO NOT delete its wrapper. Note the gap and proceed with others.

3. Delete these R source files (git rm):
   - R/ct_hazard.R
   - R/ct_cancer.R
   - R/ct_env_fate.R
   - R/ct_demographic_exposure.R (contains both ct_demographic_exposure AND ct_general_exposure)
   - R/ct_functional_use.R (contains both ct_functional_use AND ct_functional_use_probability)
   - R/ct_descriptors.R (deprecated INDIGO endpoint)
   - R/ct_genotox.R (if target verified)

4. Check R/ct_synonym.R — if it's an empty file or stub with no code, delete it too. If it has real implementation, leave it.

5. Run devtools::document() to regenerate NAMESPACE and man/ pages. The deleted functions' .Rd files will be automatically removed.

6. Verify no other R files reference the deleted function names (grep for "ct_hazard(" etc. excluding test files). If any non-test files reference them, note as known breakage (expected per user decision — no deprecation shim).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::document(); cat('document OK\n')" && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('load_all OK\n')"</automated>
  </verify>
  <done>All 8 pure pass-through wrappers deleted. ct_descriptors deleted. NAMESPACE updated. Package loads cleanly with devtools::load_all(). Generated stub targets confirmed to exist before deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Update test files and NEWS.md for breaking changes</name>
  <files>tests/testthat/test-ct_hazard.R, tests/testthat/test-ct_cancer.R, NEWS.md</files>
  <action>
1. Search tests/testthat/ for test files that directly call the deleted wrapper functions:
   - test-ct_hazard.R, test-ct_cancer.R, test-ct_env_fate.R, test-ct_demographic_exposure.R, test-ct_functional_use.R, test-ct_descriptors.R, test-ct_genotox.R

2. For each test file found:
   - If the test calls the deleted friendly-name wrapper (e.g., `ct_hazard()`), update it to call the generated stub name instead (e.g., `ct_hazard_toxval_search_bulk()`)
   - If the test file ONLY tests the deleted wrapper with no other value, delete the test file
   - Leave VCR cassettes intact — they test the underlying API behavior which is unchanged

3. For ct_descriptors tests: delete entirely (deprecated endpoint, no generated replacement)

4. Add breaking change entry to NEWS.md (create file if it doesn't exist, or append to existing):
   ```
   # ComptoxR (development version)

   ## Breaking Changes

   * Removed friendly-name wrapper functions: `ct_hazard()`, `ct_cancer()`,
     `ct_env_fate()`, `ct_demographic_exposure()`, `ct_general_exposure()`,
     `ct_functional_use()`, `ct_functional_use_probability()`, `ct_genotox()`.
     Use the generated stub names directly (e.g., `ct_hazard_toxval_search_bulk()`
     instead of `ct_hazard()`).
   * Removed deprecated `ct_descriptors()` (INDIGO endpoint not in published API schemas).
   ```

5. Run devtools::test() to verify no test failures from the deletions (some tests may already fail from VCR issues — that's pre-existing, not caused by this change).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::document(); devtools::load_all(); cat('Build clean after test updates\n')"</automated>
  </verify>
  <done>Test files updated to use generated stub names. NEWS.md documents all breaking changes. Package builds and loads cleanly. No new test failures introduced by wrapper deletion.</done>
</task>

</tasks>

<verification>
- `devtools::load_all()` succeeds (no missing function references)
- `devtools::document()` runs clean
- Deleted functions no longer appear in NAMESPACE
- Generated stub targets still exported and functional
- NEWS.md documents all breaking changes
</verification>

<success_criteria>
- 8 pass-through wrappers + ct_descriptors deleted
- Package builds cleanly after deletion
- Test files updated to reference generated stub names
- Breaking changes documented in NEWS.md
</success_criteria>

<output>
After completion, create `.planning/phases/28-thin-wrapper-migration/28-03-SUMMARY.md`
</output>
