---
phase: 28-thin-wrapper-migration
plan: 05
type: execute
wave: 4
depends_on: ["28-04"]
files_modified:
  - tests/testthat/test-ct_bioactivity.R
  - tests/testthat/test-ct_lists_all.R
  - dev/generate_tests.R
  - NEWS.md
autonomous: true
requirements:
  - HOOK-28-09
  - HOOK-28-10

must_haves:
  truths:
    - "Regenerated stubs include hook params in signatures and hook calls in bodies"
    - "devtools::check() produces no new errors from migration"
    - "Test generator updated to read hook_config.yml for test variant generation"
  artifacts:
    - path: "dev/generate_tests.R"
      provides: "Updated test generator with hook_config.yml awareness"
      contains: "hook_config"
    - path: "NEWS.md"
      provides: "Complete migration changelog with all breaking changes"
      contains: "Breaking Changes"
  key_links:
    - from: "dev/generate_tests.R"
      to: "inst/hook_config.yml"
      via: "Reads extra_params to generate test variants with hook params"
      pattern: "hook_config|extra_params"
---

<objective>
Regenerate affected stubs with hook integration, update the test generator for hook awareness, run full validation suite, and finalize the migration changelog.

Purpose: End-to-end validation that the hook system works in production. Regenerated stubs must have correct signatures, hook calls, and pass existing tests. Test generator needs hook awareness for future stub+test generation.
Output: Regenerated stub files, updated test generator, final NEWS.md, clean R CMD check
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-thin-wrapper-migration/28-CONTEXT.md
@.planning/phases/28-thin-wrapper-migration/28-01-SUMMARY.md
@.planning/phases/28-thin-wrapper-migration/28-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate hook-configured stubs and validate signatures</name>
  <files>R/ct_bioactivity_data_search.R, R/ct_chemical_list_all.R, R/ct_hazard_toxval_search.R</files>
  <action>
Run the stub generator for ONLY the functions listed in inst/hook_config.yml to regenerate them with hook parameters and hook calls. Do NOT regenerate all 300+ stubs — only the hook-configured ones.

1. Read inst/hook_config.yml to get the list of function names with hooks
2. For each hook-configured function, run the generator (or manually apply the hook parameter injection pattern if the generator doesn't support selective regeneration)
3. After regeneration, verify each regenerated stub:
   a. Has the extra_params in its formal arguments (e.g., ct_bioactivity_data_search_bulk should have `annotate = FALSE`)
   b. Has run_hook() calls in its body (pre_request, post_response, or transform as configured)
   c. Has @param documentation for each extra param
   d. Still has correct endpoint, method, batch_limit from original stub

4. If the generator doesn't support selective regeneration, manually edit the affected stub files to add:
   - Extra parameters to function signature
   - run_hook() calls at appropriate lifecycle points
   - @param docs for extra params

5. Run devtools::document() to update NAMESPACE and man/ pages with new parameters.

6. Run dev/check_hook_config.R to validate no drift between config and generated formals.

IMPORTANT: For ct_lists_all's transform hook — the generated ct_chemical_list_all stub is a static endpoint. The transform hook (lists_all_transform) replaces the default response handling. The stub should call run_hook("ct_lists_all", "transform", ...) INSTEAD of returning the raw result. If the function name in hook_config.yml doesn't match the stub name (ct_lists_all vs ct_chemical_list_all), reconcile — either rename the YAML key to match the stub, or add a mapping.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::document(); devtools::load_all(); cat('Regenerated stubs load OK\n')" && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" dev/check_hook_config.R</automated>
  </verify>
  <done>All hook-configured stubs regenerated with correct signatures, hook calls, and @param docs. CI drift check passes. Package loads cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update test generator, fix test files, run full check</name>
  <files>dev/generate_tests.R, tests/testthat/test-ct_bioactivity.R, tests/testthat/test-ct_lists_all.R, NEWS.md</files>
  <action>
**Part A: Update test generator for hook awareness**

Read dev/generate_tests.R and add a section that:
1. Reads inst/hook_config.yml (if exists)
2. For each function with extra_params, generates additional test variants that exercise the hook parameters:
   - e.g., for annotate=TRUE/FALSE: generate two test cases per function
   - e.g., for coerce=TRUE: generate a test case with the extra param
3. Ensures generated test cassette names include the param variant (e.g., "ct_bioactivity_data_search_annotated")

This is an extension, not a rewrite. Add the hook-config reading after the existing test generation logic.

**Part B: Update existing test files**

For test files that reference deleted wrapper functions:
- test-ct_bioactivity.R: If it calls ct_bioactivity(), update to call the individual stubs (ct_bioactivity_data_search_bulk, etc.) with appropriate hook params. Keep existing VCR cassettes.
- test-ct_lists_all.R: If it calls ct_lists_all(), update to call ct_chemical_list_all() with hook params. Keep existing VCR cassettes.
- Any other test files referencing deleted wrappers: update similarly.

**Part C: Final validation**

1. Run `devtools::test()` — note which tests pass/fail. Pre-existing VCR failures (297 from missing cassettes) are expected and not caused by this migration.
2. Run `devtools::check()` — target: 0 new errors, 0 new warnings beyond pre-existing doc-width warnings.
3. Run `Rscript dev/check_hook_config.R` — must pass.

**Part D: Finalize NEWS.md**

Ensure NEWS.md has a complete list of all breaking changes from the entire Phase 28:
- Deleted wrapper functions (from Plan 03)
- Deleted ct_descriptors
- New hook parameter system
- ct_bioactivity split into 4 stubs with annotate param
- ct_lists_all replaced by ct_chemical_list_all with return_dtxsid/coerce params
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::check()" 2>&1 | tail -20</automated>
  </verify>
  <done>Test generator updated with hook_config.yml awareness. Test files updated for new function names. devtools::check() produces no new errors/warnings. NEWS.md complete with all Phase 28 breaking changes. CI drift check passes.</done>
</task>

</tasks>

<verification>
- `devtools::check()` produces 0 new errors, 0 new warnings
- `Rscript dev/check_hook_config.R` passes
- `devtools::test(filter='hook')` passes all hook tests
- NEWS.md documents complete migration changelog
- Generated stubs have correct hook params and hook calls
</verification>

<success_criteria>
- All hook-configured stubs regenerated and validated
- Test generator reads hook_config.yml for variant generation
- Full R CMD check clean (no new issues)
- Complete migration changelog in NEWS.md
- Phase 28 migration fully operational
</success_criteria>

<output>
After completion, create `.planning/phases/28-thin-wrapper-migration/28-05-SUMMARY.md`
</output>
