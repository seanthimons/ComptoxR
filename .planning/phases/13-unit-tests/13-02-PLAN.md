---
phase: 13-unit-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-pipeline-openapi-parser.R
  - tests/testthat/test-pipeline-stub-generation.R
autonomous: true

must_haves:
  truths:
    - "openapi_to_spec() parses OpenAPI 3.0 schemas into tibble"
    - "openapi_to_spec() parses Swagger 2.0 schemas into tibble"
    - "get_body_schema_type() classifies string, string_array, chemical_array correctly"
    - "is_empty_post_endpoint() detects POST with no params and empty body"
    - "is_empty_post_endpoint() returns skip=FALSE for GET requests"
    - "build_function_stub() generates valid R function code"
    - "build_function_stub() output structure matches expected pattern via snapshot"
  artifacts:
    - path: "tests/testthat/test-pipeline-openapi-parser.R"
      provides: "Tests for 04_openapi_parser.R functions"
      contains: "describe.*openapi_to_spec"
    - path: "tests/testthat/test-pipeline-stub-generation.R"
      provides: "Tests for 07_stub_generation.R functions"
      contains: "describe.*is_empty_post_endpoint"
  key_links:
    - from: "tests/testthat/test-pipeline-openapi-parser.R"
      to: "dev/endpoint_eval/04_openapi_parser.R"
      via: "source_pipeline_files()"
      pattern: "source_pipeline_files"
    - from: "tests/testthat/test-pipeline-stub-generation.R"
      to: "dev/endpoint_eval/07_stub_generation.R"
      via: "source_pipeline_files()"
      pattern: "source_pipeline_files"
---

<objective>
Create unit tests for OpenAPI parsing (04_openapi_parser.R) and stub generation (07_stub_generation.R) functions.

Purpose: Complete unit test coverage for the pipeline. These functions transform parsed schemas into R function stubs, testing the end of the pipeline.

Output: Two test files covering openapi_to_spec(), get_body_schema_type(), uses_chemical_schema(), get_response_schema_type(), is_empty_post_endpoint(), and build_function_stub().
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-unit-tests/13-CONTEXT.md
@.planning/phases/13-unit-tests/13-RESEARCH.md
@tests/testthat/helper-pipeline.R
@dev/endpoint_eval/04_openapi_parser.R
@dev/endpoint_eval/07_stub_generation.R
@tests/testthat/fixtures/schemas/minimal-openapi-3.json
@tests/testthat/fixtures/schemas/minimal-swagger-2.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test-pipeline-openapi-parser.R</name>
  <files>tests/testthat/test-pipeline-openapi-parser.R</files>
  <action>
Create test file for 04_openapi_parser.R functions.

Structure with describe() blocks:

1. describe("sanitize_name()"):
   - Removes special characters
   - Collapses multiple underscores
   - Trims whitespace

2. describe("method_path_name()"):
   - Creates lowercase method_path format
   - Replaces path params {id} with by_id pattern
   - Handles multiple path segments

3. describe("get_body_schema_type()"):
   - Returns "string" for inline string schema
   - Returns "string_array" for array of strings
   - Returns "chemical_array" for chemicals property with Chemical $ref
   - Returns "simple_object" for object without chemicals
   - Returns "unknown" for NULL/empty request body

4. describe("get_response_schema_type()"):
   - Returns "array" for array responses
   - Returns "object" for object responses
   - Returns "binary" for image content types
   - Returns "unknown" for missing responses

5. describe("uses_chemical_schema()"):
   - Returns TRUE when chemicals property references Chemical schema
   - Returns FALSE for non-Chemical schemas
   - Returns FALSE for NULL request body

6. describe("openapi_to_spec()"):
   - Parses OpenAPI 3.0 fixture into tibble
   - Parses Swagger 2.0 fixture into tibble
   - Returns expected columns (route, method, summary, has_body, params, etc.)
   - Handles empty paths gracefully (should error with message)
   - Filters preflight endpoints when using ENDPOINT_PATTERNS_TO_EXCLUDE

Pattern to follow:
```r
describe("get_body_schema_type", {
  test_that("returns string for inline string schema", {
    source_pipeline_files()
    request_body <- list(
      content = list(
        "application/json" = list(
          schema = list(type = "string")
        )
      )
    )
    result <- get_body_schema_type(request_body, list())
    expect_equal(result, "string")
  })
})
```

Use inline test data for most tests (faster, clearer). Use fixtures only for integration-style tests of openapi_to_spec().

All tests should:
- Call source_pipeline_files() at start
- Use skip_on_cran()
- Follow representative edge case sampling
  </action>
  <verify>Run `Rscript -e "testthat::test_file('tests/testthat/test-pipeline-openapi-parser.R')"` - all tests pass</verify>
  <done>test-pipeline-openapi-parser.R exists with describe() blocks for parser helper functions and openapi_to_spec(). All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create test-pipeline-stub-generation.R</name>
  <files>tests/testthat/test-pipeline-stub-generation.R</files>
  <action>
Create test file for 07_stub_generation.R functions.

Structure with describe() blocks:

1. describe("%|NA|%"):
   - Returns default for NULL
   - Returns default for NA
   - Returns value for non-NULL non-NA

2. describe("is_empty_post_endpoint()"):
   - Returns skip=FALSE for GET requests
   - Returns skip=TRUE for POST with no params and empty body
   - Returns skip=FALSE if POST has query params
   - Returns skip=FALSE if POST has path params
   - Returns skip=FALSE if POST has body with properties
   - Detects suspicious endpoints (query params but empty body)
   - Returns correct reason string when skipping

3. describe("build_function_stub()"):
   - Generates function with correct signature
   - Includes roxygen documentation with title
   - Includes lifecycle badge
   - Handles deprecated endpoints
   - Generates generic_request() call for standard endpoints
   - Generates generic_chemi_request() call for chemi endpoints
   - Use expect_snapshot() for key parts of generated code (per CONTEXT.md decision)

4. describe("reset_endpoint_tracking()"):
   - Clears .StubGenEnv$skipped
   - Clears .StubGenEnv$suspicious

Pattern for snapshot testing (from RESEARCH.md):
```r
test_that("build_function_stub generates correct structure", {
  source_pipeline_files()
  clear_stubgen_env()

  stub <- build_function_stub(
    fn = "test_endpoint",
    endpoint = "/test/endpoint",
    method = "GET",
    title = "Test Endpoint",
    batch_limit = 1,
    path_param_info = list(
      has_path_params = FALSE,
      primary_param = NULL,
      fn_signature = "",
      param_docs = "",
      path_params_call = ""
    ),
    query_param_info = list(has_params = FALSE),
    body_param_info = list(has_params = FALSE),
    content_type = "application/json",
    config = list(wrapper_function = "generic_request"),
    needs_resolver = FALSE,
    body_schema_type = "unknown",
    deprecated = FALSE,
    response_schema_type = "object",
    request_type = "query_only"
  )

  expect_type(stub, "character")
  expect_true(grepl("test_endpoint <- function", stub))
  expect_true(grepl("generic_request", stub))

  # Snapshot key parts only (per CONTEXT.md)
  expect_snapshot({
    cat("Function signature:\n")
    cat(stringr::str_extract(stub, "test_endpoint <- function\\([^)]*\\)"))
  })
})
```

IMPORTANT for build_function_stub tests:
- The function has many parameters - create a helper function in the test file to provide sensible defaults
- Focus on key behaviors, not every parameter combination
- Combine assertions with snapshot testing (snapshots not verified on CRAN)

All tests should:
- Call source_pipeline_files() at start
- Call clear_stubgen_env() when testing functions that modify state
- Use skip_on_cran()
- Follow representative edge case sampling
  </action>
  <verify>Run `Rscript -e "testthat::test_file('tests/testthat/test-pipeline-stub-generation.R')"` - all tests pass</verify>
  <done>test-pipeline-stub-generation.R exists with describe() blocks for is_empty_post_endpoint() and build_function_stub(). Tests include snapshot testing for generated code. All tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Run all pipeline tests together:
   ```r
   testthat::test_dir("tests/testthat", filter = "pipeline")
   ```

2. Verify no test pollution (tests pass individually and together):
   ```r
   testthat::test_file("tests/testthat/test-pipeline-openapi-parser.R")
   testthat::test_file("tests/testthat/test-pipeline-stub-generation.R")
   ```

3. Verify snapshots created in tests/testthat/_snaps/ directory

4. Check total test count across all 4 pipeline test files (aim for 40-60 tests total)
</verification>

<success_criteria>
- test-pipeline-openapi-parser.R has tests for helper functions and openapi_to_spec()
- test-pipeline-stub-generation.R has tests for is_empty_post_endpoint() and build_function_stub()
- All tests pass on Windows
- Snapshot tests created for build_function_stub() output
- Tests use describe() blocks for organization
- Tests call source_pipeline_files() before testing
- Tests follow representative edge case sampling
</success_criteria>

<output>
After completion, create `.planning/phases/13-unit-tests/13-02-SUMMARY.md`
</output>
