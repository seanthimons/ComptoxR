---
phase: 13-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-pipeline-config.R
  - tests/testthat/test-pipeline-schema-resolution.R
autonomous: true

must_haves:
  truths:
    - "%||% operator returns default for NULL and NA"
    - "ensure_cols() adds missing columns with defaults"
    - "resolve_schema_ref() resolves normal references"
    - "resolve_schema_ref() handles circular references without infinite loop"
    - "detect_schema_version() distinguishes Swagger 2.0 from OpenAPI 3.0"
    - "validate_schema_ref() rejects invalid reference formats"
  artifacts:
    - path: "tests/testthat/test-pipeline-config.R"
      provides: "Tests for 00_config.R functions"
      contains: "describe.*%||%"
    - path: "tests/testthat/test-pipeline-schema-resolution.R"
      provides: "Tests for 01_schema_resolution.R functions"
      contains: "describe.*resolve_schema_ref"
  key_links:
    - from: "tests/testthat/test-pipeline-config.R"
      to: "dev/endpoint_eval/00_config.R"
      via: "source_pipeline_files()"
      pattern: "source_pipeline_files"
    - from: "tests/testthat/test-pipeline-schema-resolution.R"
      to: "dev/endpoint_eval/01_schema_resolution.R"
      via: "source_pipeline_files()"
      pattern: "source_pipeline_files"
---

<objective>
Create unit tests for foundational pipeline functions: configuration helpers (00_config.R) and schema resolution (01_schema_resolution.R).

Purpose: Test foundational functions first to catch failures at source, not as downstream symptoms. These functions underpin all schema parsing and stub generation.

Output: Two test files covering %||%, ensure_cols(), CHEMICAL_SCHEMA_PATTERNS, resolve_schema_ref(), validate_schema_ref(), detect_schema_version(), and extract_swagger2_body_schema().
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-unit-tests/13-CONTEXT.md
@.planning/phases/13-unit-tests/13-RESEARCH.md
@tests/testthat/helper-pipeline.R
@dev/endpoint_eval/00_config.R
@dev/endpoint_eval/01_schema_resolution.R
@tests/testthat/fixtures/schemas/minimal-openapi-3.json
@tests/testthat/fixtures/schemas/minimal-swagger-2.json
@tests/testthat/fixtures/schemas/circular-refs.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test-pipeline-config.R</name>
  <files>tests/testthat/test-pipeline-config.R</files>
  <action>
Create test file for 00_config.R functions following established patterns from test-pipeline-infrastructure.R.

Structure with describe() blocks:

1. describe("%||%") - Null coalesce operator tests:
   - Returns right side for NULL
   - Returns right side for single NA
   - Returns left side for non-NULL non-NA values (including 0, FALSE, empty string)
   - Handles vectors correctly (length > 1 should return left side)
   - Edge case: NA vector (length > 1)

2. describe("ensure_cols()") - Column defaults helper:
   - Adds missing columns with scalar defaults
   - Adds missing columns with list-column defaults
   - Preserves existing columns
   - Handles empty data frame (0 rows)
   - Handles multiple missing columns at once

3. describe("CHEMICAL_SCHEMA_PATTERNS") - Constants verification:
   - Contains expected patterns (just verify length > 0 and includes key patterns)
   - Each pattern starts with "#/components/schemas/"

4. describe("ENDPOINT_PATTERNS_TO_EXCLUDE") - Constants verification:
   - Contains expected patterns (preflight, metadata, etc.)
   - Is a single regex string

Pattern to follow (from RESEARCH.md):
```r
describe("%||%", {
  test_that("returns right side for NULL", {
    expect_equal(NULL %||% "default", "default")
  })
  # ...
})
```

All tests should:
- Call source_pipeline_files() at start of test file
- Use skip_on_cran() where appropriate
- Follow representative edge case sampling (not exhaustive)
  </action>
  <verify>Run `Rscript -e "testthat::test_file('tests/testthat/test-pipeline-config.R')"` - all tests pass</verify>
  <done>test-pipeline-config.R exists with describe() blocks for %||%, ensure_cols(), and constant verification. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create test-pipeline-schema-resolution.R</name>
  <files>tests/testthat/test-pipeline-schema-resolution.R</files>
  <action>
Create test file for 01_schema_resolution.R functions.

Structure with describe() blocks:

1. describe("detect_schema_version()"):
   - Detects OpenAPI 3.0 from openapi field (returns type="openapi", version="3.x.x")
   - Detects Swagger 2.0 from swagger field (returns type="swagger", version="2.x")
   - Returns unknown for missing version fields
   - Use fixtures: minimal-openapi-3.json, minimal-swagger-2.json

2. describe("validate_schema_ref()"):
   - Accepts valid internal references (#/components/schemas/X, #/definitions/X)
   - Errors on empty/NULL references
   - Errors on external file references (file.json#/...)
   - Warns on unusual reference paths (not #/components/ or #/definitions/)

3. describe("resolve_schema_ref()"):
   - Resolves normal OpenAPI 3.0 references
   - Resolves normal Swagger 2.0 references (fallback to definitions)
   - Handles circular references without infinite loop (verify no error, returns sentinel)
   - Enforces depth limit (depth > max_depth should error)
   - Returns input unchanged if not a character reference
   - Use fixtures: minimal-openapi-3.json, minimal-swagger-2.json, circular-refs.json

4. describe("extract_swagger2_body_schema()"):
   - Extracts body from parameters with in="body"
   - Returns unknown for empty parameters
   - Handles object schemas with properties
   - Handles string array schemas

5. describe("extract_body_properties()"):
   - Delegates to Swagger 2.0 extraction when schema_version is swagger
   - Handles OpenAPI 3.0 requestBody structure
   - Returns empty list for NULL/missing request body

Pattern to follow:
```r
describe("resolve_schema_ref", {
  test_that("resolves normal OpenAPI 3.0 references", {
    source_pipeline_files()
    schema <- load_fixture_schema("minimal-openapi-3.json")
    # Need to add a resolvable schema to fixture, or create inline test data
    # ...
  })
})
```

IMPORTANT: The minimal fixtures may need enhancement. If existing fixtures lack resolvable schemas, create inline test data within tests rather than modifying fixtures (per CONTEXT.md: Claude's discretion on test case details).

All tests should:
- Call source_pipeline_files() before testing pipeline functions
- Call clear_stubgen_env() when testing functions that modify .StubGenEnv
- Use skip_on_cran()
- Follow representative edge case sampling
  </action>
  <verify>Run `Rscript -e "testthat::test_file('tests/testthat/test-pipeline-schema-resolution.R')"` - all tests pass</verify>
  <done>test-pipeline-schema-resolution.R exists with describe() blocks for all schema resolution functions. Tests cover OpenAPI 3.0, Swagger 2.0, and edge cases. All tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Run all pipeline tests together:
   ```r
   testthat::test_dir("tests/testthat", filter = "pipeline")
   ```

2. Verify no test pollution (tests pass individually and together):
   ```r
   testthat::test_file("tests/testthat/test-pipeline-config.R")
   testthat::test_file("tests/testthat/test-pipeline-schema-resolution.R")
   ```

3. Check test count is reasonable (aim for 15-25 tests across both files)
</verification>

<success_criteria>
- test-pipeline-config.R has tests for %||%, ensure_cols(), and constants
- test-pipeline-schema-resolution.R has tests for detect_schema_version, validate_schema_ref, resolve_schema_ref, extract_swagger2_body_schema, extract_body_properties
- All tests pass on Windows
- Tests use describe() blocks for organization
- Tests call source_pipeline_files() before testing
- Tests follow representative edge case sampling (not exhaustive permutations)
</success_criteria>

<output>
After completion, create `.planning/phases/13-unit-tests/13-01-SUMMARY.md`
</output>
