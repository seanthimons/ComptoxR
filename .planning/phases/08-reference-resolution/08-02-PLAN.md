---
phase: 08-reference-resolution
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - dev/endpoint_eval/01_schema_resolution.R
  - dev/endpoint_eval/04_openapi_parser.R
autonomous: true

must_haves:
  truths:
    - "extract_body_properties() passes schema_version to resolve_schema_ref()"
    - "extract_query_params_with_refs() passes schema_version to resolve_schema_ref()"
    - "All resolve_schema_ref() calls include schema_version parameter"
    - "End-to-end resolution works for Swagger 2.0 POST endpoints"
    - "End-to-end resolution works for OpenAPI 3.0 POST endpoints"
    - "No regression in existing parsing functionality"
  artifacts:
    - path: "dev/endpoint_eval/01_schema_resolution.R"
      provides: "Updated extraction functions with version context"
    - path: "dev/endpoint_eval/04_openapi_parser.R"
      provides: "Parser passing version context through chain"
    - path: "dev/endpoint_eval/verify_08-02.R"
      provides: "End-to-end integration verification"
  key_links:
    - from: "openapi_to_spec()"
      to: "extract_body_properties()"
      via: "schema_version parameter"
      pattern: "extract_body_properties.*schema_version"
    - from: "extract_body_properties()"
      to: "resolve_schema_ref()"
      via: "schema_version parameter"
      pattern: "resolve_schema_ref.*schema_version"
---

<objective>
Wire version context through entire resolution chain from openapi_to_spec() to resolve_schema_ref()

Purpose: Ensure schema_version flows through all extraction and resolution calls (REF-02), enabling correct version-aware fallback behavior for the entire parsing pipeline.

Output: Updated extraction functions that pass schema_version to resolve_schema_ref(), verified with end-to-end tests for both Swagger 2.0 and OpenAPI 3.0 schemas.
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-reference-resolution/08-CONTEXT.md
@.planning/phases/08-reference-resolution/08-RESEARCH.md
@.planning/phases/08-reference-resolution/08-01-SUMMARY.md
@dev/endpoint_eval/01_schema_resolution.R
@dev/endpoint_eval/04_openapi_parser.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update extract_body_properties() to pass schema_version to resolve_schema_ref()</name>
  <files>dev/endpoint_eval/01_schema_resolution.R</files>
  <action>
Update extract_body_properties() (around lines 312-476) to pass schema_version to all resolve_schema_ref() calls.

Find and update these call sites:

1. Line ~330 (requestBody schema ref):
```r
# OLD:
json_schema <- resolve_schema_ref(json_schema[["$ref"]], components, max_depth = 5)

# NEW:
json_schema <- resolve_schema_ref(json_schema[["$ref"]], components, schema_version, max_depth = 3)
```

2. Line ~364 (array items ref):
```r
# OLD:
resolved <- resolve_schema_ref(items[["$ref"]], components, max_depth = 5)

# NEW:
resolved <- resolve_schema_ref(items[["$ref"]], components, schema_version, max_depth = 3)
```

Note: The function already has schema_version parameter, just need to pass it through to resolve_schema_ref() calls. Also update max_depth from 5 to 3 for consistency with the new default.
  </action>
  <verify>
Source the file and check function body:
```r
source("dev/endpoint_eval/01_schema_resolution.R")
# Visually inspect or grep for the calls
body <- deparse(extract_body_properties)
# Both resolve_schema_ref calls should include schema_version
stopifnot(any(grepl("resolve_schema_ref.*schema_version", body)))
cat("extract_body_properties passes schema_version\n")
```
  </verify>
  <done>extract_body_properties() passes schema_version to all resolve_schema_ref() calls</done>
</task>

<task type="auto">
  <name>Task 2: Update extract_query_params_with_refs() to accept and pass schema_version</name>
  <files>dev/endpoint_eval/01_schema_resolution.R</files>
  <action>
Update extract_query_params_with_refs() (around lines 480-639) to:
1. Add schema_version parameter to function signature
2. Pass schema_version to all resolve_schema_ref() calls

Function signature update:
```r
# OLD:
extract_query_params_with_refs <- function(parameters, components, max_depth = 5) {

# NEW:
extract_query_params_with_refs <- function(parameters, components, schema_version = NULL, max_depth = 3) {
```

Find and update these call sites:

1. Line ~500 (parameter schema ref):
```r
# OLD:
resolved <- resolve_schema_ref(schema_ref, components, max_depth, depth = 0)

# NEW:
resolved <- resolve_schema_ref(schema_ref, components, schema_version, max_depth, depth = 0)
```

2. Line ~529 (nested property ref):
```r
# OLD:
prop <- resolve_schema_ref(prop_ref, components, max_depth, 1)

# NEW:
prop <- resolve_schema_ref(prop_ref, components, schema_version, max_depth, 1)
```

This ensures query parameter resolution also uses version-aware fallback.
  </action>
  <verify>
Source the file and check function signature and body:
```r
source("dev/endpoint_eval/01_schema_resolution.R")
# Check function has schema_version parameter
args <- names(formals(extract_query_params_with_refs))
stopifnot("schema_version" %in% args)
# Check calls include schema_version
body <- deparse(extract_query_params_with_refs)
stopifnot(any(grepl("resolve_schema_ref.*schema_version", body)))
cat("extract_query_params_with_refs accepts and passes schema_version\n")
```
  </verify>
  <done>extract_query_params_with_refs() has schema_version parameter and passes it through</done>
</task>

<task type="auto">
  <name>Task 3: Update openapi_to_spec() to pass schema_version to extraction functions</name>
  <files>dev/endpoint_eval/04_openapi_parser.R</files>
  <action>
Update openapi_to_spec() to pass schema_version to extract_query_params_with_refs() calls.

The function already passes schema_version to extract_body_properties() (added in 07-02). Now add it to extract_query_params_with_refs() calls.

Find extract_query_params_with_refs() calls in openapi_to_spec() and add schema_version parameter:

```r
# OLD (around line where query params are extracted):
query_result <- extract_query_params_with_refs(op$parameters, components)

# NEW:
query_result <- extract_query_params_with_refs(op$parameters, components, schema_version)
```

There should be one or more calls to extract_query_params_with_refs() in the operation processing loop. Update all of them.

Note: schema_version is already detected at the top of openapi_to_spec() (from 07-02):
```r
schema_version <- detect_schema_version(openapi)
```

Just pass it through to the query params extraction.
  </action>
  <verify>
Source the file and grep for the call:
```r
source("dev/endpoint_eval/04_openapi_parser.R")
body <- deparse(openapi_to_spec)
# Check that extract_query_params_with_refs includes schema_version
stopifnot(any(grepl("extract_query_params_with_refs.*schema_version", body)))
cat("openapi_to_spec passes schema_version to query param extraction\n")
```
  </verify>
  <done>openapi_to_spec() passes schema_version to extract_query_params_with_refs()</done>
</task>

<task type="auto">
  <name>Task 4: Create end-to-end integration verification</name>
  <files>dev/endpoint_eval/verify_08-02.R</files>
  <action>
Create a comprehensive end-to-end verification script:

```r
# Verification script for Phase 8 Plan 02: Version Context Wiring
# Tests REF-02: Version context passed through entire resolution chain

cat("=== Phase 8 Plan 02 Verification ===\n\n")

# Source dependencies
source("dev/endpoint_eval/00_config.R")
source("dev/endpoint_eval/01_schema_resolution.R")
source("dev/endpoint_eval/04_openapi_parser.R")

# ==== Test 1: Swagger 2.0 End-to-End ====
cat("Test 1: Swagger 2.0 end-to-end parsing...\n")

amos_file <- "schema/AMOS_QSAR_PaDEL.json"
if (file.exists(amos_file)) {
  amos_spec <- openapi_to_spec(amos_file)

  # Should have POST endpoints with body parameters
  post_endpoints <- amos_spec[amos_spec$method == "POST", ]
  stopifnot(nrow(post_endpoints) > 0)

  # Check that body parameters were extracted
  has_body <- sum(post_endpoints$has_body, na.rm = TRUE)
  stopifnot(has_body > 0)

  cat(sprintf("  PASS: AMOS parsed with %d POST endpoints, %d with body params\n",
              nrow(post_endpoints), has_body))
} else {
  cat("  SKIP: AMOS schema file not found\n")
}

# ==== Test 2: OpenAPI 3.0 End-to-End ====
cat("Test 2: OpenAPI 3.0 end-to-end parsing...\n")

chemi_file <- "schema/chemi-resolver-prod.json"
if (file.exists(chemi_file)) {
  chemi_spec <- openapi_to_spec(chemi_file)

  # Should have POST endpoints with body parameters
  post_endpoints <- chemi_spec[chemi_spec$method == "POST", ]
  stopifnot(nrow(post_endpoints) > 0)

  # Check that body parameters were extracted
  has_body <- sum(post_endpoints$has_body, na.rm = TRUE)
  stopifnot(has_body > 0)

  cat(sprintf("  PASS: chemi-resolver parsed with %d POST endpoints, %d with body params\n",
              nrow(post_endpoints), has_body))
} else {
  cat("  SKIP: chemi-resolver schema file not found\n")
}

# ==== Test 3: CTX Chemical (OpenAPI 3.0) No Regression ====
cat("Test 3: CTX Chemical OpenAPI 3.0 no regression...\n")

ctx_file <- "schema/ctx-chemical-prod.json"
if (file.exists(ctx_file)) {
  ctx_spec <- openapi_to_spec(ctx_file)

  # Should have endpoints
  stopifnot(nrow(ctx_spec) > 0)

  # POST endpoints should have body params
  post_endpoints <- ctx_spec[ctx_spec$method == "POST", ]
  if (nrow(post_endpoints) > 0) {
    has_body <- sum(post_endpoints$has_body, na.rm = TRUE)
    cat(sprintf("  PASS: ctx-chemical parsed with %d POST endpoints, %d with body params\n",
                nrow(post_endpoints), has_body))
  } else {
    cat("  PASS: ctx-chemical parsed (no POST endpoints)\n")
  }
} else {
  cat("  SKIP: ctx-chemical schema file not found\n")
}

# ==== Test 4: RDKit (Swagger 2.0) ====
cat("Test 4: RDKit Swagger 2.0 parsing...\n")

rdkit_file <- "schema/rdkit-microservice.json"
if (file.exists(rdkit_file)) {
  rdkit_spec <- openapi_to_spec(rdkit_file)
  stopifnot(nrow(rdkit_spec) > 0)
  cat(sprintf("  PASS: RDKit parsed with %d endpoints\n", nrow(rdkit_spec)))
} else {
  cat("  SKIP: RDKit schema file not found\n")
}

# ==== Test 5: Mordred (Swagger 2.0) ====
cat("Test 5: Mordred Swagger 2.0 parsing...\n")

mordred_file <- "schema/mordred-microservice.json"
if (file.exists(mordred_file)) {
  mordred_spec <- openapi_to_spec(mordred_file)
  stopifnot(nrow(mordred_spec) > 0)
  cat(sprintf("  PASS: Mordred parsed with %d endpoints\n", nrow(mordred_spec)))
} else {
  cat("  SKIP: Mordred schema file not found\n")
}

# ==== Test 6: Version Context Flow Verification ====
cat("Test 6: Version context flow verification...\n")

# Check function signatures have schema_version
body_args <- names(formals(extract_body_properties))
query_args <- names(formals(extract_query_params_with_refs))

stopifnot("schema_version" %in% body_args)
stopifnot("schema_version" %in% query_args)

cat("  PASS: Both extraction functions accept schema_version parameter\n")

# ==== Test 7: Depth Limit Integration ====
cat("Test 7: Depth limit integration check...\n")

# Check that resolve_schema_ref uses max_depth = 3 by default
resolve_args <- formals(resolve_schema_ref)
stopifnot(resolve_args$max_depth == 3)
cat("  PASS: resolve_schema_ref() default max_depth is 3\n")

# ==== Summary ====
cat("\n=== All Phase 8 Plan 02 Tests Passed ===\n")
cat("REF-02: Version context flows through entire chain - VERIFIED\n")
cat("Swagger 2.0 schemas parse correctly - VERIFIED\n")
cat("OpenAPI 3.0 schemas parse correctly - VERIFIED\n")
cat("No regression in existing functionality - VERIFIED\n")
```
  </action>
  <verify>
Run the verification script:
```r
source("dev/endpoint_eval/verify_08-02.R")
```
All tests should pass with "All Phase 8 Plan 02 Tests Passed" message.
  </verify>
  <done>End-to-end verification passes for both Swagger 2.0 and OpenAPI 3.0 schemas</done>
</task>

</tasks>

<verification>
1. extract_body_properties() passes schema_version to resolve_schema_ref()
2. extract_query_params_with_refs() accepts schema_version and passes it through
3. openapi_to_spec() passes schema_version to all extraction functions
4. AMOS (Swagger 2.0) parses with body parameters extracted
5. chemi-resolver (OpenAPI 3.0) parses with body parameters extracted
6. No regression in CTX Chemical parsing
7. Verification script passes all tests
</verification>

<success_criteria>
- REF-02: schema_version parameter flows from openapi_to_spec() through extraction to resolution
- All Swagger 2.0 microservice schemas parse correctly
- All OpenAPI 3.0 schemas parse with no regression
- Integration test verifies end-to-end resolution for both versions
- max_depth consistently uses 3 across all call sites
</success_criteria>

<output>
After completion, create `.planning/phases/08-reference-resolution/08-02-SUMMARY.md`
</output>
