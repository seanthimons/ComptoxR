---
phase: 29-direct-template-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/ct_related.R
  - tests/testthat/test-ct_related.R
  - NEWS.md
autonomous: true
requirements:
  - REL-MIGRATE
  - REL-VALIDATE
  - NEWS-DOC

must_haves:
  truths:
    - "ct_related(query, inclusive) returns identical results using generic_request instead of raw httr2"
    - "Server URL is restored to original value even if generic_request errors"
    - "Inclusive filtering still works correctly for multi-compound queries"
    - "Single-compound queries return related substances with parent excluded"
    - "NEWS.md documents ct_related migration to generic_request"
  artifacts:
    - path: "R/ct_related.R"
      provides: "Migrated ct_related using generic_request"
      contains: "generic_request"
    - path: "tests/testthat/test-ct_related.R"
      provides: "Tests for migrated ct_related"
      contains: "ct_related"
  key_links:
    - from: "R/ct_related.R"
      to: "R/z_generic_request.R"
      via: "generic_request() call"
      pattern: "generic_request"
    - from: "R/ct_related.R"
      to: "R/zzz.R"
      via: "ctx_server() for server switching"
      pattern: "ctx_server"
---

<objective>
Migrate ct_related() from raw httr2 to generic_request(), preserving all behavior including server switching and inclusive filtering.

Purpose: Eliminate the last hand-written httr2 code in the package. ct_related uses a non-standard endpoint (server 9 / dashboard scraping) that requires careful server switching with guaranteed cleanup.
Output: Migrated ct_related.R using generic_request(batch_limit=1), updated tests, NEWS.md entry.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-direct-template-migration/29-CONTEXT.md
@.planning/phases/29-direct-template-migration/29-RESEARCH.md

@R/ct_related.R
@R/z_generic_request.R

<interfaces>
<!-- Current ct_related implementation to migrate -->

From R/ct_related.R (key behavior to preserve):
```r
ct_related <- function(query, inclusive = FALSE) {
  # Validation
  if (length(query) == 0) cli::cli_abort("Query must be a character vector of DTXSIDs.")
  if (inclusive == TRUE & length(query) == 1) cli::cli_abort("Inclusive option only valid for multiple compounds")

  # Server switch (CRITICAL: must restore even on error)
  ctx_server(server = 9)
  # ... httr2 code using req_url_query('id' = id) ...
  ctx_server(server = 1)

  # Post-processing: extract data, rename, filter parent
  # Inclusive: filter to only compounds in query set
}
```

From R/z_generic_request.R (batch_limit=1 pattern):
- batch_limit=1 means: single-item GET, appends query to URL path
- BUT ct_related uses QUERY PARAMETER (?id=DTXSID), NOT path append
- Need to check if generic_request supports query params with batch_limit=1
- If not, may need to use batch_limit=0 with manual parameter passing

IMPORTANT: ct_related's endpoint uses `req_url_query('id' = id)` pattern.
generic_request with batch_limit=1 appends query to URL PATH.
These are DIFFERENT patterns. The executor MUST read generic_request source
to determine how to pass query as a URL parameter rather than path segment.
Options:
  a) Use `...` passthrough in generic_request to add query params
  b) Manually loop with generic_request(batch_limit=0, id = single_id) per item
  c) Build the URL manually within ct_related but delegate response handling
The executor should choose the simplest approach that works.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ct_related_EXP and validate against original</name>
  <files>R/ct_related.R</files>
  <action>
1. Read R/z_generic_request.R to understand how batch_limit=1 and query parameters work. The original ct_related uses `req_url_query('id' = id)` (query parameter), NOT path append. Determine the correct way to pass the 'id' as a query parameter through generic_request. Possible approaches:
   - If generic_request supports `...` passthrough to httr2, use that for the 'id' query param
   - If not, loop manually calling generic_request(batch_limit=0, id = single_dtxsid) per query item
   - As a fallback, keep the httr2 request construction but delegate response parsing to a shared helper

2. Add ct_related_EXP function to R/ct_related.R (BELOW the existing ct_related function). Implementation:
   - Same signature: ct_related_EXP(query, inclusive = FALSE)
   - Same validation: abort on empty query, abort on inclusive with single query
   - Same cli messaging (preserve user experience): cli_rule, cli_dl showing count and inclusive flag
   - Server switch with GUARANTEED cleanup:
     ```r
     old_server <- Sys.getenv("ctx_burl")
     ctx_server(9)
     on.exit(Sys.setenv(ctx_burl = old_server), add = TRUE)
     ```
   - Use generic_request (or the approach determined in step 1) to fetch per-DTXSID
   - Post-processing must produce IDENTICAL output to original:
     a) Extract "data" field from each response
     b) Keep only "dtxsid" and "relationship" columns from each entry
     c) Bind rows with names_to = "query" column
     d) Rename dtxsid to "child"
     e) Filter out rows where child == query (remove parent compound)
     f) If inclusive && length(query) > 1: filter to only rows where both query and child are in the original query vector
   - Do NOT use `auth = TRUE` (the original does not send API key)
   - Do NOT export ct_related_EXP (it is temporary)

3. The ct_related_EXP function does NOT need to handle the purrr::safely pattern from the original -- generic_request has built-in error handling. However, preserve warning messages for failed requests if possible.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); print(body(ct_related_EXP))"</automated>
  </verify>
  <done>ct_related_EXP function exists in R/ct_related.R alongside original, uses generic_request or equivalent template approach, has guaranteed server cleanup via on.exit, is not exported.</done>
</task>

<task type="auto">
  <name>Task 2: Swap ct_related_EXP to ct_related, update tests and NEWS</name>
  <files>R/ct_related.R, tests/testthat/test-ct_related.R, NEWS.md</files>
  <action>
1. In R/ct_related.R:
   - Delete the original ct_related function (lines 1-117 approximately)
   - Rename ct_related_EXP to ct_related
   - Keep the roxygen documentation from the original (lifecycle::questioning badge, @param, @return, @export, @examples)
   - Make sure the renamed function is @exported

2. Update tests/testthat/test-ct_related.R:
   - Keep existing test structure (single, example, error tests with VCR cassettes)
   - Add a new test for server cleanup on error: mock or verify that Sys.getenv("ctx_burl") returns the original server URL after ct_related completes (even after error)
   - Add a test that inclusive=TRUE with single query aborts with expected message
   - Add a test that empty query aborts with expected message

3. Run devtools::document() to update NAMESPACE and man pages

4. Update NEWS.md -- add to the Phase 29 section (or create it if Plan 01 hasn't run yet):
   ```
   **ct_related migrated to generic_request:**
   - Function name and behavior unchanged
   - Server cleanup now guaranteed via on.exit (previously could leak on error)
   - Improved error handling from generic_request infrastructure
   ```

5. Verify package loads and tests pass:
   ```r
   devtools::load_all()
   testthat::test_file("tests/testthat/test-ct_related.R")
   ```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-ct_related.R')"</automated>
  </verify>
  <done>ct_related uses generic_request (no raw httr2), ct_related_EXP removed, all existing tests pass, new server-cleanup and validation tests pass, NEWS.md documents migration.</done>
</task>

</tasks>

<verification>
1. `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all()"` -- package loads without errors
2. `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-ct_related.R')"` -- all tests pass
3. Grep R/ct_related.R for "httr2::" or "req_perform" -- should find NONE (all httr2 code removed)
4. Grep R/ct_related.R for "on.exit" -- should find guaranteed server cleanup
5. Grep R/ct_related.R for "generic_request" -- should find the template call
</verification>

<success_criteria>
- ct_related() uses generic_request() or equivalent template (no raw httr2)
- Server URL restored even on error (on.exit pattern)
- Inclusive filtering preserved
- All existing test cassettes still work
- No ct_related_EXP remaining in final code
- NEWS.md documents migration
</success_criteria>

<output>
After completion, create `.planning/phases/29-direct-template-migration/29-02-SUMMARY.md`
</output>
