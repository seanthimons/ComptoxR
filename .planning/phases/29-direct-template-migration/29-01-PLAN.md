---
phase: 29-direct-template-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/hook_config.yml
  - R/hooks/property_hooks.R
  - R/ct_chemical_property_experimental_search.R
  - R/ct_chemical_property_predicted_search.R
  - R/ct_prop.R
  - tests/testthat/test-ct_prop.R
  - tests/testthat/test-property_hooks.R
  - man/ct_properties.Rd
  - man/prop_ids.Rd
  - NAMESPACE
  - NEWS.md
autonomous: true
requirements:
  - PROP-COERCE
  - PROP-DELETE
  - PROP-IDS
  - NEWS-DOC

must_haves:
  truths:
    - "ct_chemical_property_experimental_search_bulk(query, coerce=TRUE) splits results by propertyId into named list"
    - "ct_chemical_property_predicted_search_bulk(query, coerce=TRUE) splits results by propertyId into named list"
    - "ct_chemical_property_experimental_name() returns tibble of property names (replaces .prop_ids)"
    - "ct_chemical_property_predicted_name() returns tibble of property names (replaces .prop_ids)"
    - "ct_properties() and .prop_ids() no longer exist in NAMESPACE"
    - "NEWS.md documents ct_properties and .prop_ids removal with migration examples"
  artifacts:
    - path: "R/hooks/property_hooks.R"
      provides: "coerce_by_property_id hook function"
      contains: "coerce_by_property_id"
    - path: "inst/hook_config.yml"
      provides: "Hook config entries for property search bulk functions"
      contains: "coerce_by_property_id"
    - path: "R/ct_chemical_property_experimental_search.R"
      provides: "Regenerated stub with coerce parameter"
      contains: "coerce"
    - path: "R/ct_chemical_property_predicted_search.R"
      provides: "Regenerated stub with coerce parameter"
      contains: "coerce"
  key_links:
    - from: "inst/hook_config.yml"
      to: "R/hooks/property_hooks.R"
      via: "post_response hook name"
      pattern: "coerce_by_property_id"
    - from: "R/ct_chemical_property_experimental_search.R"
      to: "R/hooks/property_hooks.R"
      via: "run_hook() call"
      pattern: "run_hook.*coerce_by_property_id"
---

<objective>
Add coerce hook to property search bulk stubs, then delete ct_properties() and .prop_ids() wrappers.

Purpose: Eliminate the last hand-written httr2 code for property endpoints by migrating the coercion logic to the hook system and deleting the dual-mode dispatcher. Users call generated stubs directly.
Output: Property hook function, regenerated stubs with coerce parameter, deleted ct_prop.R, updated tests and NEWS.md.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-direct-template-migration/29-CONTEXT.md
@.planning/phases/29-direct-template-migration/29-RESEARCH.md

@R/ct_prop.R
@R/ct_chemical_property_experimental_search.R
@R/ct_chemical_property_predicted_search.R
@inst/hook_config.yml
@R/hooks/bioactivity_hooks.R

<interfaces>
<!-- Existing hook system contracts executor needs -->

From R/hooks/bioactivity_hooks.R (pattern to follow):
```r
annotate_assay_if_requested <- function(data) {
  if (!isTRUE(data$params$annotate)) {
    return(data$result)
  }
  # ... transform data$result ...
  return(result)
}
```

From inst/hook_config.yml (pattern to follow):
```yaml
ct_bioactivity_data_search_bulk:
  extra_params:
    annotate:
      default: "FALSE"
      type: "logical"
      description: "Join assay annotations by aeid"
  post_response:
    - annotate_assay_if_requested
```

From R/ct_chemical_property_experimental_search.R (bulk function to add hook to):
```r
ct_chemical_property_experimental_search_bulk <- function(query) {
  result <- generic_request(
    query = query,
    endpoint = "chemical/property/experimental/search/by-dtxsid/",
    method = "POST",
    batch_limit = as.numeric(Sys.getenv("batch_limit", "100"))
  )
  return(result)
}
```

From R/ct_prop.R (coercion logic to migrate):
```r
# Line 34-37: The coerce logic we are migrating to a hook
if (coerce == TRUE && nrow(df) > 0) {
  df <- df %>% split(.$propertyId)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create property coerce hook and update hook config</name>
  <files>R/hooks/property_hooks.R, inst/hook_config.yml</files>
  <action>
1. Create R/hooks/property_hooks.R with coerce_by_property_id function following the exact pattern from bioactivity_hooks.R:
   - Takes `data` parameter (list with `$result` tibble and `$params` named list)
   - Checks `if (!isTRUE(data$params$coerce))` -- if FALSE, return data$result unchanged
   - If TRUE, split data$result by `.$propertyId` using `split()` (same logic as ct_prop.R line 34-37)
   - Include cli::cli_alert_success with count of property groups
   - Add roxygen @noRd documentation

2. Add two entries to inst/hook_config.yml (append after existing entries):
   - ct_chemical_property_experimental_search_bulk: extra_params coerce (logical, default FALSE, description "Split results by propertyId into named list of data frames"), post_response coerce_by_property_id
   - ct_chemical_property_predicted_search_bulk: same config as above

3. Regenerate the two affected stubs by running the stub generator:
   ```r
   source("dev/generate_stubs.R")
   ```
   This should inject the coerce parameter and run_hook() calls into ct_chemical_property_experimental_search.R and ct_chemical_property_predicted_search.R.

4. Verify CI drift check passes:
   ```r
   source("dev/check_hook_config.R")
   ```

Note: The bulk functions are in ct_chemical_property_experimental_search.R (line 14) and ct_chemical_property_predicted_search.R (line 14). The generator modifies these files in-place.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('dev/check_hook_config.R')"</automated>
  </verify>
  <done>coerce_by_property_id hook function exists in R/hooks/property_hooks.R, hook_config.yml has entries for both property search bulk functions, regenerated stubs include coerce parameter and run_hook() calls, CI drift check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete ct_properties and .prop_ids, update tests and NEWS</name>
  <files>R/ct_prop.R, tests/testthat/test-ct_prop.R, tests/testthat/test-property_hooks.R, NEWS.md, NAMESPACE</files>
  <action>
1. Create tests/testthat/test-property_hooks.R with unit tests for coerce hook:
   - Test that coerce_by_property_id returns result unchanged when coerce=FALSE (mock data with data$params$coerce = FALSE, data$result = tibble with propertyId column)
   - Test that coerce_by_property_id splits by propertyId when coerce=TRUE (mock data with 2+ propertyId values, verify result is named list)
   - Test that coerce_by_property_id handles empty tibble gracefully (coerce=TRUE but 0 rows)
   - These are pure unit tests, no VCR needed -- just call the function directly with mock data

2. Update tests/testthat/test-ct_prop.R to test the new stubs instead of deleted ct_properties:
   - Replace ct_prop() call with ct_chemical_property_experimental_name() call (verifying it returns a tibble/data.frame)
   - Add test calling ct_chemical_property_predicted_name() (same assertion)
   - Use VCR cassettes named "ct_chemical_property_experimental_name_basic" and "ct_chemical_property_predicted_name_basic"
   - Rename file to test-ct_chemical_property_name.R (optional, executor's discretion -- the old name is confusing)

3. Delete R/ct_prop.R entirely (contains ct_properties and .prop_ids)

4. Run devtools::document() to update NAMESPACE and delete orphaned man pages (man/ct_properties.Rd, man/prop_ids.Rd if they exist)

5. Update NEWS.md -- add Phase 29 section ABOVE the Phase 28 section under "## Unreleased":
   ```
   **Phase 29 Direct Template Migration:**

   **ct_properties removed:**
   - Compound search: `ct_properties(search_param = "compound", query = dtxsids)` ->
     `ct_chemical_property_experimental_search_bulk(query = dtxsids)` or
     `ct_chemical_property_predicted_search_bulk(query = dtxsids)`
     Add `coerce = TRUE` to split results by propertyId into named list
   - Range search: `ct_properties(search_param = "property", query = "MolWeight", range = c(100, 500))` ->
     `ct_chemical_property_experimental_search_by_range(propertyName = "MolWeight", start = 100, end = 500)` or
     `ct_chemical_property_predicted_search_by_range(propertyId = "MolWeight", start = 100, end = 500)`

   **.prop_ids() removed:**
   - Use `ct_chemical_property_experimental_name()` and `ct_chemical_property_predicted_name()` directly
   ```

6. Verify package loads cleanly:
   ```r
   devtools::load_all()
   ```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-property_hooks.R')"</automated>
  </verify>
  <done>ct_prop.R deleted, ct_properties and .prop_ids no longer in NAMESPACE, property hook unit tests pass, NEWS.md documents migration paths, package loads cleanly.</done>
</task>

</tasks>

<verification>
1. `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all()"` -- package loads without errors
2. `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('dev/check_hook_config.R')"` -- CI drift check passes
3. `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-property_hooks.R')"` -- hook unit tests pass
4. Verify ct_properties is NOT in NAMESPACE: grep for "ct_properties" in NAMESPACE file
5. Verify .prop_ids is NOT in NAMESPACE: grep for "prop_ids" in NAMESPACE file
</verification>

<success_criteria>
- ct_properties() and .prop_ids() deleted from codebase
- coerce_by_property_id hook operational for both experimental and predicted property bulk search
- CI drift check passes
- Hook unit tests pass
- Package loads cleanly
- NEWS.md documents all breaking changes with migration examples
</success_criteria>

<output>
After completion, create `.planning/phases/29-direct-template-migration/29-01-SUMMARY.md`
</output>
