---
phase: 11-documentation-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev/ENDPOINT_EVAL_UTILS_GUIDE.md
  - .claude/CLAUDE.md
  - dev/generate_stubs.R
autonomous: true

must_haves:
  truths:
    - "Developer reading the guide understands the unified pipeline architecture"
    - "All code examples run without error"
    - "No references to deleted parse_chemi_schemas() function"
    - "Swagger 2.0 handling is clearly documented"
    - "Mermaid flowcharts accurately represent current processing flow"
  artifacts:
    - path: "dev/ENDPOINT_EVAL_UTILS_GUIDE.md"
      provides: "Updated documentation matching v1.5-v1.6 implementation"
      contains: "select_schema_files"
    - path: "dev/ENDPOINT_EVAL_UTILS_GUIDE.md"
      provides: "Swagger 2.0 documentation section"
      contains: "detect_schema_version"
  key_links:
    - from: "dev/ENDPOINT_EVAL_UTILS_GUIDE.md"
      to: "dev/generate_stubs.R"
      via: "documented functions match actual implementation"
      pattern: "select_schema_files|openapi_to_spec"
    - from: "dev/ENDPOINT_EVAL_UTILS_GUIDE.md"
      to: "dev/endpoint_eval/01_schema_resolution.R"
      via: "Swagger 2.0 function documentation"
      pattern: "detect_schema_version|extract_swagger2_body_schema"
---

<objective>
Update ENDPOINT_EVAL_UTILS_GUIDE.md to accurately document the current stub generation architecture following v1.5 (Swagger 2.0 support) and v1.6 (unified pipeline) changes.

Purpose: Documentation accuracy is critical for maintainability. The current guide references deleted functions and outdated flowcharts, which will confuse future developers.

Output:
- Updated ENDPOINT_EVAL_UTILS_GUIDE.md (~1300 lines)
- Minor CLAUDE.md updates if needed
- Inline comments in generate_stubs.R where logic is non-obvious
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

Key reference files (current implementation):
@dev/generate_stubs.R — Contains select_schema_files() and unified pipeline
@dev/endpoint_eval/01_schema_resolution.R — Contains Swagger 2.0 functions
@dev/endpoint_eval/04_openapi_parser.R — Contains openapi_to_spec()
@dev/ENDPOINT_EVAL_UTILS_GUIDE.md — Primary update target
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove parse_chemi_schemas references and update unified pipeline documentation</name>
  <files>dev/ENDPOINT_EVAL_UTILS_GUIDE.md</files>
  <action>
Update the guide to reflect the unified pipeline architecture:

1. **Remove all parse_chemi_schemas() references** (DOC-01):
   - Search for "parse_chemi_schemas" throughout the document
   - Remove from Architecture Overview, Schema Parsing Flow, Debugging Tips, Key Functions Reference
   - Remove the dedicated section describing this function if present

2. **Document select_schema_files() helper** (DOC-02):
   - Add new section after Architecture Overview titled "Schema File Selection"
   - Document parameters: pattern, exclude_pattern, stage_priority, schema_dir
   - Include usage examples for chemi (with stage priority) vs ct/cc (without)
   - Reference location: dev/generate_stubs.R lines 86-140

3. **Update Architecture Overview** (DOC-03):
   - Change description to show ALL generators use openapi_to_spec() directly
   - Remove any mention of divergent paths for chemi schemas
   - Update the pipeline diagram to show: schema_file -> select_schema_files -> openapi_to_spec -> spec tibble

4. **Update "Schema Loading" section (Step 1)** (DOC-05):
   - Replace parse_chemi_schemas() description with unified approach
   - Show how generate_ct_stubs(), generate_chemi_stubs(), generate_cc_stubs() all follow same pattern:
     1. List schema files (with optional stage filtering for chemi)
     2. Parse each with openapi_to_spec()
     3. Bind results into single tibble

5. **Update Key Functions Reference table** (DOC-06):
   - Remove parse_chemi_schemas() row
   - Add select_schema_files() row with: Purpose="Stage-based schema file selection", Input="pattern, stage_priority", Output="Character vector of filenames"

Current guide locations to update:
- Line 64: "parse_chemi_schemas()" in step 1 description
- Lines 546-550: "Schema Loading" subgraph references parse_chemi_schemas
- Line 811: Key Functions Reference table includes parse_chemi_schemas
  </action>
  <verify>
- `grep -i "parse_chemi_schemas" dev/ENDPOINT_EVAL_UTILS_GUIDE.md` returns no matches
- `grep -i "select_schema_files" dev/ENDPOINT_EVAL_UTILS_GUIDE.md` returns matches
- Architecture Overview mentions "unified pipeline" or equivalent
  </verify>
  <done>
- DOC-01: No parse_chemi_schemas() references remain
- DOC-02: select_schema_files() is documented with examples
- DOC-03: Architecture shows unified openapi_to_spec() pipeline
- DOC-05: Schema Loading describes unified approach
- DOC-06: Key Functions table updated
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Swagger 2.0 documentation and update flowcharts</name>
  <files>dev/ENDPOINT_EVAL_UTILS_GUIDE.md</files>
  <action>
Add comprehensive Swagger 2.0 documentation and update Mermaid flowcharts:

1. **Document detect_schema_version()** (DOC-10):
   - Add new section "Schema Version Detection" before Schema Parsing Flow
   - Explain: checks swagger field (2.x) vs openapi field (3.x)
   - Returns: list(version, type) where type is "swagger", "openapi", or "unknown"
   - Reference: dev/endpoint_eval/01_schema_resolution.R lines 251-262

2. **Document extract_swagger2_body_schema()** (DOC-11):
   - Add subsection under "Body Parameter Extraction"
   - Explain: Swagger 2.0 puts body in parameters[] array with in="body", not requestBody object
   - Handles: $ref to #/definitions/, object with properties, array with items
   - Reference: dev/endpoint_eval/01_schema_resolution.R lines 267-404

3. **Document version-aware reference resolution** (DOC-12):
   - Add subsection explaining fallback chain in resolve_schema_ref()
   - Swagger 2.0: tries #/definitions/ first, then #/components/schemas/
   - OpenAPI 3.0: tries #/components/schemas/ first, then #/definitions/
   - Reference: dev/endpoint_eval/01_schema_resolution.R lines 168-182

4. **Add Swagger 2.0 handling section** (DOC-13):
   - New major section titled "Swagger 2.0 Support"
   - Explain which schemas use Swagger 2.0 (AMOS, RDKit, Mordred - chemi microservices)
   - Show complete processing flow for Swagger 2.0 schemas
   - Include key differences table: OpenAPI 3.0 vs Swagger 2.0

5. **Update Mermaid flowcharts** (DOC-04):
   - Main Processing Flow: Remove parse_chemi_schemas branch, show single path through openapi_to_spec
   - Add version detection step after schema load
   - Add conditional branch for Swagger 2.0 body extraction
   - Update "Schema Loading" subgraph to show unified approach with select_schema_files
   - Ensure charts show: detect_schema_version() call at openapi_to_spec entry point

Key flowchart updates needed:
- Lines 543-618: Main Processing Flow - remove divergent chemi path
- Lines 620-675: Parameter Flow Detail - may need version dispatch
- New: Add Swagger 2.0 specific flowchart showing body extraction from parameters[]
  </action>
  <verify>
- `grep -i "detect_schema_version" dev/ENDPOINT_EVAL_UTILS_GUIDE.md` returns matches
- `grep -i "swagger 2.0\|swagger2" dev/ENDPOINT_EVAL_UTILS_GUIDE.md` returns matches
- Mermaid syntax valid: no parse errors when rendered
- Flowcharts show single processing path (no parse_chemi_schemas branch)
  </verify>
  <done>
- DOC-10: detect_schema_version() documented
- DOC-11: extract_swagger2_body_schema() documented
- DOC-12: Version-aware reference resolution documented
- DOC-13: Swagger 2.0 handling section exists
- DOC-04: Mermaid flowcharts updated to current architecture
  </done>
</task>

<task type="auto">
  <name>Task 3: Update developer tips, validate examples, and add inline comments</name>
  <files>dev/ENDPOINT_EVAL_UTILS_GUIDE.md, .claude/CLAUDE.md, dev/generate_stubs.R</files>
  <action>
Complete remaining documentation requirements:

1. **Update Debugging Tips** (DOC-07):
   - Remove references to parse_chemi_schemas() in debugging workflows
   - Update code examples to use direct openapi_to_spec() calls
   - Add debugging tips for Swagger 2.0 schema issues:
     ```r
     # Check schema version
     schema_version <- detect_schema_version(openapi)

     # Debug Swagger 2.0 body extraction
     if (schema_version$type == "swagger") {
       body_params <- purrr::keep(params, ~ .x[["in"]] == "body")
       # ...
     }
     ```

2. **Update Junior Developer tips** (DOC-08):
   - Remove any parse_chemi_schemas() references
   - Update "Start with the spec tibble" to reference unified pipeline
   - Add tip about checking schema version when debugging chemi endpoints

3. **Update Senior Developer tips** (DOC-09):
   - Add v1.5-v1.6 architectural context
   - Document the decision to unify pipelines (UNIFY-CHEMI decision)
   - Explain why select_schema_files() was extracted as reusable helper
   - Note: Swagger 2.0 support was added for AMOS/RDKit/Mordred microservices

4. **Update CLAUDE.md if needed** (DOC-14):
   - Check if any stub generation patterns in CLAUDE.md are outdated
   - Update any references to the old chemi pipeline
   - Ensure generic_request/generic_chemi_request documentation is current

5. **Add inline comments in generate_stubs.R** (DOC-15):
   - Add comment block at select_schema_files() explaining stage priority logic
   - Add comment in generate_chemi_stubs() explaining why it calls openapi_to_spec directly
   - Document ENDPOINT_PATTERNS_TO_EXCLUDE usage

6. **Validate code examples** (VAL-01):
   - Review all code examples in the guide
   - Ensure they reference existing functions and correct file paths
   - Update any examples that call deleted functions

7. **Validate Mermaid charts** (VAL-02):
   - Ensure all flowchart syntax is valid
   - Check that node names don't contain special characters that break rendering
   - Verify charts render correctly in a Mermaid preview

8. **Check for dead links** (VAL-03):
   - Search for any internal references to removed functions
   - Verify all "See also" and cross-references point to existing sections
  </action>
  <verify>
- All code examples use existing functions (no parse_chemi_schemas calls)
- `grep -i "parse_chemi_schemas" dev/ENDPOINT_EVAL_UTILS_GUIDE.md` returns 0 matches
- `grep -i "select_schema_files" dev/generate_stubs.R` shows inline comments
- Developer tips sections reference unified pipeline
  </verify>
  <done>
- DOC-07: Debugging Tips updated with current patterns
- DOC-08: Junior Developer tips reflect unified pipeline
- DOC-09: Senior Developer tips have v1.5-v1.6 context
- DOC-14: CLAUDE.md checked and updated if needed
- DOC-15: Inline comments added to generate_stubs.R
- VAL-01: All code examples verified
- VAL-02: Mermaid charts render correctly
- VAL-03: No dead links or removed function references
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Requirement coverage check:**
   ```bash
   # All DOC requirements should be addressed in the updated guide
   grep -c "select_schema_files\|detect_schema_version\|swagger\|unified" dev/ENDPOINT_EVAL_UTILS_GUIDE.md
   ```

2. **No legacy references:**
   ```bash
   # Should return 0 matches
   grep -i "parse_chemi_schemas" dev/ENDPOINT_EVAL_UTILS_GUIDE.md
   ```

3. **Code example validation:**
   ```r
   # In R, verify key documented functions exist
   source("dev/endpoint_eval/01_schema_resolution.R")
   exists("detect_schema_version")  # TRUE
   exists("extract_swagger2_body_schema")  # TRUE
   exists("resolve_schema_ref")  # TRUE
   ```
</verification>

<success_criteria>
Phase 11 is complete when:
- [ ] DOC-01: No parse_chemi_schemas() references in documentation
- [ ] DOC-02: select_schema_files() helper documented with examples
- [ ] DOC-03: Architecture shows unified openapi_to_spec() pipeline
- [ ] DOC-04: Mermaid flowcharts reflect current processing (no divergent paths)
- [ ] DOC-05: Schema Loading section describes unified approach
- [ ] DOC-06: Key Functions table updated (removed parse_chemi_schemas, added select_schema_files)
- [ ] DOC-07: Debugging Tips use current code patterns
- [ ] DOC-08: Junior Developer tips reflect unified pipeline
- [ ] DOC-09: Senior Developer tips have v1.5-v1.6 context
- [ ] DOC-10: detect_schema_version() documented
- [ ] DOC-11: extract_swagger2_body_schema() documented
- [ ] DOC-12: Version-aware reference resolution documented
- [ ] DOC-13: Swagger 2.0 handling section added
- [ ] DOC-14: CLAUDE.md checked/updated
- [ ] DOC-15: Inline comments in generate_stubs.R
- [ ] VAL-01: All code examples run without error
- [ ] VAL-02: Mermaid charts render correctly
- [ ] VAL-03: No dead links or references to removed functions
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation-update/11-01-SUMMARY.md`
</output>
