---
phase: 12-test-infrastructure-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/helper-pipeline.R
  - tests/testthat/fixtures/schemas/minimal-openapi-3.json
  - tests/testthat/fixtures/schemas/minimal-swagger-2.json
  - tests/testthat/fixtures/schemas/circular-refs.json
  - tests/testthat/fixtures/schemas/malformed.json
  - DESCRIPTION
autonomous: true

must_haves:
  truths:
    - "Pipeline files can be sourced in dependency order without error"
    - "Test fixtures load as valid JSON (except malformed)"
    - ".StubGenEnv cleanup utility resets state between tests"
    - "withr is available in test environment"
  artifacts:
    - path: "tests/testthat/helper-pipeline.R"
      provides: "Pipeline sourcing and cleanup utilities"
      contains: "source_pipeline_files"
    - path: "tests/testthat/fixtures/schemas/minimal-openapi-3.json"
      provides: "Minimal OpenAPI 3.0 test fixture"
      contains: "openapi"
    - path: "tests/testthat/fixtures/schemas/minimal-swagger-2.json"
      provides: "Minimal Swagger 2.0 test fixture"
      contains: "swagger"
    - path: "tests/testthat/fixtures/schemas/circular-refs.json"
      provides: "Circular reference edge case"
      contains: "$ref"
    - path: "tests/testthat/fixtures/schemas/malformed.json"
      provides: "Invalid schema for error testing"
      min_lines: 1
  key_links:
    - from: "tests/testthat/helper-pipeline.R"
      to: "dev/endpoint_eval/*.R"
      via: "source() calls in dependency order"
      pattern: "source.*endpoint_eval"
    - from: "tests/testthat/helper-pipeline.R"
      to: ".StubGenEnv"
      via: "cleanup function"
      pattern: "clear_stubgen_env|rm.*StubGenEnv"
---

<objective>
Create test infrastructure for the stub generation pipeline.

Purpose: Enable testing of dev/endpoint_eval/ code that is excluded from the package namespace but critical for stub generation correctness. Seven bugs were fixed in v1.0-v1.7 that tests will prevent from regressing.

Output:
- helper-pipeline.R with sourcing logic and cleanup utilities
- Minimal test fixtures covering OpenAPI 3.0, Swagger 2.0, edge cases
- withr added to DESCRIPTION Suggests for state cleanup
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@tests/testthat/setup.R
@tests/testthat/helper-vcr.R
@dev/endpoint_eval/00_config.R
@DESCRIPTION
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create helper-pipeline.R with sourcing and cleanup utilities</name>
  <files>tests/testthat/helper-pipeline.R</files>
  <action>
Create `tests/testthat/helper-pipeline.R` with:

1. **`source_pipeline_files()` function:**
   - Source all 8 pipeline files in dependency order:
     - 00_config.R (no dependencies)
     - 01_schema_resolution.R (depends on 00)
     - 02_path_utils.R (depends on 00)
     - 03_codebase_search.R (depends on 00, 02)
     - 04_openapi_parser.R (depends on 00, 01)
     - 05_file_scaffold.R (depends on 00)
     - 06_param_parsing.R (depends on 00, 01, 04)
     - 07_stub_generation.R (depends on all above)
   - Use `here::here()` for reliable path construction (already in Imports)
   - Source with `local = FALSE` so functions are available in global env
   - Return invisible TRUE on success
   - Wrap in tryCatch to provide helpful error messages

2. **`clear_stubgen_env()` function:**
   - Check if `.StubGenEnv` exists in global env
   - If exists, remove all objects from it OR remove the entire env
   - Use `withr::defer()` pattern compatible - design for use in test setup
   - Return invisible TRUE

3. **`get_fixture_path(filename)` helper:**
   - Return path to `tests/testthat/fixtures/schemas/{filename}`
   - Use `testthat::test_path()` for reliable resolution

4. **`load_fixture_schema(filename)` helper:**
   - Load JSON fixture from fixtures/schemas/
   - Use `jsonlite::fromJSON()` with simplifyVector = FALSE
   - Return parsed list

Follow existing helper-vcr.R patterns for consistency.
  </action>
  <verify>
Run in R console from package root:
```r
source("tests/testthat/helper-pipeline.R")
source_pipeline_files()  # Should return TRUE, no errors
ls(pattern = "openapi_to_spec")  # Should find the function
clear_stubgen_env()  # Should return TRUE
```
  </verify>
  <done>
- helper-pipeline.R exists with all 4 functions
- source_pipeline_files() loads all 8 files without error
- clear_stubgen_env() successfully clears .StubGenEnv
- Functions follow existing helper-*.R patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test fixture schemas</name>
  <files>
tests/testthat/fixtures/schemas/minimal-openapi-3.json
tests/testthat/fixtures/schemas/minimal-swagger-2.json
tests/testthat/fixtures/schemas/circular-refs.json
tests/testthat/fixtures/schemas/malformed.json
  </files>
  <action>
Create `tests/testthat/fixtures/schemas/` directory and populate with minimal test schemas:

1. **`minimal-openapi-3.json`** - Minimal valid OpenAPI 3.0 schema:
```json
{
  "openapi": "3.0.0",
  "info": { "title": "Test API", "version": "1.0" },
  "paths": {
    "/test": {
      "get": {
        "operationId": "getTest",
        "responses": { "200": { "description": "OK" } }
      }
    }
  }
}
```

2. **`minimal-swagger-2.json`** - Minimal valid Swagger 2.0 schema:
```json
{
  "swagger": "2.0",
  "info": { "title": "Test API", "version": "1.0" },
  "paths": {
    "/test": {
      "get": {
        "operationId": "getTest",
        "responses": { "200": { "description": "OK" } }
      }
    }
  }
}
```

3. **`circular-refs.json`** - Schema with circular reference for edge case testing:
```json
{
  "openapi": "3.0.0",
  "info": { "title": "Circular Test", "version": "1.0" },
  "paths": {
    "/circular": {
      "post": {
        "operationId": "postCircular",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": { "$ref": "#/components/schemas/Node" }
            }
          }
        },
        "responses": { "200": { "description": "OK" } }
      }
    }
  },
  "components": {
    "schemas": {
      "Node": {
        "type": "object",
        "properties": {
          "value": { "type": "string" },
          "children": {
            "type": "array",
            "items": { "$ref": "#/components/schemas/Node" }
          }
        }
      }
    }
  }
}
```

4. **`malformed.json`** - Invalid schema for error path testing:
```json
{
  "not_a_valid_schema": true,
  "missing": ["openapi", "swagger", "paths"]
}
```

Ensure JSON is valid (except malformed.json which should be valid JSON but invalid schema).
  </action>
  <verify>
Run in R console:
```r
# All should parse as JSON
jsonlite::fromJSON("tests/testthat/fixtures/schemas/minimal-openapi-3.json")
jsonlite::fromJSON("tests/testthat/fixtures/schemas/minimal-swagger-2.json")
jsonlite::fromJSON("tests/testthat/fixtures/schemas/circular-refs.json")
jsonlite::fromJSON("tests/testthat/fixtures/schemas/malformed.json")

# Version detection should work
schema <- jsonlite::fromJSON("tests/testthat/fixtures/schemas/minimal-openapi-3.json", simplifyVector = FALSE)
!is.null(schema$openapi)  # TRUE

schema2 <- jsonlite::fromJSON("tests/testthat/fixtures/schemas/minimal-swagger-2.json", simplifyVector = FALSE)
!is.null(schema2$swagger)  # TRUE
```
  </verify>
  <done>
- fixtures/schemas/ directory exists
- All 4 JSON files parse without error
- minimal-openapi-3.json has "openapi" field
- minimal-swagger-2.json has "swagger" field
- circular-refs.json has circular $ref pattern
- malformed.json is valid JSON but invalid schema structure
  </done>
</task>

<task type="auto">
  <name>Task 3: Add withr to DESCRIPTION and create verification test</name>
  <files>
DESCRIPTION
tests/testthat/test-pipeline-infrastructure.R
  </files>
  <action>
1. **Update DESCRIPTION** - Add `withr` to Suggests (if not already present):
   - Check current Suggests section
   - Add `withr` alphabetically if missing
   - withr is essential for `local_*()` and `defer()` patterns

2. **Create `tests/testthat/test-pipeline-infrastructure.R`** - Verification test:

```r
# Test that pipeline infrastructure works correctly

test_that("helper-pipeline.R loads successfully", {
  # Source the helper (should already be loaded by testthat)
  expect_true(exists("source_pipeline_files"))
  expect_true(exists("clear_stubgen_env"))
  expect_true(exists("get_fixture_path"))
  expect_true(exists("load_fixture_schema"))
})

test_that("pipeline files can be sourced in order", {
  skip_on_cran()  # Pipeline tests are for development, not CRAN

  # Clear any existing state
  clear_stubgen_env()

  # Source all pipeline files
  result <- source_pipeline_files()
  expect_true(result)


  # Key functions should exist after sourcing
  expect_true(exists("openapi_to_spec"))
  expect_true(exists("resolve_schema_ref"))
  expect_true(exists("detect_schema_version"))
  expect_true(exists("extract_body_properties"))

  # Clean up
  clear_stubgen_env()
})

test_that("fixtures load correctly", {
  skip_on_cran()

  # OpenAPI 3.0
  schema <- load_fixture_schema("minimal-openapi-3.json")
  expect_true(!is.null(schema$openapi))
  expect_equal(schema$openapi, "3.0.0")

  # Swagger 2.0
  schema2 <- load_fixture_schema("minimal-swagger-2.json")
  expect_true(!is.null(schema2$swagger))
  expect_equal(schema2$swagger, "2.0")

  # Circular refs (should load without error)
  schema3 <- load_fixture_schema("circular-refs.json")
  expect_true(!is.null(schema3$components$schemas$Node))

  # Malformed (valid JSON, invalid schema)
  schema4 <- load_fixture_schema("malformed.json")
  expect_true(is.null(schema4$openapi))
  expect_true(is.null(schema4$swagger))
})

test_that("clear_stubgen_env() cleans up state", {
  skip_on_cran()

  # Create some state
  if (!exists(".StubGenEnv", envir = .GlobalEnv)) {
    assign(".StubGenEnv", new.env(), envir = .GlobalEnv)
  }
  .StubGenEnv$test_var <- "test_value"

  # Clear it
  result <- clear_stubgen_env()
  expect_true(result)

  # Verify cleanup - either env removed or emptied
  if (exists(".StubGenEnv", envir = .GlobalEnv)) {
    expect_equal(length(ls(.StubGenEnv)), 0)
  }
})

test_that("withr is available for state management", {
  skip_on_cran()
  expect_true(requireNamespace("withr", quietly = TRUE))
})
```

This test file validates the entire infrastructure setup.
  </action>
  <verify>
```r
# Install withr if needed
if (!requireNamespace("withr", quietly = TRUE)) install.packages("withr")

# Run the infrastructure tests
devtools::test(filter = "pipeline-infrastructure")
# Should see: Test passed (5 tests)
```
  </verify>
  <done>
- withr is in DESCRIPTION Suggests
- test-pipeline-infrastructure.R exists
- All 5 tests pass
- Infrastructure verified working end-to-end
  </done>
</task>

</tasks>

<verification>
Run these commands to verify the phase is complete:

```r
# 1. Verify helper-pipeline.R works
source("tests/testthat/helper-pipeline.R")
source_pipeline_files()  # TRUE, no errors
clear_stubgen_env()  # TRUE

# 2. Verify fixtures exist and load
list.files("tests/testthat/fixtures/schemas/")  # 4 files
load_fixture_schema("minimal-openapi-3.json")  # Parses OK

# 3. Run infrastructure tests
devtools::test(filter = "pipeline-infrastructure")  # All pass

# 4. Verify withr available
library(withr)  # No error
```
</verification>

<success_criteria>
1. `tests/testthat/helper-pipeline.R` exists with 4 functions
2. `tests/testthat/fixtures/schemas/` contains 4 JSON files
3. All 8 pipeline files source without error via `source_pipeline_files()`
4. `clear_stubgen_env()` clears `.StubGenEnv` state
5. `withr` is in DESCRIPTION Suggests
6. `test-pipeline-infrastructure.R` passes all tests
7. No breaking changes to existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/12-test-infrastructure-setup/12-01-SUMMARY.md`
</output>
