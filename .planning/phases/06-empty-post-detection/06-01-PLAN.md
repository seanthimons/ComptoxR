---
phase: 06-empty-post-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev/endpoint_eval/07_stub_generation.R
autonomous: true

must_haves:
  truths:
    - "POST endpoints with no query params, no path params, and empty body are skipped during generation"
    - "User sees cli warning for each skipped endpoint"
    - "User sees cli warning for suspicious endpoints (optional-only params)"
    - "User sees summary count at end of generation"
    - "GET endpoints with no parameters still generate correctly"
    - "POST endpoints with ANY parameter still generate correctly"
  artifacts:
    - path: "dev/endpoint_eval/07_stub_generation.R"
      provides: "Empty POST detection and notification"
      contains: "is_empty_post_endpoint"
  key_links:
    - from: "render_endpoint_stubs()"
      to: "is_empty_post_endpoint()"
      via: "filter/detection before stub generation"
      pattern: "is_empty_post_endpoint"
    - from: "render_endpoint_stubs()"
      to: "cli::cli_warn()"
      via: "notification after detection"
      pattern: "cli_warn.*skipped"
---

<objective>
Implement detection and reporting for POST endpoints with incomplete schemas

Purpose: Users need clear feedback when POST endpoints cannot accept meaningful input (no query, no path, empty body) so they understand why certain stubs are not generated.

Output:
- Helper function `is_empty_post_endpoint()` that detects empty POST endpoints
- Updated `render_endpoint_stubs()` that skips empty POSTs and warns about suspicious endpoints
- Summary reporting at end of generation run
- Log file output for later reference
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-empty-post-detection/06-CONTEXT.md
@dev/endpoint_eval/07_stub_generation.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add detection helper function</name>
  <files>dev/endpoint_eval/07_stub_generation.R</files>
  <action>
Add helper function `is_empty_post_endpoint()` at the top of the file (after the `%|NA|%` helper).

The function should:
1. Accept parameters: `method`, `query_params`, `path_params`, `body_schema_full`, `body_schema_type`
2. Return a list with: `skip` (boolean), `reason` (character), `suspicious` (boolean)

Detection logic:
- If `method != "POST"`, return `list(skip = FALSE, reason = "", suspicious = FALSE)` immediately (SCOPE-01)
- Check for no query params: `is.null(query_params) || !nzchar(query_params)`
- Check for no path params: `is.null(path_params) || !nzchar(path_params)` OR `num_path_params == 0`
- Check for empty body using `body_schema_full`:
  - `is.null(body_schema_full)` = empty
  - `length(body_schema_full) == 0` = empty
  - `body_schema_full$type == "object"` AND (`is.null(body_schema_full$properties)` OR `length(body_schema_full$properties) == 0`) = empty
  - No `$ref` present and no schema = empty

Skip criteria (all must be true for `skip = TRUE`):
- Method is POST
- No query params (required OR optional)
- No path params
- Body schema is empty

Suspicious criteria (for `suspicious = TRUE`):
- Method is POST
- Has query params, BUT all are optional (no required params)
- Body schema is empty
- This endpoint generates a stub but gets a warning

Return structure:
```r
list(
  skip = TRUE/FALSE,
  reason = "No query parameters, no path parameters, empty body schema",
  suspicious = TRUE/FALSE,
  suspicious_reason = "Only optional query parameters, empty body schema"
)
```
  </action>
  <verify>
Source the file and test detection function manually:
```r
source("dev/endpoint_eval/07_stub_generation.R")
# Should return skip=TRUE
is_empty_post_endpoint("POST", "", "", list(type = "object"), "object")
# Should return skip=FALSE (GET method)
is_empty_post_endpoint("GET", "", "", list(type = "object"), "object")
# Should return skip=FALSE (has body properties)
is_empty_post_endpoint("POST", "", "", list(type = "object", properties = list(id = list())), "object")
```
  </verify>
  <done>
Helper function exists and correctly identifies:
- Empty POST endpoints (skip=TRUE)
- GET endpoints (skip=FALSE regardless of params)
- POST endpoints with any params (skip=FALSE)
- Suspicious POST endpoints (skip=FALSE, suspicious=TRUE)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update render_endpoint_stubs() with detection and filtering</name>
  <files>dev/endpoint_eval/07_stub_generation.R</files>
  <action>
Modify `render_endpoint_stubs()` to:

1. After the `ensure_cols()` call, add detection logic:
```r
# Detect empty POST endpoints
detection_results <- purrr::pmap(
  list(
    method = spec$method,
    query_params = spec$query_params,
    path_params = spec$path_params,
    body_schema_full = spec$body_schema_full,
    body_schema_type = spec$body_schema_type
  ),
  is_empty_post_endpoint
)

spec$skip_endpoint <- purrr::map_lgl(detection_results, "skip")
spec$skip_reason <- purrr::map_chr(detection_results, "reason")
spec$suspicious <- purrr::map_lgl(detection_results, "suspicious")
spec$suspicious_reason <- purrr::map_chr(detection_results, ~ .x$suspicious_reason %||% "")
```

2. Collect skipped and suspicious endpoints BEFORE filtering:
```r
# Collect skipped endpoints for reporting
skipped_endpoints <- spec %>%
  dplyr::filter(skip_endpoint) %>%
  dplyr::select(route, method, skip_reason)

# Collect suspicious endpoints for reporting
suspicious_endpoints <- spec %>%
  dplyr::filter(suspicious, !skip_endpoint) %>%
  dplyr::select(route, method, suspicious_reason)
```

3. Store these in a package-level environment for later summary (create `.StubGenEnv` at top of file if not exists):
```r
# At top of file (after %|NA|% helper):
.StubGenEnv <- new.env(parent = emptyenv())
.StubGenEnv$skipped <- list()
.StubGenEnv$suspicious <- list()

# In render_endpoint_stubs(), append to lists:
.StubGenEnv$skipped <- c(.StubGenEnv$skipped, list(skipped_endpoints))
.StubGenEnv$suspicious <- c(.StubGenEnv$suspicious, list(suspicious_endpoints))
```

4. Filter out skipped endpoints BEFORE the pre-process step:
```r
spec <- spec %>% dplyr::filter(!skip_endpoint)
```

5. Add early return if all endpoints were skipped:
```r
if (nrow(spec) == 0) {
  cli::cli_alert_warning("All endpoints were skipped (empty POST schemas)")
  return(tibble::tibble(
    fn = character(), endpoint = character(), title = character(), text = character()
  ))
}
```
  </action>
  <verify>
Source the file and create a test spec tibble with one empty POST endpoint:
```r
source("dev/endpoint_eval/07_stub_generation.R")
# Reset tracking
.StubGenEnv$skipped <- list()
.StubGenEnv$suspicious <- list()

# Create minimal test spec
test_spec <- tibble::tibble(
  file = "test.R",
  route = "test/endpoint",
  method = "POST",
  summary = "Test",
  query_params = "",
  path_params = "",
  body_params = "",
  body_schema_full = list(list(type = "object")),
  body_schema_type = "object",
  # Other required columns with defaults
  num_path_params = 0L,
  num_body_params = 0L,
  batch_limit = NA_integer_,
  content_type = "",
  needs_resolver = FALSE,
  deprecated = FALSE,
  response_schema_type = "unknown",
  request_type = NA_character_,
  body_item_type = NA_character_,
  path_param_metadata = list(NULL),
  query_param_metadata = list(NULL),
  body_param_metadata = list(NULL)
)

# Should return empty tibble and populate .StubGenEnv$skipped
result <- render_endpoint_stubs(test_spec, config = list(wrapper_function = "generic_request"))
nrow(result) == 0  # TRUE
length(.StubGenEnv$skipped) > 0  # TRUE
```
  </verify>
  <done>
`render_endpoint_stubs()`:
- Detects and filters out empty POST endpoints
- Tracks skipped endpoints in `.StubGenEnv$skipped`
- Tracks suspicious endpoints in `.StubGenEnv$suspicious`
- Returns empty tibble gracefully when all endpoints skipped
  </done>
</task>

<task type="auto">
  <name>Task 3: Add summary reporting function</name>
  <files>dev/endpoint_eval/07_stub_generation.R</files>
  <action>
Add function `report_skipped_endpoints()` at the end of the file (before final closing comment if any).

The function should:
1. Read from `.StubGenEnv$skipped` and `.StubGenEnv$suspicious`
2. Combine all tracked tibbles
3. Display summary using `cli` with styled output

Implementation:
```r
#' Report skipped and suspicious endpoints from stub generation
#'
#' Call this after all render_endpoint_stubs() calls to display summary.
#' Also writes to log file for later reference.
#' @param log_dir Directory for log file. Default: "dev/logs"
#' @return Invisible NULL. Called for side effects (cli output).
#' @export
report_skipped_endpoints <- function(log_dir = "dev/logs") {
  # Combine all tracked results
  skipped <- dplyr::bind_rows(.StubGenEnv$skipped)
  suspicious <- dplyr::bind_rows(.StubGenEnv$suspicious)

  n_skipped <- nrow(skipped)
  n_suspicious <- nrow(suspicious)

  if (n_skipped == 0 && n_suspicious == 0) {
    cli::cli_alert_success("All endpoints generated successfully")
    return(invisible(NULL))
  }

  # Summary count line
  cli::cli_h2("Endpoint Generation Report")

  if (n_skipped > 0) {
    cli::cli_alert_danger("{n_skipped} endpoint{?s} skipped")
    cli::cli_div(theme = list(span.skip = list(color = "red")))
    for (i in seq_len(n_skipped)) {
      cli::cli_bullets(c(
        " " = "{.skip {skipped$method[i]} {skipped$route[i]}}: {skipped$skip_reason[i]}"
      ))
    }
    cli::cli_end()
  }

  if (n_suspicious > 0) {
    cli::cli_alert_warning("{n_suspicious} endpoint{?s} suspicious (may have incomplete API docs)")
    cli::cli_div(theme = list(span.warn = list(color = "yellow")))
    for (i in seq_len(n_suspicious)) {
      cli::cli_bullets(c(
        " " = "{.warn {suspicious$method[i]} {suspicious$route[i]}}: {suspicious$suspicious_reason[i]}"
      ))
    }
    cli::cli_end()
  }

  # Write to log file
  if (!dir.exists(log_dir)) {
    dir.create(log_dir, recursive = TRUE)
  }

  log_file <- file.path(log_dir, paste0("stub_generation_", format(Sys.time(), "%Y%m%d_%H%M%S"), ".log"))

  log_lines <- c(
    paste0("Stub Generation Report - ", Sys.time()),
    paste0("Skipped: ", n_skipped),
    paste0("Suspicious: ", n_suspicious),
    ""
  )

  if (n_skipped > 0) {
    log_lines <- c(log_lines, "SKIPPED ENDPOINTS:", "")
    for (i in seq_len(n_skipped)) {
      log_lines <- c(log_lines, paste0("  ", skipped$method[i], " ", skipped$route[i], " - ", skipped$skip_reason[i]))
    }
    log_lines <- c(log_lines, "")
  }

  if (n_suspicious > 0) {
    log_lines <- c(log_lines, "SUSPICIOUS ENDPOINTS:", "")
    for (i in seq_len(n_suspicious)) {
      log_lines <- c(log_lines, paste0("  ", suspicious$method[i], " ", suspicious$route[i], " - ", suspicious$suspicious_reason[i]))
    }
  }

  writeLines(log_lines, log_file)
  cli::cli_alert_info("Log written to {.file {log_file}}")

  invisible(NULL)
}

#' Reset skipped/suspicious endpoint tracking
#'
#' Call before starting a new generation run to clear previous results.
#' @return Invisible NULL.
#' @export
reset_endpoint_tracking <- function() {
  .StubGenEnv$skipped <- list()
  .StubGenEnv$suspicious <- list()
  invisible(NULL)
}
```

Also update `dev/generate_stubs.R` to call `report_skipped_endpoints()` at the end, before the "Summary" section:
- Add `reset_endpoint_tracking()` near the top (after loading utilities)
- Add `report_skipped_endpoints()` call after all three generate_*_stubs() functions complete
  </action>
  <verify>
Test the reporting function:
```r
source("dev/endpoint_eval/07_stub_generation.R")
reset_endpoint_tracking()

# Simulate some skipped endpoints
.StubGenEnv$skipped <- list(
  tibble::tibble(route = "test/empty", method = "POST", skip_reason = "No params, empty body")
)
.StubGenEnv$suspicious <- list(
  tibble::tibble(route = "test/optional", method = "POST", suspicious_reason = "Only optional params")
)

# Should display styled summary and create log file
report_skipped_endpoints(log_dir = tempdir())
```

Verify log file was created in the specified directory.
  </verify>
  <done>
- `report_skipped_endpoints()` displays styled summary (red for skipped, yellow for suspicious)
- Log file created with full details
- `reset_endpoint_tracking()` clears tracking state
- `dev/generate_stubs.R` calls reporting at end of generation
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run full stub generation and verify output:
```r
source("dev/generate_stubs.R")
```
Expected: See skipped/suspicious summary at end, log file created in `dev/logs/`

2. Verify GET endpoints unaffected:
- Any GET endpoint with no params should still generate (static endpoints)

3. Verify POST endpoints with params unaffected:
- POST endpoints with body properties should still generate
- POST endpoints with query params should still generate

4. Verify empty POST detected:
- `API/predictor/predict` (or similar with `{type: object}` only) should be skipped
</verification>

<success_criteria>
Requirements coverage:
- DETECT-01: POST + no query params check implemented in `is_empty_post_endpoint()`
- DETECT-02: POST + no path params check implemented in `is_empty_post_endpoint()`
- DETECT-03: Empty body schema check implemented (type:object with no properties, null, missing)
- DETECT-04: Combined criteria returns `skip=TRUE` only when ALL conditions met
- NOTIFY-01: `cli::cli_warn()`/styled bullets shown for skipped endpoints
- NOTIFY-02: Skipped endpoints collected in `.StubGenEnv$skipped`
- NOTIFY-03: `report_skipped_endpoints()` shows summary at end
- SCOPE-01: GET endpoints always return `skip=FALSE` from detection
- SCOPE-02: POST endpoints with ANY parameter return `skip=FALSE`
</success_criteria>

<output>
After completion, create `.planning/phases/06-empty-post-detection/06-01-SUMMARY.md`
</output>
