# Phase 27: Test Infrastructure Stabilization - Context

**Gathered:** 2026-03-09
**Status:** Ready for planning

<domain>
## Phase Boundary

Fix mechanical test blockers (VCR key sanitization, purrr::flatten warning, cassette re-recording) so tests can run reliably. This phase delivers working infrastructure, not passing test assertions -- Phases 28-30 handle migration and correctness.

</domain>

<decisions>
## Implementation Decisions

### Cassette Re-recording Strategy
- Record ALL 400+ cassettes (full coverage, not a subset)
- Single R process using `mirai` package for async + parallel execution
- `mirai::daemons()` with `mirai_map()` over cassettes grouped by major family
- Parallel streams at the **major family level** (bioactivity, chemical, exposure, hazard, etc.) -- follows the existing endpoint taxonomy: super-family (CT vs Chemi) -> major family -> group
- Halt on failure threshold -- if failure rate exceeds a threshold, stop the run (indicates systemic issue like expired key or API outage)
- Fire-and-forget design -- user kicks it off and does other work while it churns

### Script Location
- Re-recording script: `dev/record_cassettes.R`
- Health check script: `dev/check_cassette_health.R`
- Both in `dev/` alongside existing `generate_tests.R` and `detect_test_gaps.R`
- NOT in `tests/` or `R/` -- must not be sourced during package load or test runs

### Success Threshold
- Zero mechanical failures -- all VCR/auth/infrastructure failures eliminated
- Infrastructure-only verification -- whether test assertions pass is Phase 28-30 territory
- Automated check script validates: cassette safety (no leaked keys), no HTTP error responses frozen in fixtures, all fixture files parse without VCR errors

### VCR Sanitization
- Primary: `x-api-key` header filtering (already exists in helper-vcr.R)
- Additional: known internal/staging EPA URL patterns in response bodies (user will provide specific patterns at planning time)
- Response body data is public EPA data -- no PII concerns
- Cheminformatics endpoints are unauthenticated (auth=FALSE) -- no key filtering needed for those

### NAMESPACE / Flatten Warning Fix
- Root cause: blanket `@import purrr` and `@import jsonlite` in NAMESPACE pull in deprecated `purrr::flatten` family
- Fix: replace with selective `@importFrom` declarations for functions actually used
- Source code: keep bare function calls (e.g., `map()` not `purrr::map()`) -- fix NAMESPACE only, minimize file churn
- Both purrr and jsonlite need this treatment

### Claude's Discretion
- Exact mirai daemon count and rate limiting per family
- Failure threshold percentage (e.g., 10% vs 20%)
- Internal structure of the recording script (how it discovers cassette-to-function mappings)
- How to detect which cassettes belong to which major family (filename convention vs test file parsing)

</decisions>

<specifics>
## Specific Ideas

- The endpoint taxonomy (super-family -> major family -> group) is already encoded in schema files and function naming -- the recording script should leverage this rather than reinventing grouping
- User wants a process they can "fire off and let churn" -- design for unattended execution with good logging

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- `tests/testthat/helper-vcr.R`: VCR configuration, `check_cassette_safety()`, `check_cassette_errors()`, `list_cassettes()`, `delete_cassettes()` -- foundation for health check script
- `dev/generate_tests.R`: Test generator with parameter mapping -- may inform cassette-to-function discovery
- `dev/detect_test_gaps.R`: Gap detection with manifest tracking -- knows which functions have tests
- `dev/test_manifest.json`: Tracks 42 test files, maps generated vs protected

### Established Patterns
- Cassette naming: `{domain}_{family}_{variant}.yml` (e.g., `ct_bioactivity_assay_search_by_aeid_example.yml`)
- VCR filter: `filter_sensitive_data = list("<<<API_KEY>>>" = Sys.getenv("ctx_api_key"))`
- `z_generic_request.R` uses `purrr::list_flatten()` (modern) -- no deprecated calls in core request code
- `chemi_safety_sections.R` uses `list_flatten()` extensively -- also modern

### Integration Points
- `helper-vcr.R` configures VCR for test runs -- sanitization changes here affect all test recording
- NAMESPACE file generated by `devtools::document()` from roxygen `@import`/`@importFrom` tags
- `dev/` scripts are invoked manually by developers, not by CI or package infrastructure

</code_context>

<deferred>
## Deferred Ideas

None -- discussion stayed within phase scope

</deferred>

---

*Phase: 27-test-infrastructure-stabilization*
*Context gathered: 2026-03-09*
