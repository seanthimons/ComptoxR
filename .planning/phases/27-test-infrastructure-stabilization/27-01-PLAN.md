---
phase: 27-test-infrastructure-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/ComptoxR-package.R
  - NAMESPACE
autonomous: true
requirements:
  - INFRA-27-01
  - INFRA-27-02
must_haves:
  truths:
    - "Package loads without purrr::flatten deprecation warnings"
    - "NAMESPACE contains only @importFrom declarations for purrr and jsonlite (no blanket @import)"
    - "All existing functionality remains working (no missing imports)"
  artifacts:
    - path: "R/ComptoxR-package.R"
      provides: "Selective importFrom declarations for purrr and jsonlite"
      contains: "@importFrom purrr"
    - path: "NAMESPACE"
      provides: "Generated namespace with importFrom directives"
      contains: "importFrom(purrr,"
  key_links:
    - from: "R/ComptoxR-package.R"
      to: "NAMESPACE"
      via: "devtools::document() regeneration"
      pattern: "importFrom\\(purrr,"
---

<objective>
Replace blanket `@import purrr` and `@import jsonlite` with selective `@importFrom` declarations to eliminate the purrr::flatten deprecation warning on package load.

Purpose: The blanket @import pulls in deprecated purrr::flatten family functions, triggering warnings every time the package loads. This is a mechanical blocker for reliable test execution.
Output: Clean NAMESPACE with selective imports, warning-free package load.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-test-infrastructure-stabilization/27-CONTEXT.md
@R/ComptoxR-package.R
@NAMESPACE
</context>

<interfaces>
<!-- Current state of R/ComptoxR-package.R (lines 14-27): -->
```r
#' @import dplyr
NULL
#' @import httr2
NULL
#' @import cli
NULL
#' @import purrr       # <-- PROBLEM: pulls in deprecated flatten family
NULL
#' @import stringr
NULL
#' @import tidyr
NULL
#' @import jsonlite    # <-- PROBLEM: pulls in jsonlite::flatten which conflicts
NULL
```

<!-- Actual purrr functions used across R/ (from grep): -->
<!-- map, map2, walk, imap, iwalk, compact, pluck, set_names, keep, discard_at,
     list_flatten, list_c, list_rbind, map_chr, map_dbl, map_int, map_lgl,
     map_dfr, map_if, map_vec, pmap, reduce -->

<!-- Actual jsonlite functions used across R/ (from grep): -->
<!-- fromJSON (in schema.R), flatten (in chemi_functional_use.R -- this is jsonlite::flatten, NOT purrr) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit purrr and jsonlite usage and replace @import with @importFrom</name>
  <files>R/ComptoxR-package.R, NAMESPACE</files>
  <action>
1. Audit ALL purrr function calls across R/ directory using grep. The known set from research includes: map, map2, walk, imap, iwalk, compact, pluck, set_names, keep, discard_at, list_flatten, list_c, list_rbind, map_chr, map_dbl, map_int, map_lgl, map_dfr, map_if, map_vec, pmap, reduce. Verify this is complete by grepping for any bare function calls that resolve to purrr (check purrr's exported function list).

2. Audit ALL jsonlite function calls across R/. Known: fromJSON (in schema.R), flatten (in chemi_functional_use.R -- note: this is jsonlite::flatten which flattens nested data frames, distinct from purrr::flatten). Also check for toJSON, prettify, minify, unbox, read_json, write_json.

3. In R/ComptoxR-package.R, replace:
   - `#' @import purrr` with explicit `@importFrom purrr` declarations listing EVERY purrr function found in step 1
   - `#' @import jsonlite` with explicit `@importFrom jsonlite` declarations listing EVERY jsonlite function found in step 2

   Keep the `NULL` after each block. Keep other @import lines (dplyr, httr2, cli, stringr, tidyr) unchanged -- those are not causing deprecation warnings and changing them is out of scope.

4. Run `devtools::document()` to regenerate NAMESPACE from the updated roxygen comments.

5. Verify NAMESPACE no longer contains `import(purrr)` or `import(jsonlite)` -- should only have `importFrom(purrr, ...)` and `importFrom(jsonlite, ...)` lines.

6. Load the package with `devtools::load_all()` and verify NO deprecation warnings appear.

IMPORTANT: Do NOT change any source code in the R/ files themselves. Only modify the roxygen import declarations in R/ComptoxR-package.R. Bare function calls like `map()` will still work because @importFrom makes them available in the package namespace.

NOTE on jsonlite::flatten: The call in chemi_functional_use.R line 42 uses `jsonlite::flatten()` with explicit namespace prefix, so it will work regardless of @import vs @importFrom. Still include `flatten` in the @importFrom jsonlite declaration so it's properly declared.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); message('SUCCESS: No warnings')" 2>&1 | grep -v "^Loading\|^i\|^Registered" | grep -c "flatten" || echo "PASS: No flatten warnings"</automated>
  </verify>
  <done>
- R/ComptoxR-package.R has @importFrom purrr and @importFrom jsonlite (no blanket @import for either)
- NAMESPACE has importFrom(purrr, ...) and importFrom(jsonlite, ...) lines (no import(purrr) or import(jsonlite))
- Package loads without purrr::flatten deprecation warning
- All bare function calls still resolve correctly
  </done>
</task>

</tasks>

<verification>
- `grep "import(purrr)" NAMESPACE` returns only `importFrom` lines, not `import(purrr)`
- `grep "import(jsonlite)" NAMESPACE` returns only `importFrom` lines, not `import(jsonlite)`
- Package loads cleanly: no deprecation warnings about flatten
</verification>

<success_criteria>
- NAMESPACE contains selective importFrom for purrr and jsonlite
- Package loads without purrr::flatten deprecation warning
- No missing imports (all bare function calls resolve)
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-infrastructure-stabilization/27-01-SUMMARY.md`
</output>
