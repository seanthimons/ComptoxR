---
phase: 27-test-infrastructure-stabilization
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01
  - 27-02
files_modified:
  - tests/testthat/fixtures/*.yml
autonomous: false
requirements:
  - INFRA-27-06
must_haves:
  truths:
    - "All cassettes are re-recorded with fresh production API data"
    - "No cassettes contain leaked API keys"
    - "No cassettes contain HTTP error responses (4xx/5xx)"
    - "All cassettes parse as valid YAML"
  artifacts:
    - path: "tests/testthat/fixtures/"
      provides: "717+ re-recorded VCR cassettes with production data"
      min_lines: 1
  key_links:
    - from: "dev/record_cassettes.R"
      to: "tests/testthat/fixtures/"
      via: "Parallel VCR recording via mirai"
      pattern: "testthat::test_file"
---

<objective>
Execute the cassette re-recording process and validate results.

Purpose: With NAMESPACE fixed (Plan 01) and recording infrastructure built (Plan 02), run the actual cassette re-recording against production EPA APIs. This produces the clean fixture data needed for reliable test execution in Phases 28-30.
Output: 717+ re-recorded cassettes passing all health checks.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-test-infrastructure-stabilization/27-CONTEXT.md
@.planning/phases/27-test-infrastructure-stabilization/27-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Execute cassette re-recording</name>
  <files>tests/testthat/fixtures/*.yml</files>
  <action>
The user must run the recording script with their API key set. Claude cannot do this because:
1. The ctx_api_key is a secret that must not be exposed
2. Recording hits production EPA APIs (requires valid auth)
3. The process takes 10-20 minutes (fire-and-forget design)
  </action>
  <instructions>
1. Ensure your EPA API key is set:
   ```r
   Sys.setenv(ctx_api_key = "YOUR_KEY_HERE")
   ```
   Or set it in your .Renviron file.

2. Run the recording script from the project root:
   ```bash
   "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" dev/record_cassettes.R
   ```

3. Monitor the output -- the script will:
   - Show progress per major family
   - Halt if failure rate exceeds 15% (indicates systemic issue)
   - Run health check automatically after completion

4. If the script halts on threshold:
   - Check if API key is valid/not expired
   - Check if EPA APIs are reachable
   - Re-run after fixing the issue

5. After completion, report:
   - "approved" if health check passed (0 safety issues, 0 error responses, 0 parse errors)
   - Describe any issues if health check failed
  </instructions>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('dev/check_cassette_health.R')" 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms recording completed and health check passed</done>
  <resume-signal>Type "approved" if health check passed, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Validate and commit re-recorded cassettes</name>
  <files>tests/testthat/fixtures/*.yml</files>
  <action>
After user confirms recording completed:

1. Run the health check script to independently verify:
   ```
   Rscript dev/check_cassette_health.R
   ```

2. Count cassettes to confirm expected number (should be ~717):
   ```
   ls tests/testthat/fixtures/*.yml | wc -l
   ```

3. Spot-check a few cassettes from different families:
   - One ct_* cassette: verify it has 2xx status, <<<API_KEY>>> placeholder, valid response data
   - One chemi_* cassette: verify it has no API key header at all (unauthenticated)

4. If health check passes, stage and commit the cassettes.

NOTE: The cassette files are large in aggregate. Commit them in a single commit with a clear message. Do NOT add dev/reports/ to the commit (log files are gitignored or ephemeral).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('dev/check_cassette_health.R')" 2>&1 | tail -1</automated>
  </verify>
  <done>
- Health check script reports 0 safety issues, 0 error responses, 0 parse errors
- Cassette count is ~717 or more
- Cassettes committed to git
  </done>
</task>

</tasks>

<verification>
- `Rscript dev/check_cassette_health.R` exits with status 0
- All cassettes have 2xx status codes
- No API keys leaked in cassette files
- All YAML files parse correctly
</verification>

<success_criteria>
- All 717+ cassettes re-recorded with fresh production data
- Health check passes with zero issues
- Cassettes committed to repository
- Test infrastructure is ready for Phases 28-30
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-infrastructure-stabilization/27-03-SUMMARY.md`
</output>
