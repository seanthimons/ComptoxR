---
phase: 09-integration-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev/endpoint_eval/verify_phase9.R
  - R/chemi_amos*.R
  - R/chemi_rdkit*.R
  - R/chemi_mordred*.R
autonomous: true

must_haves:
  truths:
    - "AMOS schema POST endpoints generate function stubs with correct body parameters"
    - "RDKit schema POST endpoints generate function stubs with correct body parameters"
    - "Mordred schema POST endpoints generate function stubs with correct body parameters"
    - "OpenAPI 3.0 stubs (ctx-chemical) are identical before and after changes"
    - "Empty POST detection works correctly for both schema versions"
  artifacts:
    - path: "dev/endpoint_eval/verify_phase9.R"
      provides: "End-to-end validation script"
      min_lines: 100
    - path: ".baseline/"
      provides: "Baseline stubs for comparison"
  key_links:
    - from: "openapi_to_spec()"
      to: "detect_schema_version()"
      via: "function call at entry point"
      pattern: "schema_version.*detect_schema_version"
    - from: "verify_phase9.R"
      to: "render_endpoint_stubs()"
      via: "stub generation call"
      pattern: "render_endpoint_stubs"
---

<objective>
Validate that all v1.5 integration is complete and working by regenerating microservice stubs and confirming no regressions.

Purpose: Prove that Swagger 2.0 body extraction and reference resolution work end-to-end by producing correct function stubs for AMOS, RDKit, and Mordred microservices while ensuring OpenAPI 3.0 parsing is unchanged.

Output: Verification script confirming all requirements, regenerated stubs for three microservices, and before/after comparison proving no regressions.
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-reference-resolution/08-02-SUMMARY.md
@.planning/phases/09-integration-validation/09-RESEARCH.md

# Key files for validation
@dev/endpoint_eval/00_config.R
@dev/endpoint_eval/01_schema_resolution.R
@dev/endpoint_eval/04_openapi_parser.R
@dev/endpoint_eval/07_stub_generation.R
@dev/endpoint_eval/verify_07-02_integration.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create baseline and verify INTEG-01 (version detection wired)</name>
  <files>
    dev/endpoint_eval/verify_phase9.R
    .baseline/stubs/
  </files>
  <action>
    Create comprehensive Phase 9 verification script that:

    1. **Save baseline stubs** before regeneration:
       - Create `.baseline/stubs/` directory
       - Copy all `R/chemi_amos*.R` files to baseline
       - Copy all `R/chemi_rdkit*.R` files to baseline
       - Copy all `R/chemi_mordred*.R` files to baseline
       - Copy `R/ct_chemical*.R` files for OpenAPI 3.0 regression baseline

    2. **Verify INTEG-01** (version detection wired):
       - Source all pipeline modules (00_config.R through 07_stub_generation.R)
       - Test `openapi_to_spec()` calls `detect_schema_version()` by checking schema_version in returned spec
       - For Swagger 2.0 (chemi-amos-prod.json): verify `schema_version$type == "swagger"`
       - For OpenAPI 3.0 (ctx-chemical-prod.json): verify `schema_version$type == "openapi"`
       - Use `cli::cli_alert_success()` for passing checks
       - Use `stopifnot()` for assertions that must pass

    3. **Report version detection results**:
       - Log detected version for each test schema
       - Confirm routing to correct extraction logic

    Script pattern from verify_07-02_integration.R:
    ```r
    source("dev/endpoint_eval/00_config.R")
    source("dev/endpoint_eval/01_schema_resolution.R")
    source("dev/endpoint_eval/04_openapi_parser.R")
    library(cli)

    # Create baseline
    dir.create(".baseline/stubs", recursive = TRUE, showWarnings = FALSE)
    # ... copy existing stubs

    # Test INTEG-01
    cli::cli_h2("INTEG-01: Version Detection Wired")
    # ... verification tests
    ```
  </action>
  <verify>
    Run `source("dev/endpoint_eval/verify_phase9.R")` and confirm:
    - Baseline directory created with existing stubs
    - Swagger 2.0 schema detected as type "swagger"
    - OpenAPI 3.0 schema detected as type "openapi"
    - All cli success messages printed
  </verify>
  <done>
    INTEG-01 verified: openapi_to_spec() correctly detects and routes both schema versions
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate microservice stubs (INTEG-02, INTEG-04, INTEG-05, INTEG-06)</name>
  <files>
    dev/endpoint_eval/verify_phase9.R
    R/chemi_amos*.R
    R/chemi_rdkit*.R
    R/chemi_mordred*.R
  </files>
  <action>
    Extend verify_phase9.R to regenerate stubs for all three Swagger 2.0 microservices:

    1. **Source full pipeline** (add stub generation modules):
       ```r
       source("dev/endpoint_eval/02_path_utils.R")
       source("dev/endpoint_eval/05_file_scaffold.R")
       source("dev/endpoint_eval/06_param_parsing.R")
       source("dev/endpoint_eval/07_stub_generation.R")
       ```

    2. **Configure stub generation**:
       ```r
       config <- list(
         wrapper_function = "generic_chemi_request",
         param_strategy = "extra_params",
         example_query = "DTXSID7020182",
         lifecycle_badge = "experimental"
       )
       ```

    3. **Regenerate AMOS stubs** (INTEG-04):
       - Parse schema: `amos_spec <- openapi_to_spec("schema/chemi-amos-prod.json")`
       - Reset tracking: `reset_endpoint_tracking()`
       - Generate: `amos_stubs <- render_endpoint_stubs(amos_spec, config)`
       - Report: Count of POST endpoints with body params
       - Verify: At least some POST endpoints have `has_body == TRUE`
       - Verify: Generated stubs include `@param` documentation for body parameters

    4. **Regenerate RDKit stubs** (INTEG-05):
       - Parse schema: `rdkit_spec <- openapi_to_spec("schema/chemi-rdkit-staging.json")`
       - Reset tracking: `reset_endpoint_tracking()`
       - Generate: `rdkit_stubs <- render_endpoint_stubs(rdkit_spec, config)`
       - Report: Count of POST endpoints with body params

    5. **Regenerate Mordred stubs** (INTEG-06):
       - Parse schema: `mordred_spec <- openapi_to_spec("schema/chemi-mordred-staging.json")`
       - Reset tracking: `reset_endpoint_tracking()`
       - Generate: `mordred_stubs <- render_endpoint_stubs(mordred_spec, config)`
       - Report: Count of POST endpoints with body params

    6. **Verify INTEG-02** (all Swagger 2.0 POST endpoints generate stubs):
       - Check that POST endpoints with `has_body == TRUE` produced stubs
       - Check that body_params are non-empty for endpoints with body schemas
       - Report skipped endpoints via `report_skipped_endpoints()`

    7. **Document body parameter extraction**:
       - For each microservice, log sample body_params strings
       - Verify roxygen `@param` docs include body parameter descriptions

    Note: Do NOT write files to R/ directory during verification. Instead, generate in-memory stubs and compare structure. Actual file writing is optional checkpoint at the end.
  </action>
  <verify>
    Run updated verify_phase9.R and confirm:
    - AMOS: POST endpoints with body schemas generate correct body_params
    - RDKit: POST endpoints with body schemas generate correct body_params
    - Mordred: POST endpoints with body schemas generate correct body_params
    - Empty POST detection skips endpoints with no meaningful input
    - Generated stubs include @param documentation for body parameters
  </verify>
  <done>
    INTEG-02, INTEG-04, INTEG-05, INTEG-06 verified: All Swagger 2.0 POST endpoints generate stubs with correct body parameters
  </done>
</task>

<task type="auto">
  <name>Task 3: OpenAPI 3.0 regression test (INTEG-03)</name>
  <files>
    dev/endpoint_eval/verify_phase9.R
  </files>
  <action>
    Extend verify_phase9.R to verify OpenAPI 3.0 parsing is unchanged:

    1. **Parse OpenAPI 3.0 schemas**:
       ```r
       ctx_chem_spec <- openapi_to_spec("schema/ctx-chemical-prod.json")
       chemi_resolver_spec <- openapi_to_spec("schema/chemi-resolver-prod.json")
       ```

    2. **Verify POST endpoint body extraction** (regression check):
       - Count POST endpoints with `has_body == TRUE`
       - Verify body_params are non-empty for endpoints with requestBody
       - Compare against expected counts from Phase 7 tests

    3. **Verify no parsing errors**:
       - All endpoints should parse without cli::cli_abort() errors
       - Version detection should return type "openapi"

    4. **Compare baseline to regenerated** (structural comparison):
       - Generate OpenAPI 3.0 stubs in-memory
       - Compare function signatures (parameter names, order)
       - Compare roxygen documentation (param descriptions)
       - If differences found, log them (may be improvements, not regressions)

    5. **INTEG-03 verification**:
       - Confirm OpenAPI 3.0 body extraction produces same results as before v1.5
       - Confirm reference resolution for `#/components/schemas/` still works
       - Confirm no new warnings or errors introduced

    6. **Final summary**:
       ```r
       cli::cli_h1("Phase 9 Integration Validation Complete")
       cli::cli_alert_success("INTEG-01: Version detection wired")
       cli::cli_alert_success("INTEG-02: Swagger 2.0 POST endpoints generate stubs")
       cli::cli_alert_success("INTEG-03: OpenAPI 3.0 unchanged (no regression)")
       cli::cli_alert_success("INTEG-04: AMOS stubs regenerated")
       cli::cli_alert_success("INTEG-05: RDKit stubs regenerated")
       cli::cli_alert_success("INTEG-06: Mordred stubs regenerated")
       ```
  </action>
  <verify>
    Run complete verify_phase9.R and confirm:
    - OpenAPI 3.0 schemas parse without errors
    - POST endpoints extract body parameters correctly
    - No regression in existing functionality
    - All INTEG-01 through INTEG-06 requirements pass
    - Final summary shows all checks successful
  </verify>
  <done>
    INTEG-03 verified: OpenAPI 3.0 stubs unchanged, no regression in existing functionality
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Run full verification script**:
   ```r
   source("dev/endpoint_eval/verify_phase9.R")
   ```

2. **Check all requirements pass**:
   - [ ] INTEG-01: Version detection wired (verified by schema_version check)
   - [ ] INTEG-02: Swagger 2.0 POST endpoints generate stubs (body_params populated)
   - [ ] INTEG-03: OpenAPI 3.0 unchanged (no errors, same output)
   - [ ] INTEG-04: AMOS stubs have correct body parameters
   - [ ] INTEG-05: RDKit stubs have correct body parameters
   - [ ] INTEG-06: Mordred stubs have correct body parameters

3. **Verify empty POST detection works for both versions**:
   - Check `report_skipped_endpoints()` output
   - Confirm suspicious endpoints flagged appropriately

4. **Optional: Write regenerated stubs to R/ directory**:
   - Only if verification passes and user requests actual regeneration
   - Use `write_stubs_to_files()` or manual file operations
</verification>

<success_criteria>
- verify_phase9.R runs without errors (all stopifnot() pass)
- All 6 INTEG requirements show cli::cli_alert_success()
- Swagger 2.0 schemas (AMOS, RDKit, Mordred) produce stubs with body parameters
- OpenAPI 3.0 schemas produce identical results to pre-v1.5 baseline
- Empty POST detection correctly filters non-functional endpoints
- No regressions introduced by v1.5 changes
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-validation/09-01-SUMMARY.md` following the summary template with:
- Tasks completed
- Requirements satisfied (INTEG-01 through INTEG-06)
- Files modified
- Verification results
- Any deviations from plan
</output>
