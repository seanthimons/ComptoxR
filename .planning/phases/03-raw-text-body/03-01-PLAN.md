# Plan 03-01: Fix Stub Generation for Raw Text Body Endpoint

## Frontmatter

```yaml
phase: 3
plan: 01
wave: 1
depends_on: []
files_modified:
  - dev/endpoint_eval/07_stub_generation.R
  - R/ct_chemical_search_equal.R
autonomous: true
```

## Objective

Add special case handling in stub generation for `/chemical/search/equal/` POST endpoint to generate code that sends raw text body instead of JSON.

## Context

**Problem:** The `/chemical/search/equal/` POST endpoint expects a raw text body with newline-separated values, but the current stub generation produces code that uses `generic_request()` which sends JSON.

**API Documentation says:** "Values must be separated by EOL characters and a maximum of 200 values is allowed per request."

**Reference implementation:** Old `ct_search.R` lines 127-136:
```r
httr2::req_body_raw(
  body = paste(chunk$searchValue, collapse = "\n"),
  type = "text/plain"
)
```

## Tasks

<task id="1" type="code">
<title>Add special case detection in build_function_stub()</title>
<description>
In `dev/endpoint_eval/07_stub_generation.R`, add early detection for the `/chemical/search/equal/` POST endpoint pattern. This should be checked BEFORE the existing `is_simple_body` handling.
</description>
<file>dev/endpoint_eval/07_stub_generation.R</file>
<location>After line ~370, before the `if (isTRUE(is_simple_body))` block</location>
<implementation>
Add detection logic:
```r
# Special case: /chemical/search/equal/ POST endpoint needs raw text body
is_raw_text_endpoint <- (
 endpoint == "chemical/search/equal/" &&
 toupper(method) == "POST" &&
 body_schema_type == "string"
)
```
</implementation>
</task>

<task id="2" type="code" depends_on="1">
<title>Generate raw text body function code</title>
<description>
Add code generation block for the raw text endpoint that produces a standalone function using httr2::req_body_raw() instead of generic_request().
</description>
<file>dev/endpoint_eval/07_stub_generation.R</file>
<location>After the detection logic, add new conditional block</location>
<implementation>
Generate function that:
1. Takes `query` parameter (character vector)
2. Handles batching (max 200 per API docs, but use env var for configurability)
3. Collapses values to newlines
4. Uses httr2::req_body_raw() with type = "text/plain"
5. Parses JSON response
6. Returns tidy tibble

Template structure:
```r
if (isTRUE(is_raw_text_endpoint)) {
  # Build roxygen header
  roxygen_header <- glue::glue('
#\' {title}
#\'
#\' @description
#\' `r lifecycle::badge("{lifecycle_badge}")`
#\'
#\' Search for chemicals using exact string matching (batch).
#\' Values are sent as newline-delimited plain text.
#\'
#\' @param query Character vector of search terms (chemical names, DTXSIDs, CAS, InChIKey)
#\' @return A tibble with search results
#\' @export
#\'
#\' @examples
#\' \\dontrun{{
#\' {fn}(query = c("DTXSID7020182", "DTXSID9020112"))
#\' }}')

  fn_body <- glue::glue('
{fn} <- function(query) {{
  # Input validation
  if (is.list(query) && !is.data.frame(query)) {{
    query <- as.character(unlist(query, use.names = FALSE))
  }}
  query <- unique(as.vector(query))
  query <- query[!is.na(query) & nzchar(query)]

  if (length(query) == 0) {{
    cli::cli_abort("Query must contain at least one non-empty value.")
  }}

  # Batch configuration (API max is 200)
  batch_limit <- as.numeric(Sys.getenv("batch_limit", "200"))
  batch_limit <- min(batch_limit, 200)  # Enforce API maximum

  # Split into batches
  if (length(query) > batch_limit) {{
    batches <- split(query, ceiling(seq_along(query) / batch_limit))
  }} else {{
    batches <- list(query)
  }}

  # Build and execute requests
  results <- purrr::map(batches, function(batch) {{
    body_text <- paste(batch, collapse = "\\n")

    req <- httr2::request(Sys.getenv("ctx_burl")) |>
      httr2::req_url_path_append("{endpoint}") |>
      httr2::req_headers(
        Accept = "application/json",
        `x-api-key` = ct_api_key()
      ) |>
      httr2::req_body_raw(body_text, type = "text/plain")

    resp <- httr2::req_perform(req)

    if (httr2::resp_status(resp) >= 300) {{
      cli::cli_warn("Request failed with status {{httr2::resp_status(resp)}}")
      return(NULL)
    }}

    httr2::resp_body_json(resp)
  }}, .progress = length(batches) > 1) |>
    purrr::list_flatten() |>
    purrr::compact()

  if (length(results) == 0) {{
    return(tibble::tibble())
  }}

  # Convert to tibble
  results |>
    purrr::map(function(x) {{
      x[purrr::map_lgl(x, is.null)] <- NA
      tibble::as_tibble(x)
    }}) |>
    purrr::list_rbind()
}}

')

  return(paste0(roxygen_header, "\\n", fn_body, "\\n\\n"))
}
```
</implementation>
</task>

<task id="3" type="execute" depends_on="2">
<title>Regenerate ct_chemical_search_equal_bulk stub</title>
<description>
Run the stub generation pipeline to regenerate the function file with the new code.
</description>
<commands>
```r
# In R console from project root
source("dev/endpoint_eval/00_config.R")
source("dev/endpoint_eval/01_schema_resolution.R")
source("dev/endpoint_eval/04_openapi_parser.R")
source("dev/endpoint_eval/07_stub_generation.R")

# Load the schema
schema <- jsonlite::fromJSON("schema/ctx-chemical-prod.json", simplifyVector = FALSE)
spec <- openapi_to_spec(schema)

# Filter to just the equal endpoint
equal_spec <- spec |> dplyr::filter(route == "/chemical/search/equal/", method == "POST")

# Generate stub with ctx config
ctx_config <- list(
  wrapper_function = "generic_request",
  param_strategy = "extra_params",
  example_query = "DTXSID7020182",
  lifecycle_badge = "experimental"
)

result <- render_endpoint_stubs(equal_spec, ctx_config)

# Write to file (append to existing or create new section)
# The function file should be R/ct_chemical_search_equal.R
```
</commands>
</task>

<task id="4" type="execute" depends_on="3">
<title>Generate documentation</title>
<description>
Run devtools::document() to generate man pages from roxygen comments.
</description>
<commands>
```r
devtools::document()
```
</commands>
</task>

<task id="5" type="test" depends_on="4">
<title>Test against live API</title>
<description>
Verify the regenerated function works correctly against the live CompTox API.
</description>
<test_approach>
1. Load the package or source the function
2. Call with known test chemicals
3. Verify response structure
4. Check that batch handling works (test with >200 items if practical)
</test_approach>
<commands>
```r
# Test single query
result <- ct_chemical_search_equal_bulk(c("DTXSID7020182"))
print(result)

# Test multiple queries
result <- ct_chemical_search_equal_bulk(c("DTXSID7020182", "DTXSID9020112", "50-00-0"))
print(result)
```
</commands>
</task>

<task id="6" type="test" depends_on="5">
<title>Record VCR cassette</title>
<description>
Create VCR cassette for the function to enable offline testing.
</description>
<file>tests/testthat/test-ct_chemical_search_equal.R</file>
<test_code>
```r
test_that("ct_chemical_search_equal_bulk returns results for valid queries", {
  vcr::use_cassette("ct_chemical_search_equal_bulk", {
    result <- ct_chemical_search_equal_bulk(c("DTXSID7020182", "DTXSID9020112"))
    expect_s3_class(result, "tbl_df
")
    expect_true(nrow(result) > 0)
  })
})
```
</test_code>
</task>

<task id="7" type="commit" depends_on="6">
<title>Commit changes</title>
<description>
Commit the stub generation fix and regenerated function.
</description>
<commit_pattern>
feat(03-01): fix stub generation for raw text body endpoint

- Add special case detection for /chemical/search/equal/ POST
- Generate httr2::req_body_raw() code instead of generic_request()
- Enforce API max batch limit of 200
- Regenerate ct_chemical_search_equal_bulk with correct implementation
</commit_pattern>
</task>

## Verification Criteria

- [ ] `build_function_stub()` detects `/chemical/search/equal/` POST endpoint
- [ ] Generated code uses `httr2::req_body_raw()` with `type = "text/plain"`
- [ ] Values collapsed with newlines: `paste(batch, collapse = "\n")`
- [ ] Batch limit enforced at 200 (API maximum)
- [ ] `ct_chemical_search_equal_bulk()` returns valid results from live API
- [ ] VCR cassette recorded successfully

## must_haves

These must be true for phase goal to be achieved:

1. The generated `ct_chemical_search_equal_bulk()` function sends raw text body, not JSON
2. The function successfully queries the live CompTox API
3. Response parsing returns a tibble with expected columns
