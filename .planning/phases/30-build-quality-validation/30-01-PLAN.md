---
phase: 30-build-quality-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESCRIPTION
  - R/ct_bioactivity_assay_search_by_endpoint.R
  - NAMESPACE
  - man/ct_bioactivity_assay_search_by_endpoint.Rd
autonomous: true
requirements:
  - BUILD-CLEAN
  - YAML-DEP
  - DUP-ARG

must_haves:
  truths:
    - "Package loads without error (library(ComptoxR) succeeds)"
    - "Hook config loads at .onLoad time (yaml dependency present)"
    - "R CMD check produces 0 errors"
    - "ct_bioactivity_assay_search_by_endpoint has no duplicate formal argument"
  artifacts:
    - path: "DESCRIPTION"
      provides: "yaml in Imports section"
      contains: "yaml"
    - path: "R/ct_bioactivity_assay_search_by_endpoint.R"
      provides: "Fixed stub without duplicate endpoint argument"
    - path: "NAMESPACE"
      provides: "Updated namespace reflecting yaml import"
  key_links:
    - from: "R/zzz.R"
      to: "yaml::read_yaml"
      via: ".onLoad -> load_hook_config()"
      pattern: "yaml::read_yaml"
    - from: "DESCRIPTION"
      to: "NAMESPACE"
      via: "devtools::document() regeneration"
      pattern: "import.*yaml"
---

<objective>
Fix the two blocking R CMD check errors: missing yaml dependency in DESCRIPTION and duplicate endpoint formal argument in a bioactivity stub.

Purpose: Achieve 0 errors from R CMD check — the hard requirement for Phase 30 and v2.2 milestone completion.
Output: Clean DESCRIPTION with yaml in Imports, fixed bioactivity stub, regenerated NAMESPACE.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-build-quality-validation/30-CONTEXT.md
@.planning/phases/30-build-quality-validation/30-RESEARCH.md

<interfaces>
<!-- Key files the executor needs to understand -->

From DESCRIPTION (current Imports section, line 24-38):
```
Imports:
    cli,
    dplyr (>= 1.1.4),
    glue,
    here,
    httr2,
    jsonlite (>= 1.8.8),
    lifecycle,
    magrittr (>= 2.0.3),
    purrr (>= 1.0.2),
    rlang,
    scales,
    stringi,
    stringr (>= 1.5.1),
    tibble,
    tidyr (>= 1.3.1)
```

From R/ct_bioactivity_assay_search_by_endpoint.R (the broken stub):
```r
ct_bioactivity_assay_search_by_endpoint <- function(endpoint) {
  result <- generic_request(
    endpoint = "bioactivity/assay/search/by-endpoint/",  # named arg to generic_request
    method = "GET",
    batch_limit = 0,
    `endpoint` = endpoint  # DUPLICATE — same name as line above
  )
  return(result)
}
```

From R/ct_bioactivity_assay_by-endpoint.R (correct pattern for reference):
```r
ct_bioactivity_assay_by_endpoint <- function(endpoint) {
  result <- generic_request(
    query = endpoint,  # CORRECT — uses query= not endpoint=
    endpoint = "bioactivity/assay/search/by-endpoint/",
    method = "GET",
    batch_limit = 1
  )
  return(result)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add yaml to DESCRIPTION Imports and fix duplicate endpoint argument</name>
  <files>DESCRIPTION, R/ct_bioactivity_assay_search_by_endpoint.R</files>
  <action>
Two mechanical fixes:

1. **DESCRIPTION**: Add `yaml` to the Imports section in alphabetical order. It goes after `tidyr (>= 1.3.1)` since y comes after t. The line should read `    yaml` (4-space indent, no version constraint needed). Make sure the previous line (`tidyr (>= 1.3.1)`) ends with a comma.

2. **R/ct_bioactivity_assay_search_by_endpoint.R**: Fix the duplicate `endpoint` argument by changing the function to pass the user's endpoint value via the `query` parameter instead of a backtick-quoted duplicate. Replace the body with:
```r
ct_bioactivity_assay_search_by_endpoint <- function(endpoint) {
  result <- generic_request(
    query = endpoint,
    endpoint = "bioactivity/assay/search/by-endpoint/",
    method = "GET",
    batch_limit = 1
  )
  return(result)
}
```
This matches the pattern used by `ct_bioactivity_assay_by_endpoint()` — use `query=` for the user parameter, `endpoint=` for the API path, and `batch_limit = 1` for path-based GET (append query to URL path). Remove `batch_limit = 0` since this function takes a query parameter, not a static endpoint.

Do NOT fix doc line width warnings, cosmetic notes, or any other non-error issues.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('Package loads OK\n')"</automated>
  </verify>
  <done>DESCRIPTION has yaml in Imports. Bioactivity stub has no duplicate endpoint argument. Package loads cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate NAMESPACE and documentation, verify build</name>
  <files>NAMESPACE, man/ct_bioactivity_assay_search_by_endpoint.Rd</files>
  <action>
Run devtools::document() to regenerate NAMESPACE (picks up yaml from DESCRIPTION Imports) and update any .Rd files affected by the bioactivity stub fix.

Then run devtools::load_all() to verify the package loads with the yaml dependency properly declared.

Final verification: run devtools::check() and confirm 0 errors. Warnings (doc line widths) and notes (cosmetic) are expected and acceptable per user decision. Do NOT attempt to fix warnings or notes.

If devtools::check() reveals additional errors beyond the two fixed issues, report them but do not fix unless they are trivially related to the changes made (e.g., a NAMESPACE entry that document() missed).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::document(); devtools::load_all(); cat('Build OK\n')"</automated>
  </verify>
  <done>NAMESPACE regenerated with yaml import. devtools::check() produces 0 errors. Package is build-clean.</done>
</task>

</tasks>

<verification>
- `devtools::load_all()` succeeds without error
- `devtools::check()` produces 0 errors (warnings and notes acceptable)
- Hook config loads at package startup (existing 42 hook tests still pass)
- No duplicate formal argument warnings for ct_bioactivity_assay_search_by_endpoint
</verification>

<success_criteria>
R CMD check produces 0 errors. The yaml dependency is declared in DESCRIPTION Imports. The duplicate endpoint argument in ct_bioactivity_assay_search_by_endpoint is eliminated. Package loads and builds cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/30-build-quality-validation/30-01-SUMMARY.md`
</output>
