---
phase: 07-version-detection
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - dev/endpoint_eval/04_openapi_parser.R
autonomous: true

must_haves:
  truths:
    - "Version detection routes to appropriate extraction logic in parser"
    - "Swagger 2.0 POST endpoints extract body properties correctly"
    - "OpenAPI 3.0 parsing unchanged (no regression)"
    - "Parser logs detected version during processing"
  artifacts:
    - path: "dev/endpoint_eval/04_openapi_parser.R"
      provides: "Version-aware parser with Swagger 2.0 support"
      contains: "detect_schema_version"
  key_links:
    - from: "dev/endpoint_eval/04_openapi_parser.R"
      to: "detect_schema_version"
      via: "function call"
      pattern: "detect_schema_version\\(openapi\\)"
    - from: "dev/endpoint_eval/04_openapi_parser.R"
      to: "extract_body_properties"
      via: "version parameter"
      pattern: "schema_version\\s*="
---

<objective>
Wire version detection into the main OpenAPI parser pipeline.

Purpose: Enable openapi_to_spec() to detect schema versions and use the appropriate body extraction logic for Swagger 2.0 vs OpenAPI 3.0 schemas.

Output:
- Updated openapi_to_spec() that detects schema version at entry point
- Version-aware body property extraction for POST/PUT/PATCH endpoints
- Logging of detected version during parsing
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-version-detection/07-01-SUMMARY.md
@dev/endpoint_eval/04_openapi_parser.R
@dev/endpoint_eval/01_schema_resolution.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version detection to openapi_to_spec entry point</name>
  <files>dev/endpoint_eval/04_openapi_parser.R</files>
  <action>
Modify `openapi_to_spec()` to detect schema version at the entry point and pass it through the parsing pipeline.

**Step 1: Add version detection after preprocessing**

After line 336 (after `openapi <- preprocess_schema(openapi)`), add:

```r
  # Detect schema version (Swagger 2.0 vs OpenAPI 3.0)
  schema_version <- detect_schema_version(openapi)
  cli::cli_alert_info("Detected schema version: {schema_version$type} {schema_version$version}")
```

**Step 2: Handle definitions vs components based on version**

After detecting version, set up the appropriate schema container:

```r
  # Get schema definitions/components based on version
  # Swagger 2.0 uses "definitions", OpenAPI 3.0 uses "components/schemas"
  if (identical(schema_version$type, "swagger")) {
    definitions <- openapi[["definitions"]] %||% list()
    # For Swagger 2.0, we'll pass definitions to body extraction
    components <- list(schemas = definitions)  # Normalize for resolve_schema_ref compatibility
  } else {
    definitions <- NULL
    components <- openapi[["components"]] %||% list()
  }
```

**Step 3: Update body_props extraction call**

In the `purrr::map_dfr(meths, function(method) { ... })` block, update the body_props extraction (around line 369) to be version-aware:

```r
      # Extract request body schema metadata for POST/PUT/PATCH
      body_props <- if (method %in% c("post", "put", "patch")) {
        if (identical(schema_version$type, "swagger")) {
          # Swagger 2.0: body is in parameters array, resolve against definitions
          extract_body_properties(op$parameters, definitions, schema_version = schema_version)
        } else {
          # OpenAPI 3.0: body is in requestBody object
          extract_body_properties(op$requestBody, components)
        }
      } else {
        list(type = "unknown", properties = list())
      }
```

**Important notes:**
- The `components` variable for query parameter extraction should continue to use the existing logic (line 359-360)
- For Swagger 2.0, `definitions` is used for body extraction, but `components` (normalized) can be used for reference resolution elsewhere
- Keep existing `has_body` detection: for Swagger 2.0, check if any parameter has `in="body"`
  </action>
  <verify>
```r
source("dev/endpoint_eval/00_config.R")
source("dev/endpoint_eval/01_schema_resolution.R")
source("dev/endpoint_eval/04_openapi_parser.R")

# Test Swagger 2.0 parsing
amos_spec <- openapi_to_spec("schema/chemi-amos-prod.json")
# Should see "Detected schema version: swagger 2.0" in output

# Check that POST endpoints have body params extracted
post_endpoints <- amos_spec[amos_spec$method == "POST", ]
nrow(post_endpoints) > 0
any(nchar(post_endpoints$body_params) > 0)
```
  </verify>
  <done>
- openapi_to_spec() detects schema version at entry point
- Logs "Detected schema version: swagger 2.0" or "openapi 3.x.x"
- Sets up definitions/components correctly based on version
  </done>
</task>

<task type="auto">
  <name>Task 2: Update has_body detection for Swagger 2.0</name>
  <files>dev/endpoint_eval/04_openapi_parser.R</files>
  <action>
Update the `has_body` detection logic to work correctly for both schema versions.

For Swagger 2.0, `has_body` should be TRUE if any parameter has `in="body"`.
For OpenAPI 3.0, `has_body` should be TRUE if `requestBody` exists (current behavior).

Find the `has_body` assignment (around line 420) and update it:

```r
      # Detect if endpoint has request body
      has_body <- if (identical(schema_version$type, "swagger")) {
        # Swagger 2.0: check for body parameter in parameters array
        any(purrr::map_lgl(op$parameters %||% list(), ~ identical(.x[["in"]], "body")))
      } else {
        # OpenAPI 3.0: check for requestBody object
        !is.null(op$requestBody)
      }
```

Also update the `needs_resolver` and `body_schema_type` calculations to use the same version-aware logic:

```r
      # Detect if the request body uses the Chemical schema (needs resolver)
      needs_resolver <- if (method %in% c("post", "put", "patch") && has_body) {
        if (identical(schema_version$type, "swagger")) {
          # Swagger 2.0: check body parameter schema for Chemical patterns
          # For now, return FALSE - chemi schemas don't use Chemical resolver pattern
          FALSE
        } else {
          uses_chemical_schema(op$requestBody, openapi)
        }
      } else {
        FALSE
      }

      # Get body schema type for more specific code generation
      body_schema_type <- if (method %in% c("post", "put", "patch") && has_body) {
        if (identical(schema_version$type, "swagger")) {
          # Use the type from body_props which was already extracted
          body_props$type %||% "unknown"
        } else {
          get_body_schema_type(op$requestBody, openapi)
        }
      } else {
        "unknown"
      }
```
  </action>
  <verify>
```r
source("dev/endpoint_eval/00_config.R")
source("dev/endpoint_eval/01_schema_resolution.R")
source("dev/endpoint_eval/04_openapi_parser.R")

# Test Swagger 2.0 has_body detection
amos_spec <- openapi_to_spec("schema/chemi-amos-prod.json")

# batch_search is POST with body
batch_search <- amos_spec[amos_spec$route == "/api/amos/batch_search", ]
batch_search$has_body == TRUE
batch_search$body_schema_type == "object"

# GET endpoints should have has_body = FALSE
get_endpoints <- amos_spec[amos_spec$method == "GET", ]
all(get_endpoints$has_body == FALSE)
```
  </verify>
  <done>
- has_body correctly detects body parameters for Swagger 2.0 (in="body")
- has_body correctly detects requestBody for OpenAPI 3.0
- body_schema_type derived from body_props for Swagger 2.0
  </done>
</task>

<task type="auto">
  <name>Task 3: Ensure OpenAPI 3.0 parsing unchanged (no regression)</name>
  <files>dev/endpoint_eval/04_openapi_parser.R</files>
  <action>
Verify that OpenAPI 3.0 parsing (existing CompTox Dashboard schemas) continues to work correctly by running the parser against an OpenAPI 3.0 schema and comparing key outputs.

This is primarily a verification task, but may require minor adjustments if the version detection changes broke anything.

**Verification approach:**

1. Parse an OpenAPI 3.0 schema (chemi-resolver-prod.json or ctx-chemical-prod.json)
2. Check that POST endpoints have body_params extracted correctly
3. Check that path_params and query_params are correct
4. Check that needs_resolver detection still works

If any issues found, make minimal targeted fixes to restore OpenAPI 3.0 behavior.

**Common issues to check for:**
- `components` variable scope - ensure it's still available for query parameter extraction
- Reference resolution - ensure `#/components/schemas/` still resolves correctly
- `uses_chemical_schema()` - ensure it still receives correct arguments
  </action>
  <verify>
```r
source("dev/endpoint_eval/00_config.R")
source("dev/endpoint_eval/01_schema_resolution.R")
source("dev/endpoint_eval/04_openapi_parser.R")

# Test OpenAPI 3.0 parsing (chemi-resolver-prod.json is OpenAPI 3.0)
chemi_spec <- openapi_to_spec("schema/chemi-resolver-prod.json")
# Should see "Detected schema version: openapi 3.x.x" in output

# Check that POST endpoints work correctly
chemi_post <- chemi_spec[chemi_spec$method == "POST", ]
nrow(chemi_post) > 0

# BODY-02: Verify OpenAPI 3.0 body extraction produces body_params content
stopifnot(any(nchar(chemi_post$body_params) > 0))

# Check that needs_resolver detection works for Chemical schemas
if (any(chemi_post$needs_resolver)) {
  cli::cli_alert_success("needs_resolver detection working for OpenAPI 3.0")
}

# Test CompTox Dashboard schema (ctx-chemical-prod.json)
if (file.exists("schema/ctx-chemical-prod.json")) {
  ctx_spec <- openapi_to_spec("schema/ctx-chemical-prod.json")
  nrow(ctx_spec) > 0
  cli::cli_alert_success("CompTox Dashboard schema parsed successfully")
}
```
  </verify>
  <done>
- OpenAPI 3.0 schemas parse correctly with detected version logged
- POST endpoints have body_params extracted (verified with stopifnot assertion)
- needs_resolver detection works for Chemical schemas
- No regression in existing functionality
  </done>
</task>

</tasks>

<verification>
Run comprehensive integration test:

```r
source("dev/endpoint_eval/00_config.R")
source("dev/endpoint_eval/01_schema_resolution.R")
source("dev/endpoint_eval/04_openapi_parser.R")

# Test 1: Swagger 2.0 (AMOS)
cli::cli_h2("Testing Swagger 2.0 (AMOS)")
amos_spec <- openapi_to_spec("schema/chemi-amos-prod.json")
amos_post <- amos_spec[amos_spec$method == "POST", ]
stopifnot(nrow(amos_post) > 0)
stopifnot(any(amos_post$has_body))
stopifnot(any(nchar(amos_post$body_params) > 0))
cli::cli_alert_success("AMOS Swagger 2.0: {nrow(amos_post)} POST endpoints with body params")

# Test 2: Swagger 2.0 (RDKit)
cli::cli_h2("Testing Swagger 2.0 (RDKit)")
rdkit_spec <- openapi_to_spec("schema/chemi-rdkit-staging.json")
rdkit_post <- rdkit_spec[rdkit_spec$method == "POST", ]
stopifnot(nrow(rdkit_post) > 0)
cli::cli_alert_success("RDKit Swagger 2.0: {nrow(rdkit_post)} POST endpoints")

# Test 3: Swagger 2.0 (Mordred)
cli::cli_h2("Testing Swagger 2.0 (Mordred)")
mordred_spec <- openapi_to_spec("schema/chemi-mordred-staging.json")
mordred_post <- mordred_spec[mordred_spec$method == "POST", ]
stopifnot(nrow(mordred_post) > 0)
cli::cli_alert_success("Mordred Swagger 2.0: {nrow(mordred_post)} POST endpoints")

# Test 4: OpenAPI 3.0 (chemi-resolver-prod)
cli::cli_h2("Testing OpenAPI 3.0 (chemi-resolver-prod)")
chemi_spec <- openapi_to_spec("schema/chemi-resolver-prod.json")
chemi_post <- chemi_spec[chemi_spec$method == "POST", ]
stopifnot(nrow(chemi_post) > 0)
# BODY-02: Verify OpenAPI 3.0 body extraction produces actual content
stopifnot(any(nchar(chemi_post$body_params) > 0))
cli::cli_alert_success("Chemi OpenAPI 3.0: {nrow(chemi_post)} POST endpoints with body params")

# Test 5: OpenAPI 3.0 (ctx-chemical-prod)
cli::cli_h2("Testing OpenAPI 3.0 (ctx-chemical-prod)")
ctx_spec <- openapi_to_spec("schema/ctx-chemical-prod.json")
ctx_post <- ctx_spec[ctx_spec$method == "POST", ]
stopifnot(nrow(ctx_post) > 0)
stopifnot(any(nchar(ctx_post$body_params) > 0))
cli::cli_alert_success("CTX Chemical OpenAPI 3.0: {nrow(ctx_post)} POST endpoints with body params")

# Test 6: Verify preflight endpoints are excluded
all_routes <- c(amos_spec$route, rdkit_spec$route, mordred_spec$route, chemi_spec$route, ctx_spec$route)
stopifnot(!any(grepl("preflight", all_routes, ignore.case = TRUE)))
cli::cli_alert_success("Preflight endpoints correctly excluded")

# Summary
cli::cli_h2("Phase 7 Plan 02 Verification Complete")
cli::cli_alert_success("All schema versions detected and parsed correctly")
cli::cli_alert_success("Body extraction works for Swagger 2.0 and OpenAPI 3.0")
cli::cli_alert_success("No regression in OpenAPI 3.0 parsing")
```
</verification>

<success_criteria>
1. VERS-03: Version detection routes to appropriate extraction logic
2. Parser logs detected version ("Detected schema version: swagger 2.0" or "openapi 3.x.x")
3. Swagger 2.0 POST endpoints (AMOS, RDKit, Mordred) have body_params extracted
4. OpenAPI 3.0 parsing unchanged (chemi-resolver-prod.json and ctx-chemical-prod.json work correctly)
5. BODY-02: OpenAPI 3.0 body extraction verified with stopifnot(any(nchar(body_params) > 0))
6. has_body detection correct for both versions
7. body_schema_type derived correctly for both versions
8. Preflight endpoints filtered out
</success_criteria>

<output>
After completion, create `.planning/phases/07-version-detection/07-02-SUMMARY.md`
</output>
