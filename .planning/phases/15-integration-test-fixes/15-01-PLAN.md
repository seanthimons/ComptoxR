---
phase: 15-integration-test-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/helper-pipeline.R
  - .github/workflows/pipeline-tests.yml
autonomous: true

must_haves:
  truths:
    - "Integration tests execute without 'could not find function get_stubgen_config' error"
    - "E2E flow: schema -> stub -> execution completes successfully"
    - "Cassette deletion path in workflow matches vcr_dir configuration"
  artifacts:
    - path: "tests/testthat/helper-pipeline.R"
      provides: "get_stubgen_config() function"
      contains: "get_stubgen_config"
    - path: ".github/workflows/pipeline-tests.yml"
      provides: "Corrected cassette path"
      contains: "fixtures/integration-"
  key_links:
    - from: "tests/testthat/test-pipeline-integration.R"
      to: "tests/testthat/helper-pipeline.R"
      via: "get_stubgen_config() function call"
      pattern: "get_stubgen_config\\(\\)"
    - from: ".github/workflows/pipeline-tests.yml"
      to: "tests/testthat/fixtures/"
      via: "cassette deletion path"
      pattern: "fixtures/integration-"
---

<objective>
Close gaps identified in v1.8 milestone audit by implementing missing get_stubgen_config() function and fixing cassette path in CI workflow.

Purpose: Integration tests at lines 88 and 236 of test-pipeline-integration.R call get_stubgen_config() which does not exist, causing test failures. Additionally, the cassette deletion path in pipeline-tests.yml references a non-existent `_vcr/` subdirectory.

Output: Working integration tests that can execute the full E2E pipeline flow without function-not-found errors.
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/testthat/helper-pipeline.R
@tests/testthat/test-pipeline-integration.R
@.github/workflows/pipeline-tests.yml
@dev/endpoint_eval/07_stub_generation.R (lines 153-202 for config usage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement get_stubgen_config() function</name>
  <files>tests/testthat/helper-pipeline.R</files>
  <action>
Add get_stubgen_config() function to helper-pipeline.R that returns the default configuration list expected by build_function_stub().

The function must return a list with these fields (based on usage in 07_stub_generation.R lines 194-984):
- wrapper_function: "generic_request" (the standard wrapper)
- example_query: "DTXSID7020182" (Aspirin - well-known test chemical)
- lifecycle_badge: "experimental" (default for generated stubs)
- default_query_doc: "#' @param query A list of DTXSIDs to search for\n"
- example_dtxsids: NULL (use default sampling)
- param_strategy: "extra_params" (default strategy)

Add the function after the existing load_fixture_schema() function, following the same documentation style with roxygen comments.
  </action>
  <verify>
Run in R console:
```r
source("tests/testthat/helper-pipeline.R")
config <- get_stubgen_config()
stopifnot(is.list(config))
stopifnot(config$wrapper_function == "generic_request")
stopifnot(config$example_query == "DTXSID7020182")
stopifnot(config$lifecycle_badge == "experimental")
stopifnot(!is.null(config$default_query_doc))
stopifnot(config$param_strategy == "extra_params")
cat("get_stubgen_config() verified successfully\n")
```
  </verify>
  <done>
get_stubgen_config() function exists in helper-pipeline.R and returns a valid configuration list with all required fields for build_function_stub().
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix cassette path in pipeline-tests.yml</name>
  <files>.github/workflows/pipeline-tests.yml</files>
  <action>
Fix line 47 in .github/workflows/pipeline-tests.yml to remove the erroneous `_vcr/` subdirectory from the cassette deletion path.

Current (incorrect):
```yaml
run: rm -rf tests/testthat/fixtures/_vcr/integration-*.yml
```

Should be (matches vcr_dir in helper-vcr.R line 4):
```yaml
run: rm -rf tests/testthat/fixtures/integration-*.yml
```

The vcr_dir is configured as "../testthat/fixtures" which resolves to tests/testthat/fixtures/ - there is no _vcr/ subdirectory.
  </action>
  <verify>
```bash
grep "fixtures/integration-" .github/workflows/pipeline-tests.yml
# Should show: rm -rf tests/testthat/fixtures/integration-*.yml
# Should NOT contain _vcr/
```
  </verify>
  <done>
Cassette deletion path in pipeline-tests.yml matches the vcr_dir configuration (tests/testthat/fixtures/) without the erroneous _vcr/ subdirectory.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify integration tests run without errors</name>
  <files></files>
  <action>
Run the integration tests to verify both fixes work together:
1. The get_stubgen_config() function is found and returns valid config
2. The E2E flow can proceed past the config parameter

Note: Full E2E execution requires API key and cassettes. The test will skip gracefully if neither is available, but it should NOT fail with "could not find function get_stubgen_config" error.

Run test with focus on the function availability check:
```r
# Source helper to make functions available
source("tests/testthat/helper-pipeline.R")

# Verify function exists
stopifnot(exists("get_stubgen_config"))

# Run tests (will skip if no cassettes/API key, but should not error on missing function)
testthat::test_file("tests/testthat/test-pipeline-integration.R")
```
  </action>
  <verify>
```r
# Run integration tests
devtools::test(filter = "pipeline-integration")
# Should either:
# - Pass (if cassettes exist or API key available)
# - Skip gracefully with "First run requires ctx_api_key" message
# - NOT fail with "could not find function 'get_stubgen_config'"
```
  </verify>
  <done>
Integration tests execute without "could not find function" errors. Tests either pass (with cassettes/API key) or skip gracefully (without).
  </done>
</task>

</tasks>

<verification>
1. Function existence: `exists("get_stubgen_config", envir = globalenv())` returns TRUE after sourcing helper
2. Config structure: `get_stubgen_config()` returns list with wrapper_function, example_query, lifecycle_badge, default_query_doc, example_dtxsids, param_strategy
3. Workflow path: `grep "fixtures/integration-" .github/workflows/pipeline-tests.yml` matches without `_vcr/`
4. Integration tests: `devtools::test(filter = "pipeline-integration")` does not fail on missing function
</verification>

<success_criteria>
- get_stubgen_config() exists and returns valid configuration list
- pipeline-tests.yml cassette path is corrected to fixtures/integration-*.yml
- Integration tests run without "could not find function" error
- E2E flow proceeds to skip (if no cassettes) or execute (if cassettes present)
</success_criteria>

<output>
After completion, create `.planning/phases/15-integration-test-fixes/15-01-SUMMARY.md`
</output>
