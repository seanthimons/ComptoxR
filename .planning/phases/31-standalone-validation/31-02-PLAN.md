---
phase: 31-standalone-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - dev/lifestage/validate_lifestage.R
autonomous: true
requirements:
  - KWCL-03
  - VALD-01
  - VALD-02

must_haves:
  truths:
    - "All 10 two-axis deterministic assertions pass (classifier separates developmental stage from reproductive flag)"
    - "Dictionary completeness: all 139 lifestage_codes descriptions present in new dictionary"
    - "No 'Reproductive' category in dictionary"
    - "No 'Larva/Juvenile' category in dictionary"
    - "Keyword classifier achieves >=125/139 non-Other/Unknown matches on known descriptions"
    - "Six misclassification fixes verified individually"
    - "Script exits 0 when all assertions pass, exits 1 when any fail"
    - "Classification diff output shows every changed term"
  artifacts:
    - path: "dev/lifestage/validate_lifestage.R"
      provides: "Complete validation script with DB connection, assertions, diff output, summary"
      exports: ["assert()", "life_stage_new", ".classify_lifestage_keywords()"]
  key_links:
    - from: "dev/lifestage/validate_lifestage.R"
      to: "ecotox.duckdb"
      via: "DBI::dbConnect(duckdb::duckdb(), read_only = TRUE)"
      pattern: "dbGetQuery.*lifestage_codes"
    - from: "dev/lifestage/validate_lifestage.R"
      to: "ecotox.duckdb"
      via: "DBI::dbReadTable"
      pattern: "dbReadTable.*lifestage_dictionary"
---

<objective>
Add the DuckDB connection, assertion battery (18 assertions grouped into 4 categories), classification diff output, and summary/exit logic to complete the validation script. Then run the script and verify all assertions pass.

Purpose: This plan wires up the proof that the dictionary and classifier from Plan 01 are correct. The 18 assertions constitute the "test suite" for this dev script. The diff output gives the developer a human-readable view of every classification change.

Output: Complete, runnable `dev/lifestage/validate_lifestage.R` that can be executed with `Rscript dev/lifestage/validate_lifestage.R` and exits 0 when all assertions pass.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-standalone-validation/31-CONTEXT.md
@.planning/phases/31-standalone-validation/31-RESEARCH.md
@.planning/phases/31-standalone-validation/31-PATTERNS.md
@LIFESTAGE_HARMONIZATION_PLAN.md (Verification section, lines 266-297 for assertion spec)

<!-- Plan 01 SUMMARY needed: the executor must know what Plan 01 produced -->
@.planning/phases/31-standalone-validation/31-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - the executor will find these already defined in the script -->

From dev/lifestage/validate_lifestage.R (created by Plan 01):
```r
# Already defined: .classify_lifestage_keywords(descriptions)
# Returns tibble with: org_lifestage, harmonized_life_stage, ontology_id, reproductive_stage, classification_source

# Already defined: life_stage_new (tribble)
# Columns: org_lifestage, harmonized_life_stage, ontology_id, reproductive_stage, classification_source
# Has 139+ rows, all classification_source = "dictionary"
```

From .planning/phases/31-standalone-validation/31-RESEARCH.md section 5:
```
Assertion A1-A10: Two-axis classifier tests (input -> expected dev stage + repro flag)
Assertion A11: Dictionary completeness (all 139 DB descriptions present)
Assertion A12: No "Reproductive" category
Assertion A13: No "Larva/Juvenile" category
Assertion A14: Column completeness (exactly 5 columns)
Assertion A15: classification_source uniformity (all "dictionary")
Assertion A16: Six misclassification fixes verified
Assertion A17: Keyword classifier coverage (>=125/139 non-Other/Unknown)
Assertion A18: No NAs in required columns
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DB connection, assertion infrastructure, and all 18 assertions</name>
  <files>dev/lifestage/validate_lifestage.R</files>
  <read_first>
    - dev/lifestage/validate_lifestage.R (current state from Plan 01 -- MUST read to see what exists)
    - .planning/phases/31-standalone-validation/31-RESEARCH.md (sections 2.3, 2.5, 5.1-5.3 for DB connection pattern, assertion accumulator, assertion specs)
    - .planning/phases/31-standalone-validation/31-PATTERNS.md (DBI/DuckDB read-only pattern, collect-all assertion accumulator pattern, DB query pattern)
    - LIFESTAGE_HARMONIZATION_PLAN.md (Verification section lines 266-297 for the 10 two-axis assertions)
    - dev/check_cassette_health.R (collect-then-report pattern for reference)
  </read_first>
  <action>
Append the following sections to `dev/lifestage/validate_lifestage.R` after the `life_stage_new` tribble:

**Section 4: DuckDB connection (per D-03, read_only = TRUE)**

```r
# ==============================================================================
# 4. Database Connection
# ==============================================================================

cli::cli_h1("Lifestage Standalone Validation")

db_path <- file.path(tools::R_user_dir("ComptoxR", "data"), "ecotox.duckdb")
if (!file.exists(db_path)) {
  cli::cli_abort(c(
    "ECOTOX DuckDB not found at expected path.",
    "i" = "Path: {db_path}",
    "i" = "Run the ECOTOX build first to create the database."
  ))
}
cli::cli_alert_info("DB path: {db_path}")

eco_con <- DBI::dbConnect(duckdb::duckdb(), dbdir = db_path, read_only = TRUE)
on.exit(DBI::dbDisconnect(eco_con, shutdown = TRUE), add = TRUE)

db_lifestages <- DBI::dbGetQuery(
  eco_con,
  "SELECT DISTINCT description FROM lifestage_codes ORDER BY description"
)$description
cli::cli_alert_info("lifestage_codes descriptions: {length(db_lifestages)}")

current_dict <- DBI::dbReadTable(eco_con, "lifestage_dictionary")
cli::cli_alert_info("Current dictionary rows: {nrow(current_dict)}")
```

**Section 5: Assertion infrastructure + all 18 assertions (per D-06, collect-all)**

```r
# ==============================================================================
# 5. Assertion Battery
# ==============================================================================

results <- list()

assert <- function(label, condition, detail = "") {
  if (isTRUE(condition)) {
    cli::cli_alert_success("PASS: {label}")
    results[[length(results) + 1]] <<- list(label = label, pass = TRUE)
  } else {
    cli::cli_alert_danger("FAIL: {label}{if (nzchar(detail)) paste0(' \u2014 ', detail) else ''}")
    results[[length(results) + 1]] <<- list(label = label, pass = FALSE, detail = detail)
  }
}
```

Then add the 18 assertions in 4 groups:

**Group 1: Two-Axis Classifier Assertions (A1-A10)** (per VALD-01)

```r
cli::cli_h2("Group 1: Two-Axis Classifier Assertions (A1-A10)")
```

For each assertion, call `.classify_lifestage_keywords()` with a single test string and check both `harmonized_life_stage` and `reproductive_stage`:

| ID | Input | Expected harmonized_life_stage | Expected reproductive_stage |
|----|-------|-------------------------------|----------------------------|
| A1 | `"Spawning adult"` | `"Adult"` | `TRUE` |
| A2 | `"Mature female"` | `"Adult"` | `FALSE` |
| A3 | `"Reproductive adult"` | `"Adult"` | `TRUE` |
| A4 | `"Post-spawning adult"` | `"Adult"` | `TRUE` |
| A5 | `"Gestating juvenile"` | `"Juvenile"` | `TRUE` |
| A6 | `"Flowering seedling"` | `"Juvenile"` | `TRUE` |
| A7 | `"Larva"` | `"Larva"` | `FALSE` |
| A8 | `"Egg-laying female"` | `"Adult"` | `TRUE` |
| A9 | `"Dormant seed"` | `"Senescent/Dormant"` | `FALSE` |
| A10 | `"Pollen"` | `"Adult"` | `TRUE` |

Implementation pattern for each assertion (example for A1):
```r
a1 <- .classify_lifestage_keywords("Spawning adult")
assert("A1: 'Spawning adult' -> Adult", a1$harmonized_life_stage == "Adult",
       paste0("got: ", a1$harmonized_life_stage))
assert("A1: 'Spawning adult' -> reproductive", a1$reproductive_stage == TRUE,
       paste0("got: ", a1$reproductive_stage))
```

Note: A5 ("Gestating juvenile") tests that `juvenile` keyword at priority 3 fires before `gestat` at priority 5. The classifier should match `juvenile` first because priority 3 < priority 5.

Note: A6 ("Flowering seedling") tests that `seedling` keyword at priority 3 fires before `flower` at priority 5. The classifier should match `seedling` first.

**Group 2: Dictionary Structure (A11-A15, A18)** (per VALD-02, DICT-01, DICT-04)

```r
cli::cli_h2("Group 2: Dictionary Structure (A11-A15, A18)")
```

- **A11 — Dictionary completeness:** `setdiff(db_lifestages, life_stage_new$org_lifestage)` must have length 0. If not, report the missing terms in the `detail` string.
- **A12 — No "Reproductive" category:** `!"Reproductive" %in% life_stage_new$harmonized_life_stage`
- **A13 — No "Larva/Juvenile" category:** `!"Larva/Juvenile" %in% life_stage_new$harmonized_life_stage`
- **A14 — Column completeness:** `identical(names(life_stage_new), c("org_lifestage", "harmonized_life_stage", "ontology_id", "reproductive_stage", "classification_source"))`
- **A15 — classification_source uniformity:** `all(life_stage_new$classification_source == "dictionary")`
- **A18 — No NAs in required columns:** Check that `org_lifestage`, `harmonized_life_stage`, `classification_source`, and `reproductive_stage` have zero NAs. `ontology_id` NAs are expected/allowed.

For A11, construct the detail string to show which terms are missing:
```r
missing_terms <- setdiff(db_lifestages, life_stage_new$org_lifestage)
assert("A11: All DB lifestage_codes present in dictionary",
       length(missing_terms) == 0,
       paste0(length(missing_terms), " missing: ", paste(missing_terms, collapse = ", ")))
```

**Group 3: Misclassification Fixes (A16)** (per CORR-01)

```r
cli::cli_h2("Group 3: Misclassification Fixes (A16)")
```

Six sub-assertions, one per fix. For each, look up the row in `life_stage_new` and check `harmonized_life_stage`:
```r
lookup <- function(term) {
  life_stage_new$harmonized_life_stage[life_stage_new$org_lifestage == term]
}

assert("A16a: 'Germinated seed' -> Juvenile", lookup("Germinated seed") == "Juvenile",
       paste0("got: ", lookup("Germinated seed")))
assert("A16b: 'Spat' -> Juvenile", lookup("Spat") == "Juvenile",
       paste0("got: ", lookup("Spat")))
assert("A16c: 'Seed' -> Senescent/Dormant", lookup("Seed") == "Senescent/Dormant",
       paste0("got: ", lookup("Seed")))
assert("A16d: 'Sapling' -> Juvenile", lookup("Sapling") == "Juvenile",
       paste0("got: ", lookup("Sapling")))
assert("A16e: 'Cocoon' -> Senescent/Dormant", lookup("Cocoon") == "Senescent/Dormant",
       paste0("got: ", lookup("Cocoon")))
assert("A16f: 'Corm' -> Senescent/Dormant", lookup("Corm") == "Senescent/Dormant",
       paste0("got: ", lookup("Corm")))
```

**Group 4: Keyword Classifier Coverage (A17)** (per KWCL-03)

```r
cli::cli_h2("Group 4: Keyword Classifier Coverage (A17)")
```

Pass all dictionary `org_lifestage` values through `.classify_lifestage_keywords()`, count how many get a non-Other/Unknown classification, assert >= 125:

```r
kw_results <- .classify_lifestage_keywords(life_stage_new$org_lifestage)
kw_non_other <- sum(kw_results$harmonized_life_stage != "Other/Unknown")
assert("A17: Keyword classifier coverage >= 125/139 non-Other/Unknown",
       kw_non_other >= 125,
       paste0("got: ", kw_non_other, "/", nrow(life_stage_new)))
```

Note: The denominator is `nrow(life_stage_new)` which may be > 139 if historical tribble-only terms are included. The threshold of 125 is the minimum for the DB-authoritative 139 terms; more is fine.
  </action>
  <verify>
    <automated>grep -c "assert(" dev/lifestage/validate_lifestage.R</automated>
  </verify>
  <acceptance_criteria>
    - File contains `DBI::dbConnect(duckdb::duckdb(), dbdir = db_path, read_only = TRUE)`
    - File contains `on.exit(DBI::dbDisconnect(eco_con, shutdown = TRUE), add = TRUE)`
    - File contains `tools::R_user_dir("ComptoxR", "data")`
    - File contains `assert <- function(label, condition, detail = "")`
    - File contains `results <<-` inside the assert function
    - File contains `cli::cli_h2("Group 1: Two-Axis Classifier Assertions (A1-A10)")`
    - File contains `cli::cli_h2("Group 2: Dictionary Structure (A11-A15, A18)")`
    - File contains `cli::cli_h2("Group 3: Misclassification Fixes (A16)")`
    - File contains `cli::cli_h2("Group 4: Keyword Classifier Coverage (A17)")`
    - A1 assertion checks `"Spawning adult"` -> Adult + reproductive TRUE
    - A5 assertion checks `"Gestating juvenile"` -> Juvenile + reproductive TRUE
    - A9 assertion checks `"Dormant seed"` -> Senescent/Dormant + reproductive FALSE
    - A11 assertion uses `setdiff(db_lifestages, life_stage_new$org_lifestage)`
    - A14 assertion checks for exactly 5 column names
    - A16 has 6 sub-assertions for the 6 misclassification fixes
    - A17 assertion threshold is >= 125
    - grep for `assert(` returns at least 30 occurrences (10 two-axis x 2 checks each + 6 structure + 6 fixes + 1 coverage)
  </acceptance_criteria>
  <done>All 18 assertions (with sub-assertions) are defined in 4 groups. DB connection opens read-only. Assertion accumulator collects results without halting on first failure.</done>
</task>

<task type="auto">
  <name>Task 2: Add diff output, summary, exit logic, and run validation</name>
  <files>dev/lifestage/validate_lifestage.R</files>
  <read_first>
    - dev/lifestage/validate_lifestage.R (current state from Task 1 -- MUST read to see what was appended)
    - .planning/phases/31-standalone-validation/31-RESEARCH.md (section 2.6 for diff output format, section 2.5 for summary + exit pattern)
    - .planning/phases/31-standalone-validation/31-PATTERNS.md (diff computation pattern, summary + exit code pattern)
    - dev/check_cassette_health.R (lines 44-60 for summary + exit pattern reference)
  </read_first>
  <action>
Append the following sections to `dev/lifestage/validate_lifestage.R` after the assertion battery:

**Section 6: Classification diff output**

```r
# ==============================================================================
# 6. Classification Diff (current 2-col vs proposed 5-col)
# ==============================================================================

cli::cli_h2("Classification Changes")

diff_rows <- dplyr::left_join(
  life_stage_new,
  current_dict,
  by = "org_lifestage",
  suffix = c(".new", ".old")
) |>
  dplyr::filter(harmonized_life_stage.new != harmonized_life_stage.old) |>
  dplyr::select(
    org_lifestage,
    old_category = harmonized_life_stage.old,
    new_category = harmonized_life_stage.new,
    reproductive_stage
  )

if (nrow(diff_rows) > 0) {
  cli::cli_alert_info("{nrow(diff_rows)} term(s) changed classification:")
  print(diff_rows, n = Inf)
} else {
  cli::cli_alert_info("No classification changes detected.")
}
```

Note on column suffixes: `left_join` with `suffix = c(".new", ".old")` because `life_stage_new` is the left table (new classifications) and `current_dict` is the right table (old 2-column). The `.x` suffix goes to the left table columns, `.y` to the right. Using explicit suffixes `.new`/`.old` makes the code self-documenting. The `select()` renames them for readability.

**Also show terms in the new dictionary that are NOT in the current dictionary** (the 2 new terms):
```r
new_terms <- dplyr::anti_join(life_stage_new, current_dict, by = "org_lifestage")
if (nrow(new_terms) > 0) {
  cli::cli_alert_info("{nrow(new_terms)} new term(s) added to dictionary:")
  print(dplyr::select(new_terms, org_lifestage, harmonized_life_stage, reproductive_stage), n = Inf)
}
```

**Section 7: Summary + exit code (per D-06, matching dev/check_cassette_health.R pattern)**

```r
# ==============================================================================
# 7. Summary
# ==============================================================================

cli::cli_h2("Summary")

n_pass <- sum(vapply(results, `[[`, logical(1), "pass"))
n_fail <- length(results) - n_pass

cli::cli_alert_info("Total assertions: {length(results)}")
cli::cli_alert_info("Passed: {n_pass}")
cli::cli_alert_info("Failed: {n_fail}")
cli::cli_alert_info("Dictionary rows: {nrow(life_stage_new)}")
cli::cli_alert_info("DB lifestage_codes: {length(db_lifestages)}")

if (n_fail > 0) {
  cli::cli_alert_danger("{n_fail} assertion(s) FAILED \u2014 review output above")
  quit(status = 1)
} else {
  cli::cli_alert_success("All assertions passed.")
  quit(status = 0)
}
```

After writing these sections, format the file:
```bash
air format dev/lifestage/validate_lifestage.R
```

Then run the complete validation script:
```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" dev/lifestage/validate_lifestage.R
```

**Expected outcome:** All assertions pass, script exits 0. The classification diff output shows the ~40-50 terms that changed category (Larva/Juvenile split, Reproductive elimination, 6 misclassification fixes, Spore/Tuber corrections).

**If any assertions fail:** Diagnose from the FAIL output, fix the root cause in the tribble or classifier (earlier in the file), and re-run. The most likely failures are:
- A11 (missing DB term in tribble) — add the missing term to the tribble
- A5/A6 (priority ordering) — verify the classifier regex priority order
- A16 (misclassification fix) — verify the tribble row has the correct new category
- A17 (coverage threshold) — verify that the classifier regex patterns cover enough terms

Do NOT modify the assertion thresholds or expected values to make them pass. Fix the data/code instead.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" dev/lifestage/validate_lifestage.R</automated>
  </verify>
  <acceptance_criteria>
    - `Rscript dev/lifestage/validate_lifestage.R` exits with status 0
    - Script output contains `"All assertions passed."`
    - Script output contains `"PASS: A1: 'Spawning adult' -> Adult"`
    - Script output contains `"PASS: A11: All DB lifestage_codes present in dictionary"`
    - Script output contains `"PASS: A17: Keyword classifier coverage"`
    - Script output contains a "Classification Changes" section showing changed terms
    - Script output contains summary with total/passed/failed counts
    - File contains `quit(status = 1)` for failure path
    - File contains `quit(status = 0)` for success path
    - File contains `diff_rows` computation using `dplyr::left_join`
    - File contains `n_pass` and `n_fail` summary variables
  </acceptance_criteria>
  <done>Complete validation script runs successfully. All 18 assertions pass. Classification diff shows all changed terms. Script exits 0. Phase 31 standalone validation is proven correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script -> DuckDB | Script reads from local DuckDB file; opened read_only=TRUE so no write possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | DuckDB file | accept | File is local build artifact owned by user; read_only=TRUE connection prevents writes; low-value development script |
| T-31-02 | Information Disclosure | dev/ script | accept | Script is a dev-only utility not shipped in package; no secrets or API keys involved |
| T-31-03 | Denial of Service | Large tribble parse | accept | 139-row tribble is trivial memory; script runs locally on developer machine |
</threat_model>

<verification>
After Plan 02 completes:
1. `Rscript dev/lifestage/validate_lifestage.R` exits 0
2. All 18 assertions pass (including 10 two-axis, completeness, structure, fixes, coverage)
3. Classification diff output shows every term that changed category
4. Summary statistics are printed
5. The script is fully self-contained per D-02
</verification>

<success_criteria>
- Script runs end-to-end without errors
- All assertions pass (exit code 0)
- Keyword classifier achieves >= 125/139 non-Other/Unknown coverage
- All 139 DB lifestage_codes descriptions are present in the dictionary
- Classification diff is printed showing all changed terms
- Summary shows 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/31-standalone-validation/31-02-SUMMARY.md`
</output>
