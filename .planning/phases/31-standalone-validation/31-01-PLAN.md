---
phase: 31-standalone-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev/lifestage/validate_lifestage.R
autonomous: true
requirements:
  - DICT-01
  - DICT-02
  - DICT-03
  - DICT-04
  - KWCL-01
  - KWCL-02
  - CORR-01
  - CORR-02
  - CORR-03

must_haves:
  truths:
    - ".classify_lifestage_keywords() function exists and classifies character input via priority-ordered regex"
    - "5-column dictionary tribble covers all 139 lifestage_codes descriptions plus any historical tribble-only terms"
    - "Six misclassification fixes applied (Germinated seed, Spat, Seed, Sapling, Cocoon, Corm)"
    - "No 'Reproductive' category exists -- former terms reclassified to developmental stage with reproductive_stage = TRUE"
    - "No 'Larva/Juvenile' category exists -- terms split into Larva or Juvenile per ontology criteria"
    - "reproductive_stage flag fires independently of harmonized_life_stage"
  artifacts:
    - path: "dev/lifestage/validate_lifestage.R"
      provides: "Classifier function + 5-column dictionary tribble"
      contains: ".classify_lifestage_keywords"
  key_links:
    - from: "dev/lifestage/validate_lifestage.R"
      to: "LIFESTAGE_HARMONIZATION_PLAN.md"
      via: "regex patterns and category schema"
      pattern: "classify_lifestage_keywords"
---

<objective>
Create the validation script skeleton with the complete `.classify_lifestage_keywords()` function and the full 5-column lifestage dictionary tribble, applying all data corrections (misclassification fixes, Larva/Juvenile split, Reproductive category elimination, two new terms).

Purpose: This is the data-heavy core of Phase 31. The tribble (139+ rows x 5 columns) and classifier function must be authored carefully before any assertions can run. Getting the categorizations right here prevents rework downstream.

Output: `dev/lifestage/validate_lifestage.R` with sections 1-3 complete (header, classifier function, dictionary tribble). Sections 4-7 (DB connection, assertions, diff, summary) are added in Plan 02.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-standalone-validation/31-CONTEXT.md
@.planning/phases/31-standalone-validation/31-RESEARCH.md
@.planning/phases/31-standalone-validation/31-PATTERNS.md
@LIFESTAGE_HARMONIZATION_PLAN.md
@inst/ecotox/ecotox_build.R (lines 972-1118 for current tribble)
@dev/generate_stubs.R (lines 1-30 for header + package loading pattern)
@dev/check_cassette_health.R (lines 1-10 for shebang + header pattern)

<interfaces>
<!-- Current 2-column tribble structure from inst/ecotox/ecotox_build.R lines 976-1115 -->
<!-- The executor must convert every row from 2 columns to 5 columns -->
<!-- Current tribble has 137 data rows; new tribble must include 2 additional DB terms: "Not coded" and "Turion" -->

Current tribble format (2 columns):
```r
life_stage <- tibble::tribble(
  ~org_lifestage, ~harmonized_life_stage,
  "Adult",        "Adult",
  ...
)
```

New tribble format (5 columns per DICT-01):
```r
life_stage_new <- tibble::tribble(
  ~org_lifestage, ~harmonized_life_stage, ~ontology_id, ~reproductive_stage, ~classification_source,
  "Adult",        "Adult",                "UBERON:0000113", FALSE,            "dictionary",
  ...
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create script skeleton with classifier function</name>
  <files>dev/lifestage/validate_lifestage.R</files>
  <read_first>
    - dev/generate_stubs.R (lines 1-30 for shebang + suppressPackageStartupMessages pattern)
    - dev/check_cassette_health.R (lines 1-10 for shebang + header comment pattern)
    - LIFESTAGE_HARMONIZATION_PLAN.md (lines 92-123 for classifier function spec)
    - .planning/phases/31-standalone-validation/31-PATTERNS.md (all pattern assignments)
    - .planning/phases/31-standalone-validation/31-RESEARCH.md (sections 2.1-2.5 for script structure)
  </read_first>
  <action>
Create `dev/lifestage/` directory and `dev/lifestage/validate_lifestage.R` with the following sections:

**Section 1: Header + package loading** (per D-05, matching dev/generate_stubs.R pattern)

```r
#!/usr/bin/env Rscript
# ==============================================================================
# Lifestage Standalone Validation
# ==============================================================================
#
# Defines the 5-column lifestage dictionary tribble, .classify_lifestage_keywords(),
# and 18 assertions. Reads ecotox.duckdb read-only. Does NOT modify any
# production files. Validated code gets lifted into ecotox_build.R in Phase 32.
#
# Usage:
#   Rscript dev/lifestage/validate_lifestage.R
#
# Exit codes:
#   0 -- all assertions pass
#   1 -- one or more assertions failed
# ==============================================================================

suppressPackageStartupMessages({
  library(DBI)
  library(duckdb)
  library(tibble)
  library(dplyr)
  library(cli)
  library(here)
})
```

**Section 2: `.classify_lifestage_keywords()` function definition** (per KWCL-01, KWCL-02)

Define the function EXACTLY as specified in LIFESTAGE_HARMONIZATION_PLAN.md lines 93-123, with these priority-ordered regex rules:

```r
.classify_lifestage_keywords <- function(descriptions) {
  rules <- tibble::tribble(
    ~priority, ~pattern,                                                                                                                                      ~harmonized_life_stage,
    1L,        "(?i)egg|embryo|blastula|gastrula|morula|zygot|oocyte|cleavage|neurula",                                                                       "Egg/Embryo",
    2L,        "(?i)larva|fry|naupli|nymph|tadpole|veliger|zoea|instar|pupa|prepupal|protozoea|mysis|glochidia|trochophore|caterpillar|maggot",               "Larva",
    3L,        "(?i)fingerling|froglet|smolt|parr|seedling|elver|alevin|juvenile|weanling|yearling|pullet|young(?!.*adult)|post-larva|post-smolt|copepodid|copepodite|swim-up|underyearling|spat|sapling", "Juvenile",
    4L,        "(?i)subadult|immature|peripubertal|sexually immature|pre-.*adult|young adult",                                                                "Subadult",
    5L,        "(?i)adult|mature(?!.*dormant)|bloom|boot|heading|tiller|jointing|internode|shoot|imago|post-emergence|sexually mature|spawn|reproduct|gestat|lactat|gamete|gametophyte|pollen|partum|F\\d+\\s*gen|flower|prebloom|laying", "Adult",
    6L,        "(?i)dormant|senescen|cyst|stationary.*phase|\\bseed\\b|\\bspore\\b|\\bcorm\\b|\\bcocoon\\b|\\btuber\\b",                                     "Senescent/Dormant",
    99L,       ".*",                                                                                                                                           "Other/Unknown"
  )

  repro_pattern <- "(?i)spawn|reproduct|gestat|lactat|gamete|gametophyte|pollen|partum|F\\d+\\s*gen|flower|prebloom|laying"

  tibble::tibble(
    org_lifestage       = descriptions,
    harmonized_life_stage = vapply(descriptions, function(desc) {
      for (i in seq_len(nrow(rules))) {
        if (grepl(rules$pattern[i], desc, perl = TRUE)) return(rules$harmonized_life_stage[i])
      }
      "Other/Unknown"
    }, character(1)),
    ontology_id           = NA_character_,
    reproductive_stage    = grepl(repro_pattern, descriptions, perl = TRUE),
    classification_source = "keyword_fallback"
  )
}
```

Key properties the classifier must have:
- Priority ordering: Egg/Embryo (1) > Larva (2) > Juvenile (3) > Subadult (4) > Adult (5) > Senescent/Dormant (6) > Other/Unknown (99)
- `young(?!.*adult)` negative lookahead at priority 3 prevents "Young adult" from matching Juvenile (per KWCL-01)
- `mature(?!.*dormant)` negative lookahead at priority 5 prevents "Mature dormant" from matching Adult
- `repro_pattern` fires independently of developmental rules — the `reproductive_stage` column is computed via `grepl()` on the full description, not inside the priority loop (per KWCL-02, DICT-03)
- `classification_source` is always `"keyword_fallback"` for classifier output (per the plan spec — dictionary rows will have `"dictionary"`)

**Section 3 placeholder:** Add a comment `# Section 3: Dictionary tribble -- see Task 2` so the script is syntactically valid but incomplete. Task 2 replaces this placeholder with the full tribble.

After writing the file, format it:
```bash
air format dev/lifestage/validate_lifestage.R
```
  </action>
  <verify>
    <automated>grep -c "classify_lifestage_keywords" dev/lifestage/validate_lifestage.R</automated>
  </verify>
  <acceptance_criteria>
    - `dev/lifestage/validate_lifestage.R` exists
    - File starts with `#!/usr/bin/env Rscript`
    - File contains `suppressPackageStartupMessages({` with DBI, duckdb, tibble, dplyr, cli, here
    - File contains `.classify_lifestage_keywords <- function(descriptions)`
    - Classifier rules tribble has 7 rows (priorities 1, 2, 3, 4, 5, 6, 99)
    - `repro_pattern` variable is defined outside the priority loop
    - `reproductive_stage` is computed via `grepl(repro_pattern, descriptions, perl = TRUE)` (independent of developmental classification)
    - `classification_source` in classifier output is `"keyword_fallback"`
    - File contains `young(?!.*adult)` in priority 3 pattern
    - File contains `mature(?!.*dormant)` in priority 5 pattern
  </acceptance_criteria>
  <done>Script skeleton exists with shebang, package loading, and complete .classify_lifestage_keywords() function matching the LIFESTAGE_HARMONIZATION_PLAN.md spec</done>
</task>

<task type="auto">
  <name>Task 2: Author complete 5-column dictionary tribble with all corrections</name>
  <files>dev/lifestage/validate_lifestage.R</files>
  <read_first>
    - dev/lifestage/validate_lifestage.R (current state from Task 1)
    - inst/ecotox/ecotox_build.R (lines 972-1118 for current 2-column tribble — every row must be converted)
    - LIFESTAGE_HARMONIZATION_PLAN.md (Misclassification Fixes table, Category Schema table, Two-Axis Design section)
    - .planning/phases/31-standalone-validation/31-RESEARCH.md (section 1.3 for six fixes, section 1.4 for DB state, section 1.5 for two new terms, section 4.2 for ontology IDs)
    - .planning/phases/31-standalone-validation/31-PATTERNS.md (tribble dictionary definition pattern)
  </read_first>
  <action>
Replace the Section 3 placeholder in `dev/lifestage/validate_lifestage.R` with the complete 5-column dictionary tribble.

The tribble variable is named `life_stage_new` and uses `#fmt: table` comment to preserve column alignment (per 31-PATTERNS.md tribble pattern from inst/ecotox/ecotox_build.R).

**Columns (per DICT-01):**
- `org_lifestage` (character) — original ECOTOX description, unchanged from current tribble
- `harmonized_life_stage` (character) — one of 7 new categories: Egg/Embryo, Larva, Juvenile, Subadult, Adult, Senescent/Dormant, Other/Unknown
- `ontology_id` (character) — UBERON/PO/S11 ID where clear mapping exists, `NA_character_` otherwise
- `reproductive_stage` (logical) — TRUE when description indicates reproductive activity, FALSE otherwise
- `classification_source` (character) — always `"dictionary"` (per DICT-04)

**Row-by-row conversion rules:**

Apply these transformations to every row from the current 2-column tribble at `inst/ecotox/ecotox_build.R` lines 976-1115:

1. **Category renames (mechanical):**
   - `"Dormant/Senescent"` -> `"Senescent/Dormant"` (order flip to match UBERON convention)
   - `"Subadult/Immature"` -> `"Subadult"` (simplified name per DICT-02)

2. **Larva/Juvenile split (per CORR-02):** Every row currently classified as `"Larva/Juvenile"` must be reassigned to either `"Larva"` or `"Juvenile"` based on ontology-backed criteria:
   - **Move to Larva:** Alevin, Fry, Glochidia, Instar, Larva, Larva-pupa, Megalopa, Mysis, Naiad, Nauplii, Neonate, Newborn, "New, newly or recent hatch", Nymph, Post-nauplius, Pre-larva, Prepupal, Prolarva, Protolarvae, Protozoea, Pupa, "Sac fry, yolk sac fry", Tadpole, Veliger, "Yolk sac larvae, sac larvae", Zoea
   - **Move to Juvenile:** Copepodid, Copepodite, Elver, Fingerling, Froglet, Parr, Post-larva, Post-smolt, Pre-smolt, Pullet, Seedling, Smolt, Sporeling, Swim-up, Underyearling, Weanling, Yearling, Young, "Young of year"

3. **Six misclassification fixes (per CORR-01):**
   - `"Germinated seed"`: was `"Dormant/Senescent"` -> now `"Juvenile"` (actively growing plant equivalent of neonate)
   - `"Spat"`: was `"Adult"` -> now `"Juvenile"` (recently settled bivalve larvae)
   - `"Seed"`: was `"Adult"` -> now `"Senescent/Dormant"` (dormant propagule)
   - `"Sapling"`: was `"Adult"` -> now `"Juvenile"` (pre-reproductive juvenile tree)
   - `"Cocoon"`: was `"Adult"` -> now `"Senescent/Dormant"` (quiescent pupal stage)
   - `"Corm"`: was `"Adult"` -> now `"Senescent/Dormant"` (underground storage organ, dormant phase)

4. **Reproductive category elimination (per CORR-03):** Every row currently classified as `"Reproductive"` moves to its developmental stage with `reproductive_stage = TRUE`:
   - `"F0 generation"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"F1 generation"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"F11 generation"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"F2 generation"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"F3 generation"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"F6 generation"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"F7 generation"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Female gametophyte"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Flower opening"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Gestation"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Gamete"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Lactational"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Egg laying"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Male gametophyte"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Pollen, pollen grain"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Postpartum"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Prebloom"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Pre-spawning"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Post-spawning"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Mature reproductive"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Sperm"` -> `"Adult"`, `reproductive_stage = TRUE`
   - `"Spawning"` -> `"Adult"`, `reproductive_stage = TRUE`

5. **Additional reproductive_stage = TRUE** (not from Reproductive category but description indicates reproductive activity):
   - `"Grain or seed formation stage"` -> stays `"Adult"`, `reproductive_stage = TRUE`
   - `"Sexually mature"` -> stays `"Adult"`, `reproductive_stage = FALSE` (sexually mature is about maturity not active reproduction)

6. **Two new terms (per RESEARCH.md section 1.5):**
   - `"Not coded"` -> `"Other/Unknown"`, `ontology_id = NA_character_`, `reproductive_stage = FALSE`, `classification_source = "dictionary"`
   - `"Turion"` -> `"Senescent/Dormant"`, `ontology_id = NA_character_`, `reproductive_stage = FALSE`, `classification_source = "dictionary"`

7. **Ontology ID assignments (per D-07, D-08):**
   Use category-anchor IDs for unambiguous generic terms, and granular IDs for clearly specific terms:
   - Generic "Egg" -> `"UBERON:0000068"` (embryo stage)
   - Generic "Embryo" -> `"UBERON:0000068"`
   - "Blastula" -> `"UBERON:0000108"` (blastula stage)
   - "Gastrula" -> `"UBERON:0000109"` (gastrula stage)
   - "Morula" -> `"UBERON:0000085"` (morula)
   - "Zygote" -> `"UBERON:0000106"` (zygote stage)
   - "Larva" -> `"UBERON:0000069"` (larval stage)
   - "Tadpole" -> `"UBERON:0002548"` (tadpole)
   - "Pupa" -> `"UBERON:0003143"` (pupal stage)
   - "Juvenile" -> `"UBERON:0034919"` (juvenile stage)
   - "Adult" -> `"UBERON:0000113"` (post-juvenile adult stage)
   - "Imago" -> `"UBERON:0000066"` (fully formed stage)
   - "Nymph" -> `"UBERON:0014405"` (nymph stage)
   - All other terms -> `NA_character_` (no clear 1:1 mapping or uncertain ID)

8. **All remaining terms** that are not covered by rules 1-7 keep their current category (renamed if needed per rule 1) with `reproductive_stage = FALSE`, `ontology_id = NA_character_`, `classification_source = "dictionary"`.

**Also important:** The `"Spore"` row was `"Other/Unknown"` in the old tribble. Per the new schema, `"Spore"` should be `"Senescent/Dormant"` (spores are dormant propagules — the Senescent/Dormant regex in the classifier includes `\\bspore\\b`). Similarly, `"Tuber"` was `"Adult"` but is `"Senescent/Dormant"` (underground dormant storage organ — the classifier includes `\\btuber\\b`). Apply these corrections.

The tribble must be formatted with `#fmt: table` comment and column-aligned tilde syntax matching the existing tribble style. Use `NA_character_` for missing ontology IDs (not `NA` or `""`).

After writing the tribble, format the file:
```bash
air format dev/lifestage/validate_lifestage.R
```

Then verify the tribble is syntactically valid R:
```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('dev/lifestage/validate_lifestage.R', echo = FALSE); cat('Tribble rows:', nrow(life_stage_new), '\n')"
```

Note: this will fail with an error about DB connection or assertions not existing yet — that is expected. The script must at minimum parse without syntax errors. To check just parsing, use:
```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "parse('dev/lifestage/validate_lifestage.R'); cat('Parse OK\n')"
```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "parse('dev/lifestage/validate_lifestage.R'); cat('Parse OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - `dev/lifestage/validate_lifestage.R` parses without syntax errors
    - Tribble variable is named `life_stage_new`
    - Tribble has 5 columns: `org_lifestage`, `harmonized_life_stage`, `ontology_id`, `reproductive_stage`, `classification_source`
    - Tribble contains `#fmt: table` comment before it
    - grep for `"Reproductive"` in `harmonized_life_stage` column returns 0 matches (category eliminated per CORR-03)
    - grep for `"Larva/Juvenile"` in `harmonized_life_stage` column returns 0 matches (split per CORR-02)
    - grep for `"Dormant/Senescent"` returns 0 matches (renamed to `"Senescent/Dormant"`)
    - grep for `"Subadult/Immature"` returns 0 matches (renamed to `"Subadult"`)
    - `"Germinated seed"` row has `"Juvenile"` (not Dormant/Senescent)
    - `"Spat"` row has `"Juvenile"` (not Adult)
    - `"Seed"` row has `"Senescent/Dormant"` (not Adult)
    - `"Sapling"` row has `"Juvenile"` (not Adult)
    - `"Cocoon"` row has `"Senescent/Dormant"` (not Adult)
    - `"Corm"` row has `"Senescent/Dormant"` (not Adult)
    - `"Spawning"` row has `harmonized_life_stage = "Adult"` and `reproductive_stage = TRUE`
    - `"Not coded"` row exists with `"Other/Unknown"`
    - `"Turion"` row exists with `"Senescent/Dormant"`
    - All `classification_source` values are `"dictionary"`
    - `"Adult"` row has `ontology_id = "UBERON:0000113"`
    - `"Tadpole"` row has `ontology_id = "UBERON:0002548"`
    - Tribble has at least 139 data rows (137 current + Not coded + Turion)
  </acceptance_criteria>
  <done>Complete 5-column dictionary tribble with all corrections authored. File parses without errors. All six misclassification fixes applied, Larva/Juvenile split complete, Reproductive category eliminated, two new terms added, ontology IDs assigned where unambiguous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script -> DuckDB | Script reads from local DuckDB file; opened read_only=TRUE so no write possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | DuckDB file | accept | File is local build artifact owned by user; read_only=TRUE connection prevents writes; low-value development script |
| T-31-02 | Information Disclosure | dev/ script | accept | Script is a dev-only utility not shipped in package; no secrets or API keys involved |
| T-31-03 | Denial of Service | Large tribble parse | accept | 139-row tribble is trivial memory; script runs locally on developer machine |
</threat_model>

<verification>
After Plan 01 completes:
1. `dev/lifestage/validate_lifestage.R` exists and parses cleanly
2. The file contains `.classify_lifestage_keywords()` with 7 priority rules
3. The file contains a `life_stage_new` tribble with 5 columns and 139+ rows
4. No old category names remain (Reproductive, Larva/Juvenile, Dormant/Senescent, Subadult/Immature)
5. All six misclassification fixes can be verified by grepping for the specific term + new category
</verification>

<success_criteria>
- Script file exists at dev/lifestage/validate_lifestage.R
- File parses without R syntax errors
- Classifier function defined with correct priority-ordered regex
- Dictionary tribble has 5 columns and 139+ rows
- All data corrections applied
- No old category names present in harmonized_life_stage values
</success_criteria>

<output>
After completion, create `.planning/phases/31-standalone-validation/31-01-SUMMARY.md`
</output>
