---
phase: 04-json-body-default
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - tests/testthat/test-ct_hazard_skin_eye_search.R
autonomous: false
user_setup:
  - service: CompTox API
    why: "Live API verification requires valid API key"
    env_vars:
      - name: ctx_api_key
        source: "Request from ccte_api@epa.gov"

must_haves:
  truths:
    - "Regenerated bulk function successfully calls live API"
    - "API returns valid data (not error)"
    - "Response can be parsed to tibble"
    - "Request Content-Type header is application/json"
  artifacts:
    - path: "tests/testthat/fixtures/ct_hazard_skin_eye_search_bulk.yml"
      provides: "VCR cassette for offline testing"
  key_links:
    - from: "ct_hazard_skin_eye_search_bulk()"
      to: "CompTox API"
      via: "HTTP POST with JSON body"
      pattern: "application/json"
---

<objective>
Verify at least one regenerated bulk function works against the live API

Purpose: Confirm the fix is correct by testing a regenerated function against the production CompTox API.

Output: VCR cassette recorded for `ct_hazard_skin_eye_search_bulk()` proving the fix works
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-json-body-default/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VCR test for ct_hazard_skin_eye_search_bulk</name>
  <files>tests/testthat/test-ct_hazard_skin_eye_search.R</files>
  <action>
Create or update the test file with VCR cassette tests for the bulk function:

```r
test_that("ct_hazard_skin_eye_search_bulk returns data for multiple DTXSIDs", {
  skip_if(Sys.getenv("ctx_api_key") == "", "API key not set")

  vcr::use_cassette("ct_hazard_skin_eye_search_bulk", {
    result <- ct_hazard_skin_eye_search_bulk(
      query = c("DTXSID7020182", "DTXSID9020112")
    )

    expect_type(result, "list")
    expect_true(length(result) > 0 || nrow(result) > 0)
  })
})

test_that("ct_hazard_skin_eye_search_bulk handles single DTXSID", {
  skip_if(Sys.getenv("ctx_api_key") == "", "API key not set")

  vcr::use_cassette("ct_hazard_skin_eye_search_bulk_single", {
    result <- ct_hazard_skin_eye_search_bulk(
      query = "DTXSID7020182"
    )

    expect_type(result, "list")
  })
})
```

If the test file already exists with tests for the non-bulk version, add these tests to the existing file.
  </action>
  <verify>
Run in R: `any(grepl("ct_hazard_skin_eye_search_bulk", readLines("tests/testthat/test-ct_hazard_skin_eye_search.R")))` returns TRUE
  </verify>
  <done>
- VCR test created for bulk function
- Test covers multiple DTXSID input
- Test covers single DTXSID input
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify JSON encoding via code inspection (fallback for no API key)</name>
  <files>R/ct_hazard_skin_eye_search.R, R/z_generic_request.R</files>
  <action>
Even without API key, verify the fix is correct by inspecting the generated code:

1. Check regenerated function passes query directly (not collapsed):
```r
lines <- readLines("R/ct_hazard_skin_eye_search.R")
# Should NOT have: body_string <- paste(query, collapse = "\n")
stopifnot(!any(grepl("body_string <- paste", lines)))
# Should have: query = query (direct passing)
stopifnot(any(grepl("query = query", lines)))
```

2. Check generic_request uses JSON encoding by default:
```r
lines <- readLines("R/z_generic_request.R")
# Should have req_body_json for array bodies
stopifnot(any(grepl("req_body_json", lines)))
```

3. Document the encoding path in the function:
```r
# Trace: ct_hazard_skin_eye_search_bulk(c("DTXSID1", "DTXSID2"))
#   -> generic_request(query = c("DTXSID1", "DTXSID2"), ...)
#   -> req_body_json(c("DTXSID1", "DTXSID2"))
#   -> Content-Type: application/json
#   -> Body: ["DTXSID1","DTXSID2"]
```

This provides verification even when API key is unavailable.
  </action>
  <verify>
Code inspection confirms:
1. No paste collapse in regenerated function
2. generic_request uses req_body_json for arrays
3. Encoding path traced and documented
  </verify>
  <done>
- JSON encoding verified via code inspection
- Encoding path documented
- Fallback verification complete (no API key required)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>VCR test for ct_hazard_skin_eye_search_bulk ready to run against live API, with fallback code verification complete</what-built>
  <how-to-verify>
**Option A: With API Key (preferred)**

1. Ensure ctx_api_key environment variable is set:
   ```r
   Sys.setenv(ctx_api_key = "YOUR_KEY_HERE")
   ```

2. Run the test to record VCR cassette:
   ```r
   testthat::test_file("tests/testthat/test-ct_hazard_skin_eye_search.R")
   ```

3. Verify:
   - Test passes (API returns data)
   - Cassette file created: `tests/testthat/fixtures/ct_hazard_skin_eye_search_bulk.yml`
   - Cassette shows `Content-Type: application/json` in request (NOT text/plain)

4. If API returns error:
   - Check if the endpoint exists and is accessible
   - Try with different DTXSIDs
   - Check API key validity

**Option B: Without API Key (fallback)**

If no API key is available, the code inspection in Task 2 provides verification:
1. Confirm Task 2 passed (no paste collapse, req_body_json present)
2. Type "verified-code-only" to indicate code-level verification passed

Expected outcome: Either test passes with JSON body cassette, OR code inspection confirms correct encoding.
  </how-to-verify>
  <resume-signal>Type "verified" if test passed with JSON body, "verified-code-only" if code inspection passed without API, or describe the error encountered</resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval:

1. Either: VCR cassette exists at `tests/testthat/fixtures/ct_hazard_skin_eye_search_bulk.yml` with JSON body
2. Or: Code inspection confirms JSON encoding path (fallback)
3. API returned valid response (200 status) if live test ran
4. Test can run offline using cassette if recorded

**JSON encoding verification methods:**
- Primary: Inspect VCR cassette for `Content-Type: application/json` header
- Fallback: Inspect `R/z_generic_request.R` for `req_body_json` usage
</verification>

<success_criteria>
1. VAL-01 satisfied: At least one regenerated bulk function verified (live API or code inspection)
2. VCR cassette proves JSON body encoding works correctly (if API key available)
3. Code inspection provides fallback verification (always available)
4. Offline testing enabled for CI/CD (if cassette recorded)
</success_criteria>

<output>
After completion, create `.planning/phases/04-json-body-default/04-03-SUMMARY.md`
</output>
