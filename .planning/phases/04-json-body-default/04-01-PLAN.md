---
phase: 04-json-body-default
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev/endpoint_eval/07_stub_generation.R
autonomous: true
user_setup: []

must_haves:
  truths:
    - "string_array body types use JSON encoding by default"
    - "Only /chemical/search/equal/ POST uses raw text body"
    - "Stub generation produces correct code for both cases"
  artifacts:
    - path: "dev/endpoint_eval/07_stub_generation.R"
      provides: "Fixed stub generation logic"
      contains: "body_type = \"raw_text\""
  key_links:
    - from: "07_stub_generation.R"
      to: "generic_request()"
      via: "Generated function code"
      pattern: "generic_request\\("
---

<objective>
Fix stub generation to use JSON encoding for string_array body types instead of newline collapsing

Purpose: The current stub generation incorrectly collapses all string_array bodies to newline-delimited text. Only `/chemical/search/equal/` POST should use raw text encoding; all other bulk endpoints expect JSON arrays.

Output: Updated `07_stub_generation.R` with corrected detection logic
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-json-body-default/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix string_array body type handling in stub generation</name>
  <files>dev/endpoint_eval/07_stub_generation.R</files>
  <action>
**Part A: Modify the is_raw_text_body detection (lines 370-376)**

Current state (too broad - triggers for ANY string POST):
```r
is_raw_text_body <- (
  body_schema_type == "string" &&
  toupper(method) == "POST" &&
  wrapper_fn == "generic_request"
)
```

Required change: Add a 4th condition to check for the specific endpoint.

Modified code:
```r
is_raw_text_body <- (
  body_schema_type == "string" &&
  toupper(method) == "POST" &&
  wrapper_fn == "generic_request" &&
  (endpoint == "chemical/search/equal/" || grepl("chemical/search/equal/$", endpoint))
)
```

NOTE: The trailing slash is REQUIRED - the endpoint value includes the trailing slash (e.g., "chemical/search/equal/"). Use exact match `endpoint == "chemical/search/equal/"` as primary check, with grepl fallback for robustness.

**Part B: Remove newline collapsing for string_array (lines 457-474)**

Step 1: Locate the string_array handling block at lines 457-474. It currently contains:
```r
if (body_schema_type == "string_array") {
  # ... code with paste(query, collapse = "\n") ...
  body_string <- paste(query, collapse = "\n")
  # ... more code using body_string ...
}
```

Step 2: Delete the entire if block (lines 457-474).

Step 3: Replace with this new code that passes query directly:
```r
if (body_schema_type == "string_array") {
  # Array body: pass directly, generic_request() handles JSON encoding
  fn_body <- glue::glue('
{fn} <- function(query) {{
  result <- generic_request(
    query = query,
    endpoint = "{endpoint}",
    method = "{method}",
    batch_limit = as.numeric(Sys.getenv("batch_limit", "100"))
  )

  return(result)
}}

')
}
```

**Part C: Preserve string case unchanged (lines 475-490)**

IMPORTANT: Keep the `string` case (lines 475-490) unchanged - single string bodies should still pass directly without collapsing.

WHY: The newline collapsing was added as a workaround for `/chemical/search/equal/` but incorrectly applied to ALL string_array body types. The correct approach is:
- `body_schema_type == "string"` + `/chemical/search/equal/` endpoint = raw text (body_type = "raw_text")
- `body_schema_type == "string_array"` = JSON array (no body_type parameter, default behavior)
  </action>
  <verify>
1. Run in R: `grep("paste\\(query, collapse", readLines("dev/endpoint_eval/07_stub_generation.R"))` - should return character(0)
2. Run in R: `grep("string_array", readLines("dev/endpoint_eval/07_stub_generation.R"), value = TRUE)` - should show the new pattern WITHOUT newline collapsing
3. Run in R: `grep("chemical/search/equal/", readLines("dev/endpoint_eval/07_stub_generation.R"), value = TRUE)` - should show endpoint-specific check with trailing slash
4. Run in R: `any(grepl("body_string <- paste", readLines("dev/endpoint_eval/07_stub_generation.R")))` - should return FALSE
  </verify>
  <done>
- string_array body types generate code without `paste(query, collapse = "\n")`
- Only `/chemical/search/equal/` POST triggers the `body_type = "raw_text"` path
- Detection logic uses exact endpoint match with trailing slash
- Old pattern `body_string <- paste` is completely removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Add comment documentation explaining the body type logic</name>
  <files>dev/endpoint_eval/07_stub_generation.R</files>
  <action>
Add clear documentation comments above the is_raw_text_body check (around line 370) explaining:

```r
  # ===========================================================================
  # Body Type Handling for POST Requests
  # ===========================================================================
  # Most POST endpoints expect JSON-encoded request bodies. The CompTox API has
  # exactly ONE endpoint that requires raw text (newline-delimited):
  #   - POST /chemical/search/equal/ (body_schema_type == "string")
  #
  # All other POST endpoints with array bodies (body_schema_type == "string_array")
  # should use the default JSON encoding via generic_request().
  #
  # Decision: RAW-TEXT-01 (2026-01-27) - Special case in stub generation
  # ===========================================================================
```

This documents WHY the endpoint-specific check exists and references the decision ID from Phase 3.
  </action>
  <verify>
Run in R: `grep("Body Type Handling", readLines("dev/endpoint_eval/07_stub_generation.R"), value = TRUE)` - should show the documentation header
  </verify>
  <done>
- Clear documentation explains the body type logic
- References decision RAW-TEXT-01
- Future maintainers understand why endpoint check exists
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Run in R: `sum(grepl("paste\\(query, collapse", readLines("dev/endpoint_eval/07_stub_generation.R")))` returns 0
2. The `is_raw_text_body` condition includes `endpoint == "chemical/search/equal/"` or equivalent with trailing slash
3. The `string_array` code path generates direct `query = query` without collapsing
4. Documentation comment exists explaining the logic
</verification>

<success_criteria>
1. STUB-01 satisfied: string_array body types generate JSON-encoding code
2. STUB-02 satisfied: Only /chemical/search/equal/ uses raw text body
3. Code is documented for future maintainability
</success_criteria>

<output>
After completion, create `.planning/phases/04-json-body-default/04-01-SUMMARY.md`
</output>
