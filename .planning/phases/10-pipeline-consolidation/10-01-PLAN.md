---
phase: 10-pipeline-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev/generate_stubs.R
  - dev/endpoint_eval/04_openapi_parser.R
autonomous: true

must_haves:
  truths:
    - "Running `Rscript dev/generate_stubs.R` succeeds without errors"
    - "generate_chemi_stubs() calls openapi_to_spec() directly (not via parse_chemi_schemas)"
    - "All three generators (ct, chemi, cc) follow identical parsing pattern"
    - "Chemi stubs regenerate with same or improved content (no regression)"
    - "parse_chemi_schemas() is removed from codebase"
  artifacts:
    - path: "dev/generate_stubs.R"
      provides: "Unified stub generation with select_schema_files() helper"
      contains: "select_schema_files"
    - path: "dev/endpoint_eval/04_openapi_parser.R"
      provides: "OpenAPI parser without parse_chemi_schemas()"
      missing: "parse_chemi_schemas"
  key_links:
    - from: "dev/generate_stubs.R"
      to: "dev/endpoint_eval/04_openapi_parser.R"
      via: "openapi_to_spec() call"
      pattern: "openapi_to_spec\\(openapi\\)"
    - from: "generate_chemi_stubs()"
      to: "select_schema_files()"
      via: "stage-based file selection"
      pattern: "select_schema_files.*stage_priority"
---

<objective>
Consolidate generate_stubs.R to use openapi_to_spec() for all schema types (ct, chemi, cc), eliminating the divergent parse_chemi_schemas() code path.

Purpose: Ensure all schema parsing goes through the same pipeline for consistent version detection, body extraction, and filtering across all three schema types.

Output: Unified generate_stubs.R where all three generator functions follow the same pattern: select files -> load JSON -> openapi_to_spec() -> post-process -> render stubs.
</objective>

<execution_context>
@C:\Users\sxthi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sxthi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pipeline-consolidation/10-CONTEXT.md
@.planning/phases/10-pipeline-consolidation/10-RESEARCH.md

# Key source files
@dev/generate_stubs.R
@dev/endpoint_eval/04_openapi_parser.R
@dev/endpoint_eval/00_config.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract select_schema_files() and unify generate_chemi_stubs()</name>
  <files>
    dev/generate_stubs.R
  </files>
  <action>
1. Add `select_schema_files()` helper function after the Configuration section (before generate_ct_stubs). Extract the stage-based file selection logic from parse_chemi_schemas() lines 648-668:

```r
#' Select schema files with optional stage-based prioritization
#'
#' For schemas with multiple stage variants (e.g., chemi-mordred-prod.json,
#' chemi-mordred-staging.json), selects the highest priority stage per domain.
#'
#' @param pattern Regex pattern to match schema files (e.g., "^chemi-.*\\.json$")
#' @param exclude_pattern Optional pattern to exclude (e.g., "ui"). NULL to skip.
#' @param stage_priority Character vector of stage names in priority order.
#'   NULL for schemas without stage variants (ct, cc).
#' @param schema_dir Path to schema directory. Defaults to here::here("schema").
#' @return Character vector of selected filenames (not full paths)
select_schema_files <- function(
  pattern,
  exclude_pattern = NULL,
  stage_priority = NULL,
  schema_dir = NULL
) {
  if (is.null(schema_dir)) schema_dir <- here::here("schema")

  # List matching files
  files <- list.files(path = schema_dir, pattern = pattern, full.names = FALSE)

  if (length(files) == 0) return(character(0))

  # Apply exclusion filter
  if (!is.null(exclude_pattern) && nzchar(exclude_pattern)) {
    files <- files[!grepl(exclude_pattern, files, ignore.case = TRUE)]
  }

  if (length(files) == 0) return(character(0))

  # Stage-based selection (if stage_priority provided)
  if (!is.null(stage_priority)) {
    schema_meta <- tibble(file = files) %>%
      tidyr::separate_wider_delim(
        cols = file,
        delim = "-",
        names = c("origin", "domain", "stage"),
        cols_remove = FALSE
      ) %>%
      mutate(
        stage = str_remove(stage, "\\.json$"),
        stage = factor(stage, levels = stage_priority)
      )

    files <- schema_meta %>%
      group_by(domain) %>%
      arrange(stage, .by_group = TRUE) %>%
      slice(1) %>%
      ungroup() %>%
      pull(file)
  }

  files
}
```

2. Refactor `generate_chemi_stubs()` to match the ct/cc pattern:
   - Use `select_schema_files()` with `stage_priority = c("prod", "staging", "dev")`
   - Call `openapi_to_spec()` directly in the map loop (not via parse_chemi_schemas)
   - Add source_file column for traceability
   - Apply `ENDPOINT_PATTERNS_TO_EXCLUDE` filter after parsing
   - Keep existing post-processing (route/domain/name/file derivation, fn naming)

The refactored generate_chemi_stubs() should look like:

```r
generate_chemi_stubs <- function() {
  cli_h2("Cheminformatics (chemi_*)")

  # Select schema files with stage prioritization
  chemi_schema_files <- select_schema_files(
    pattern = "^chemi-.*\\.json$",
    exclude_pattern = "ui",
    stage_priority = c("prod", "staging", "dev")
  )

  if (length(chemi_schema_files) == 0) {
    cli_alert_warning("No chemi schema files found, skipping chemi_* generation")
    return(tibble(action = character(), file = character()))
  }

  cli_alert_info("Found {length(chemi_schema_files)} chemi schema file(s)")

  # Parse all schemas using openapi_to_spec directly
  chemi_endpoints <- tryCatch({
    map(
      chemi_schema_files,
      ~ {
        openapi <- jsonlite::fromJSON(here::here('schema', .x), simplifyVector = FALSE)
        spec <- openapi_to_spec(openapi)
        spec$source_file <- .x
        spec
      },
      .progress = FALSE
    ) %>%
      list_rbind() %>%
      filter(
        str_detect(method, 'GET|POST'),
        !str_detect(route, ENDPOINT_PATTERNS_TO_EXCLUDE)
      ) %>%
      # ... rest of existing post-processing unchanged
  }, error = function(e) {
    cli_alert_warning("Error parsing chemi schemas: {e$message}")
    return(tibble())
  })

  # ... rest of function unchanged
}
```

3. Verify `generate_ct_stubs()` and `generate_cc_stubs()` could optionally use `select_schema_files()` but leave them as-is since they work correctly. The key change is chemi using the same `openapi_to_spec()` pattern.
  </action>
  <verify>
Run: `cd C:/Users/sxthi/Documents/ComptoxR && Rscript -e "source('dev/generate_stubs.R')"`

Expected:
- Script completes without errors
- All three generators run (ct_*, chemi_*, cc_*)
- Chemi endpoints are parsed correctly (check log output for endpoint count)
  </verify>
  <done>
- select_schema_files() helper exists in generate_stubs.R
- generate_chemi_stubs() calls openapi_to_spec() directly (grep confirms no parse_chemi_schemas call)
- Script runs successfully end-to-end
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove parse_chemi_schemas() and verify no regression</name>
  <files>
    dev/endpoint_eval/04_openapi_parser.R
  </files>
  <action>
1. Delete the `parse_chemi_schemas()` function from `04_openapi_parser.R` (lines 590-690 approximately). This includes:
   - The roxygen documentation block
   - The entire function definition
   - The `@export` tag

2. Verify that openapi_to_spec() remains intact and functional (it's the unified entry point now).

3. Create a quick verification by regenerating stubs and checking output:
   - Save current chemi stubs to baseline (optional - for manual diff)
   - Run stub generation
   - Confirm chemi stubs have has_body and body_params populated for POST endpoints
   - Spot-check one chemi stub file for correct structure

Note: Per CONTEXT.md, "improvements OK" - new/better stubs from improved body extraction are acceptable differences, not regressions. A regression would be missing endpoints, broken syntax, or fewer parameters than before.
  </action>
  <verify>
Run: `cd C:/Users/sxthi/Documents/ComptoxR && Rscript -e "source('dev/generate_stubs.R')"`

Verify parse_chemi_schemas removed:
`grep -r "parse_chemi_schemas" dev/endpoint_eval/`
Expected: No matches

Verify stubs generated correctly:
- Check that R/chemi_*.R files exist
- Spot-check one file for proper roxygen and function structure
  </verify>
  <done>
- parse_chemi_schemas() deleted from 04_openapi_parser.R
- generate_stubs.R runs successfully without it
- Chemi stubs regenerate correctly (no syntax errors, proper structure)
- No regression in stub content (improvements are acceptable)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Unified pipeline verified:**
   ```bash
   cd C:/Users/sxthi/Documents/ComptoxR
   grep -c "openapi_to_spec" dev/generate_stubs.R  # Should show 3+ occurrences
   grep "parse_chemi_schemas" dev/generate_stubs.R  # Should return nothing
   ```

2. **Script runs end-to-end:**
   ```bash
   Rscript dev/generate_stubs.R
   # Should complete with "Stub generation complete!" message
   ```

3. **parse_chemi_schemas deleted:**
   ```bash
   grep -r "parse_chemi_schemas" dev/endpoint_eval/
   # Should return nothing
   ```

4. **Chemi stubs valid:**
   - Check R/chemi_*.R files exist
   - Open one chemi stub and verify it has proper roxygen comments and function signature
</verification>

<success_criteria>
1. `generate_chemi_stubs()` calls `openapi_to_spec()` directly (UNIFY-01)
2. All three generators follow identical pattern: select files -> parse -> post-process -> render (UNIFY-02)
3. `ENDPOINT_PATTERNS_TO_EXCLUDE` applied in chemi generator (UNIFY-03)
4. Script runs without errors - version detection works for all schemas (UNIFY-04)
5. `parse_chemi_schemas()` removed from codebase (CLEAN-01)
6. Code duplication reduced via `select_schema_files()` helper (CLEAN-02)
7. All existing stubs regenerate correctly (VAL-02)
8. Chemi stubs benefit from v1.5 Swagger 2.0 body extraction (VAL-03)

Note: VAL-01 (GH Action alignment) is deferred per CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/10-pipeline-consolidation/10-01-SUMMARY.md`
</output>
