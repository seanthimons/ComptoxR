---
phase: 16-ci-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/unicode_map.R
  - data-raw/unicode_map.R
  - .github/workflows/schema-check.yml
autonomous: true
must_haves:
  truths:
    - "pkgload::load_all() succeeds in CI without usethis dependency error"
    - "unicode_map object is available at runtime via sysdata.rda"
    - "R/unicode_map.R no longer exists in the R/ directory"
    - "data-raw/unicode_map.R generates sysdata.rda when executed"
  artifacts:
    - path: "data-raw/unicode_map.R"
      provides: "Script that builds unicode_map and saves to sysdata.rda"
      contains: "usethis::use_data"
    - path: "R/sysdata.rda"
      provides: "Pre-built internal data containing unicode_map"
    - path: ".github/workflows/schema-check.yml"
      provides: "CI workflow with usethis dependency and sysdata.rda fallback"
      contains: "any::usethis"
  key_links:
    - from: "data-raw/unicode_map.R"
      to: "R/sysdata.rda"
      via: "usethis::use_data(internal = TRUE)"
      pattern: "use_data.*internal.*TRUE"
    - from: "R/clean_unicode.R"
      to: "R/sysdata.rda"
      via: "internal data lazy-load"
      pattern: "unicode_map"
    - from: ".github/workflows/schema-check.yml"
      to: "data-raw/unicode_map.R"
      via: "source('data-raw/unicode_map.R') fallback step"
      pattern: "data-raw/unicode_map"
---

<objective>
Fix the broken schema-check GitHub Action workflow by moving the unicode_map data generation script out of R/ (where it runs at source time and fails without usethis) into data-raw/ (standard R package convention for data generation scripts). Update the CI workflow to handle sysdata.rda generation.

Purpose: pkgload::load_all() in CI currently fails because R/unicode_map.R calls usethis::use_data() at source time, and usethis is not a CI dependency. This blocks the entire schema-check workflow.

Output: Working CI workflow that can load the package and run schema checks without errors.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@R/unicode_map.R
@R/clean_unicode.R
@.github/workflows/schema-check.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move unicode_map from R/ to data-raw/ and verify sysdata.rda</name>
  <files>data-raw/unicode_map.R, R/unicode_map.R, R/sysdata.rda</files>
  <action>
1. Create the `data-raw/` directory (it does not currently exist).

2. Create `data-raw/unicode_map.R` with the unicode map data generation logic. Copy the full mapping definitions (greek_map, math_map, script_map, misc_map, latin_map) from the current `R/unicode_map.R`. The script must:
   - Define all mapping vectors exactly as they exist in R/unicode_map.R
   - Combine them into `unicode_map` and deduplicate
   - Call `usethis::use_data(unicode_map, overwrite = TRUE, internal = TRUE)` at the end
   - This is a standalone script (not a function) — it runs via `source()` or `Rscript`

3. Delete `R/unicode_map.R` entirely. No migration shims or comments — clean removal per user decision.

4. Run the new data-raw script to regenerate sysdata.rda:
   ```
   "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" data-raw/unicode_map.R
   ```

5. Verify pkgload::load_all() succeeds locally:
   ```
   "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "pkgload::load_all(); cat('unicode_map has', length(unicode_map), 'entries\n')"
   ```
   Expected: loads without error and unicode_map has entries (should be ~100+).

Note: sysdata.rda already exists and is tracked in git. The data-raw/ script regenerates it. The .Rbuildignore already excludes data-raw/ from the built package.
  </action>
  <verify>
- `R/unicode_map.R` does not exist
- `data-raw/unicode_map.R` exists and contains `usethis::use_data(unicode_map, overwrite = TRUE, internal = TRUE)`
- `R/sysdata.rda` exists (regenerated by running data-raw script)
- `pkgload::load_all()` succeeds without error
- `unicode_map` object is accessible after load_all()
  </verify>
  <done>R/unicode_map.R is deleted, data-raw/unicode_map.R generates sysdata.rda correctly, and pkgload::load_all() succeeds locally with unicode_map available at runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Update schema-check CI workflow for usethis and sysdata.rda fallback</name>
  <files>.github/workflows/schema-check.yml</files>
  <action>
1. In `.github/workflows/schema-check.yml`, add `any::usethis` to the `extra-packages` list in the "Install dependencies" step.

2. Add a new step BEFORE the "Download schemas" step (which calls `pkgload::load_all()`). This step handles sysdata.rda generation as a safety net. Name it "Ensure internal data" and structure it as:

```yaml
- name: Ensure internal data
  run: |
    if (!file.exists("R/sysdata.rda")) {
      cli::cli_alert_warning("sysdata.rda missing, regenerating from data-raw/")
      source("data-raw/unicode_map.R")
    } else {
      cli::cli_alert_info("sysdata.rda found, skipping regeneration")
    }
  shell: Rscript {0}
```

This ensures that even if sysdata.rda were somehow missing from the repo, CI can regenerate it by running the data-raw script before load_all() is called.

3. Do NOT remove any existing extra-packages entries — only add `any::usethis`.
  </action>
  <verify>
- `.github/workflows/schema-check.yml` contains `any::usethis` in extra-packages
- A new "Ensure internal data" step exists before the "Download schemas" step
- The step checks for sysdata.rda and runs source("data-raw/unicode_map.R") to regenerate if missing
- No existing workflow steps or dependencies were removed
  </verify>
  <done>CI workflow has usethis as a dependency and a fallback step using source("data-raw/unicode_map.R") to regenerate sysdata.rda if missing, ensuring pkgload::load_all() will always succeed.</done>
</task>

</tasks>

<verification>
1. `R/unicode_map.R` does not exist (moved to data-raw/)
2. `data-raw/unicode_map.R` exists with `usethis::use_data(internal = TRUE)` call
3. `R/sysdata.rda` exists and contains unicode_map
4. `pkgload::load_all()` succeeds without usethis dependency error
5. `.github/workflows/schema-check.yml` includes `any::usethis` in extra-packages
6. CI workflow has fallback step for missing sysdata.rda
</verification>

<success_criteria>
- pkgload::load_all() works in a clean environment (no usethis installed) because R/unicode_map.R no longer exists
- unicode_map data is available at runtime via sysdata.rda
- CI workflow can regenerate sysdata.rda if needed via source("data-raw/unicode_map.R")
- All four requirements (CI-01 through CI-04) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/16-ci-fix/16-01-SUMMARY.md`
</output>
