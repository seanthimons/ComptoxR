---
phase: 35-shared-helper-layer-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/eco_lifestage_patch.R
  - tests/testthat/test-eco_lifestage_gate.R
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03]

must_haves:
  truths:
    - "NVS SPARQL failure emits cli_warn and returns empty tibble instead of aborting"
    - "OLS4 HTTP failure emits cli_warn and returns empty tibble instead of crashing"
    - "OLS4 results contain only UBERON: or PO: prefixed IDs after post-filter"
    - "Scoring tiers (100/90/75) and status assignment (resolved/ambiguous/unresolved) produce correct output shapes"
    - "Existing 8 tests in test-eco_lifestage_gate.R still pass after adapter changes"
  artifacts:
    - path: "R/eco_lifestage_patch.R"
      provides: "Fixed NVS and OLS4 adapters with tryCatch + cli_warn + empty tibble return"
      contains: "tryCatch"
    - path: "tests/testthat/test-eco_lifestage_gate.R"
      provides: "PROV-02 unit test for NVS failure handling"
      contains: "NVS failure emits warning"
  key_links:
    - from: ".eco_lifestage_nvs_index()"
      to: "tryCatch error handler"
      via: "wraps httr2 pipeline in tryCatch, returns NULL on error"
      pattern: "tryCatch.*httr2::request.*vocab\\.nerc"
    - from: ".eco_lifestage_query_ols4()"
      to: "tryCatch error handler"
      via: "wraps httr2 pipeline in tryCatch, returns NULL on error"
      pattern: "tryCatch.*httr2::request.*ebi\\.ac\\.uk"
    - from: ".eco_lifestage_query_ols4()"
      to: "dplyr::filter with startsWith"
      via: "prefix post-filter after NA filter"
      pattern: "startsWith.*source_term_id.*toupper.*ontology"
---

<objective>
Fix two compliance gaps in the HTTP adapters and add a unit test for NVS failure handling.

Purpose: Make the provider adapters individually resilient so that one provider being down does not crash the entire resolve pipeline. Fix OLS4 cross-ontology contamination (4 confirmed GO: prefix entries in baseline CSV).

Output: Patched `R/eco_lifestage_patch.R` with tryCatch wrappers on both adapters and obo_id prefix post-filter on OLS4. New PROV-02 unit test in `test-eco_lifestage_gate.R`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-shared-helper-layer-validation/35-CONTEXT.md
@.planning/phases/35-shared-helper-layer-validation/35-RESEARCH.md
@.planning/phases/35-shared-helper-layer-validation/35-PATTERNS.md
@.planning/phases/34-teardown/34-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/eco_lifestage_patch.R lines 8-24 (cache schema - output shape for empty returns):
```r
.eco_lifestage_cache_schema <- function() {
  tibble::tibble(
    org_lifestage = character(), source_provider = character(),
    source_ontology = character(), source_term_id = character(),
    source_term_label = character(), source_term_definition = character(),
    source_release = character(), source_match_method = character(),
    source_match_status = character(), candidate_rank = integer(),
    candidate_score = double(), candidate_reason = character(),
    ecotox_release = character()
  )
}
```

From R/eco_lifestage_patch.R line 440 (NVS index function signature):
```r
.eco_lifestage_nvs_index <- function(refresh = FALSE)
```

From R/eco_lifestage_patch.R line 498 (OLS4 query function signature):
```r
.eco_lifestage_query_ols4 <- function(term, ontology = c("UBERON", "PO"))
```

From R/eco_lifestage_patch.R line 548 (NVS query function signature):
```r
.eco_lifestage_query_nvs <- function(term)
```

From R/eco_lifestage_patch.R line 666 (resolve_term - the consumer of both adapters):
```r
.eco_lifestage_resolve_term <- function(org_lifestage, ecotox_release) {
  release_id <- ecotox_release
  candidates <- dplyr::bind_rows(
    .eco_lifestage_query_ols4(org_lifestage, "UBERON"),
    .eco_lifestage_query_ols4(org_lifestage, "PO"),
    .eco_lifestage_query_nvs(org_lifestage)
  )
  .eco_lifestage_rank_candidates(org_lifestage, candidates) |>
    dplyr::mutate(ecotox_release = release_id)
}
```

From tests/testthat/test-eco_lifestage_gate.R lines 245-251 (existing mocking pattern):
```r
testthat::with_mocked_bindings(
  .eco_lifestage_query_ols4 = function(...) stop("live lookup should not run"),
  .eco_lifestage_query_nvs = function(...) stop("live lookup should not run"),
  .package = "ComptoxR",
  { ... }
)
```

From R/eco_connection.R lines 128-138 (project tryCatch + cli_warn pattern):
```r
tryCatch(
  .db_download_release("ecotox", dest, tag = tag),
  error = function(e) {
    cli::cli_warn(c(
      "Could not download ECOTOX database from GitHub Release.",
      "i" = conditionMessage(e),
      "i" = "Falling back to build-from-source."
    ))
    .eco_build_from_source(dest)
  }
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NVS and OLS4 adapter resilience + prefix post-filter</name>
  <files>R/eco_lifestage_patch.R</files>
  <read_first>
    - R/eco_lifestage_patch.R (full file - the target being modified)
    - R/eco_connection.R (lines 128-138 - analog for tryCatch + cli_warn pattern)
    - .planning/phases/35-shared-helper-layer-validation/35-PATTERNS.md (exact before/after code for all three changes)
  </read_first>
  <action>
Three surgical edits to `R/eco_lifestage_patch.R`. No other files touched.

**Edit 1 — NVS tryCatch + cli_warn (D-03, D-04, PROV-02):**
Replace lines 457-467 (the bare `httr2::request(...)` pipeline through the `cli_abort` block) with:

```r
payload <- tryCatch(
  {
    httr2::request("https://vocab.nerc.ac.uk/sparql/sparql") |>
      httr2::req_body_form(query = query) |>
      httr2::req_headers(Accept = "application/sparql-results+json") |>
      httr2::req_perform() |>
      httr2::resp_body_string() |>
      jsonlite::fromJSON(simplifyDataFrame = TRUE)
  },
  error = function(e) {
    cli::cli_warn(c(
      "NVS S11 SPARQL endpoint unreachable.",
      "i" = "NVS candidates will be skipped for this resolution run.",
      "x" = conditionMessage(e)
    ))
    NULL
  }
)

if (is.null(payload)) {
  return(tibble::tibble())
}

bindings <- payload$results$bindings
if (is.null(bindings) || nrow(bindings) == 0) {
  cli::cli_warn("NVS S11 lookup returned no concepts.")
  return(tibble::tibble())
}
```

CRITICAL ordering: Both `return(tibble::tibble())` calls MUST appear BEFORE the `index <- tibble::tibble(...)` construction at line 469 and the `.ComptoxREnv$eco_lifestage_nvs_index <- index` cache assignment at line 493. This prevents caching an empty index that would suppress retries.

**Edit 2 — OLS4 tryCatch (D-06, PROV-01):**
Replace lines 501-509 (the bare `httr2::request(...)` pipeline for OLS4) with:

```r
response <- tryCatch(
  {
    httr2::request("https://www.ebi.ac.uk/ols4/api/search") |>
      httr2::req_url_query(
        q = term,
        ontology = tolower(ontology),
        rows = 25
      ) |>
      httr2::req_perform() |>
      httr2::resp_body_string() |>
      jsonlite::fromJSON(simplifyDataFrame = TRUE)
  },
  error = function(e) {
    cli::cli_warn(c(
      "OLS4 endpoint unreachable for {ontology}.",
      "i" = "OLS4 candidates will be skipped for this resolution run.",
      "x" = conditionMessage(e)
    ))
    NULL
  }
)

if (is.null(response)) {
  return(tibble::tibble())
}
```

This replaces only the HTTP pipeline. The existing `docs <- response$response$docs` line and everything after it remains unchanged.

**Edit 3 — OLS4 prefix post-filter (D-05, PROV-01):**
Find the existing filter at line 544:
```r
dplyr::filter(!is.na(.data$source_term_id), !is.na(.data$source_term_label))
```

Replace with:
```r
dplyr::filter(
  !is.na(.data$source_term_id),
  !is.na(.data$source_term_label),
  startsWith(.data$source_term_id, paste0(toupper(ontology), ":"))
)
```

CRITICAL: Use `toupper(ontology)` not `tolower(ontology)`. OLS4 returns IDs as `UBERON:0000069`, `PO:0009010` (uppercase prefix). The `ontology` parameter is already validated by `rlang::arg_match(ontology)` against `c("UBERON", "PO")` so `toupper()` is technically a no-op but makes intent explicit and guards against future changes.

After all three edits, run `air format R/eco_lifestage_patch.R` and `jarl check R/eco_lifestage_patch.R` per project conventions.
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/ComptoxR && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::test(filter='eco_lifestage_gate')"</automated>
  </verify>
  <acceptance_criteria>
    - R/eco_lifestage_patch.R contains `tryCatch` wrapping the NVS httr2 pipeline (search for `tryCatch` near `vocab.nerc.ac.uk`)
    - R/eco_lifestage_patch.R contains `cli::cli_warn` with text "NVS S11 SPARQL endpoint unreachable" (not cli_abort)
    - R/eco_lifestage_patch.R contains `tryCatch` wrapping the OLS4 httr2 pipeline (search for `tryCatch` near `ebi.ac.uk`)
    - R/eco_lifestage_patch.R contains `cli::cli_warn` with text "OLS4 endpoint unreachable for" (not cli_abort)
    - R/eco_lifestage_patch.R contains `startsWith(.data$source_term_id, paste0(toupper(ontology), ":"))` in the OLS4 query function
    - R/eco_lifestage_patch.R does NOT contain `cli::cli_abort("NVS S11 lookup returned no concepts.")` (the old abort)
    - Both `return(tibble::tibble())` guards in `.eco_lifestage_nvs_index()` appear BEFORE the line `.ComptoxREnv$eco_lifestage_nvs_index <- index`
    - All existing tests pass: `devtools::test(filter='eco_lifestage_gate')` exits with 0 failures
    - `air format R/eco_lifestage_patch.R` produces no changes (already formatted)
    - `jarl check R/eco_lifestage_patch.R` reports no errors
  </acceptance_criteria>
  <done>NVS adapter uses tryCatch + cli_warn + empty tibble on HTTP failure. OLS4 adapter uses tryCatch + cli_warn + empty tibble on HTTP failure. OLS4 results are post-filtered by obo_id prefix. All 8 existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add PROV-02 unit test for NVS failure handling</name>
  <files>tests/testthat/test-eco_lifestage_gate.R</files>
  <read_first>
    - tests/testthat/test-eco_lifestage_gate.R (full file - the target being modified)
    - R/eco_lifestage_patch.R (lines 440-495 - the fixed NVS adapter, to understand what the test validates)
    - .planning/phases/35-shared-helper-layer-validation/35-PATTERNS.md (PROV-02 unit test pattern at bottom)
  </read_first>
  <action>
Add a new `test_that()` block to `tests/testthat/test-eco_lifestage_gate.R` BEFORE the `test_that("section 16 remains identical...")` test at line 556. This placement keeps it with the other provider-behavior tests.

Insert the following test:

```r
test_that("NVS failure emits warning and returns empty tibble", {
  testthat::expect_warning(
    testthat::with_mocked_bindings(
      .eco_lifestage_nvs_index = function(refresh = FALSE) {
        cli::cli_warn("NVS S11 SPARQL endpoint unreachable. [TEST]")
        tibble::tibble()
      },
      .package = "ComptoxR",
      result <- .eco_lifestage_query_nvs("Adult")
    ),
    "NVS S11 SPARQL"
  )
  testthat::expect_s3_class(result, "tbl_df")
  testthat::expect_equal(nrow(result), 0L)
})
```

This test validates PROV-02: when NVS is unreachable, `.eco_lifestage_query_nvs()` receives an empty index, the function filters against an empty tibble, and returns an empty tibble — no error, no abort, pipeline continues. The `expect_warning` confirms `cli_warn` fires with the expected message pattern.

After adding the test, run `air format tests/testthat/test-eco_lifestage_gate.R` and `jarl check tests/testthat/test-eco_lifestage_gate.R`.
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/ComptoxR && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::test(filter='eco_lifestage_gate')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test-eco_lifestage_gate.R contains `test_that("NVS failure emits warning and returns empty tibble"`
    - tests/testthat/test-eco_lifestage_gate.R contains `expect_warning` wrapping `with_mocked_bindings` with `.eco_lifestage_nvs_index` mock
    - tests/testthat/test-eco_lifestage_gate.R contains `expect_equal(nrow(result), 0L)` in the NVS failure test
    - `devtools::test(filter='eco_lifestage_gate')` reports 9 passing tests (was 8, now +1 new)
    - `air format tests/testthat/test-eco_lifestage_gate.R` produces no changes
  </acceptance_criteria>
  <done>New PROV-02 test passes: NVS failure emits cli_warn, returns empty tibble, no abort. Total test count is 9 (8 existing + 1 new).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R process -> OLS4 REST API | Outbound HTTP GET to ebi.ac.uk (public, no auth) |
| R process -> NVS SPARQL endpoint | Outbound HTTP POST to vocab.nerc.ac.uk (public, no auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Denial of Service | .eco_lifestage_nvs_index | mitigate | tryCatch wrapper prevents external endpoint unavailability from crashing the entire resolve pipeline; cli_warn emitted, empty tibble returned |
| T-35-02 | Denial of Service | .eco_lifestage_query_ols4 | mitigate | tryCatch wrapper prevents OLS4 unavailability from crashing pipeline; cli_warn emitted, empty tibble returned |
| T-35-03 | Information Disclosure | OLS4 search results | mitigate | obo_id prefix post-filter strips cross-ontology results (GO: terms) that could produce incorrect ontology assignments in the dictionary |
| T-35-04 | Tampering | NVS session cache | accept | Early return before cache assignment prevents caching empty/poisoned NVS index. Existing code clears cache on `refresh=TRUE`. Risk is low: NVS is read-only public endpoint. |
</threat_model>

<verification>
1. `devtools::test(filter='eco_lifestage_gate')` passes with 9 tests (8 existing + 1 new PROV-02)
2. `grep -n "cli_abort.*NVS" R/eco_lifestage_patch.R` returns zero matches (old abort removed)
3. `grep -n "tryCatch" R/eco_lifestage_patch.R` returns at least 2 matches (NVS + OLS4 wrappers)
4. `grep -n "startsWith.*source_term_id.*toupper" R/eco_lifestage_patch.R` returns 1 match (prefix filter)
5. `devtools::load_all()` completes without errors or warnings attributable to eco_lifestage_patch.R
</verification>

<success_criteria>
- NVS HTTP failure produces cli_warn + empty tibble (not cli_abort)
- OLS4 HTTP failure produces cli_warn + empty tibble (not crash)
- OLS4 results contain only UBERON: or PO: prefixed IDs
- All 9 tests (8 existing + 1 new) pass
- No regressions in package load
</success_criteria>

<output>
After completion, create `.planning/phases/35-shared-helper-layer-validation/35-01-SUMMARY.md`
</output>
