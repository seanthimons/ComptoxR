---
phase: 36-bootstrap-data-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/extdata/ecotox/lifestage_baseline.csv
  - inst/extdata/ecotox/lifestage_derivation.csv
  - tests/testthat/test-eco_lifestage_data.R
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "lifestage_baseline.csv has 139 rows and 13 columns matching the cache schema"
    - "No row in lifestage_baseline.csv has source_term_id == 'GO:0040007'"
    - "lifestage_derivation.csv has 53 rows (47 existing + 6 new) and 5 columns"
    - "Every resolved baseline key (source_ontology, source_term_id) has a matching derivation row"
    - "devtools::test(filter='eco_lifestage_data') passes with 0 failures"
  artifacts:
    - path: "inst/extdata/ecotox/lifestage_baseline.csv"
      provides: "Complete ECOTOX lifestage baseline with GO:0040007 contamination removed"
      contains: "139 data rows, 13 columns, no GO:0040007 source_term_id"
    - path: "inst/extdata/ecotox/lifestage_derivation.csv"
      provides: "Complete derivation map with all resolved baseline keys covered"
      contains: "53 data rows, 5 columns including S1116, S1122, S1106, S1128, PO:0000055, PO:0009010"
    - path: "tests/testthat/test-eco_lifestage_data.R"
      provides: "CI-enforced cross-check gate for CSV artifact integrity"
      contains: "test_that"
  key_links:
    - from: "tests/testthat/test-eco_lifestage_data.R"
      to: "inst/extdata/ecotox/lifestage_baseline.csv"
      via: "system.file + readr::read_csv"
      pattern: "system.file.*lifestage_baseline"
    - from: "tests/testthat/test-eco_lifestage_data.R"
      to: "inst/extdata/ecotox/lifestage_derivation.csv"
      via: "system.file + readr::read_csv"
      pattern: "system.file.*lifestage_derivation"
---

<objective>
Fix cross-check gaps in both committed CSV artifacts and create a permanent CI-enforced testthat gate that prevents future regressions.

Purpose: The baseline has 3 rows contaminated with GO:0040007 cross-ontology IDs (D-03) and the derivation map is missing 6 keys that resolved baseline rows reference (D-01). Until both are fixed and gated, the cross-check invariant (DATA-03) is violated and downstream phases cannot trust the data.

Output: Corrected lifestage_baseline.csv (GO:0040007 rows re-resolved), expanded lifestage_derivation.csv (6 new curator rows), and a testthat test that CI enforces forever.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-bootstrap-data-artifacts/36-CONTEXT.md
@.planning/phases/36-bootstrap-data-artifacts/36-RESEARCH.md
@.planning/phases/36-bootstrap-data-artifacts/36-PATTERNS.md

<interfaces>
<!-- Key functions and schemas the executor needs from eco_lifestage_patch.R -->

From R/eco_lifestage_patch.R (line 8):
```r
.eco_lifestage_cache_schema <- function() {
  tibble::tibble(
    org_lifestage = character(),
    source_provider = character(),
    source_ontology = character(),
    source_term_id = character(),
    source_term_label = character(),
    source_term_definition = character(),
    source_release = character(),
    source_match_method = character(),
    source_match_status = character(),
    candidate_rank = integer(),
    candidate_score = double(),
    candidate_reason = character(),
    ecotox_release = character()
  )
}
```

From R/eco_lifestage_patch.R (line 92):
```r
.eco_lifestage_baseline_path <- function()
# Returns path to inst/extdata/ecotox/lifestage_baseline.csv
# Falls back to file.path("inst", "extdata", "ecotox", "lifestage_baseline.csv") in dev context
```

From R/eco_lifestage_patch.R (line 112):
```r
.eco_lifestage_derivation_path <- function()
# Returns path to inst/extdata/ecotox/lifestage_derivation.csv
```

From R/eco_lifestage_patch.R:
```r
.eco_lifestage_resolve_term <- function(term, ecotox_release)
# Orchestrates OLS4 + NVS + ranking; returns a 13-column tibble row
# Handles prefix filtering (UBERON:/PO:) and NVS resilience
```

Derivation CSV schema (5 columns):
```
source_ontology, source_term_id, harmonized_life_stage, reproductive_stage, derivation_source
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CSV data artifacts — re-resolve GO:0040007 rows and hand-author 6 derivation rows</name>
  <files>inst/extdata/ecotox/lifestage_baseline.csv, inst/extdata/ecotox/lifestage_derivation.csv</files>
  <read_first>
    - inst/extdata/ecotox/lifestage_baseline.csv (current 139-row baseline — identify the 3 GO:0040007 rows)
    - inst/extdata/ecotox/lifestage_derivation.csv (current 47-row derivation — verify existing keys before appending)
    - R/eco_lifestage_patch.R (lines 1-30 for cache schema; lines 85-130 for path helpers; .eco_lifestage_resolve_term for re-resolution)
    - .planning/phases/36-bootstrap-data-artifacts/36-PATTERNS.md (write patterns, pitfall 3 and 4 warnings)
  </read_first>
  <action>
**Step A — Re-resolve 3 GO:0040007 baseline rows (per D-03, D-04, D-05):**

Write a temporary R script to `$TEMP/fix_baseline.R` that:
1. Calls `suppressPackageStartupMessages(devtools::load_all(".", quiet = TRUE))`
2. Reads baseline via `readr::read_csv(.eco_lifestage_baseline_path(), show_col_types = FALSE)`
3. Filters the 3 contaminated rows by `source_term_id == "GO:0040007"` (these are: "Exponential growth phase (log)", "Lag growth phase", "Stationary growth phase") — do NOT filter by description text, filter by the term ID
4. Re-resolves each term's `org_lifestage` value via `purrr::map_dfr(go_terms$org_lifestage, .eco_lifestage_resolve_term, ecotox_release = ecotox_release)` where `ecotox_release` is extracted from the baseline as `unique(stats::na.omit(baseline$ecotox_release))`
5. Replaces the 3 rows in the full baseline using `dplyr::rows_update(baseline, re_resolved, by = "org_lifestage", unmatched = "ignore")`
6. Writes the entire updated baseline with `utils::write.csv(baseline_updated, baseline_path, row.names = FALSE, na = "")`
7. Prints the 3 replaced rows to confirm `source_match_status` is `"unresolved"` (expected per D-04) and `source_term_id` is no longer `"GO:0040007"`

**IMPORTANT:** If the re-resolution returns `source_match_status == "resolved"` for any of the 3 terms (unexpected but possible), note the new `source_term_id` — a 7th derivation row may be needed per D-01.

Run the script with `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" $TEMP/fix_baseline.R`.

**Step B — Hand-author 6 derivation rows (per D-01):**

Read the current `inst/extdata/ecotox/lifestage_derivation.csv`. Append exactly these 6 rows (maintaining alphabetical order by source_ontology then source_term_id):

```
"PO","PO:0000055","Adult",TRUE,"baseline_curated_source_id"
"PO","PO:0009010","Egg/Embryo",FALSE,"baseline_curated_source_id"
"S11","S1106","Egg/Embryo",FALSE,"baseline_curated_source_id"
"S11","S1116","Adult",FALSE,"baseline_curated_source_id"
"S11","S1122","Egg/Embryo",FALSE,"baseline_curated_source_id"
"S11","S1128","Larva",FALSE,"baseline_curated_source_id"
```

Write the updated derivation CSV using `utils::write.csv(derivation, path, row.names = FALSE, na = "")` to ensure consistent quoting. The final file should have 53 data rows (47 + 6). Rows MUST be sorted by `source_ontology` then `source_term_id` (the existing file is already sorted this way — insert the 6 new rows at the correct sorted positions: PO rows after existing PO block, S11 rows interspersed in existing S11 block by term ID).

**Curator sign-off note for PO:0000055 and PO:0009010 (from CONTEXT.md specifics):**
- PO:0000055 (bud/inflorescence) mapped to Adult with reproductive_stage=TRUE (plant bud is a reproductive structure)
- PO:0009010 (seed) mapped to Egg/Embryo with reproductive_stage=FALSE (seed is a propagule/embryonic stage)
These are provisional mappings per CONTEXT.md. The checkpoint in Plan 02 will allow curator review.
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/ComptoxR && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "b <- readr::read_csv('inst/extdata/ecotox/lifestage_baseline.csv', show_col_types=FALSE); cat('baseline rows:', nrow(b), '\n'); cat('GO:0040007 rows:', sum(b$source_term_id == 'GO:0040007', na.rm=TRUE), '\n'); d <- readr::read_csv('inst/extdata/ecotox/lifestage_derivation.csv', show_col_types=FALSE); cat('derivation rows:', nrow(d), '\n')"</automated>
  </verify>
  <acceptance_criteria>
    - lifestage_baseline.csv has exactly 139 data rows (unchanged count)
    - lifestage_baseline.csv contains 0 rows where source_term_id == "GO:0040007"
    - lifestage_baseline.csv has 13 columns matching cache schema names
    - lifestage_derivation.csv has exactly 53 data rows (47 original + 6 new)
    - lifestage_derivation.csv contains rows with source_term_id values: S1106, S1116, S1122, S1128, PO:0000055, PO:0009010
    - All 6 new derivation rows have derivation_source == "baseline_curated_source_id"
    - The derivation CSV is sorted by source_ontology then source_term_id
  </acceptance_criteria>
  <done>Both CSVs are updated: baseline has GO:0040007 contamination removed (3 rows re-resolved to unresolved), derivation has 6 new curator-authored rows closing all cross-check gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Create permanent testthat cross-check gate (CI-enforced)</name>
  <files>tests/testthat/test-eco_lifestage_data.R</files>
  <read_first>
    - inst/extdata/ecotox/lifestage_baseline.csv (verify column names to hardcode in test)
    - inst/extdata/ecotox/lifestage_derivation.csv (verify column names to hardcode in test)
    - tests/testthat/test-eco_lifestage_gate.R (lines 1-5 for file header pattern; lines 260-306 for test_that style)
    - .planning/phases/36-bootstrap-data-artifacts/36-PATTERNS.md (Pattern 1: anti-join, Pattern 2: schema columns, Pitfall 5: no schema function calls, Pitfall 6: distinct keys)
  </read_first>
  <action>
Create `tests/testthat/test-eco_lifestage_data.R` with the following structure per D-09, D-10:

**File header** (match test-eco_lifestage_gate.R style):
```r
# Tests for committed ECOTOX lifestage CSV artifact integrity
# -----------------------------------------------------------
```

**Test 1 — Baseline schema (D-10 assertion 3, DATA-01):**
- Read baseline via `system.file("extdata", "ecotox", "lifestage_baseline.csv", package = "ComptoxR")`
- Load with `readr::read_csv(path, show_col_types = FALSE)`
- Hardcode the 13 expected column names (do NOT call `.eco_lifestage_cache_schema()` — Pitfall 5):
  `c("org_lifestage", "source_provider", "source_ontology", "source_term_id", "source_term_label", "source_term_definition", "source_release", "source_match_method", "source_match_status", "candidate_rank", "candidate_score", "candidate_reason", "ecotox_release")`
- `expect_equal(sort(names(df)), sort(expected_cols))`

**Test 2 — Derivation schema (D-10 assertion 2, DATA-02):**
- Read derivation via `system.file("extdata", "ecotox", "lifestage_derivation.csv", package = "ComptoxR")`
- Load with `readr::read_csv(path, show_col_types = FALSE)`
- Hardcode the 5 expected column names:
  `c("source_ontology", "source_term_id", "harmonized_life_stage", "reproductive_stage", "derivation_source")`
- `expect_equal(sort(names(df)), sort(expected_cols))`

**Test 3 — Cross-check gate (D-10 assertion 1, DATA-03):**
- Read both CSVs as above (can share read code at top of describe block or re-read)
- Filter baseline: `resolved <- dplyr::filter(baseline, source_match_status == "resolved")`
- Deduplicate keys: `resolved_keys <- dplyr::distinct(resolved, source_ontology, source_term_id)` (Pitfall 6 — multiple org_lifestage values share one key)
- Anti-join: `gaps <- dplyr::anti_join(resolved_keys, derivation, by = c("source_ontology", "source_term_id"))`
- Assert: `expect_equal(nrow(gaps), 0L, label = paste0(nrow(gaps), " resolved baseline key(s) have no derivation partner: ", paste(unique(paste0(gaps$source_ontology, ":", gaps$source_term_id)), collapse = ", ")))`

**Test 4 — Baseline has non-zero rows (DATA-01 basic integrity):**
- `expect_gt(nrow(baseline), 0L)`

**Test 5 — Derivation has non-zero rows (DATA-02 basic integrity):**
- `expect_gt(nrow(derivation), 0L)`

**No `skip_on_cran()` needed** — these are pure file reads, fully CI-safe. No network or DB calls.

After creating the file, run `air format tests/testthat/test-eco_lifestage_data.R` and `jarl check tests/testthat/test-eco_lifestage_data.R` per project conventions.
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/ComptoxR && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::test(filter='eco_lifestage_data')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test-eco_lifestage_data.R exists
    - File contains exactly 3 test_that blocks minimum (schema baseline, schema derivation, cross-check anti-join)
    - File does NOT contain ".eco_lifestage_cache_schema" (Pitfall 5 — no internal function calls)
    - File contains the string "source_match_status == \"resolved\"" (cross-check filter)
    - File contains "dplyr::anti_join" (cross-check mechanism)
    - File contains "dplyr::distinct" (Pitfall 6 — dedup keys before anti-join)
    - devtools::test(filter='eco_lifestage_data') exits with 0 failures and 0 errors
    - air format and jarl check produce no issues
  </acceptance_criteria>
  <done>Permanent CI gate at tests/testthat/test-eco_lifestage_data.R passes all assertions: baseline has 13 columns, derivation has 5 columns, every resolved baseline key has a derivation partner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This plan modifies committed CSV data files and creates a test file. No network endpoints, no user input handling, no auth changes, no runtime code modifications.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | lifestage_derivation.csv | accept | Hand-authored rows follow existing pattern. File is committed to git with full version history. CI gate (test-eco_lifestage_data.R) detects schema violations on every push. |
| T-36-02 | Information Disclosure | lifestage_baseline.csv | accept | Contains only public ECOTOX lifestage descriptions and ontology IDs. No PII, no secrets, no API keys. |
</threat_model>

<verification>
1. `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::test(filter='eco_lifestage_data')"` — all tests pass (0 failures)
2. lifestage_baseline.csv has 139 rows, 13 columns, zero GO:0040007 entries
3. lifestage_derivation.csv has 53 rows, 5 columns, all 6 new keys present
4. `devtools::test()` — full test suite still green
</verification>

<success_criteria>
- Cross-check anti-join between resolved baseline keys and derivation returns 0 gaps
- CI gate runs without DB or network dependencies (pure file read)
- GO:0040007 contamination is eliminated from baseline
- All 6 missing derivation keys (S1116, S1122, S1106, S1128, PO:0000055, PO:0009010) have rows in derivation CSV
</success_criteria>

<output>
After completion, create `.planning/phases/36-bootstrap-data-artifacts/36-01-SUMMARY.md`
</output>
