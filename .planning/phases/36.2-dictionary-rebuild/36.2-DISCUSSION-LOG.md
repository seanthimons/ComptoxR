# Phase 36.2: Dictionary Rebuild - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-24
**Phase:** 36.2-dictionary-rebuild
**Areas discussed:** Adapter scope, Resolution strategy, Derivation authoring, Pipeline wiring

---

## Adapter Scope

### Probe-first vs commit upfront

| Option | Description | Selected |
|--------|-------------|----------|
| Run probes first (Recommended) | Execute all 4 probe scripts, review hit rates, then decide which become permanent adapters | |
| Commit all 4 upfront | Wire all 4 sources without waiting for probe results | ✓ |
| OLS4-broader + BioPortal only | Skip Wikidata and AGROVOC as permanent adapters | |

**User's choice:** Commit all 4 upfront
**Notes:** None

### Alias normalization layer location

| Option | Description | Selected |
|--------|-------------|----------|
| Inside resolve pipeline (Recommended) | Normalization step at top of .eco_lifestage_resolve_term() | ✓ |
| Separate pre-processing pass | Standalone function before resolve pipeline | |
| Embedded in each adapter | Each adapter does its own synonym expansion via API-native search | |

**User's choice:** Inside resolve pipeline (Recommended)
**Notes:** None

### Alias data source

| Option | Description | Selected |
|--------|-------------|----------|
| inst/extdata/ecotox/ CSV (Recommended) | Committed alongside baseline/derivation/audit, editable by users via system.file() | ✓ |
| User-writable copy in R_user_dir | Ship default in inst/extdata/, copy to R_user_dir on first use | |

**User's choice:** inst/extdata/ecotox/ CSV (Recommended)
**Notes:** User clarified: "The solution must be accessible to a non dev environment: a user rebuilding or patching the database must have the ability to edit the file + use it to patch the database"

---

## Resolution Strategy

### Re-resolution scope

| Option | Description | Selected |
|--------|-------------|----------|
| All 139 terms (Recommended) | Re-resolve everything from scratch through expanded pipeline | ✓ |
| Only 69 unresolved | Keep existing 70 resolved rows, only run 69 unresolved through new pipeline | |

**User's choice:** All 139 terms (Recommended)
**Notes:** User specified in initial notes: "rerun the full baseline against all 139 distinct ECOTOX terms, not the targeted 3-row patch model from Phase 36"

### Coverage target

| Option | Description | Selected |
|--------|-------------|----------|
| 80%+ resolved | At least 112/139 terms resolve | |
| 90%+ resolved | At least 125/139 terms resolve | |
| No numeric target | Ship when zero alias_synonym unresolved + all have reason codes | |

**User's choice:** 100% coverage or resolution
**Notes:** User provided free text: "100% coverage or resolution." Every term must resolve or have a derivation row with explicit reason.

### Handling unresolvable terms

| Option | Description | Selected |
|--------|-------------|----------|
| Derivation row required (Recommended) | Every term gets a derivation row, even out_of_scope terms | ✓ |
| Reason code sufficient | Out_of_scope terms don't need derivation rows | |
| Exclude from baseline | Remove out_of_scope terms from baseline entirely | |

**User's choice:** Derivation row required (Recommended)
**Notes:** None

---

## Derivation Authoring

### Derivation workflow model

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-derive obvious, review rest | High-confidence auto-generates, lower goes to proposals CSV | |
| All to proposals first | Strict Phase 36 pattern: everything to proposals CSV | |
| Auto-derive all, curator audits after | Generate all derivation rows automatically, curator reviews the whole file | ✓ |

**User's choice:** Auto-derive all, curator audits after
**Notes:** Supersedes Phase 36 D-02/D-14 curator-only model for this batch rebuild

### Harmonized category assignment method

| Option | Description | Selected |
|--------|-------------|----------|
| Ontology-driven mapping (Recommended) | Use resolved term's parent/ancestor in UBERON/PO hierarchy to determine category | ✓ |
| Term-text heuristic | Match resolved term label against 7 categories | |
| You decide | Claude picks best approach during implementation | |

**User's choice:** Ontology-driven mapping (Recommended)
**Notes:** None

---

## Pipeline Wiring

### Permanence of new adapters

| Option | Description | Selected |
|--------|-------------|----------|
| Permanent pipeline (Recommended) | Wire all 4 into .eco_lifestage_resolve_term() for future releases | ✓ |
| Dev-script only | Keep in dev/lifestage/ scripts, baseline CSV is source of truth | |
| Configurable per-run | Wire in but make sources toggleable via options parameter | |

**User's choice:** Permanent pipeline (Recommended)
**Notes:** None

### Query order

| Option | Description | Selected |
|--------|-------------|----------|
| Alias → OLS4 → BioPortal → NVS → Wikidata → AGROVOC | Broadest sources first, specialist sources last | ✓ |
| Alias → OLS4 → NVS → BioPortal → Wikidata → AGROVOC | Keep existing OLS4 → NVS order, append new sources | |
| You decide | Claude determines based on probe hit rates | |

**User's choice:** Alias → OLS4 → BioPortal → NVS → Wikidata → AGROVOC
**Notes:** None

---

## Claude's Discretion

- Exact alias CSV columns beyond org_lifestage and normalized_query
- Ontology-to-category mapping table structure and location
- Adapter function naming conventions
- Whether OLS4-broader replaces or supplements existing .eco_lifestage_query_ols4()
- Validation script structure

## Deferred Ideas

- Phase 37: Rebuild path UX (cli_alert_info for quarantined rows)
- Future: SPARQL-based OLS4 ancestor traversal
- Future: GO provider for microbial growth phases
- Future: Configurable provider enable/disable per run
- Future: ECOX-01 automated ontology version tracking
