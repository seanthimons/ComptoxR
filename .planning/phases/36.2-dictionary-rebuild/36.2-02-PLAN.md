---
phase: 36.2-dictionary-rebuild
plan: 02
type: execute
wave: 2
depends_on: [36.2-01]
files_modified:
  - R/eco_lifestage_patch.R
autonomous: true
requirements:
  - D-01
  - D-02
  - D-13
  - D-14
must_haves:
  truths:
    - "OLS4 adapter searches broadly across all ontologies with queryFields and post-filters by 8 relevant prefixes"
    - "BioPortal adapter gracefully degrades when BIOPORTAL_API_KEY is not set"
    - "Wikidata adapter uses dual SPARQL pattern (P1554 + P3841) with User-Agent header"
    - "AGROVOC adapter uses two-step search-then-detail pattern"
    - "All 4 adapters return the same 8-column tibble schema or empty tibble on failure"
    - "All adapters include rate limiting via Sys.sleep()"
  artifacts:
    - path: "R/eco_lifestage_patch.R"
      provides: "Modified .eco_lifestage_query_ols4(), new .eco_lifestage_query_bioportal(), .eco_lifestage_query_wikidata(), .eco_lifestage_query_agrovoc()"
  key_links:
    - from: ".eco_lifestage_query_ols4()"
      to: "https://www.ebi.ac.uk/ols4/api/search"
      via: "httr2 GET with queryFields=label,synonym,description"
      pattern: "queryFields.*label,synonym,description"
    - from: ".eco_lifestage_query_bioportal()"
      to: "https://data.bioontology.org/annotator"
      via: "httr2 GET with apikey"
      pattern: "data\\.bioontology\\.org/annotator"
    - from: ".eco_lifestage_query_wikidata()"
      to: "https://query.wikidata.org/sparql"
      via: "httr2 GET with SPARQL query"
      pattern: "query\\.wikidata\\.org/sparql"
    - from: ".eco_lifestage_query_agrovoc()"
      to: "https://agrovoc.fao.org/browse/rest/v1/"
      via: "httr2 GET two-step search+detail"
      pattern: "agrovoc\\.fao\\.org"
---

<objective>
Implement the 4 provider adapters in the permanent pipeline: broaden the existing OLS4 adapter, and create new BioPortal, Wikidata, and AGROVOC adapters. All 4 follow the established tryCatch + cli_warn + empty tibble resilience pattern from Phase 35.

Purpose: Per D-01, all 4 probed sources become permanent adapters. Per D-02, they will be wired in order: OLS4 -> BioPortal -> NVS -> Wikidata -> AGROVOC (wiring happens in Plan 03). Per D-13, each adapter follows the tryCatch + cli_warn pattern. Per D-14, rate limiting is carried forward from probe scripts.

Output: 4 working adapter functions in R/eco_lifestage_patch.R, each returning the standard 8-column candidate tibble.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36.2-dictionary-rebuild/36.2-CONTEXT.md
@.planning/phases/36.2-dictionary-rebuild/36.2-RESEARCH.md
@.planning/phases/36.2-dictionary-rebuild/36.2-PATTERNS.md
@.planning/phases/36.2-dictionary-rebuild/36.2-01-SUMMARY.md

<interfaces>
<!-- Adapter return schema (8 columns) -- from eco_lifestage_patch.R lines 572-604 -->
```r
tibble::tibble(
  source_provider   = character(),   # "OLS4", "BioPortal", "Wikidata", "AGROVOC"
  source_ontology   = character(),   # "UBERON", "ZFA", "AGROVOC", etc.
  source_term_id    = character(),   # "UBERON:0000066"
  source_term_label = character(),   # preferred label
  source_term_definition = character(),
  candidate_aliases = character(),   # pipe-separated: "a | b | c"
  source_release    = character(),   # "current"
  source_match_method = character()  # "ols4_search", "bioportal_annotator", etc.
)
```

<!-- Current OLS4 adapter signature (to be replaced) -->
```r
.eco_lifestage_query_ols4 <- function(term, ontology = c("UBERON", "PO")) {
  ontology <- rlang::arg_match(ontology)
  # ... httr2 request with ontology restriction ...
  # ... post-filter: startsWith(source_term_id, paste0(toupper(ontology), ":"))
}
```

<!-- Current NVS adapter (unchanged, reference only) -->
```r
.eco_lifestage_query_nvs <- function(term) {
  # Returns same 8-column tibble shape from cached index
}
```

<!-- Helper functions used in OLS4 adapter -->
```r
.eco_lifestage_json_col(docs, "obo_id")       # safe column extractor
.eco_lifestage_json_list_col(docs, "description")  # list column extractor
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broaden OLS4 adapter and create BioPortal adapter</name>
  <files>R/eco_lifestage_patch.R</files>
  <read_first>
    - R/eco_lifestage_patch.R
    - dev/lifestage/probe_ols4.R
    - dev/lifestage/probe_bioportal.R
  </read_first>
  <action>
**Modify `.eco_lifestage_query_ols4()` (currently at line 538):**

Replace the existing function with a broader version. Key changes:
1. Remove `ontology = c("UBERON", "PO")` parameter and `rlang::arg_match(ontology)` call
2. Accept `term` as the only parameter
3. Add `queryFields = "label,synonym,description"` to the httr2 request
4. Remove `ontology = tolower(ontology)` from the request query params
5. Define `RELEVANT_PREFIXES` vector inside the function (or as a module-level constant above the function):
   ```r
   RELEVANT_PREFIXES <- c("UBERON:", "PO:", "ECOCORE:", "BTO:", "EFO:", "ZFA:", "FBdv:", "MeSH:")
   ```
6. Replace the final `dplyr::filter(startsWith(.data$source_term_id, paste0(toupper(ontology), ":")))` with a prefix post-filter using `purrr::map_lgl()`:
   ```r
   dplyr::filter(
     !is.na(.data$source_term_id),
     !is.na(.data$source_term_label),
     purrr::map_lgl(.data$source_term_id, function(id) {
       any(startsWith(id, RELEVANT_PREFIXES))
     })
   )
   ```
7. Set `source_ontology` dynamically by extracting the prefix from `obo_id`:
   ```r
   source_ontology = sub(":.*", "", .eco_lifestage_json_col(docs, "obo_id"))
   ```
   (Instead of the current hardcoded `toupper(ontology)`)
8. Keep existing tryCatch + cli_warn pattern and response parsing
9. Add `Sys.sleep(0.3)` at end of function, before return
10. Keep `source_match_method = "ols4_search"`
11. Keep `source_release = "current"`

**Create `.eco_lifestage_query_bioportal(term)` (insert after OLS4 adapter):**

New function following the tryCatch + empty tibble pattern. Reference `dev/lifestage/probe_bioportal.R` lines 49-132 for the exact API pattern.

```r
#' @keywords internal
.eco_lifestage_query_bioportal <- function(term) {
  bioportal_key <- Sys.getenv("BIOPORTAL_API_KEY")
  if (!nzchar(bioportal_key)) {
    cli::cli_warn(c(
      "BIOPORTAL_API_KEY is not set.",
      "i" = "BioPortal candidates will be skipped.",
      "i" = "Obtain a free key at {.url https://bioportal.bioontology.org}"
    ))
    return(tibble::tibble())
  }

  response <- tryCatch(
    {
      httr2::request("https://data.bioontology.org/annotator") |>
        httr2::req_url_query(
          text = term,
          ontologies = "UBERON,ZFA,FBdv,ECOCORE,BTO,EFO,MeSH",
          apikey = bioportal_key
        ) |>
        httr2::req_perform() |>
        httr2::resp_body_json()
    },
    error = function(e) {
      cli::cli_warn(c(
        "BioPortal endpoint unreachable for {.val {term}}.",
        "i" = "BioPortal candidates will be skipped for this resolution run.",
        "x" = conditionMessage(e)
      ))
      NULL
    }
  )

  if (is.null(response) || length(response) == 0) {
    Sys.sleep(0.5)
    return(tibble::tibble())
  }

  # Parse annotations into candidate tibble
  rows <- purrr::compact(purrr::map(response, function(annotation) {
    tryCatch({
      ac <- annotation$annotatedClass
      if (is.null(ac)) return(NULL)

      class_id_raw <- ac$`@id`
      if (is.null(class_id_raw)) return(NULL)

      # Convert URL to OBO format: http://purl.obolibrary.org/obo/UBERON_0000066 -> UBERON:0000066
      obo_id <- if (grepl("obo/", class_id_raw)) {
        id_part <- sub(".*obo/", "", class_id_raw)
        sub("_", ":", id_part)
      } else {
        class_id_raw
      }

      ontology_acronym <- if (!is.null(ac$links$ontology)) {
        sub(".*ontologies/([^/]+).*", "\\1", ac$links$ontology)
      } else {
        sub(":.*", "", obo_id)
      }

      label <- if (!is.null(ac$prefLabel)) ac$prefLabel else NA_character_

      list(
        source_provider = "BioPortal",
        source_ontology = ontology_acronym,
        source_term_id = obo_id,
        source_term_label = label,
        source_term_definition = NA_character_,
        candidate_aliases = NA_character_,
        source_release = "current",
        source_match_method = "bioportal_annotator"
      )
    }, error = function(e) NULL)
  }))

  Sys.sleep(0.5)

  if (length(rows) == 0) {
    return(tibble::tibble())
  }

  dplyr::bind_rows(purrr::map(rows, tibble::as_tibble))
}
```

Both functions must have `#' @keywords internal` roxygen tags.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all('.', quiet=TRUE); cat('OLS4 exists:', exists('.eco_lifestage_query_ols4', envir=asNamespace('ComptoxR')), '\n'); cat('BioPortal exists:', exists('.eco_lifestage_query_bioportal', envir=asNamespace('ComptoxR')), '\n'); f <- formals(ComptoxR:::.eco_lifestage_query_ols4); cat('OLS4 params:', paste(names(f), collapse=', '), '\n'); stopifnot(length(f) == 1, names(f) == 'term'); f2 <- formals(ComptoxR:::.eco_lifestage_query_bioportal); cat('BioPortal params:', paste(names(f2), collapse=', '), '\n'); stopifnot(length(f2) == 1, names(f2) == 'term')"</automated>
  </verify>
  <acceptance_criteria>
    - `devtools::load_all()` completes without errors
    - `.eco_lifestage_query_ols4` has exactly 1 parameter: `term` (no `ontology` parameter)
    - `.eco_lifestage_query_bioportal` has exactly 1 parameter: `term`
    - OLS4 adapter request URL contains `queryFields = "label,synonym,description"`
    - OLS4 adapter post-filters using `RELEVANT_PREFIXES` vector containing `"UBERON:", "PO:", "ECOCORE:", "BTO:", "EFO:", "ZFA:", "FBdv:", "MeSH:"`
    - OLS4 adapter sets `source_ontology` dynamically from `obo_id` prefix, not hardcoded
    - BioPortal adapter checks `Sys.getenv("BIOPORTAL_API_KEY")` and returns empty tibble with `cli_warn` if unset
    - BioPortal adapter targets ontologies string: `"UBERON,ZFA,FBdv,ECOCORE,BTO,EFO,MeSH"`
    - Both adapters include `Sys.sleep()` rate limiting (OLS4: 0.3, BioPortal: 0.5)
    - Both adapters use `tryCatch` with `cli::cli_warn` on error
    - Both adapters have `#' @keywords internal` roxygen tags
    - `air format R/eco_lifestage_patch.R` passes
  </acceptance_criteria>
  <done>OLS4 adapter broadened with queryFields and prefix post-filter; BioPortal adapter created with API key guard and annotator pattern</done>
</task>

<task type="auto">
  <name>Task 2: Create Wikidata SPARQL and AGROVOC REST adapters</name>
  <files>R/eco_lifestage_patch.R</files>
  <read_first>
    - R/eco_lifestage_patch.R
    - dev/lifestage/probe_wikidata.R
    - dev/lifestage/probe_agrovoc.R
  </read_first>
  <action>
**Create `.eco_lifestage_query_wikidata(term)` (insert after BioPortal adapter):**

Reference `dev/lifestage/probe_wikidata.R` for exact SPARQL patterns (lines 23-45 for queries, lines 49-71 for request function, lines 75-113 for binding extraction).

```r
#' @keywords internal
.eco_lifestage_query_wikidata <- function(term) {
  # Dual SPARQL query: P1554 (UBERON cross-ref) + P3841 (ADW ID)
  uberon_sparql <- glue::glue(
    'SELECT ?item ?itemLabel ?uberonId WHERE {{
      ?item wdt:P1554 ?uberonId .
      ?item rdfs:label ?itemLabel .
      FILTER(LANG(?itemLabel) = "en")
      FILTER(CONTAINS(LCASE(?itemLabel), "{tolower(term)}"))
    }}
    LIMIT 10'
  )

  adw_sparql <- glue::glue(
    'SELECT ?item ?itemLabel ?adwId WHERE {{
      ?item wdt:P3841 ?adwId .
      ?item rdfs:label ?itemLabel .
      FILTER(LANG(?itemLabel) = "en")
      FILTER(CONTAINS(LCASE(?itemLabel), "{tolower(term)}"))
    }}
    LIMIT 10'
  )

  query_wikidata <- function(sparql) {
    tryCatch(
      {
        httr2::request("https://query.wikidata.org/sparql") |>
          httr2::req_url_query(query = sparql) |>
          httr2::req_headers(
            Accept = "application/sparql-results+json",
            `User-Agent` = "ComptoxR/dev lifestage resolver (verfassergeist@gmail.com)"
          ) |>
          httr2::req_perform() |>
          httr2::resp_body_string() |>
          jsonlite::fromJSON(simplifyDataFrame = TRUE)
      },
      error = function(e) {
        cli::cli_warn(c(
          "Wikidata SPARQL endpoint unreachable for {.val {term}}.",
          "i" = "Wikidata candidates will be skipped for this resolution run.",
          "x" = conditionMessage(e)
        ))
        NULL
      }
    )
  }

  # Query 1: UBERON cross-references
  uberon_payload <- query_wikidata(uberon_sparql)
  uberon_rows <- tibble::tibble()

  if (!is.null(uberon_payload)) {
    bindings <- uberon_payload$results$bindings
    if (is.data.frame(bindings) && nrow(bindings) > 0 && "uberonId" %in% names(bindings)) {
      uberon_rows <- tibble::tibble(
        source_provider = "Wikidata",
        source_ontology = "UBERON",
        source_term_id = paste0("UBERON:", bindings$uberonId$value),
        source_term_label = bindings$itemLabel$value,
        source_term_definition = NA_character_,
        candidate_aliases = NA_character_,
        source_release = "current",
        source_match_method = "wikidata_sparql_P1554"
      )
    }
  }

  Sys.sleep(1)

  # Query 2: Animal Diversity Web IDs (broader coverage)
  adw_payload <- query_wikidata(adw_sparql)
  adw_rows <- tibble::tibble()

  if (!is.null(adw_payload)) {
    bindings <- adw_payload$results$bindings
    if (is.data.frame(bindings) && nrow(bindings) > 0) {
      adw_rows <- tibble::tibble(
        source_provider = "Wikidata",
        source_ontology = "ADW",
        source_term_id = if ("adwId" %in% names(bindings)) {
          paste0("ADW:", bindings$adwId$value)
        } else {
          NA_character_
        },
        source_term_label = bindings$itemLabel$value,
        source_term_definition = NA_character_,
        candidate_aliases = NA_character_,
        source_release = "current",
        source_match_method = "wikidata_sparql_P3841"
      )
    }
  }

  Sys.sleep(1)

  dplyr::bind_rows(uberon_rows, adw_rows)
}
```

**Create `.eco_lifestage_query_agrovoc(term)` (insert after Wikidata adapter):**

Reference `dev/lifestage/probe_agrovoc.R` for exact two-step pattern (lines 32-119).

```r
#' @keywords internal
.eco_lifestage_query_agrovoc <- function(term) {
  # Step 1: search for term
  search_result <- tryCatch(
    {
      httr2::request("https://agrovoc.fao.org/browse/rest/v1/search/") |>
        httr2::req_url_query(query = term, lang = "en") |>
        httr2::req_perform() |>
        httr2::resp_body_json()
    },
    error = function(e) {
      cli::cli_warn(c(
        "AGROVOC search endpoint unreachable for {.val {term}}.",
        "i" = "AGROVOC candidates will be skipped for this resolution run.",
        "x" = conditionMessage(e)
      ))
      NULL
    }
  )

  if (is.null(search_result)) {
    Sys.sleep(0.5)
    return(tibble::tibble())
  }

  results_list <- search_result$results
  if (is.null(results_list) || length(results_list) == 0) {
    Sys.sleep(0.5)
    return(tibble::tibble())
  }

  # Take first result
  first_result <- results_list[[1]]
  concept_uri <- first_result$uri
  preferred_label <- if (!is.null(first_result$prefLabel)) first_result$prefLabel else NA_character_

  # Step 2: fetch concept detail for broader terms
  broader_labels <- character(0)
  detail <- tryCatch(
    {
      httr2::request("https://agrovoc.fao.org/browse/rest/v1/data/") |>
        httr2::req_url_query(uri = concept_uri) |>
        httr2::req_perform() |>
        httr2::resp_body_json()
    },
    error = function(e) {
      cli::cli_warn(c(
        "AGROVOC detail endpoint unreachable for {.val {term}}.",
        "x" = conditionMessage(e)
      ))
      NULL
    }
  )

  if (!is.null(detail)) {
    broader_raw <- detail$broader
    if (!is.null(broader_raw) && length(broader_raw) > 0) {
      broader_labels <- vapply(
        broader_raw,
        function(b) if (!is.null(b$prefLabel)) b$prefLabel else NA_character_,
        character(1)
      )
      broader_labels <- broader_labels[!is.na(broader_labels)]
    }
  }

  Sys.sleep(0.5)

  tibble::tibble(
    source_provider = "AGROVOC",
    source_ontology = "AGROVOC",
    source_term_id = concept_uri,
    source_term_label = preferred_label,
    source_term_definition = NA_character_,
    candidate_aliases = if (length(broader_labels) > 0) {
      paste(broader_labels, collapse = " | ")
    } else {
      NA_character_
    },
    source_release = "current",
    source_match_method = "agrovoc_rest_search"
  )
}
```

Both functions must have `#' @keywords internal` roxygen tags.
Both must use `tryCatch` with `cli::cli_warn` on error and return empty `tibble::tibble()` on failure.
Both must include `Sys.sleep()` rate limiting (Wikidata: 1s after each SPARQL call; AGROVOC: 0.5s).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all('.', quiet=TRUE); cat('Wikidata exists:', exists('.eco_lifestage_query_wikidata', envir=asNamespace('ComptoxR')), '\n'); cat('AGROVOC exists:', exists('.eco_lifestage_query_agrovoc', envir=asNamespace('ComptoxR')), '\n'); f1 <- formals(ComptoxR:::.eco_lifestage_query_wikidata); f2 <- formals(ComptoxR:::.eco_lifestage_query_agrovoc); cat('Wikidata params:', paste(names(f1), collapse=', '), '\n'); cat('AGROVOC params:', paste(names(f2), collapse=', '), '\n'); stopifnot(length(f1) == 1, length(f2) == 1, names(f1) == 'term', names(f2) == 'term')"</automated>
  </verify>
  <acceptance_criteria>
    - `devtools::load_all()` completes without errors
    - `.eco_lifestage_query_wikidata` has exactly 1 parameter: `term`
    - `.eco_lifestage_query_agrovoc` has exactly 1 parameter: `term`
    - Wikidata adapter constructs dual SPARQL queries using `glue::glue()` with P1554 and P3841 properties
    - Wikidata adapter sends `User-Agent` header containing "ComptoxR"
    - Wikidata adapter includes `Sys.sleep(1)` after each SPARQL call (2 total)
    - AGROVOC adapter uses two-step search+detail pattern via `agrovoc.fao.org/browse/rest/v1/`
    - AGROVOC adapter includes `Sys.sleep(0.5)`
    - Both adapters use `tryCatch` with `cli::cli_warn` on error
    - Both adapters have `#' @keywords internal` roxygen tags
    - `air format R/eco_lifestage_patch.R` passes
  </acceptance_criteria>
  <done>Wikidata SPARQL and AGROVOC REST adapters created with correct API patterns, rate limiting, and resilience</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R process -> OLS4 API | External HTTP GET, public, no auth |
| R process -> BioPortal API | External HTTP GET, API key via env var |
| R process -> Wikidata SPARQL | External HTTP GET, public, SPARQL injection possible |
| R process -> AGROVOC REST | External HTTP GET, public, no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.2-03 | S (Spoofing) | BioPortal API key | mitigate | Key read from `Sys.getenv("BIOPORTAL_API_KEY")`, never logged or written to output; graceful degradation if missing |
| T-36.2-04 | T (Tampering) | SPARQL injection in Wikidata query | mitigate | `tolower(term)` used in FILTER, term comes from controlled CSV of ECOTOX lifestage descriptions, not user input |
| T-36.2-05 | D (Denial of Service) | Rate limiting | mitigate | Sys.sleep() between calls: OLS4 0.3s, BioPortal 0.5s, Wikidata 1s, AGROVOC 0.5s |
| T-36.2-06 | I (Info Disclosure) | API key in URL | accept | BioPortal uses `apikey` query param per their API design; no alternative auth method |
</threat_model>

<verification>
- `devtools::load_all()` loads without errors
- All 4 adapter functions exist and have correct single-parameter signatures
- No hardcoded `ontology` parameter on OLS4 adapter
- BioPortal gracefully degrades without API key
- Rate limiting present in all adapters
</verification>

<success_criteria>
- All 4 adapter functions load cleanly via devtools::load_all()
- Each adapter follows tryCatch + cli_warn + empty tibble resilience pattern
- Each adapter returns the standard 8-column candidate tibble schema
- Rate limiting present per adapter: OLS4 0.3s, BioPortal 0.5s, Wikidata 1s, AGROVOC 0.5s
</success_criteria>

<output>
After completion, create `.planning/phases/36.2-dictionary-rebuild/36.2-02-SUMMARY.md`
</output>
