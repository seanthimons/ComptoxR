---
phase: 33-build-confirmation
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified: []
autonomous: true
requirements: [VALD-05]
must_haves:
  truths:
    - "devtools::check() with --no-tests --no-examples --no-vignettes returns 0 errors"
    - "Existing warnings (doc line widths) and notes are acceptable and do not block"
  artifacts: []
  key_links: []
---

<objective>
Run scoped `devtools::check()` to confirm the package passes R CMD check with 0 errors after Phase 32 integration. This proves VALD-05 — the lifestage dictionary expansion, classifier, gate, and roxygen changes do not break the package.

Purpose: Final build gate for v2.3 milestone. After this plan, all 3 phase requirements (VALD-03, VALD-04, VALD-05) are satisfied.

Output: devtools::check() output confirming 0 errors. No files created or modified.
</objective>

<execution_context>
@.claude/CLAUDE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-build-confirmation/33-CONTEXT.md
@.planning/phases/33-build-confirmation/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run scoped devtools::check()</name>
  <files></files>
  <read_first>
    - .planning/phases/33-build-confirmation/33-CONTEXT.md (D-08, D-09, D-10 for check configuration)
    - DESCRIPTION (verify package metadata is intact before running check)
  </read_first>
  <action>
Run a scoped `devtools::check()` per D-08 configuration. Write a temp R script and execute it.

The R script must contain exactly:
```r
result <- devtools::check(
  pkg = ".",
  args = c("--no-tests", "--no-examples", "--no-vignettes"),
  error_on = "error"
)
```

**Per D-08:** Use `--no-tests --no-examples --no-vignettes` flags. Do NOT run the full test suite (tests require API keys + live DB).

**Per D-09:** Pass criterion is 0 errors. Pre-existing warnings (6 doc line width warnings) and notes are acceptable. Only errors are blockers.

**Per D-10:** The lifestage gate tests from Plan 01 were already run via `testthat::test_file()` in that plan's verification step. They are NOT re-run here.

**If `devtools::check()` reports errors:**
- Read the error output carefully
- Determine if the error is caused by Phase 32 changes (lifestage dictionary, classifier, gate, roxygen) or pre-existing
- If caused by Phase 32 changes: diagnose and fix the root cause, then re-run the check
- If pre-existing: document the error and note it is not caused by this milestone's changes

**Expected output:** The check should complete with output similar to:
```
-- R CMD check results ---- ComptoxR X.Y.Z ----
Duration: ...
0 errors | N warnings | N notes
```
Where N warnings includes the pre-existing doc line width warnings.
  </action>
  <verify>
    <automated>
      "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::check(pkg='.', args=c('--no-tests','--no-examples','--no-vignettes'), error_on='error')"
    </automated>
  </verify>
  <acceptance_criteria>
    - devtools::check() completes without R-level error (error_on="error" does not trigger)
    - Output contains "0 errors" in the R CMD check results summary line
    - Any warnings present are pre-existing (doc line widths) — not new warnings introduced by v2.3 changes
  </acceptance_criteria>
  <done>devtools::check() returns 0 errors with scoped flags. VALD-05 is satisfied. The package builds, documents, and passes structural checks after the full lifestage harmonization integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable. devtools::check() is a local build tool operating on package source. No network access, no authentication, no user-facing surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | No security-relevant components in this plan |
</threat_model>

<verification>
1. `devtools::check(args=c("--no-tests","--no-examples","--no-vignettes"), error_on="error")` completes without error
2. Output summary shows "0 errors"
</verification>

<success_criteria>
- VALD-05: devtools::check() returns 0 errors after full v2.3 integration
- No new warnings introduced by lifestage harmonization changes
</success_criteria>

<output>
After completion, create `.planning/phases/33-build-confirmation/33-02-SUMMARY.md`
</output>
