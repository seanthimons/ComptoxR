---
phase: 34-teardown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev/lifestage/purge_and_rebuild.R
  - LIFESTAGE_HARMONIZATION_PLAN.md
autonomous: true
requirements:
  - TEAR-01
  - TEAR-02
  - TEAR-03

must_haves:
  truths:
    - "No reference to .classify_lifestage_keywords() exists in R/, inst/, or data-raw/"
    - "No reference to ontology_id exists in R/, inst/, or data-raw/ source code"
    - "ontology_id references in tests/ are exclusively absence assertions (expect_false)"
    - "lifestage_dictionary table in ecotox.duckdb has 13-column v2.4 schema with no ontology_id"
    - "lifestage_review table exists in ecotox.duckdb after rebuild"
    - "LIFESTAGE_HARMONIZATION_PLAN.md (v2.3 plan doc) is deleted"
    - "LIFESTAGE_HARMONIZATION_PLAN2.md (v2.4 plan doc) is preserved"
  artifacts:
    - path: "dev/lifestage/purge_and_rebuild.R"
      provides: "TEAR-03 DB purge and rebuild script"
      contains: ".eco_patch_lifestage"
  key_links:
    - from: "dev/lifestage/purge_and_rebuild.R"
      to: "R/eco_lifestage_patch.R"
      via: "source() + .eco_patch_lifestage(refresh = 'baseline')"
      pattern: "\\.eco_patch_lifestage.*baseline"
    - from: "dev/lifestage/purge_and_rebuild.R"
      to: "R/eco_connection.R"
      via: "source() + eco_path()"
      pattern: "eco_path\\(\\)"
---

<objective>
Remove all v2.3 regex-first lifestage artifacts from the codebase and confirm the DB is in a clean state ready for the v2.4 source-backed pipeline.

Purpose: Phase 34 is the teardown gate — v2.3 remnants must be fully purged before building the v2.4 pipeline on top. This confirms the source tree is clean (TEAR-01, TEAR-02), deletes the superseded plan document (D-02), and rebuilds the DB tables from scratch with the v2.4 schema (TEAR-03).

Output: `dev/lifestage/purge_and_rebuild.R` script, deletion of `LIFESTAGE_HARMONIZATION_PLAN.md`, verified clean source tree, rebuilt DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-teardown/34-CONTEXT.md
@.planning/phases/34-teardown/34-RESEARCH.md
@.planning/phases/34-teardown/34-PATTERNS.md

<interfaces>
<!-- Key functions and patterns the executor needs. Extracted from codebase. -->

From R/eco_lifestage_patch.R (lines 27-43):
```r
.eco_lifestage_dictionary_schema <- function() {
  tibble::tibble(
    org_lifestage = character(),
    source_ontology = character(),
    source_term_id = character(),
    source_term_label = character(),
    source_term_definition = character(),
    source_provider = character(),
    source_match_method = character(),
    source_match_status = character(),
    source_release = character(),
    ecotox_release = character(),
    harmonized_life_stage = character(),
    reproductive_stage = logical(),
    derivation_source = character()
  )
}
```

From R/eco_lifestage_patch.R (lines 825-827):
```r
.eco_patch_lifestage <- function(db_path = eco_path(),
                                 refresh = c("auto", "cache", "baseline", "live"),
                                 force = FALSE)
```

Return value of .eco_patch_lifestage():
```r
invisible(list(
  db_path = db_path,
  ecotox_release = ecotox_release,
  dictionary_rows = nrow(materialized$dictionary),
  review_rows = nrow(materialized$review),
  refresh_mode = materialized$refresh_mode
))
```

From R/eco_connection.R:
```r
eco_path()       # Returns canonical DB path
.eco_close_con() # Releases cached read connection
```

From dev/lifestage/confirm_gate.R (header pattern):
```r
#!/usr/bin/env Rscript
suppressPackageStartupMessages({
  library(DBI)
  library(duckdb)
  library(dplyr)
  library(cli)
})
source("R/eco_connection.R")
source("R/eco_lifestage_patch.R")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create purge-and-rebuild script and verify source tree is clean</name>
  <files>dev/lifestage/purge_and_rebuild.R, LIFESTAGE_HARMONIZATION_PLAN.md</files>

  <read_first>
    dev/lifestage/confirm_gate.R
    R/eco_lifestage_patch.R
    R/eco_connection.R
    LIFESTAGE_HARMONIZATION_PLAN.md
    LIFESTAGE_HARMONIZATION_PLAN2.md
  </read_first>

  <action>
**Step A — Create `dev/lifestage/purge_and_rebuild.R`** (per D-03).

Write the script following the exact header pattern from `dev/lifestage/confirm_gate.R`. The script has three sections:

Section 1 — Drop v2.3 tables:
```r
#!/usr/bin/env Rscript
# TEAR-03: Purge v2.3 lifestage tables and rebuild via .eco_patch_lifestage(baseline)
# Run from: project root directory

suppressPackageStartupMessages({
  library(DBI)
  library(duckdb)
  library(dplyr)
  library(cli)
})

source("R/eco_connection.R")
source("R/eco_lifestage_patch.R")

db_path <- eco_path()

if (!file.exists(db_path)) {
  cli::cli_abort("ECOTOX DuckDB not found at {.path {db_path}}.")
}

cli::cli_h1("TEAR-03: Purge v2.3 Lifestage Tables and Rebuild")
cli::cli_alert_info("Target DB: {.path {db_path}}")

# Step 1: Drop v2.3 tables
.eco_close_con()
con <- DBI::dbConnect(duckdb::duckdb(), dbdir = db_path, read_only = FALSE)

for (tbl in c("lifestage_dictionary", "lifestage_review")) {
  if (DBI::dbExistsTable(con, tbl)) {
    DBI::dbRemoveTable(con, tbl)
    cli::cli_alert_success("Dropped {.code {tbl}}")
  } else {
    cli::cli_alert_info("{.code {tbl}} not present -- skipped")
  }
}

DBI::dbDisconnect(con, shutdown = TRUE)
.eco_close_con()
```

Section 2 — Rebuild via baseline:
```r
# Step 2: Rebuild via baseline
cli::cli_h2("Rebuilding via .eco_patch_lifestage(refresh = 'baseline')")
result <- .eco_patch_lifestage(db_path = db_path, refresh = "baseline")
cli::cli_alert_success("Dictionary rows: {result$dictionary_rows}")
cli::cli_alert_info("Review rows: {result$review_rows}")
cli::cli_alert_info("Refresh mode: {result$refresh_mode}")
```

Section 3 — Schema assertion:
```r
# Step 3: Schema assertion
.eco_close_con()
con2 <- DBI::dbConnect(duckdb::duckdb(), dbdir = db_path, read_only = TRUE)
on.exit(DBI::dbDisconnect(con2, shutdown = TRUE), add = TRUE)

actual_cols <- DBI::dbListFields(con2, "lifestage_dictionary")
expected_cols <- names(.eco_lifestage_dictionary_schema())

if ("ontology_id" %in% actual_cols) {
  cli::cli_abort("FAIL: ontology_id still present in lifestage_dictionary schema")
}
missing <- setdiff(expected_cols, actual_cols)
if (length(missing) > 0) {
  cli::cli_abort("FAIL: missing v2.4 columns: {paste(missing, collapse = ', ')}")
}
extra <- setdiff(actual_cols, expected_cols)
if (length(extra) > 0) {
  cli::cli_abort("FAIL: unexpected columns: {paste(extra, collapse = ', ')}")
}
if (!DBI::dbExistsTable(con2, "lifestage_review")) {
  cli::cli_abort("FAIL: lifestage_review table not created")
}

cli::cli_alert_success("Schema assertion passed -- v2.4 schema confirmed")
cli::cli_alert_success("TEAR-03 complete")
```

**CRITICAL connection lifecycle notes** (per RESEARCH.md and PATTERNS.md):
- Call `.eco_close_con()` BEFORE every read-write `DBI::dbConnect()` call
- Use `shutdown = TRUE` on every `DBI::dbDisconnect()` call
- The manual drop connection MUST be fully disconnected before calling `.eco_patch_lifestage()` because that function opens its own read-write connection internally (line 836-837)
- The read-only assertion connection in Step 3 also needs `.eco_close_con()` first to release the write connection from Step 2

**Step B — Delete `LIFESTAGE_HARMONIZATION_PLAN.md`** (per D-02).

Delete the file at the repo root: `LIFESTAGE_HARMONIZATION_PLAN.md` (19 KB, v2.3 plan doc).
Do NOT delete `LIFESTAGE_HARMONIZATION_PLAN2.md` (v2.4 plan doc -- must be preserved).

**Step C — Run grep verification for TEAR-01 and TEAR-02** (per D-01).

Run these grep checks. Scope to specific directories to avoid false positives from `.claude/worktrees/`:

TEAR-01 check: `grep -rn "classify_lifestage_keywords" R/ inst/ data-raw/`
Expected: no output, exit code 1 (no matches). If any match is found, investigate and remove.

TEAR-02 check: `grep -rn "ontology_id" R/ inst/ data-raw/`
Expected: no output, exit code 1 (no matches). If any match is found, investigate and remove.

TEAR-02 test-file audit: `grep -rn "ontology_id" tests/`
Expected: matches ONLY in absence assertions. Specifically:
- `tests/testthat/test-eco_functions.R` — `expect_false("ontology_id" %in% names(...))`
- `tests/testthat/test-eco_lifestage_gate.R` — similar absence assertion
Any match that is NOT an absence assertion (expect_false / expect_true(!"ontology_id" %in% ...)) must be removed.

Per D-04: Leave `test-eco_lifestage_gate.R` as-is — these are v2.4-forward tests.
  </action>

  <verify>
    <automated>grep -rn "classify_lifestage_keywords" R/ inst/ data-raw/ && echo "TEAR-01 FAIL" || echo "TEAR-01 PASS"</automated>
    <automated>grep -rn "ontology_id" R/ inst/ data-raw/ && echo "TEAR-02 FAIL" || echo "TEAR-02 PASS"</automated>
    <automated>test -f dev/lifestage/purge_and_rebuild.R && echo "purge_and_rebuild.R exists" || echo "MISSING"</automated>
    <automated>test ! -f LIFESTAGE_HARMONIZATION_PLAN.md && echo "old plan deleted" || echo "STILL EXISTS"</automated>
    <automated>test -f LIFESTAGE_HARMONIZATION_PLAN2.md && echo "v2.4 plan preserved" || echo "MISSING - DO NOT DELETE"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "classify_lifestage_keywords" R/ inst/ data-raw/` returns exit code 1 (zero matches)
    - `grep -rn "ontology_id" R/ inst/ data-raw/` returns exit code 1 (zero matches)
    - `dev/lifestage/purge_and_rebuild.R` exists and contains the string `.eco_patch_lifestage`
    - `dev/lifestage/purge_and_rebuild.R` contains the string `eco_path()`
    - `dev/lifestage/purge_and_rebuild.R` contains the string `.eco_close_con()`
    - `dev/lifestage/purge_and_rebuild.R` contains the string `DBI::dbRemoveTable`
    - `dev/lifestage/purge_and_rebuild.R` contains the string `.eco_lifestage_dictionary_schema()`
    - `dev/lifestage/purge_and_rebuild.R` contains the string `shutdown = TRUE`
    - `dev/lifestage/purge_and_rebuild.R` contains the string `refresh = "baseline"`
    - `LIFESTAGE_HARMONIZATION_PLAN.md` does NOT exist at repo root
    - `LIFESTAGE_HARMONIZATION_PLAN2.md` DOES exist at repo root
  </acceptance_criteria>

  <done>
    purge_and_rebuild.R script created with correct connection lifecycle; LIFESTAGE_HARMONIZATION_PLAN.md deleted; grep verifications for TEAR-01 and TEAR-02 pass with zero matches in R/, inst/, data-raw/
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute DB purge-and-rebuild and confirm v2.4 schema</name>
  <files>dev/lifestage/purge_and_rebuild.R</files>

  <read_first>
    dev/lifestage/purge_and_rebuild.R
    R/eco_lifestage_patch.R
    R/eco_connection.R
  </read_first>

  <action>
**Step A — Run the purge-and-rebuild script** (per D-03).

Execute from the project root directory:
```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" dev/lifestage/purge_and_rebuild.R
```

This script:
1. Drops `lifestage_dictionary` and `lifestage_review` from `ecotox.duckdb` (the live DB at `tools::R_user_dir("ComptoxR", "data")`)
2. Calls `.eco_patch_lifestage(db_path = db_path, refresh = "baseline")` to rebuild both tables from the committed CSVs at `inst/extdata/ecotox/lifestage_baseline.csv` and `inst/extdata/ecotox/lifestage_derivation.csv`
3. Asserts the resulting schema: 13 columns matching `.eco_lifestage_dictionary_schema()`, no `ontology_id`, `lifestage_review` table exists

Expected output:
- "Dropped lifestage_dictionary" (or "not present -- skipped" if already absent)
- "Dropped lifestage_review" (or "not present -- skipped")
- "Dictionary rows: {N}" where N > 0
- "Review rows: {N}"
- "Schema assertion passed -- v2.4 schema confirmed"
- "TEAR-03 complete"

If the script aborts with a DuckDB write-lock error: the most likely cause is a cached read-only connection. Verify `.eco_close_con()` is called before each `DBI::dbConnect(..., read_only = FALSE)` call in the script, and that the drop-step connection is disconnected with `shutdown = TRUE` before `.eco_patch_lifestage()` is called.

**Step B — Run existing v2.4 tests to confirm no regressions.**

Write a temp R script that runs:
```r
devtools::test(filter = "eco_lifestage_gate")
```

This confirms the v2.4 patch pipeline tests still pass after the DB rebuild. All tests must pass.

**Step C — Verify ontology_id is absent from the rebuilt dictionary.**

Write a temp R script to directly confirm:
```r
source("R/eco_connection.R")
con <- DBI::dbConnect(duckdb::duckdb(), dbdir = eco_path(), read_only = TRUE)
cols <- DBI::dbListFields(con, "lifestage_dictionary")
DBI::dbDisconnect(con, shutdown = TRUE)
stopifnot(!"ontology_id" %in% cols)
stopifnot("source_ontology" %in% cols)
stopifnot("source_term_id" %in% cols)
stopifnot("harmonized_life_stage" %in% cols)
stopifnot("reproductive_stage" %in% cols)
stopifnot("derivation_source" %in% cols)
cat("All v2.4 schema assertions passed\n")
```
  </action>

  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" dev/lifestage/purge_and_rebuild.R</automated>
  </verify>

  <acceptance_criteria>
    - `Rscript dev/lifestage/purge_and_rebuild.R` exits with code 0
    - Script output contains "Schema assertion passed"
    - Script output contains "TEAR-03 complete"
    - Script output contains "Dictionary rows:" followed by a number > 0
    - `devtools::test(filter = "eco_lifestage_gate")` passes with 0 failures
    - Direct DB query confirms `ontology_id` is NOT in `DBI::dbListFields(con, "lifestage_dictionary")`
    - Direct DB query confirms `source_ontology` IS in `DBI::dbListFields(con, "lifestage_dictionary")`
    - Direct DB query confirms `lifestage_review` table exists via `DBI::dbExistsTable(con, "lifestage_review")`
  </acceptance_criteria>

  <done>
    DB purge-and-rebuild script runs successfully; lifestage_dictionary has 13-column v2.4 schema with no ontology_id; lifestage_review table exists; existing v2.4 tests pass with zero failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase has no trust boundaries. No network operations, no user input handling, no authentication. The only I/O is local filesystem and a local DuckDB database file at a package-managed path.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | ecotox.duckdb write operations | accept | Dev script only; local-only DB; no production deployment path. DuckDB single-writer exclusivity prevents concurrent corruption. |

No ASVS L1 categories apply (V2/V3/V4/V5/V6 all irrelevant per RESEARCH.md security domain analysis).
</threat_model>

<verification>
Phase 34 is complete when ALL of these hold:

1. **TEAR-01**: `grep -rn "classify_lifestage_keywords" R/ inst/ data-raw/` returns exit code 1 (zero matches)
2. **TEAR-02**: `grep -rn "ontology_id" R/ inst/ data-raw/` returns exit code 1 (zero matches)
3. **TEAR-02 (tests)**: `grep -rn "ontology_id" tests/` shows matches ONLY in `expect_false` / absence-assertion context
4. **TEAR-03**: `Rscript dev/lifestage/purge_and_rebuild.R` exits 0 with "TEAR-03 complete" in output
5. **D-02**: `LIFESTAGE_HARMONIZATION_PLAN.md` does not exist; `LIFESTAGE_HARMONIZATION_PLAN2.md` does exist
6. **Regression**: `devtools::test(filter = "eco_lifestage_gate")` passes with 0 failures
</verification>

<success_criteria>
- No `.classify_lifestage_keywords()` reference in R/, inst/, or data-raw/
- No `ontology_id` reference in R/, inst/, or data-raw/ source code
- `lifestage_dictionary` table in ecotox.duckdb has v2.4 13-column schema
- `lifestage_review` table exists in ecotox.duckdb
- `LIFESTAGE_HARMONIZATION_PLAN.md` deleted from repo root
- `LIFESTAGE_HARMONIZATION_PLAN2.md` preserved at repo root
- `dev/lifestage/purge_and_rebuild.R` exists and runs successfully
- Existing v2.4 tests pass with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/34-teardown/34-01-SUMMARY.md`
</output>
