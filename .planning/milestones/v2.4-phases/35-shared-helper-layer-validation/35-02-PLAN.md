---
phase: 35-shared-helper-layer-validation
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - dev/lifestage/validate_35.R
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04]

must_haves:
  truths:
    - "devtools::load_all() completes without errors or warnings attributable to eco_lifestage_patch.R"
    - "All 3 schema functions return tibbles with correct column counts (cache=13, dictionary=13, review=9)"
    - "Scoring tiers produce correct values: exact match=100, normalized=90, token/boundary=75"
    - "rank_candidates assigns resolved status to unique top-score >=90 candidate"
    - "rank_candidates assigns ambiguous status to tied top-score candidates"
    - "NVS failure simulation returns empty tibble with cli_warn, no abort"
    - "OLS4 prefix filter produces only UBERON: or PO: prefixed IDs on live query"
  artifacts:
    - path: "dev/lifestage/validate_35.R"
      provides: "Phase 35 comprehensive validation script covering all 14 functions"
      contains: "Phase 35 Validation"
  key_links:
    - from: "dev/lifestage/validate_35.R"
      to: "devtools::load_all()"
      via: "package load gate at script header"
      pattern: "devtools::load_all"
    - from: "dev/lifestage/validate_35.R"
      to: ".eco_lifestage_query_ols4()"
      via: "live OLS4 call with prefix assertion"
      pattern: "startsWith.*UBERON:"
    - from: "dev/lifestage/validate_35.R"
      to: "testthat::with_mocked_bindings"
      via: "NVS and OLS4 failure simulation"
      pattern: "with_mocked_bindings"
---

<objective>
Create a comprehensive validation script that exercises all 14 helper functions in `eco_lifestage_patch.R` with known terms, verifies scoring tiers, and simulates provider failures.

Purpose: Proves the load gate (D-09), adapter fixes (D-03/D-05/D-06), scoring correctness (PROV-03), and failure resilience (D-08) all work together end-to-end. Marks PROV-04 (BioPortal) as deferred per D-01/D-02.

Output: `dev/lifestage/validate_35.R` script that can be run with `Rscript dev/lifestage/validate_35.R` to validate the entire shared helper layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-shared-helper-layer-validation/35-CONTEXT.md
@.planning/phases/35-shared-helper-layer-validation/35-RESEARCH.md
@.planning/phases/35-shared-helper-layer-validation/35-PATTERNS.md
@.planning/phases/35-shared-helper-layer-validation/35-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/eco_lifestage_patch.R (all 14 function signatures the script must exercise):
```r
# Schema functions (3)
.eco_lifestage_cache_schema()          # returns 13-col tibble
.eco_lifestage_dictionary_schema()     # returns 13-col tibble
.eco_lifestage_review_schema()         # returns 9-col tibble

# Path/IO functions (5)
.eco_lifestage_release_id(con)         # extracts release from DB or path
.eco_lifestage_cache_path(ecotox_release)  # returns release-scoped cache path
.eco_lifestage_baseline_path()         # returns inst/extdata/ecotox/lifestage_baseline.csv path
.eco_lifestage_derivation_path()       # returns inst/extdata/ecotox/lifestage_derivation.csv path
.eco_lifestage_read_csv(path)          # reads CSV with NA handling

# Validation + cache (3 - validate_cache, cache_read, cache_write)
.eco_lifestage_validate_cache(cache, ecotox_release)
.eco_lifestage_cache_read(ecotox_release, required = FALSE)
.eco_lifestage_cache_write(cache, ecotox_release)

# Provider adapters (3)
.eco_lifestage_query_ols4(term, ontology = c("UBERON", "PO"))
.eco_lifestage_nvs_index(refresh = FALSE)
.eco_lifestage_query_nvs(term)

# Scoring (3)
.eco_lifestage_normalize_term(term, mode = c("strict", "loose"))
.eco_lifestage_score_text(term, candidate)
.eco_lifestage_token_score(term, candidate)

# Ranking + orchestration (4)
.eco_lifestage_rank_candidates(org_lifestage, candidates)
.eco_lifestage_resolve_term(org_lifestage, ecotox_release)
.eco_lifestage_review_from_cache(cache_rows)
.eco_lifestage_derive_fields(resolved_rows)
```

From R/eco_lifestage_patch.R lines 628-644 (scoring status logic):
```r
top_score <- ranked$candidate_score[[1]]
accepted <- ranked |> dplyr::filter(.data$candidate_score >= 75)
if (!nrow(accepted)) {
  # returns unresolved with candidate_reason = "no_candidate_ge75"
}
resolved <- top_score >= 90 && sum(ranked$candidate_score == top_score) == 1
status <- if (resolved) "resolved" else "ambiguous"
```

From dev/lifestage/validate_lifestage.R (existing dev script pattern):
```r
#!/usr/bin/env Rscript
suppressPackageStartupMessages({
  library(DBI)
  library(duckdb)
  library(dplyr)
  library(cli)
})
source("R/eco_connection.R")
source("R/eco_lifestage_patch.R")
# ... validation logic ...
```

From dev/lifestage/confirm_gate.R (cli header pattern):
```r
cli::cli_h1("...")
cli::cli_h2("...")
cli::cli_alert_success("...")
cli::cli_alert_info("...")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 35 validation script</name>
  <files>dev/lifestage/validate_35.R</files>
  <read_first>
    - dev/lifestage/validate_lifestage.R (analog - existing dev script structure and pattern)
    - dev/lifestage/confirm_gate.R (analog - cli section structure)
    - R/eco_lifestage_patch.R (the 14 functions being validated - read for current signatures after Plan 01 fixes)
    - .planning/phases/35-shared-helper-layer-validation/35-PATTERNS.md (NVS/OLS4 failure simulation patterns)
    - .planning/phases/35-shared-helper-layer-validation/35-RESEARCH.md (scoring tier reference, pitfalls 1-5)
  </read_first>
  <action>
Create `dev/lifestage/validate_35.R` with the following sections. Use `devtools::load_all()` (not manual `source()`) per D-09 and Pitfall 3.

**Header:**
```r
#!/usr/bin/env Rscript
# Phase 35: Shared Helper Layer Validation
# Exercises all 14 functions in R/eco_lifestage_patch.R
# Run from project root: Rscript dev/lifestage/validate_35.R

suppressPackageStartupMessages(devtools::load_all(".", quiet = TRUE))
```

This load_all call IS the D-09 load gate. If eco_lifestage_patch.R has parse errors, it will abort here. The vcr warning ("package 'vcr' was built under R version 4.5.2") is NOT attributable to eco_lifestage_patch.R — ignore it per Pitfall 4.

**Section 1: Schema Functions (3 functions)**
```r
cli::cli_h1("Phase 35 Validation")

cli::cli_h2("1. Schema Functions")
cache_schema <- .eco_lifestage_cache_schema()
stopifnot(ncol(cache_schema) == 13)
cli::cli_alert_success("cache_schema: {ncol(cache_schema)} columns")

dict_schema <- .eco_lifestage_dictionary_schema()
stopifnot(ncol(dict_schema) == 13)
cli::cli_alert_success("dictionary_schema: {ncol(dict_schema)} columns")

review_schema <- .eco_lifestage_review_schema()
stopifnot(ncol(review_schema) == 9)
cli::cli_alert_success("review_schema: {ncol(review_schema)} columns")
```

**Section 2: Path/IO Functions (5 functions)**
```r
cli::cli_h2("2. Path/IO Functions")
baseline_path <- .eco_lifestage_baseline_path()
stopifnot(file.exists(baseline_path))
cli::cli_alert_success("baseline_path exists: {.path {baseline_path}}")

derivation_path <- .eco_lifestage_derivation_path()
stopifnot(file.exists(derivation_path))
cli::cli_alert_success("derivation_path exists: {.path {derivation_path}}")

baseline <- .eco_lifestage_read_csv(baseline_path)
stopifnot(is.data.frame(baseline), nrow(baseline) > 0)
cli::cli_alert_success("baseline CSV loaded: {nrow(baseline)} rows")

derivation <- .eco_lifestage_read_csv(derivation_path)
stopifnot(is.data.frame(derivation), nrow(derivation) > 0)
cli::cli_alert_success("derivation CSV loaded: {nrow(derivation)} rows")

# cache_path requires a release string - just verify it returns a non-empty path
cache_p <- .eco_lifestage_cache_path("test_release")
stopifnot(nzchar(cache_p))
cli::cli_alert_success("cache_path returns non-empty path for test release")
```

Note: `.eco_lifestage_release_id(con)` requires a DuckDB connection. Skip in this validation script (it is exercised in `validate_lifestage.R` which opens the DB). Document this skip.

**Section 3: Scoring Functions (PROV-03) (3 functions)**
```r
cli::cli_h2("3. Scoring Functions (PROV-03)")

# Normalization
strict <- .eco_lifestage_normalize_term("  Adult ", mode = "strict")
stopifnot(strict == "adult")
cli::cli_alert_success("normalize_term strict: '  Adult ' -> '{strict}'")

loose <- .eco_lifestage_normalize_term("Adults.", mode = "loose")
stopifnot(nzchar(loose))
cli::cli_alert_success("normalize_term loose: 'Adults.' -> '{loose}'")

# Score tiers
score_exact <- .eco_lifestage_score_text("adult", "adult")
stopifnot(score_exact$score == 100)
cli::cli_alert_success("score_text exact: {score_exact$score} (expected 100)")

score_normalized <- .eco_lifestage_score_text("Adults.", "adult")
stopifnot(score_normalized$score == 90)
cli::cli_alert_success("score_text normalized: {score_normalized$score} (expected 90)")

# Token scoring - use a partial match scenario
token_result <- .eco_lifestage_token_score("adult male", "adult")
stopifnot(token_result$score == 75)
cli::cli_alert_success("token_score partial: {token_result$score} (expected 75)")
```

**Section 4: Ranking Function (PROV-03)**
```r
cli::cli_h2("4. Ranking (PROV-03)")

# Single high-score candidate -> resolved
single_candidate <- tibble::tibble(
  source_provider = "OLS4",
  source_ontology = "UBERON",
  source_term_id = "UBERON:0000113",
  source_term_label = "post-juvenile adult stage",
  source_term_definition = NA_character_,
  candidate_aliases = NA_character_,
  source_release = "current",
  source_match_method = "ols4_search"
)
ranked_single <- .eco_lifestage_rank_candidates("adult", single_candidate)
stopifnot(ranked_single$source_match_status[[1]] %in% c("resolved", "ambiguous", "unresolved"))
cli::cli_alert_success("rank_candidates single: status = '{ranked_single$source_match_status[[1]]}'")

# No candidates -> unresolved
ranked_empty <- .eco_lifestage_rank_candidates("Xylophage", tibble::tibble())
stopifnot(ranked_empty$source_match_status[[1]] == "unresolved")
stopifnot(ranked_empty$candidate_reason[[1]] == "no_provider_candidates")
cli::cli_alert_success("rank_candidates empty: status = 'unresolved', reason = 'no_provider_candidates'")
```

**Section 5: NVS Failure Simulation (D-08, PROV-02)**
```r
cli::cli_h2("5. NVS Failure Simulation (D-08)")
nvs_fail_result <- tryCatch(
  testthat::with_mocked_bindings(
    .eco_lifestage_nvs_index = function(refresh = FALSE) {
      cli::cli_warn("NVS S11 SPARQL endpoint unreachable. [SIMULATED]")
      tibble::tibble()
    },
    .package = "ComptoxR",
    .eco_lifestage_query_nvs("Adult")
  ),
  error = function(e) {
    cli::cli_abort(c(
      "NVS failure simulation produced an error instead of a warning.",
      "x" = conditionMessage(e)
    ))
  }
)
stopifnot(is.data.frame(nvs_fail_result))
stopifnot(nrow(nvs_fail_result) == 0)
cli::cli_alert_success("NVS failure: empty tibble returned, no abort")
```

**Section 6: OLS4 Failure Simulation (discretionary per D-07 — including it since both adapters now have tryCatch)**
```r
cli::cli_h2("6. OLS4 Failure Simulation")
ols4_fail_result <- tryCatch(
  testthat::with_mocked_bindings(
    .eco_lifestage_query_ols4 = function(term, ontology = c("UBERON", "PO")) {
      cli::cli_warn("OLS4 endpoint unreachable for {ontology}. [SIMULATED]")
      tibble::tibble()
    },
    .package = "ComptoxR",
    {
      candidates <- dplyr::bind_rows(
        .eco_lifestage_query_ols4("adult", "UBERON"),
        .eco_lifestage_query_ols4("adult", "PO")
      )
      candidates
    }
  ),
  error = function(e) {
    cli::cli_abort(c(
      "OLS4 failure simulation produced an error instead of a warning.",
      "x" = conditionMessage(e)
    ))
  }
)
stopifnot(is.data.frame(ols4_fail_result))
stopifnot(nrow(ols4_fail_result) == 0)
cli::cli_alert_success("OLS4 failure: empty tibble returned, no abort")
```

**Section 7: Live OLS4 Prefix Filter (PROV-01)**
```r
cli::cli_h2("7. Live OLS4 Prefix Filter (PROV-01)")
ols4_uberon <- tryCatch(
  .eco_lifestage_query_ols4("adult", "UBERON"),
  error = function(e) {
    cli::cli_alert_warning("OLS4 live call failed (network may be unavailable): {conditionMessage(e)}")
    NULL
  }
)
if (!is.null(ols4_uberon) && nrow(ols4_uberon) > 0) {
  bad_prefix <- ols4_uberon[!startsWith(ols4_uberon$source_term_id, "UBERON:"), ]
  if (nrow(bad_prefix) > 0) {
    cli::cli_abort("PROV-01 FAIL: OLS4 returned non-UBERON IDs: {paste(bad_prefix$source_term_id, collapse = ', ')}")
  }
  cli::cli_alert_success("OLS4 prefix filter: all {nrow(ols4_uberon)} row(s) have UBERON: prefix")
} else if (is.null(ols4_uberon)) {
  cli::cli_alert_info("OLS4 live check skipped (network unavailable)")
} else {
  cli::cli_alert_info("OLS4 returned 0 rows for 'adult' (unexpected but not a failure)")
}
```

**Section 8: PROV-04 Deferred Notice**
```r
cli::cli_h2("8. BioPortal Adapter (PROV-04)")
cli::cli_alert_info("PROV-04 DEFERRED per D-01: BioPortal adapter does not exist yet.")
cli::cli_alert_info("A new phase will be inserted after Phase 35 for BioPortal adapter creation.")
```

**Footer:**
```r
cli::cli_h1("Phase 35 Validation Complete")
cli::cli_alert_success("All checks passed.")
```

After writing the script, run `air format dev/lifestage/validate_35.R` and `jarl check dev/lifestage/validate_35.R`.
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/ComptoxR && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" dev/lifestage/validate_35.R</automated>
  </verify>
  <acceptance_criteria>
    - dev/lifestage/validate_35.R exists and is non-empty
    - dev/lifestage/validate_35.R contains `devtools::load_all(".", quiet = TRUE)` (D-09 load gate)
    - dev/lifestage/validate_35.R contains `cli::cli_h1("Phase 35 Validation")` (header)
    - dev/lifestage/validate_35.R contains `stopifnot(ncol(cache_schema) == 13)` (schema check)
    - dev/lifestage/validate_35.R contains `stopifnot(score_exact$score == 100)` (scoring tier check)
    - dev/lifestage/validate_35.R contains `with_mocked_bindings` (NVS failure simulation)
    - dev/lifestage/validate_35.R contains `"PROV-04 DEFERRED per D-01"` (deferred notice)
    - dev/lifestage/validate_35.R contains `startsWith.*UBERON:` pattern (live prefix check)
    - `Rscript dev/lifestage/validate_35.R` exits with status 0 and prints "All checks passed"
    - `air format dev/lifestage/validate_35.R` produces no changes
  </acceptance_criteria>
  <done>Validation script exercises all 14 functions, verifies schema column counts, scoring tiers (100/90/75), status assignment (resolved/ambiguous/unresolved), NVS failure simulation, OLS4 failure simulation, live OLS4 prefix filter, and documents PROV-04 as deferred. Script exits 0 with "All checks passed".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev script -> OLS4 REST API | Live HTTP GET to ebi.ac.uk (public, no auth) — Section 7 only |
| dev script -> devtools::load_all() | Loads entire package namespace including all dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Denial of Service | validate_35.R live OLS4 call | accept | Script wraps live call in tryCatch; network unavailability prints info message and skips, does not fail validation |
| T-35-06 | Information Disclosure | validate_35.R | accept | Dev script is not shipped with package (dev/ directory); no secrets or credentials involved; all API calls are to public endpoints |
</threat_model>

<verification>
1. `Rscript dev/lifestage/validate_35.R` exits 0 and prints "All checks passed"
2. Script output includes "cache_schema: 13 columns", "dictionary_schema: 13 columns", "review_schema: 9 columns"
3. Script output includes "score_text exact: 100", "score_text normalized: 90", "token_score partial: 75"
4. Script output includes "NVS failure: empty tibble returned, no abort"
5. Script output includes "OLS4 failure: empty tibble returned, no abort"
6. Script output includes "PROV-04 DEFERRED per D-01"
7. If network available: "OLS4 prefix filter: all N row(s) have UBERON: prefix"
</verification>

<success_criteria>
- devtools::load_all() completes without errors from eco_lifestage_patch.R (D-09)
- All 3 schema functions return correct column counts
- Scoring tiers produce 100/90/75 for exact/normalized/token matches (PROV-03)
- NVS failure simulation returns empty tibble with warning, no abort (PROV-02, D-08)
- OLS4 failure simulation returns empty tibble with warning, no abort (D-06)
- Live OLS4 query returns only UBERON: prefixed IDs (PROV-01, D-05)
- PROV-04 documented as deferred (D-01, D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/35-shared-helper-layer-validation/35-02-SUMMARY.md`
</output>
