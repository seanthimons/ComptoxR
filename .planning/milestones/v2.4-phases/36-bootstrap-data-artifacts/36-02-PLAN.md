---
phase: 36-bootstrap-data-artifacts
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - dev/lifestage/validate_36.R
  - dev/lifestage/refresh_baseline.R
  - dev/lifestage/README.md
autonomous: false
requirements:
  - DATA-01
  - DATA-03

must_haves:
  truths:
    - "Running validate_36.R produces all-green output with cross-check pass, schema pass, and completeness pass (if DB present)"
    - "refresh_baseline.R is a working dev script that resolves all DB lifestage terms and writes proposals for new keys"
    - "dev/lifestage/README.md documents the full refresh workflow for future maintainers"
  artifacts:
    - path: "dev/lifestage/validate_36.R"
      provides: "One-shot human-readable Phase 36 verification with DB-optional completeness check"
      contains: "cli::cli_h1"
    - path: "dev/lifestage/refresh_baseline.R"
      provides: "Future-release baseline refresh script with derivation proposals staging"
      contains: "derivation_proposals.csv"
    - path: "dev/lifestage/README.md"
      provides: "Curator documentation for lifestage refresh workflow"
      contains: "refresh_baseline.R"
  key_links:
    - from: "dev/lifestage/validate_36.R"
      to: "inst/extdata/ecotox/lifestage_baseline.csv"
      via: ".eco_lifestage_baseline_path() + readr::read_csv"
      pattern: "eco_lifestage_baseline_path"
    - from: "dev/lifestage/refresh_baseline.R"
      to: "inst/extdata/ecotox/lifestage_baseline.csv"
      via: ".eco_lifestage_baseline_path() + utils::write.csv"
      pattern: "write.csv"
    - from: "dev/lifestage/refresh_baseline.R"
      to: "dev/lifestage/derivation_proposals.csv"
      via: "utils::write.csv for new unseen keys"
      pattern: "derivation_proposals"
---

<objective>
Create the Phase 36 verification script and the future-release refresh workflow with curator documentation.

Purpose: validate_36.R provides the one-shot human-readable verification that the data fixes from Plan 01 are correct and complete (including DB completeness). refresh_baseline.R and README.md give future maintainers a documented path to regenerate the baseline when a new ECOTOX release is installed, without any auto-commit of derivation rows (D-02, D-14).

Output: Three dev-script files — a verification script, a refresh script, and a README.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-bootstrap-data-artifacts/36-CONTEXT.md
@.planning/phases/36-bootstrap-data-artifacts/36-RESEARCH.md
@.planning/phases/36-bootstrap-data-artifacts/36-PATTERNS.md
@.planning/phases/36-bootstrap-data-artifacts/36-01-SUMMARY.md

<interfaces>
<!-- Key functions the executor needs from the package -->

From R/eco_lifestage_patch.R:
```r
.eco_lifestage_baseline_path()   # Returns path to inst/extdata/ecotox/lifestage_baseline.csv
.eco_lifestage_derivation_path() # Returns path to inst/extdata/ecotox/lifestage_derivation.csv
.eco_lifestage_derivation_map()  # Reads + validates derivation CSV, returns tibble
.eco_lifestage_resolve_term(term, ecotox_release) # OLS4+NVS resolution, returns 13-col row
.eco_lifestage_release_id(con)   # Reads release from _metadata table
.eco_lifestage_read_csv(path)    # Reads CSV with na.strings handling
```

From R/eco_connection.R:
```r
eco_path() # Returns path to ecotox.duckdb via option or R_user_dir fallback
```

Dev script header pattern (from dev/lifestage/validate_35.R):
```r
#!/usr/bin/env Rscript
# Phase NN: Title
# Run from project root: Rscript dev/lifestage/validate_NN.R
suppressPackageStartupMessages(devtools::load_all(".", quiet = TRUE))
```

CLI messaging pattern:
```r
cli::cli_h1("Title")       # top-level section
cli::cli_h2("Subsection")  # subsection
cli::cli_alert_success("Passed: ...")
cli::cli_warn("Warning: ...")
cli::cli_abort("Error: ...")
```

DB connection pattern:
```r
con <- DBI::dbConnect(duckdb::duckdb(), dbdir = db_path, read_only = TRUE)
on.exit(DBI::dbDisconnect(con, shutdown = TRUE), add = TRUE)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validate_36.R verification script and refresh_baseline.R dev script</name>
  <files>dev/lifestage/validate_36.R, dev/lifestage/refresh_baseline.R</files>
  <read_first>
    - dev/lifestage/validate_35.R (exact structural pattern to follow for validate_36.R)
    - dev/lifestage/purge_and_rebuild.R (DB connection + helper call pattern for refresh_baseline.R)
    - dev/lifestage/validate_lifestage.R (completeness anti-join and release ID patterns)
    - R/eco_lifestage_patch.R (lines 85-130 for path helpers; .eco_lifestage_resolve_term signature; .eco_lifestage_release_id signature; line 248 for write.csv pattern)
    - R/eco_connection.R (eco_path function for DB path resolution)
    - .planning/phases/36-bootstrap-data-artifacts/36-PATTERNS.md (validate_36 and refresh_baseline pattern sections)
  </read_first>
  <action>
**File 1: `dev/lifestage/validate_36.R`** (per D-11)

Create a verification script following the validate_35.R pattern exactly. Structure:

```r
#!/usr/bin/env Rscript
# Phase 36: Bootstrap Data Artifacts Validation
# Run from project root: Rscript dev/lifestage/validate_36.R

suppressPackageStartupMessages(devtools::load_all(".", quiet = TRUE))

cli::cli_h1("Phase 36 Validation")
```

**Section 1: Schema Checks** (re-runs D-10 assertions with verbose output)
- Read baseline via `.eco_lifestage_baseline_path()` using `readr::read_csv(path, show_col_types = FALSE)`
- Read derivation via `.eco_lifestage_derivation_path()` using `readr::read_csv(path, show_col_types = FALSE)`
- Assert baseline has 13 columns via `stopifnot(ncol(baseline) == 13L)`
- Assert derivation has 5 columns via `stopifnot(ncol(derivation) == 5L)`
- Print column names with `cli::cli_alert_success`

**Section 2: Cross-Check Gate** (re-runs D-10 anti-join)
- Filter: `resolved <- dplyr::filter(baseline, source_match_status == "resolved")`
- Dedup: `resolved_keys <- dplyr::distinct(resolved, source_ontology, source_term_id)`
- Anti-join: `gaps <- dplyr::anti_join(resolved_keys, derivation, by = c("source_ontology", "source_term_id"))`
- `stopifnot(nrow(gaps) == 0L)` — abort if gaps remain
- `cli::cli_alert_success("Cross-check: {nrow(resolved_keys)} resolved key(s) all have derivation partners.")`

**Section 3: GO:0040007 Contamination Check**
- Check: `go_rows <- baseline[!is.na(baseline$source_term_id) & baseline$source_term_id == "GO:0040007", ]`
- `stopifnot(nrow(go_rows) == 0L)` — abort if any remain
- `cli::cli_alert_success("No GO:0040007 contamination in baseline.")`

**Section 4: Completeness Check** (D-06, D-07 — DB-optional)
- Call `db_path <- eco_path()`
- If `!file.exists(db_path)`: emit `cli::cli_warn("ECOTOX DB not found at {.path {db_path}} -- completeness check skipped.")` and skip section (per D-07 DB-absent handling)
- If DB exists:
  - Open read-only connection: `con <- DBI::dbConnect(duckdb::duckdb(), dbdir = db_path, read_only = TRUE)` with `on.exit(DBI::dbDisconnect(con, shutdown = TRUE), add = TRUE)`
  - Release match (D-07): compare `db_release <- .eco_lifestage_release_id(con)` with `baseline_release <- unique(stats::na.omit(baseline$ecotox_release))`. If `!identical(db_release, baseline_release)`, call `cli::cli_abort("Release mismatch: DB={.val {db_release}}, baseline={.val {baseline_release}}")`
  - Completeness anti-join (D-06): `db_terms <- DBI::dbGetQuery(con, "SELECT DISTINCT description FROM lifestage_codes ORDER BY description")$description`
  - `missing_from_baseline <- setdiff(db_terms, baseline$org_lifestage)`
  - If `length(missing_from_baseline) == 0`: `cli::cli_alert_success("Completeness: all {length(db_terms)} DB terms present in baseline.")`
  - Else: `cli::cli_abort("Baseline missing {length(missing_from_baseline)} DB term(s): {missing_from_baseline}")`

**Footer:**
```r
cli::cli_h1("Phase 36 Validation Complete")
cli::cli_alert_success("All checks passed.")
```

**File 2: `dev/lifestage/refresh_baseline.R`** (per D-12, D-13, D-14, D-16)

Create a future-release refresh script. Structure:

```r
#!/usr/bin/env Rscript
# Lifestage Baseline Refresh Script
# Run when a new ECOTOX release is installed in ecotox.duckdb.
# Run from project root: Rscript dev/lifestage/refresh_baseline.R
#
# Prerequisites:
#   - ecotox.duckdb exists with new release data
#   - OLS4 and NVS APIs are reachable (network access required)
#
# Output:
#   - Updates inst/extdata/ecotox/lifestage_baseline.csv (D-13)
#   - Writes dev/lifestage/derivation_proposals.csv for new unseen keys (D-14)
#   - NEVER writes directly to lifestage_derivation.csv (D-02)

suppressPackageStartupMessages(devtools::load_all(".", quiet = TRUE))
```

**Step 1: Fetch DB Terms**
- `cli::cli_h1("Lifestage Baseline Refresh")`
- `cli::cli_h2("Step 1: Fetch DB Terms")`
- `db_path <- eco_path()`
- If `!file.exists(db_path)`: `cli::cli_abort("ECOTOX DuckDB not found at {.path {db_path}}.")`
- Open read-only connection with on.exit cleanup
- `ecotox_release <- .eco_lifestage_release_id(con)`
- `org_lifestages <- DBI::dbGetQuery(con, "SELECT DISTINCT description FROM lifestage_codes ORDER BY description")$description`
- `cli::cli_alert_info("Found {length(org_lifestages)} distinct lifestage term(s) for release {.val {ecotox_release}}.")`
- Close connection (let on.exit handle it)

**Step 2: Re-resolve All Terms**
- `cli::cli_h2("Step 2: Re-resolve All Terms")`
- `cli::cli_alert_info("Resolving {length(org_lifestages)} term(s) via OLS4 + NVS...")`
- `re_resolved <- purrr::map_dfr(org_lifestages, .eco_lifestage_resolve_term, ecotox_release = ecotox_release)`
- Write to baseline: `utils::write.csv(re_resolved, .eco_lifestage_baseline_path(), row.names = FALSE, na = "")`
- `cli::cli_alert_success("Baseline written: {nrow(re_resolved)} rows to {.path {.eco_lifestage_baseline_path()}}.")`

**Step 3: Check Derivation Coverage**
- `cli::cli_h2("Step 3: Check Derivation Coverage")`
- Read derivation: `derivation <- .eco_lifestage_derivation_map()`
- Filter resolved new keys: `resolved_new <- re_resolved |> dplyr::filter(source_match_status == "resolved") |> dplyr::distinct(source_ontology, source_term_id)`
- Anti-join: `new_keys <- dplyr::anti_join(resolved_new, derivation, by = c("source_ontology", "source_term_id"))`
- If `nrow(new_keys) > 0`:
  - Write proposals: `proposals_path <- file.path("dev", "lifestage", "derivation_proposals.csv")`
  - `proposals <- new_keys |> dplyr::mutate(harmonized_life_stage = NA_character_, reproductive_stage = NA, derivation_source = "curator_review")`
  - `utils::write.csv(proposals, proposals_path, row.names = FALSE, na = "")`
  - Emit `cli::cli_warn(c("{nrow(new_keys)} new resolved key(s) have no derivation partner.", "i" = "Review {.path {proposals_path}} and promote approved rows to {.path inst/extdata/ecotox/lifestage_derivation.csv}."))` (D-16: warn, not abort)
- If `nrow(new_keys) == 0`:
  - `cli::cli_alert_success("All resolved keys already have derivation partners.")`

**Footer:**
```r
cli::cli_alert_success("Refresh complete.")
```

After creating both files, run `air format dev/lifestage/validate_36.R dev/lifestage/refresh_baseline.R` and `jarl check dev/lifestage/validate_36.R dev/lifestage/refresh_baseline.R`.
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/ComptoxR && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" dev/lifestage/validate_36.R</automated>
  </verify>
  <acceptance_criteria>
    - dev/lifestage/validate_36.R exists and is executable
    - validate_36.R contains "cli::cli_h1" (section headers per pattern)
    - validate_36.R contains "stopifnot(ncol(baseline) == 13L)" (schema check)
    - validate_36.R contains "stopifnot(ncol(derivation) == 5L)" (schema check)
    - validate_36.R contains "dplyr::anti_join" (cross-check mechanism)
    - validate_36.R contains "eco_path()" (DB completeness check)
    - validate_36.R contains 'cli::cli_warn' for DB-absent case (D-07 graceful skip)
    - validate_36.R contains 'cli::cli_abort' for release mismatch case (D-07)
    - validate_36.R contains "GO:0040007" (contamination check)
    - validate_36.R outputs "Phase 36 Validation Complete" and "All checks passed" when run
    - dev/lifestage/refresh_baseline.R exists
    - refresh_baseline.R contains ".eco_lifestage_resolve_term" (resolution calls)
    - refresh_baseline.R contains "derivation_proposals.csv" (D-14 proposal staging)
    - refresh_baseline.R contains "cli::cli_warn" for derivation gaps (D-16 warn not abort)
    - refresh_baseline.R does NOT contain any write to "lifestage_derivation.csv" (D-02 curator-only)
    - air format and jarl check produce no issues on both files
  </acceptance_criteria>
  <done>validate_36.R runs successfully confirming all Phase 36 data integrity checks pass (schema, cross-check, contamination, completeness). refresh_baseline.R is ready for future use by maintainers.</done>
</task>

<task type="auto">
  <name>Task 2: Create dev/lifestage/README.md curator documentation</name>
  <files>dev/lifestage/README.md</files>
  <read_first>
    - dev/lifestage/refresh_baseline.R (the script being documented — must exist from Task 1)
    - dev/lifestage/validate_36.R (referenced in the docs)
    - .planning/phases/36-bootstrap-data-artifacts/36-CONTEXT.md (D-15 requirements for README content)
    - .planning/phases/36-bootstrap-data-artifacts/36-PATTERNS.md (README section — required content per D-15)
  </read_first>
  <action>
Create `dev/lifestage/README.md` per D-15 with the following content:

```markdown
# ECOTOX Lifestage Data Maintenance

This directory contains developer scripts for maintaining the lifestage ontology resolution data shipped with ComptoxR.

## Directory Contents

| File | Purpose |
|------|---------|
| `validate_35.R` | Phase 35 helper layer validation (historical) |
| `validate_36.R` | Phase 36 data artifact integrity check |
| `validate_lifestage.R` | General lifestage pipeline validation |
| `refresh_baseline.R` | Regenerate baseline CSV for new ECOTOX releases |
| `purge_and_rebuild.R` | Drop and rebuild lifestage tables from scratch |
| `confirm_gate.R` | Gate logic confirmation script |

## When to Run `refresh_baseline.R`

Run this script after installing a new ECOTOX release into `ecotox.duckdb`:

1. Download the new ECOTOX ASCII export from EPA
2. Rebuild the DuckDB database (via `ecotox_build.R` or the package build pipeline)
3. Run the refresh script from the project root:
   ```
   Rscript dev/lifestage/refresh_baseline.R
   ```

## Prerequisites

- **Database:** `ecotox.duckdb` must exist with the new release data.
  Location is resolved via `eco_path()` (typically `tools::R_user_dir("ComptoxR", "data")/ecotox.duckdb`).
- **Network:** OLS4 and NVS SPARQL APIs must be reachable. The script calls
  `.eco_lifestage_resolve_term()` which queries these services for ontology resolution.
- **Package loaded:** The script calls `devtools::load_all(".")` — run from the package root.

## Refresh Workflow

### Step 1: Regenerate Baseline

```
Rscript dev/lifestage/refresh_baseline.R
```

This script:
- Reads all distinct lifestage descriptions from `lifestage_codes` in the DB
- Re-resolves each term via OLS4 + NVS providers
- Writes the updated baseline to `inst/extdata/ecotox/lifestage_baseline.csv`

### Step 2: Review Derivation Proposals

If new resolved terms have `(source_ontology, source_term_id)` keys not present in
`inst/extdata/ecotox/lifestage_derivation.csv`, the script writes them to:

```
dev/lifestage/derivation_proposals.csv
```

This file contains proposed derivation rows with `harmonized_life_stage` and
`reproductive_stage` set to NA. **You must fill these in manually.**

For each proposed row:
1. Look up the `source_term_label` in the baseline CSV
2. Assign the appropriate `harmonized_life_stage` category:
   Adult, Egg/Embryo, Larva, Juvenile, Subadult, Senescent/Dormant, or Other/Unknown
3. Set `reproductive_stage` to TRUE or FALSE
4. Set `derivation_source` to `"baseline_curated_source_id"`

### Step 3: Promote Approved Rows

Copy approved rows from `dev/lifestage/derivation_proposals.csv` into
`inst/extdata/ecotox/lifestage_derivation.csv`. Maintain alphabetical sort order
by `source_ontology` then `source_term_id`.

### Step 4: Repatch the Database

After both CSVs are committed, repatch the live database:

```r
devtools::load_all(".")
.eco_patch_lifestage(refresh = "baseline")
```

This rebuilds `lifestage_dictionary` from the committed CSVs without hitting any
live API.

### Step 5: Verify

```
Rscript dev/lifestage/validate_36.R
```

Confirms schema integrity, cross-check gate, and DB completeness.

## Important Rules

- **Never auto-commit derivation rows.** The `lifestage_derivation.csv` file is
  curator-authored only. All automation writes to `derivation_proposals.csv` for review.
- **`cli_warn`, not `cli_abort`, for derivation gaps.** The refresh script warns about
  missing derivation entries but does not abort. You control the pacing of baseline
  commit vs. derivation commit.
- **The CI gate catches regressions.** `tests/testthat/test-eco_lifestage_data.R`
  runs on every `devtools::test()` and will fail if the cross-check invariant is violated.
```

This is a documentation-only file. No formatting tools (air/jarl) apply to markdown.
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/ComptoxR && test -f dev/lifestage/README.md && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - dev/lifestage/README.md exists
    - File contains "refresh_baseline.R" (D-15 documents the refresh script)
    - File contains "derivation_proposals.csv" (D-15 documents the proposal workflow)
    - File contains "eco_patch_lifestage" (D-15 documents the repatch command)
    - File contains "Prerequisites" section (D-15 prerequisite documentation)
    - File contains "devtools::test()" or "test-eco_lifestage_data" (references CI gate)
    - File does NOT contain any suggestion to auto-commit derivation rows (D-02 prohibition)
  </acceptance_criteria>
  <done>dev/lifestage/README.md documents the complete refresh workflow: when to run, prerequisites, proposal review, promotion, repatching, and verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 36 data artifacts are complete:
1. lifestage_baseline.csv has 3 GO:0040007 rows re-resolved (expected: unresolved status)
2. lifestage_derivation.csv has 6 new curator-authored rows (S1116, S1122, S1106, S1128, PO:0000055, PO:0009010)
3. testthat gate passes (cross-check, schema)
4. validate_36.R passes all checks including DB completeness
5. refresh_baseline.R is ready for future releases
6. README.md documents the curator workflow

**Curator sign-off needed for two provisional mappings:**
- PO:0000055 (bud/inflorescence) mapped to Adult with reproductive_stage=TRUE
- PO:0009010 (seed) mapped to Egg/Embryo with reproductive_stage=FALSE
  </what-built>
  <how-to-verify>
1. Run the validation script:
   ```
   Rscript dev/lifestage/validate_36.R
   ```
   Expected: all sections pass, "Phase 36 Validation Complete" at end

2. Run the testthat gate:
   ```r
   devtools::test(filter = "eco_lifestage_data")
   ```
   Expected: all tests pass, 0 failures

3. Review PO mapping decisions in lifestage_derivation.csv:
   - PO:0000055 (bud) mapped as Adult/reproductive=TRUE — correct?
   - PO:0009010 (seed) mapped as Egg/Embryo/reproductive=FALSE — correct?

4. Spot-check the 3 re-resolved baseline rows (search for "Exponential growth phase", "Lag growth phase", "Stationary growth phase") — confirm source_match_status is "unresolved"
  </how-to-verify>
  <resume-signal>Type "approved" if PO mappings are correct and all checks pass, or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This plan creates dev-only scripts (not shipped with the package) and a documentation file. No network endpoints, no user input handling, no auth changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Tampering | refresh_baseline.R | accept | Script is a dev tool, not shipped. Writes only to package source files under git version control. Never auto-commits derivation rows (D-02). |
| T-36-04 | Information Disclosure | validate_36.R DB access | accept | Opens DB read-only. No data leaves the local machine. DB path resolved via R_user_dir, not hardcoded. |
</threat_model>

<verification>
1. `Rscript dev/lifestage/validate_36.R` — exits 0 with "All checks passed"
2. `dev/lifestage/refresh_baseline.R` exists and parses without error
3. `dev/lifestage/README.md` exists with complete workflow documentation
4. Human confirms PO:0000055 and PO:0009010 mappings are correct
</verification>

<success_criteria>
- validate_36.R confirms schema, cross-check, contamination-free, and DB completeness in one run
- refresh_baseline.R implements the full refresh workflow per D-12 through D-16
- README.md covers all D-15 documentation requirements: when to run, prerequisites, proposal review, promotion, repatch
- Curator approves PO:0000055 and PO:0009010 provisional mappings
</success_criteria>

<output>
After completion, create `.planning/phases/36-bootstrap-data-artifacts/36-02-SUMMARY.md`
</output>
