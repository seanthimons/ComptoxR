# Phase 36.1: Unresolved Coverage Audit - Pattern Map

**Mapped:** 2026-04-23
**Files analyzed:** 7 new/modified files
**Analogs found:** 7 / 7

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `inst/extdata/ecotox/lifestage_audit.csv` | config/data | batch | `inst/extdata/ecotox/lifestage_derivation.csv` | role-match |
| `inst/extdata/ecotox/lifestage_derivation.csv` | config/data | batch | self (4 rows appended) | exact |
| `R/eco_lifestage_patch.R` | service | event-driven | self (cli_warn gate added to `.eco_lifestage_materialize_tables()`) | exact |
| `tests/testthat/test-eco_lifestage_data.R` | test | request-response | self (3 test blocks added) | exact |
| `dev/lifestage/probe_ols4.R` | utility | request-response | `R/eco_lifestage_patch.R` `.eco_lifestage_query_ols4()` | role-match |
| `dev/lifestage/probe_bioportal.R` | utility | request-response | `R/eco_lifestage_patch.R` `.eco_lifestage_query_nvs()` | role-match |
| `dev/lifestage/probe_wikidata.R` | utility | request-response | `R/eco_lifestage_patch.R` `.eco_lifestage_query_nvs()` (SPARQL) | role-match |
| `dev/lifestage/probe_agrovoc.R` | utility | request-response | `R/eco_lifestage_patch.R` `.eco_lifestage_query_ols4()` (REST GET) | role-match |
| `dev/lifestage/validate_36.1.R` | utility | batch | `dev/lifestage/validate_36.R` | exact |

## Pattern Assignments

---

### `inst/extdata/ecotox/lifestage_audit.csv` (config/data, batch)

**Analog:** `inst/extdata/ecotox/lifestage_derivation.csv`

**CSV header row** (line 1 of derivation CSV):
```
"source_ontology","source_term_id","harmonized_life_stage","reproductive_stage","derivation_source"
```

**Audit CSV header to use** (5 columns per D-08):
```
"org_lifestage","triage_bucket","resolution_path","candidate_source","notes"
```

**Allowed triage_bucket values** (D-01):
```
alias_synonym
needs_ontology_expansion
ambiguous
administrative_noise
out_of_scope_biology
```

**Example rows to seed the file** (from RESEARCH.md triage pre-classification):
```
"org_lifestage","triage_bucket","resolution_path","candidate_source","notes"
"Not coded","administrative_noise","manual_derivation",NA,"Maps to Unspecified; no ontology source"
"Not intact","administrative_noise","manual_derivation",NA,"Maps to Unspecified; no ontology source"
"Not reported","administrative_noise","manual_derivation",NA,"Maps to Unspecified; no ontology source"
"Unspecified","administrative_noise","manual_derivation",NA,"Maps to Unspecified; no ontology source"
"Fry","alias_synonym","agrovoc","AGROVOC c_37240890","Fish juvenile stage"
"Copepodid","alias_synonym","ols4_broader","ECOCORE","Copepodid stages 2-6 found in ECOCORE"
```

**Loading pattern** (mirrors `.eco_lifestage_baseline_path()` in `R/eco_lifestage_patch.R` lines 92-109):
```r
# Path resolution: installed package path falls back to dev path
installed <- system.file(
  "extdata", "ecotox", "lifestage_audit.csv",
  package = "ComptoxR"
)
if (nzchar(installed) && file.exists(installed)) {
  return(installed)
}
dev_path <- file.path("inst", "extdata", "ecotox", "lifestage_audit.csv")
if (file.exists(dev_path)) {
  return(dev_path)
}
cli::cli_abort("Committed lifestage audit CSV not found.")
```

---

### `inst/extdata/ecotox/lifestage_derivation.csv` (config/data, batch — 4 rows appended)

**Analog:** self (existing file, format locked)

**Existing schema** (`R/eco_lifestage_patch.R` lines 112-129 and test-eco_lifestage_data.R lines 48-55):
```
source_ontology, source_term_id, harmonized_life_stage, reproductive_stage, derivation_source
```

**4 administrative rows to append** (D-02, from RESEARCH.md code examples):
```
NA,NA,"Unspecified",FALSE,"administrative_noise_audit"
```
One row for each of: Not coded, Not intact, Not reported, Unspecified.

**Note:** `source_ontology` and `source_term_id` are NA because these terms have no ontology source. The join in `.eco_lifestage_derive_fields()` uses `by = c("source_ontology", "source_term_id")` — administrative terms (unresolved) do not flow through that join path, so NA keys do not corrupt the cross-check gate. Confirm this during implementation by running the cross-check gate test after appending.

---

### `R/eco_lifestage_patch.R` (service, event-driven — cli_warn gate only)

**Analog:** self; insertion point is `.eco_lifestage_materialize_tables()` at line 817 (after `cache_rows` is arranged)

**Existing cli_warn pattern** (`R/eco_lifestage_patch.R` lines 471-477 — NVS failure warn):
```r
cli::cli_warn(c(
  "NVS S11 SPARQL endpoint unreachable.",
  "i" = "NVS candidates will be skipped for this resolution run.",
  "x" = conditionMessage(e)
))
```

**cli_warn gate to insert after line 818** (from RESEARCH.md Pattern + D-10, D-11):
```r
# Audit coverage gate: warn (never abort) for unclassified unresolved terms
audit_path <- system.file(
  "extdata", "ecotox", "lifestage_audit.csv",
  package = "ComptoxR"
)
if (nzchar(audit_path) && file.exists(audit_path)) {
  audit <- readr::read_csv(audit_path, show_col_types = FALSE)
  unresolved_terms <- cache_rows |>
    dplyr::filter(.data$source_match_status == "unresolved") |>
    dplyr::pull(.data$org_lifestage) |>
    unique()
  unclassified <- setdiff(unresolved_terms, audit$org_lifestage)
  if (length(unclassified) > 0) {
    cli::cli_warn(c(
      "{length(unclassified)} unresolved term(s) not found in lifestage audit CSV.",
      "i" = "Terms: {.val {unclassified}}",
      "i" = "Edit {.path {audit_path}} to classify these terms."
    ))
  }
}
```

**readr pattern** (established throughout file, e.g., `validate_36.R` line 16):
```r
readr::read_csv(path, show_col_types = FALSE)
```

---

### `tests/testthat/test-eco_lifestage_data.R` (test, request-response — 3 blocks added)

**Analog:** self (existing file `tests/testthat/test-eco_lifestage_data.R`)

**Existing test block skeleton** (lines 35-56 — schema check pattern):
```r
test_that("lifestage_derivation.csv has correct schema columns", {
  testthat::skip_if_not_installed("readr")
  path <- system.file(
    "extdata", "ecotox", "lifestage_derivation.csv",
    package = "ComptoxR"
  )
  testthat::skip_if(
    nchar(path) == 0,
    "lifestage_derivation.csv not found in installed package"
  )
  df <- readr::read_csv(path, show_col_types = FALSE)
  expected_cols <- c(
    "source_ontology", "source_term_id", "harmonized_life_stage",
    "reproductive_stage", "derivation_source"
  )
  testthat::expect_equal(sort(names(df)), sort(expected_cols))
})
```

**Existing anti-join pattern** (lines 58-98 — cross-check gate):
```r
test_that("every resolved baseline key has a derivation partner (cross-check gate)", {
  ...
  gaps <- dplyr::anti_join(
    resolved_keys, derivation,
    by = c("source_ontology", "source_term_id")
  )
  testthat::expect_equal(
    nrow(gaps), 0L,
    label = paste0(nrow(gaps), " resolved baseline key(s) have no derivation partner: ", ...)
  )
})
```

**3 new test blocks to add** (following existing patterns exactly):

**Block 1 — audit CSV schema check** (mirrors derivation schema test, lines 35-56):
```r
test_that("lifestage_audit.csv has correct schema columns", {
  testthat::skip_if_not_installed("readr")
  path <- system.file(
    "extdata", "ecotox", "lifestage_audit.csv",
    package = "ComptoxR"
  )
  testthat::skip_if(
    nchar(path) == 0,
    "lifestage_audit.csv not found in installed package"
  )
  df <- readr::read_csv(path, show_col_types = FALSE)
  expected_cols <- c(
    "org_lifestage", "triage_bucket", "resolution_path",
    "candidate_source", "notes"
  )
  testthat::expect_equal(sort(names(df)), sort(expected_cols))
})
```

**Block 2 — audit coverage anti-join** (mirrors cross-check gate pattern, lines 58-98):
```r
test_that("every unresolved baseline term has an audit classification", {
  testthat::skip_if_not_installed("readr")
  baseline_path <- system.file(
    "extdata", "ecotox", "lifestage_baseline.csv",
    package = "ComptoxR"
  )
  audit_path <- system.file(
    "extdata", "ecotox", "lifestage_audit.csv",
    package = "ComptoxR"
  )
  testthat::skip_if(
    nchar(baseline_path) == 0 || nchar(audit_path) == 0,
    "CSV artifacts not found in installed package"
  )
  baseline <- readr::read_csv(baseline_path, show_col_types = FALSE)
  audit <- readr::read_csv(audit_path, show_col_types = FALSE)
  unresolved <- dplyr::filter(baseline, source_match_status == "unresolved")
  gaps <- dplyr::anti_join(unresolved, audit, by = "org_lifestage")
  testthat::expect_equal(
    nrow(gaps),
    0L,
    label = paste0(
      nrow(gaps),
      " unresolved term(s) have no audit classification: ",
      paste(unique(gaps$org_lifestage), collapse = ", ")
    )
  )
})
```

**Block 3 — cli_warn gate unit test** (uses `expect_warning`, pattern from validate_35.R lines 121-149):
```r
test_that("materialize emits cli_warn for unclassified terms (audit gate)", {
  # This test verifies D-10: rebuild continues but warns when terms missing from audit
  # Implementation note: mock or use a temp audit CSV without the target term
  testthat::skip_if_not_installed("readr")
  audit_path <- system.file(
    "extdata", "ecotox", "lifestage_audit.csv",
    package = "ComptoxR"
  )
  testthat::skip_if(
    nchar(audit_path) == 0,
    "lifestage_audit.csv not found in installed package"
  )
  # Verify audit CSV is loadable (gate pre-condition check)
  audit <- readr::read_csv(audit_path, show_col_types = FALSE)
  testthat::expect_gt(nrow(audit), 0L)
})
```

---

### `dev/lifestage/probe_ols4.R` (utility, request-response)

**Analog:** `R/eco_lifestage_patch.R` `.eco_lifestage_query_ols4()` (lines 530-597)

**Script header pattern** (from `dev/lifestage/validate_36.R` lines 1-6):
```r
#!/usr/bin/env Rscript
# Phase 36.1: Broader OLS4 Search Probe
# Run from project root: Rscript dev/lifestage/probe_ols4.R

suppressPackageStartupMessages(devtools::load_all(".", quiet = TRUE))
```

**Broader OLS4 request pattern** — copy `.eco_lifestage_query_ols4()` structure but remove ontology restriction and add queryFields (RESEARCH.md Pattern 1):
```r
# Current restricted pattern (lines 535-543 of eco_lifestage_patch.R):
httr2::request("https://www.ebi.ac.uk/ols4/api/search") |>
  httr2::req_url_query(
    q = term,
    ontology = tolower(ontology),
    rows = 25
  ) |>
  httr2::req_perform() |>
  httr2::resp_body_string() |>
  jsonlite::fromJSON(simplifyDataFrame = TRUE)

# Broader pattern (remove ontology param, add queryFields):
httr2::request("https://www.ebi.ac.uk/ols4/api/search") |>
  httr2::req_url_query(
    q = term,
    queryFields = "label,synonym,description",
    rows = 25
  ) |>
  httr2::req_perform() |>
  httr2::resp_body_string() |>
  jsonlite::fromJSON(simplifyDataFrame = TRUE)
```

**Error handling pattern** (lines 545-553 of eco_lifestage_patch.R):
```r
response <- tryCatch(
  { ... httr2 call ... },
  error = function(e) {
    cli::cli_warn(c(
      "OLS4 endpoint unreachable.",
      "i" = "Check network connectivity.",
      "x" = conditionMessage(e)
    ))
    NULL
  }
)
if (is.null(response)) return(tibble::tibble())
```

**Post-filter pattern for noise reduction** (RESEARCH.md Common Pitfall 1):
```r
# Filter to life-stage-relevant ontology prefixes after broad search
RELEVANT_PREFIXES <- c("UBERON:", "PO:", "ECOCORE:", "BTO:", "EFO:", "ZFA:", "FBdv:")
docs_filtered <- docs |>
  dplyr::filter(purrr::map_lgl(
    .data$obo_id,
    function(id) any(startsWith(id, RELEVANT_PREFIXES))
  ))
```

**Test terms to probe** (D-05):
```r
# Simple terms (expected hits):
test_simple <- c("Fry", "Fingerling", "Alevin", "Yearling")
# Complex terms (may miss current restricted search):
test_complex <- c("Copepodid", "Glochidia", "Trophozoite", "Naiad")
```

---

### `dev/lifestage/probe_bioportal.R` (utility, request-response)

**Analog:** `R/eco_lifestage_patch.R` `.eco_lifestage_query_nvs()` (lines 600-624) — same read-index-then-filter structure adapted for REST

**API key guard** (RESEARCH.md Common Pitfall 2):
```r
bioportal_key <- Sys.getenv("BIOPORTAL_API_KEY")
if (!nzchar(bioportal_key)) {
  cli::cli_abort(c(
    "BIOPORTAL_API_KEY environment variable not set.",
    "i" = "Obtain a free key at bioportal.bioontology.org",
    "i" = "Set with: Sys.setenv(BIOPORTAL_API_KEY = 'your_key')"
  ))
}
```

**httr2 request pattern** (mirrors `.eco_lifestage_query_ols4()` structure, adapted for BioPortal):
```r
response <- tryCatch(
  {
    httr2::request("https://data.bioontology.org/annotator") |>
      httr2::req_url_query(
        text = term,
        ontologies = "UBERON,ZFA,FBdv,ECOCORE",
        apikey = bioportal_key
      ) |>
      httr2::req_perform() |>
      httr2::resp_body_json()
  },
  error = function(e) {
    cli::cli_warn(c(
      "BioPortal endpoint unreachable for {.val {term}}.",
      "x" = conditionMessage(e)
    ))
    NULL
  }
)
```

**Rate limiting** (RESEARCH.md Anti-Patterns):
```r
Sys.sleep(0.5)  # between per-term calls
```

---

### `dev/lifestage/probe_wikidata.R` (utility, request-response)

**Analog:** `R/eco_lifestage_patch.R` `.eco_lifestage_nvs_index()` lines 450-478 — SPARQL over httr2

**SPARQL request pattern** (mirrors NVS SPARQL pattern, lines 461-478):
```r
# NVS existing pattern:
httr2::request("https://vocab.nerc.ac.uk/sparql/sparql") |>
  httr2::req_body_form(query = query) |>
  httr2::req_headers(Accept = "application/sparql-results+json") |>
  httr2::req_perform() |>
  httr2::resp_body_string() |>
  jsonlite::fromJSON(simplifyDataFrame = TRUE)

# Wikidata adaptation (GET with query param + User-Agent required):
httr2::request("https://query.wikidata.org/sparql") |>
  httr2::req_url_query(query = sparql_query) |>
  httr2::req_headers(
    Accept = "application/sparql-results+json",
    `User-Agent` = "ComptoxR/dev probe script (verfassergeist@gmail.com)"
  ) |>
  httr2::req_perform() |>
  httr2::resp_body_string() |>
  jsonlite::fromJSON(simplifyDataFrame = TRUE)
```

**SPARQL query template** (RESEARCH.md Pattern 4, using glue for parameterization):
```r
make_wikidata_query <- function(term) {
  glue::glue('
    SELECT ?item ?itemLabel ?uberonId WHERE {{
      ?item wdt:P1554 ?uberonId .
      ?item rdfs:label ?itemLabel .
      FILTER(LANG(?itemLabel) = "en")
      FILTER(CONTAINS(LCASE(?itemLabel), "{tolower(term)}"))
    }}
    LIMIT 10
  ')
}
```

**Rate limiting** (RESEARCH.md Common Pitfall 4):
```r
Sys.sleep(1)  # between per-term calls to avoid 429
```

**Error handling** (same tryCatch pattern as NVS, lines 461-479):
```r
payload <- tryCatch(
  { ... httr2 call ... },
  error = function(e) {
    cli::cli_warn(c(
      "Wikidata SPARQL endpoint unreachable.",
      "i" = "Wikidata candidates will be skipped.",
      "x" = conditionMessage(e)
    ))
    NULL
  }
)
```

---

### `dev/lifestage/probe_agrovoc.R` (utility, request-response)

**Analog:** `R/eco_lifestage_patch.R` `.eco_lifestage_query_ols4()` (lines 530-597) — REST GET with JSON parse

**REST GET pattern** (same httr2 structure as OLS4, adapted for AGROVOC endpoint):
```r
# OLS4 pattern (lines 534-543):
httr2::request("https://www.ebi.ac.uk/ols4/api/search") |>
  httr2::req_url_query(q = term, ontology = ..., rows = 25) |>
  httr2::req_perform() |>
  httr2::resp_body_string() |>
  jsonlite::fromJSON(simplifyDataFrame = TRUE)

# AGROVOC adaptation (RESEARCH.md Pattern 2):
httr2::request("https://agrovoc.fao.org/browse/rest/v1/search/") |>
  httr2::req_url_query(query = term, lang = "en") |>
  httr2::req_perform() |>
  httr2::resp_body_json()
```

**Two-step fetch pattern** (RESEARCH.md Pattern 2 — search then detail):
```r
# Step 1: search for term URI
search_result <- httr2::request("https://agrovoc.fao.org/browse/rest/v1/search/") |>
  httr2::req_url_query(query = term, lang = "en") |>
  httr2::req_perform() |>
  httr2::resp_body_json()

# Step 2: fetch concept detail if URI found
if (!is.null(search_result$results) && length(search_result$results) > 0) {
  uri <- search_result$results[[1]]$uri
  detail <- httr2::request("https://agrovoc.fao.org/browse/rest/v1/data/") |>
    httr2::req_url_query(uri = uri) |>
    httr2::req_perform() |>
    httr2::resp_body_json()
}
```

**Known gaps to document in probe output** (RESEARCH.md Common Pitfall 3):
```r
# AGROVOC known empty terms (document results regardless):
# "jointing", "sapling", "tiller" return empty despite being agricultural concepts
# Try alternate spellings: "tillering" instead of "tiller"
```

---

### `dev/lifestage/validate_36.1.R` (utility, batch)

**Analog:** `dev/lifestage/validate_36.R` (exact match — same section structure)

**Script structure pattern** (validate_36.R lines 1-104):
```r
#!/usr/bin/env Rscript
# Phase 36.1: Unresolved Coverage Audit Validation
# Run from project root: Rscript dev/lifestage/validate_36.1.R

suppressPackageStartupMessages(devtools::load_all(".", quiet = TRUE))

cli::cli_h1("Phase 36.1 Validation")

# -- Section 1: Audit CSV Schema Check --------------------------------------
cli::cli_h2("1. Audit CSV Schema Check")
audit_path <- system.file("extdata", "ecotox", "lifestage_audit.csv", package = "ComptoxR")
# OR dev path fallback:
if (!nzchar(audit_path)) audit_path <- file.path("inst", "extdata", "ecotox", "lifestage_audit.csv")
stopifnot(file.exists(audit_path))
audit <- readr::read_csv(audit_path, show_col_types = FALSE)
expected_cols <- c("org_lifestage", "triage_bucket", "resolution_path", "candidate_source", "notes")
stopifnot(all(expected_cols %in% names(audit)))
cli::cli_alert_success("Audit schema: {ncol(audit)} columns OK")

# -- Section 2: 100% Classification Coverage --------------------------------
cli::cli_h2("2. 100% Classification Coverage (D-03)")
# ... anti-join logic ...
stopifnot(nrow(gaps) == 0L)
cli::cli_alert_success("All {nrow(unresolved)} unresolved term(s) classified in audit CSV.")

# -- Section 3: Derivation Administrative Rows ------------------------------
cli::cli_h2("3. Administrative Derivation Rows (D-02)")
# ... check 4 admin rows exist in derivation CSV ...

# -- Section 4: cli_warn Gate Smoke Test ------------------------------------
cli::cli_h2("4. cli_warn Gate")
# ... verify gate fires with temp empty audit CSV ...

# -- Footer -----------------------------------------------------------------
cli::cli_h1("Phase 36.1 Validation Complete")
cli::cli_alert_success("All checks passed.")
```

**stopifnot + cli_alert_success pattern** (validate_36.R lines 19-27):
```r
stopifnot(ncol(baseline) == 13L)
cli::cli_alert_success(
  "Baseline schema: {ncol(baseline)} columns ({.val {paste(names(baseline), collapse = ', ')}})"
)
```

**DB-optional skip pattern** (validate_36.R lines 60-66):
```r
if (!file.exists(db_path)) {
  cli::cli_warn(
    "ECOTOX DB not found at {.path {db_path}} -- completeness check skipped."
  )
} else {
  ...
}
```

---

## Shared Patterns

### httr2 HTTP Request
**Source:** `R/eco_lifestage_patch.R` lines 534-543 (`.eco_lifestage_query_ols4()`)
**Apply to:** All 4 probe scripts
```r
response <- tryCatch(
  {
    httr2::request(BASE_URL) |>
      httr2::req_url_query(...) |>
      httr2::req_perform() |>
      httr2::resp_body_string() |>    # or resp_body_json() for pre-parsed
      jsonlite::fromJSON(simplifyDataFrame = TRUE)
  },
  error = function(e) {
    cli::cli_warn(c(
      "Endpoint unreachable.",
      "i" = "Candidates will be skipped.",
      "x" = conditionMessage(e)
    ))
    NULL
  }
)
if (is.null(response)) return(tibble::tibble())
```

### cli Non-Fatal Warning
**Source:** `R/eco_lifestage_patch.R` lines 471-477 (NVS failure warn)
**Apply to:** All probe scripts, cli_warn gate in materialize
```r
cli::cli_warn(c(
  "Primary message here.",
  "i" = "Informational detail.",
  "x" = conditionMessage(e)   # or custom message
))
```

### system.file() Path Resolution with Dev Fallback
**Source:** `R/eco_lifestage_patch.R` lines 92-109 (`.eco_lifestage_baseline_path()`)
**Apply to:** `R/eco_lifestage_patch.R` cli_warn gate, all test blocks
```r
installed <- system.file("extdata", "ecotox", "FILENAME.csv", package = "ComptoxR")
if (nzchar(installed) && file.exists(installed)) {
  return(installed)
}
dev_path <- file.path("inst", "extdata", "ecotox", "FILENAME.csv")
if (file.exists(dev_path)) {
  return(dev_path)
}
cli::cli_abort("FILENAME.csv not found.")
```

### testthat CSV Schema Test Block
**Source:** `tests/testthat/test-eco_lifestage_data.R` lines 35-56
**Apply to:** New audit CSV schema test block
```r
test_that("ARTIFACT has correct schema columns", {
  testthat::skip_if_not_installed("readr")
  path <- system.file("extdata", "ecotox", "ARTIFACT.csv", package = "ComptoxR")
  testthat::skip_if(nchar(path) == 0, "ARTIFACT.csv not found in installed package")
  df <- readr::read_csv(path, show_col_types = FALSE)
  expected_cols <- c(...)
  testthat::expect_equal(sort(names(df)), sort(expected_cols))
})
```

### testthat anti-join Coverage Test
**Source:** `tests/testthat/test-eco_lifestage_data.R` lines 58-98
**Apply to:** New audit coverage CI gate test block
```r
gaps <- dplyr::anti_join(left_df, right_df, by = "join_key")
testthat::expect_equal(
  nrow(gaps),
  0L,
  label = paste0(nrow(gaps), " items have no partner: ", paste(unique(gaps$join_key), collapse = ", "))
)
```

### Dev Script Header + devtools::load_all
**Source:** `dev/lifestage/validate_36.R` lines 1-6
**Apply to:** All 4 probe scripts, validate_36.1.R
```r
#!/usr/bin/env Rscript
# Phase 36.1: [Script purpose]
# Run from project root: Rscript dev/lifestage/SCRIPT.R

suppressPackageStartupMessages(devtools::load_all(".", quiet = TRUE))
```

### readr CSV Read
**Source:** `dev/lifestage/validate_36.R` lines 16-17; `test-eco_lifestage_data.R` line 16
**Apply to:** Every file that reads a CSV artifact
```r
df <- readr::read_csv(path, show_col_types = FALSE)
```

---

## No Analog Found

All 7 files have close analogs in the existing codebase. No external-only patterns required.

The 4 external API patterns (BioPortal, Wikidata, AGROVOC, broader OLS4) are documented in RESEARCH.md with live-tested examples and are captured in the probe script sections above.

---

## Metadata

**Analog search scope:** `R/`, `dev/lifestage/`, `tests/testthat/`, `inst/extdata/ecotox/`
**Files scanned:** 9 source files read in full or part
**Pattern extraction date:** 2026-04-23
