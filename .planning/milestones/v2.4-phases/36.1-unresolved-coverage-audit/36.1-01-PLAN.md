---
phase: 36.1-unresolved-coverage-audit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/extdata/ecotox/lifestage_audit.csv
  - inst/extdata/ecotox/lifestage_derivation.csv
  - tests/testthat/test-eco_lifestage_data.R
  - R/eco_lifestage_patch.R
  - dev/lifestage/validate_36.1.R
autonomous: true
requirements: [D-01, D-02, D-03, D-07, D-08, D-09, D-10, D-11, D-12]

must_haves:
  truths:
    - "Every one of the 69 unresolved baseline terms appears in lifestage_audit.csv with a non-empty triage_bucket"
    - "The 4 administrative terms (Not coded, Not reported, Not intact, Unspecified) have derivation rows mapping to Unspecified"
    - "CI test fails if any unresolved baseline term is missing from the audit CSV"
    - "Rebuild path emits cli_warn (never cli_abort) for terms not in audit CSV"
    - "Audit CSV is loadable via system.file() from the installed package"
  artifacts:
    - path: "inst/extdata/ecotox/lifestage_audit.csv"
      provides: "69-row triage classification of all unresolved terms"
      contains: "org_lifestage,triage_bucket,resolution_path,candidate_source,notes"
    - path: "inst/extdata/ecotox/lifestage_derivation.csv"
      provides: "4 new administrative_noise_audit rows appended"
      contains: "administrative_noise_audit"
    - path: "tests/testthat/test-eco_lifestage_data.R"
      provides: "2 new test blocks (audit schema + coverage anti-join)"
      contains: "lifestage_audit.csv has correct schema"
    - path: "R/eco_lifestage_patch.R"
      provides: "cli_warn gate in .eco_lifestage_materialize_tables()"
      contains: "not found in lifestage audit CSV"
    - path: "dev/lifestage/validate_36.1.R"
      provides: "Phase validation script"
      contains: "Phase 36.1 Validation"
  key_links:
    - from: "tests/testthat/test-eco_lifestage_data.R"
      to: "inst/extdata/ecotox/lifestage_audit.csv"
      via: "system.file() read + anti_join"
      pattern: "anti_join.*audit.*org_lifestage"
    - from: "R/eco_lifestage_patch.R"
      to: "inst/extdata/ecotox/lifestage_audit.csv"
      via: "system.file() read in .eco_lifestage_materialize_tables()"
      pattern: "lifestage_audit\\.csv"
---

<objective>
Create the lifestage audit CSV with all 69 unresolved terms classified into triage buckets, append 4 administrative derivation rows, add CI-enforced test coverage, and wire a cli_warn runtime gate into the rebuild path.

Purpose: This plan delivers the core committed artifacts that make every unresolved term accountable -- end users can read the audit CSV, CI catches regressions, and the rebuild path warns (never blocks) on unclassified terms.

Output: lifestage_audit.csv (69 rows), updated lifestage_derivation.csv (+4 rows), 2 new test blocks, cli_warn gate in materialize, validation script.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-unresolved-coverage-audit/36.1-CONTEXT.md
@.planning/phases/36.1-unresolved-coverage-audit/36.1-RESEARCH.md
@.planning/phases/36.1-unresolved-coverage-audit/36.1-PATTERNS.md
@.planning/phases/36-bootstrap-data-artifacts/36-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From inst/extdata/ecotox/lifestage_baseline.csv (header):
```
org_lifestage,source_provider,source_ontology,source_term_id,source_term_label,source_term_definition,source_release,source_match_method,source_match_status,candidate_rank,candidate_score,candidate_reason,ecotox_release
```
- 139 rows total, 69 with source_match_status == "unresolved"

From inst/extdata/ecotox/lifestage_derivation.csv (header):
```
source_ontology,source_term_id,harmonized_life_stage,reproductive_stage,derivation_source
```
- Currently 53 rows

From tests/testthat/test-eco_lifestage_data.R:
- 6 existing test_that blocks (baseline schema, derivation schema, cross-check gate, GO:0040007, baseline non-zero, derivation non-zero)
- Pattern: skip_if_not_installed("readr"), system.file() path resolution, readr::read_csv(show_col_types = FALSE)

From R/eco_lifestage_patch.R .eco_lifestage_materialize_tables() lines 817-818:
```r
  cache_rows <- cache_rows |>
    dplyr::arrange(.data$org_lifestage, .data$candidate_rank)
```
- cli_warn gate inserts AFTER line 818, BEFORE the write_cache block at line 820
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lifestage_audit.csv and append administrative derivation rows</name>
  <files>inst/extdata/ecotox/lifestage_audit.csv, inst/extdata/ecotox/lifestage_derivation.csv</files>
  <read_first>
    - inst/extdata/ecotox/lifestage_baseline.csv (source of truth for the 69 unresolved org_lifestage values)
    - inst/extdata/ecotox/lifestage_derivation.csv (current 53 rows; append 4 admin rows)
    - .planning/phases/36.1-unresolved-coverage-audit/36.1-RESEARCH.md (section "Unresolved Term Triage" for pre-classification)
    - .planning/phases/36.1-unresolved-coverage-audit/36.1-PATTERNS.md (audit CSV header and example rows)
  </read_first>
  <action>
Write an R script to `$TEMP/create_audit.R` and run it. The script does two things:

**Step 1 -- Create `inst/extdata/ecotox/lifestage_audit.csv`** with exactly 69 rows (one per unresolved baseline term). Columns per D-08: `org_lifestage`, `triage_bucket`, `resolution_path`, `candidate_source`, `notes`.

Extract `org_lifestage` values programmatically from the baseline CSV by filtering `source_match_status == "unresolved"`. Values MUST match exactly (case-sensitive, including the comma in "New, newly or recent hatch").

Build a lookup tibble mapping each term to its bucket. Use these assignments per RESEARCH.md pre-classification (D-01):

- **alias_synonym** (38 terms): Alevin, Copepodid, Elver, Fingerling, Froglet, Fry, Glochidia, Imago, Intermolt, Mid-neurula, Molt, Naiad, Neurala, Parr, Post-emergence, Post-hatch, Post-molt, Pre-hatch, Pre-molt, Prebloom, Prepupal, Prolarva, Protolarvae, Pullet, Rooted cuttings, Sapling, Spawning, Sporeling, Subadult, Swim-up, Trophozoite, Turion, Underyearling, Virgin, Weanling, Yearling, Young, Zygospore

- **needs_ontology_expansion** (10 terms): Grain or seed formation stage, Heading, Jointing, Mature (full-bloom stage) organism, Mature dormant, Mature reproductive, Mature vegetative, Rootstock, Scape elongation, Tiller stage

- **ambiguous** (3 terms): "New, newly or recent hatch", Sexually mature, Young of year

- **administrative_noise** (4 terms per D-02): Not coded, Not intact, Not reported, Unspecified

- **out_of_scope_biology** (14 terms): Exponential growth phase (log), F0 generation, F1 generation, F11 generation, F2 generation, F3 generation, F6 generation, F7 generation, Lag growth phase, Lactational, Post-spawning, Postpartum, Pre-spawning, Stationary growth phase

Set `resolution_path` per bucket:
- alias_synonym: `"pending_probe"`
- needs_ontology_expansion: `"pending_probe"`
- ambiguous: `"pending_probe"`
- administrative_noise: `"manual_derivation"`
- out_of_scope_biology: `"permanently_unresolved"` for generational (F0-F11) and microbial growth (Exponential/Lag/Stationary) terms; `"manual_curator"` for reproductive-adjacent terms (Lactational, Post-spawning, Postpartum, Pre-spawning)

Set `candidate_source` to `NA_character_` for all rows initially.

Set `notes` with brief domain rationale per term (e.g. "Fish juvenile synonym" for Fry, "Invertebrate larval stage" for Copepodid, "Maps to Unspecified; no ontology source" for admin terms, "Generational designation, not a developmental stage" for F0-F11, "Microbial growth phase, not a life stage" for Exponential/Lag/Stationary).

After joining the lookup to the extracted unresolved terms, assert that the result has exactly 69 rows with zero NA triage_bucket values. Sort alphabetically by `org_lifestage`. Write with `readr::write_csv()`.

**Step 2 -- Append 4 rows to `inst/extdata/ecotox/lifestage_derivation.csv`** per D-02. Each row:
```
source_ontology = NA, source_term_id = NA, harmonized_life_stage = "Unspecified", reproductive_stage = FALSE, derivation_source = "administrative_noise_audit"
```
Read existing CSV, bind the 4 new rows, sort by `source_ontology` then `source_term_id` (NAs last), write back with `readr::write_csv()`.

The script must stop with an informative error if the audit row count is not 69 or if any term has a missing bucket.
  </action>
  <verify>
    <automated>Write a temp R verification script to $TEMP/verify_task1.R that: (1) reads the audit CSV and confirms nrow == 69, (2) reads baseline CSV, filters unresolved, does anti_join against audit by org_lifestage, confirms 0 gaps, (3) confirms audit has exactly 5 columns named org_lifestage/triage_bucket/resolution_path/candidate_source/notes, (4) reads derivation CSV and confirms it has rows with derivation_source == "administrative_noise_audit", (5) prints "PASS" if all checks succeed. Run with Rscript.exe.</automated>
  </verify>
  <acceptance_criteria>
    - `inst/extdata/ecotox/lifestage_audit.csv` exists with exactly 69 data rows
    - Column names are exactly: org_lifestage, triage_bucket, resolution_path, candidate_source, notes
    - Every `triage_bucket` value is one of: alias_synonym, needs_ontology_expansion, ambiguous, administrative_noise, out_of_scope_biology
    - Zero gaps when anti-joining baseline unresolved terms against audit by org_lifestage
    - `inst/extdata/ecotox/lifestage_derivation.csv` contains at least 4 rows where derivation_source == "administrative_noise_audit"
    - Derivation CSV has 57 total rows (53 existing + 4 new)
  </acceptance_criteria>
  <done>Audit CSV committed with 69 classified rows covering 100% of unresolved baseline terms; derivation CSV has 4 new administrative rows.</done>
</task>

<task type="auto">
  <name>Task 2: Add CI tests and cli_warn gate</name>
  <files>tests/testthat/test-eco_lifestage_data.R, R/eco_lifestage_patch.R</files>
  <read_first>
    - tests/testthat/test-eco_lifestage_data.R (current 6 test blocks; append 2 new blocks)
    - R/eco_lifestage_patch.R (lines 775-825 for .eco_lifestage_materialize_tables insertion point)
    - .planning/phases/36.1-unresolved-coverage-audit/36.1-PATTERNS.md (exact code for test blocks and cli_warn gate)
  </read_first>
  <action>
**Part A -- Add 2 test blocks to `tests/testthat/test-eco_lifestage_data.R`** after the existing 6 blocks.

Block 1 -- audit CSV schema check (mirrors derivation schema test at lines 35-56):
```r
test_that("lifestage_audit.csv has correct schema columns", {
  testthat::skip_if_not_installed("readr")
  path <- system.file(
    "extdata", "ecotox", "lifestage_audit.csv",
    package = "ComptoxR"
  )
  testthat::skip_if(
    nchar(path) == 0,
    "lifestage_audit.csv not found in installed package"
  )
  df <- readr::read_csv(path, show_col_types = FALSE)
  expected_cols <- c(
    "candidate_source", "notes", "org_lifestage",
    "resolution_path", "triage_bucket"
  )
  testthat::expect_equal(sort(names(df)), sort(expected_cols))
})
```

Block 2 -- audit coverage anti-join (mirrors cross-check gate at lines 58-98, per D-12):
```r
test_that("every unresolved baseline term has an audit classification", {
  testthat::skip_if_not_installed("readr")
  baseline_path <- system.file(
    "extdata", "ecotox", "lifestage_baseline.csv",
    package = "ComptoxR"
  )
  audit_path <- system.file(
    "extdata", "ecotox", "lifestage_audit.csv",
    package = "ComptoxR"
  )
  testthat::skip_if(
    nchar(baseline_path) == 0 || nchar(audit_path) == 0,
    "CSV artifacts not found in installed package"
  )
  baseline <- readr::read_csv(baseline_path, show_col_types = FALSE)
  audit <- readr::read_csv(audit_path, show_col_types = FALSE)
  unresolved <- dplyr::filter(baseline, source_match_status == "unresolved")
  gaps <- dplyr::anti_join(unresolved, audit, by = "org_lifestage")
  testthat::expect_equal(
    nrow(gaps),
    0L,
    label = paste0(
      nrow(gaps),
      " unresolved term(s) have no audit classification: ",
      paste(unique(gaps$org_lifestage), collapse = ", ")
    )
  )
})
```

**Part B -- Add cli_warn gate to `R/eco_lifestage_patch.R`** inside `.eco_lifestage_materialize_tables()`.

Insert the following code block AFTER the line `dplyr::arrange(.data$org_lifestage, .data$candidate_rank)` (currently line 818) and BEFORE the `if (isTRUE(write_cache))` block (currently line 820). Add a blank line before and after for readability.

```r
  # -- Audit coverage gate (D-10): warn for unclassified unresolved terms ------
  audit_path <- system.file(
    "extdata", "ecotox", "lifestage_audit.csv",
    package = "ComptoxR"
  )
  if (nzchar(audit_path) && file.exists(audit_path)) {
    audit <- readr::read_csv(audit_path, show_col_types = FALSE)
    unresolved_terms <- cache_rows |>
      dplyr::filter(.data$source_match_status == "unresolved") |>
      dplyr::pull(.data$org_lifestage) |>
      unique()
    unclassified <- setdiff(unresolved_terms, audit$org_lifestage)
    if (length(unclassified) > 0) {
      cli::cli_warn(c(
        "{length(unclassified)} unresolved term(s) not found in lifestage audit CSV.",
        "i" = "Terms: {.val {unclassified}}",
        "i" = "Edit {.path {audit_path}} to classify these terms."
      ))
    }
  }
```

This gate per D-10/D-11: warns but never aborts. If the audit CSV does not exist (not installed), the gate silently skips.

After editing, run `air format R/eco_lifestage_patch.R` and `jarl check R/eco_lifestage_patch.R` per project conventions.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-eco_lifestage_data.R')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test-eco_lifestage_data.R contains the string "lifestage_audit.csv has correct schema columns"
    - tests/testthat/test-eco_lifestage_data.R contains the string "every unresolved baseline term has an audit classification"
    - tests/testthat/test-eco_lifestage_data.R has 8 test_that blocks total (6 existing + 2 new)
    - R/eco_lifestage_patch.R contains the string "not found in lifestage audit CSV"
    - R/eco_lifestage_patch.R contains `cli::cli_warn` inside .eco_lifestage_materialize_tables()
    - R/eco_lifestage_patch.R does NOT contain cli_abort or cli::cli_abort in the audit gate block
    - All 8 tests in test-eco_lifestage_data.R pass (testthat::test_file exits 0)
  </acceptance_criteria>
  <done>CI anti-join gate enforces 100% audit coverage; cli_warn gate wired into rebuild path; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create Phase 36.1 validation script</name>
  <files>dev/lifestage/validate_36.1.R</files>
  <read_first>
    - dev/lifestage/validate_36.R (exact analog -- copy section structure)
    - inst/extdata/ecotox/lifestage_audit.csv (must exist from Task 1)
    - inst/extdata/ecotox/lifestage_derivation.csv (must have admin rows from Task 1)
    - .planning/phases/36.1-unresolved-coverage-audit/36.1-PATTERNS.md (validate_36.1.R skeleton)
  </read_first>
  <action>
Create `dev/lifestage/validate_36.1.R` following the exact structure of `dev/lifestage/validate_36.R`. The script has 4 sections:

```r
#!/usr/bin/env Rscript
# Phase 36.1: Unresolved Coverage Audit Validation
# Run from project root: Rscript dev/lifestage/validate_36.1.R

suppressPackageStartupMessages(devtools::load_all(".", quiet = TRUE))

cli::cli_h1("Phase 36.1 Validation")

# -- Section 1: Audit CSV Schema Check ----------------------------------------
cli::cli_h2("1. Audit CSV Schema Check")
# Load audit CSV via dev path (inst/extdata/ecotox/lifestage_audit.csv)
# Assert file exists, has 5 expected columns, print column names
# stopifnot(all(expected_cols %in% names(audit)))

# -- Section 2: 100% Classification Coverage (D-03) ---------------------------
cli::cli_h2("2. 100% Classification Coverage (D-03)")
# Load baseline CSV, filter unresolved, anti-join against audit
# stopifnot(nrow(gaps) == 0L)
# Print count of terms per triage_bucket

# -- Section 3: Administrative Derivation Rows (D-02) -------------------------
cli::cli_h2("3. Administrative Derivation Rows (D-02)")
# Load derivation CSV, filter derivation_source == "administrative_noise_audit"
# stopifnot count >= 4
# Print the admin rows

# -- Section 4: cli_warn Gate Presence Check -----------------------------------
cli::cli_h2("4. cli_warn Gate Presence")
# grep "not found in lifestage audit CSV" in R/eco_lifestage_patch.R
# Confirm the string exists (file content check, not runtime)
# Use readLines + grepl to verify

# -- Footer -------------------------------------------------------------------
cli::cli_h1("Phase 36.1 Validation Complete")
cli::cli_alert_success("All checks passed.")
```

Use `stopifnot()` + `cli::cli_alert_success()` pattern from validate_36.R. Use dev path `file.path("inst", "extdata", "ecotox", ...)` since this runs from project root before package install.

After writing, run `air format dev/lifestage/validate_36.1.R`.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" dev/lifestage/validate_36.1.R</automated>
  </verify>
  <acceptance_criteria>
    - dev/lifestage/validate_36.1.R exists and is executable via Rscript
    - Script prints "Phase 36.1 Validation" header
    - Script prints "All checks passed" at the end
    - Script exits with code 0 (no stopifnot failures)
    - Script contains sections for: schema check, coverage check, admin rows check, cli_warn gate check
  </acceptance_criteria>
  <done>Validation script runs cleanly confirming all Plan 01 artifacts are correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan. All work is local CSV manipulation, test creation, and a runtime warning gate. No external API calls, no user input processing, no authentication.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-01 | Tampering | lifestage_audit.csv | accept | File is user-editable by design (D-09); CI gate catches regressions |
</threat_model>

<verification>
1. `inst/extdata/ecotox/lifestage_audit.csv` has exactly 69 rows, 5 columns, zero NA triage_bucket values
2. `inst/extdata/ecotox/lifestage_derivation.csv` has 57 rows total (53 + 4 admin)
3. `testthat::test_file('tests/testthat/test-eco_lifestage_data.R')` passes all 8 tests
4. `R/eco_lifestage_patch.R` contains "not found in lifestage audit CSV" string
5. `Rscript dev/lifestage/validate_36.1.R` exits 0
</verification>

<success_criteria>
- 100% of the 69 unresolved baseline terms are classified in the audit CSV (D-03)
- 4 administrative terms have derivation rows (D-02)
- CI gate catches any future unclassified term (D-12)
- Rebuild path warns but never aborts for unclassified terms (D-10, D-11)
- All files pass air format and jarl check
</success_criteria>

<output>
After completion, create `.planning/phases/36.1-unresolved-coverage-audit/36.1-01-SUMMARY.md`
</output>
