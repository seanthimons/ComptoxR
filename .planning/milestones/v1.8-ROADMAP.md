# Milestone v1.8: Testing Infrastructure

**Status:** SHIPPED 2026-01-31
**Phases:** 12-15
**Total Plans:** 6

## Overview

Add comprehensive test coverage to the stub generation pipeline (dev/endpoint_eval/) to ensure correctness, prevent regressions, and prepare the package for CRAN/rOpenSci submission.

## Phases

### Phase 12: Test Infrastructure Setup

**Goal:** Create the foundation for testing dev/endpoint_eval/ code
**Depends on:** None (first phase)
**Plans:** 1 plan

Plans:
- [x] 12-01-PLAN.md — Create helper-pipeline.R, test fixtures, and withr integration

**Deliverables:**
- `tests/testthat/helper-pipeline.R` — Sourcing logic for pipeline files
- `tests/testthat/fixtures/schemas/` — Test schema directory
- Minimal test fixtures (OpenAPI 3.0, Swagger 2.0, circular refs, malformed)
- `.StubGenEnv` cleanup utility
- Add `withr` to DESCRIPTION Suggests

**Success Criteria:**
- `source()` pattern works for all 8 pipeline files
- Fixtures load without error
- State cleanup verified with simple test

---

### Phase 13: Unit Tests

**Goal:** Achieve 80%+ coverage for dev/endpoint_eval/ code
**Depends on:** Phase 12 (infrastructure must exist)
**Plans:** 2 plans

Plans:
- [x] 13-01-PLAN.md — Test config (00_config.R) and schema resolution (01_schema_resolution.R)
- [x] 13-02-PLAN.md — Test OpenAPI parser (04_openapi_parser.R) and stub generation (07_stub_generation.R)

**Deliverables:**
- `test-pipeline-config.R` — Tests for 00_config.R (%||%, ensure_cols(), constants)
- `test-pipeline-schema-resolution.R` — Tests for 01_schema_resolution.R (resolve_schema_ref, circular refs)
- `test-pipeline-openapi-parser.R` — Tests for 04_openapi_parser.R (openapi_to_spec, detect_schema_version)
- `test-pipeline-stub-generation.R` — Tests for 07_stub_generation.R (build_function_stub, snapshots)

**Success Criteria:**
- All HIGH priority functions have tests
- Edge cases covered (circular refs, malformed schemas)
- Tests pass on all platforms (Windows, macOS, Linux)

---

### Phase 14: Integration & CI

**Goal:** End-to-end verification and CI/CD integration
**Depends on:** Phase 13 (unit tests must pass first)
**Plans:** 2 plans

Plans:
- [x] 14-01-PLAN.md — End-to-end integration tests with real schemas and VCR cassettes
- [x] 14-02-PLAN.md — GHA workflow, Codecov config, and coverage verification

**Deliverables:**
- `test-pipeline-integration.R` — End-to-end tests (schema → stub → execution)
- Coverage verification (R/ >= 75%, dev/ >= 80%)
- `.github/workflows/pipeline-tests.yml` — GHA workflow for PRs
- `codecov.yml` — Codecov configuration with thresholds

**Success Criteria:**
- Integration tests pass with real schemas
- Coverage thresholds met
- GHA workflow runs successfully

---

### Phase 15: Integration Test Fixes

**Goal:** Close gaps identified in v1.8 milestone audit
**Depends on:** Phase 14 (integration tests must exist)
**Plans:** 1 plan

Plans:
- [x] 15-01-PLAN.md — Implement get_stubgen_config() and fix cassette path

**Deliverables:**
- `get_stubgen_config()` function in helper-pipeline.R
- Fixed cassette path in pipeline-tests.yml (removed _vcr/)

**Gaps Closed:**
- FR-03: Integration tests (blocked by missing get_stubgen_config)
- Cassette path mismatch in CI workflow

---

## Milestone Summary

**Key Decisions:**

| Decision | Rationale | Impact |
|----------|-----------|--------|
| TEST-HELPER-PATTERN | Follow helper-vcr.R for consistency | Consistent test setup pattern |
| FIXTURE-MINIMAL | Small focused fixtures | Tests specific edge cases |
| WITHR-SUGGESTS | Test-only dependency | Minimal footprint for users |
| TEST-ORGANIZATION | describe() blocks | Readable test structure |
| INTEGRATION-SKIP-PATTERN | Skip without cassettes/API key | Offline-capable tests |
| DUAL-COVERAGE | R/ via Codecov, dev/ via GHA | Separate thresholds for different code |

**Issues Resolved:**

- get_stubgen_config() function implemented
- Cassette path corrected to match vcr_dir configuration
- All 8 requirements satisfied

**Tech Debt:**

- VCR cassettes not yet recorded (expected - first run with API key will record)

---

_For current project status, see .planning/MILESTONES.md_

---
*Archived: 2026-01-31 as part of v1.8 milestone completion*
