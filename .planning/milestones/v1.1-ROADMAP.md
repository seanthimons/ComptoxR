# Roadmap: ComptoxR Stub Generation v1.1 (ARCHIVED)

**Created:** 2026-01-27
**Milestone:** v1.1 — Raw Text Body Fix
**Archived:** 2026-01-27
**Status:** COMPLETE

## Overview

Fix `ct_chemical_search_equal_bulk()` to send raw text body instead of JSON-encoded string. The `/chemical/search/equal/` POST endpoint expects newline-delimited values as plain text, not JSON.

## Phase 3: Raw Text Body Special Case

**Goal:** Generate correct stub code for `/chemical/search/equal/` that sends raw text body

**Requirements covered:** STUB-01, STUB-02, STUB-03, VAL-01, VAL-02

**Success Criteria:**
1. ✓ Stub generator detects `/chemical/search/equal/` POST endpoint
2. ✓ Generated code uses `generic_request()` with `body_type = "raw_text"`
3. ✓ Values are collapsed with newlines via `generic_request()`
4. ✓ `ct_chemical_search_equal_bulk()` successfully queries live API
5. ✓ VCR cassette captures successful response

**Key Files:**
- `dev/endpoint_eval/07_stub_generation.R` — Special case detection for raw text body endpoints
- `R/z_generic_request.R` — Added `body_type` parameter
- `R/ct_chemical_search_equal.R` — Regenerated stub using `generic_request()`
- `tests/testthat/test-ct_chemical_search_equal_bulk.R` — VCR tests

**Implementation:**
1. Extended `generic_request()` with `body_type` parameter ("json" or "raw_text")
2. Updated stub generator to detect `body_schema_type == "string"` POST endpoints
3. Generated stubs use `body_type = "raw_text"` for raw text body encoding
4. Function now inherits `run_debug` and `run_verbose` support from `generic_request()`

**Plans:**
- [x] 03-01-PLAN.md — Add special case detection and code generation
- [x] 03-02-PLAN.md — Re-record VCR cassettes with raw text body format

---

## Summary

| Phase | Name | Goal | Requirements | Status |
|-------|------|------|--------------|--------|
| 3 | Raw Text Body Special Case | Fix `/chemical/search/equal/` stub | STUB-01, STUB-02, STUB-03, VAL-01, VAL-02 | Complete ✓ |

**Total:** 1 phase, 5 requirements, 2 plans — all complete ✓

---
*Roadmap created: 2026-01-27*
*Archived: 2026-01-27*
